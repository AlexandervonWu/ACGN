/**
 * Theory-faithful typed flexible-arity slotted e-graph implementation path.
 *
 * <p>This package is intentionally independent from the legacy normalization,
 * ablation, experiment, and command-line paths. Its public constructors enforce
 * the algebraic boundaries used by the formal graph implementation. Phase E
 * exposes both reference and production canonicalizers behind one stable API.
 * The graph remains disconnected from reproducibility runners until Phase I.</p>
 */
package is.fivefivefive.CanDis.theory;
