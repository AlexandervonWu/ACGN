# Independent P3-03 Bounded Review

## Final Disposition

**PASS for the bounded independent P3-03 reviewed surface. No remaining
findings on the final handoff.** Both concrete initial findings below were
reproduced independently and repaired by main/builder. Final snapshot E reran
the same registered area checks after the coordinated writer/pin integration;
it does not inherit snapshot D's results. The final five executable area files,
all seven production source pins, generated trace, source census and replay
match the frozen final handoff. The final notes are separately hash-bound and
reviewed. No implementation edits were made by this reviewer.

This is an audit of FF-P3-03, not a whole fourth-five closure execution or a
universal discharge of the original parent statement. In-progress documentation
and unrelated areas are not findings.
The required two-clean-build closure, complete provenance/determinism checks,
and user-required full-corpus rerun before release remain main's responsibility.
This bounded PASS is neither a global VERIFIED decision nor release clearance.

Frozen scope: `closure-config.json`, SHA-256
`0695e99e9117a81c4e7b9e161a146ebbd556ac2a94867ac9c76da79c092898cc`.
Original P3-03 claim SHA-256:
`c5733bd96ae6281cc4f3f9fe6d51e66261c20b0b0bf4b3aa426aaa210c7b0fca`.
The mechanical-closure-verification skill and its normative closure protocol
govern the distinction between model proofs, finite observations and trust.
No internet was used.

## Resolved Initial Findings

The line references in this section refer to preserved initial snapshot C.

1. **[P1] Untouched supported source profiles reject before reconstruction.**
   `certificate-verifier/src/org/acgn/cert/SemanticEvidenceVerifier.java:36`
   pins `alloy-command-options-v2`; its check at line 496 rejects the
   `alloy-command-options-v4-independent-search-domain` emitted by
   `src/is/fivefivefive/CanDis/theory/AlloySemanticProfileFactory.java:17`.
   All six real parser-owned source fixtures (widths 3/4/6, FORBID/MODULAR)
   were exported through the actual writer and then passed, unmodified, to
   public `IndependentVerifier.verify(bytes, Profile.KERNEL, policy)`.
   All returned `REJECTED / THEORY_MISMATCH` with detail
   `Source-command semantic context has an unsupported version`; both fixed
   compatibility controls returned `VERIFIED / NONE`. Canonical re-encoding
   was byte-identical for every original certificate. This is a concrete
   supported path, not a hostile intermediate mutation. The source baselines
   must remain expected accepts. A version repair needs fresh source pins and
   fresh root-bound evidence, not an expected-outcome relaxation.
   Affected claim: FF-P3-03; blocking code: `VERIFIER_FAILURE`.
   Resolved: the verifier now requires the exact complete v4 context version;
   all six original certificate bytes verify, while twelve resealed old/future
   version cases reject at the version guard. Final source pins were refreshed.

2. **[P2] Profile main does not implement the registered TSV-path interface.**
   `src/is/fivefivefive/CanDis/theory/SemanticProfileWireRegressionTest.java:222`
   treats its argument as a directory and line 255 appends the trace filename.
   The pinned engine, `scripts/run_next_obligation_repairs.py:604`, passes
   `build/semantic-profile-wire.tsv` and then reads that exact path at line
   607. The configured invocation therefore creates a directory with the
   expected TSV name and nests the actual file inside it. Independently
   reproduced: Java exits 0, then the registered reader's `is_file()` guard at
   line 228 raises `Blocked: MISSING_WITNESS: missing TSV`.
   Align the profile main with the registered file-path contract without
   weakening the reader. Resolved: the final main writes the exact configured
   TSV path, and the registered reader accepts it in the independent recheck.
   Affected claim: FF-P3-03; blocking code: `MISSING_WITNESS`.

## Initial Reviewed Boundary

- PROVED: the 13 general Lean theorems compile under Lean 4.33.0 with the
  declared axiom audit. They concern five wire texts, exact reconstruction,
  explicit authority and encoding agreement. They do not prove a Java parser
  or arbitrary certificate-byte decoder.
- CHECKED: the producer and independent verifier construct tag
  `semantic-profile`, exactly five scalars in bitwidth/overflow/temporal/rewrite/
  signature order, zero children, and UTF-16-code-unit length framing.
  Supplementary Unicode counts twice; no normalization is introduced.
- TESTED: 132 serialization observations, 120 public verifier observations and
  eight custom equal-spelling clone controls are generated by the Java main.
  Mutations really replace all five wire fields individually, both with stale
  and recomputed profile fingerprints, reseal the vocabulary content ID and
  binary encoding, and invoke the public verifier. Fixed-profile rehashed
  overflow changes legitimately describe another allowed compatibility profile.
- CHECKED: seven complete javac-normalized source trees and resolved bindings
  match the independently fixed source census. Sixteen encoder tests pass,
  including missing/duplicate/reordered rows, exact fields/columns, source-pin
  mismatches, Base64/UTF-8 canonicality and output-to-proposition binding.
- The `independentKey` trace column is a test-side independent reconstruction,
  not a purported observation of private verifier state. Public acceptance/
  rejection and exact details are separately recorded. No fabricated verifier
  result, private access or verified flag supplies evidence.
- TRUSTED: Lean kernel and declared axioms; JDK17 compiler/JVM/string semantics;
  Alloy parser, options and source interpretation; public reflection dispatch;
  hashed extractor/encoder and Python runtime; SHA-256 implementation and
  collision resistance; fixture provenance, OS, filesystem and hardware.
  The nullary CALL-free fixture pins an external empty occurrence commitment
  from its fixed identifier/content, not from bundle inspection. No hash
  injectivity is asserted. Isolated Java surrogates and whole-parser/JVM/codec
  semantics remain outside the Lean Unicode-scalar model.

The only admitted review steps target FF-P3-03 PASS-to-BLOCK transitions through
novel evidence: untouched public certificate results, configured TSV handoff,
and actual-observation Lean replay/schema checks. No new theory obligation or
general discovery pass is added.

## Initial Evidence

Immutable source snapshot and raw evidence:
`/tmp/acgn-profile-review-20260908-C`.
Canonical 446-file **review** input root:
`2e69ce44b264d947acbf010136a0987f7b58a10b8b2761877eeef549e13091b6`.
This is not advertised as the full closure engine's input root.

- `manifest.json` records every captured input and hash; `commands.json`
  records raw command arguments, working directories, exit codes and log hashes.
- `public-original-certificates.log` is the direct public API reproduction;
  SHA-256 `8d8733b166a78bf7bb918a1b4d1aede9caa006d3bcb44c8f2bbbf012a57fdcd3`.
- `observed/profile-source:3:FORBID.acgncert` is one decisive untouched input;
  SHA-256 `864193fc956c16b96c081398e03dfe9c4a33881d6b7cdd09518277fa811951c4`.
- `PublicProfileReview.java` is the public-API-only reproducer;
  `observed/semantic-profile-wire.tsv` is the actual Java census;
  `profile-source-census.log`, `profile-encoder-tests.log`, and
  `general-proof.log` preserve source, schema and proof checks.
- `initial-review-results.json` is the machine-readable initial result;
  SHA-256 `f10e18fa862ecf6c5955dfccec1ec4b768d2c766d0c63025e7b6b5fe2f44916b`.
  `commands-addendum.json` records the two decisive follow-up commands.
  `source-baseline-proposition.log` records exit 1 from false `decide` on the
  unchanged generated source baseline, SHA-256
  `49fc0e6b0a6140ea1c112eb9f5b85c17126b1ac43fc25d1d4fe20d65497dea23`.
  `exact-configured-read-tsv.log` records the second blocker, SHA-256
  `aee63be790b3eec19b9fed5774d3cb434540ecf0f7f12033ad74018c27ff2e54`.
  The initial 260-row census contains 54 mismatched outcomes/details; none
  is converted to a successful baseline. The full old replay timed out at
  300 seconds and is not used as a semantic-rejection result; the isolated
  baseline proposition supplies the decisive Lean evidence.
- Review setup attempts A and B are retained separately. A stopped on a
  temporary reviewer log-adapter error; B tried a src-only compilation while
  an unrelated in-progress area test imported verifier classes. Neither is
  used as evidence for a P3-03 defect or PASS. C independently compiles the
  standalone verifier, then compiles producer/tests with that API classpath.

Initial implementation hashes (SHA-256):

| Input | Hash |
| --- | --- |
| `SemanticProfileWire.lean` | `476564f063a644be032262f8c3207a7c8674fdb44ce37a595f044495436bdf49` |
| `SemanticProfileWireRegressionTest.java` | `e686b6c57dd1734df6b03bf43a2716453f99b8400bb768ccd0cd9eddcd303140` |
| `SemanticProfileWireExtractor.java` | `f0570da73e03dd831e98f3497377a816268e25d947f8c200b2e8c535f507f3a7` |
| `fourth_profile_replays.py` | `45aea3f5cc361f5ebe4170ffc0411fffa439f33eff7c936e4c4e388e12765980` |
| `test_fourth_profile_replays.py` | `125f1161d54f1600a062cd00d5e71c8ba1758ffa45288dfdd1c7ad3e36cf00fc` |
| `SemanticEvidenceVerifier.java` | `4a05263577e428f49d9d93eba775938abdc4f0b841b6a465458a26aa31c7f7d0` |

Full closure clean-build count, determinism and provenance are not discharged
by this bounded review. No global VERIFIED status is asserted or inherited.

## Recheck D

`/tmp/acgn-profile-review-20260908-D` binds the successfully completed area
checks to review root
`c95c177b30674d6ae96c5fab4c8fff4bf23b6349da4c71343cbcba7d1b4e786d`.
The independently compiled verifier hash was
`ec63aab42919edce0483dd93b50b755da5cedcb8dedc184291f3dfd08a238037`.

- 17 general theorems and all 272 actual-observation propositions pass their
  kernel/axiom checks. Scalar-sequence replay preserves exact Unicode through
  the new proved string roundtrip; UTF-16 framing is unchanged.
- All eight saved original certificates pass the public KERNEL verifier,
  including the exact six unmodified counterexamples from the initial run.
- The actual census has 132 serialization rows, 132 boundary rows and eight
  clones, with zero mismatched outcomes/details. All six source baselines
  accept; all twelve resealed old/future-version cases reject at the exact
  version guard. No expected source baseline was relaxed to a rejection.
- All 11 Lean controls exit 1 through registered false-proposition diagnostics;
  all 15 source controls exit 1 through registered source-pin diagnostics and
  leave no accepted output. Snapshot source restoration is hash-checked.
- The configured TSV-path invocation exits 0 and the registered reader accepts
  the exact output file. Both initial findings are therefore resolved here.
- Source publication-mode mutations correctly reach the later exact-type-ID
  guard. They are explicitly negative TEST_ONLY fixture-policy controls, not
  source baselines or claims of valid publication provenance.

Machine result: `review-results.json`, SHA-256
`2652ea4781ca0dc70006f86dafd8ba95cf06e4430907f40a0434884074f23da4`.
Command/result/log-hash inventory: `commands.json`, SHA-256
`68cf8492c45e3a6c8e976dea327a93cf9b63ba7d7a96a4daacf1f2395d0e485d`.
`live-input-changes.json` explicitly records the in-flight writer/test and
documentation changes. These are integration changes, not newly discovered
profile defects. The final binding is recorded below.

## Final Recheck E

Evidence directory: `/tmp/acgn-profile-review-20260908-E`.
Canonical 465-file execution **review** input root:
`dd32fdbc4404df12546ae707a590502e583958744ad482f3a8a35176b67062c5`.
Environment: OpenJDK/javac 17.0.20, installed Lean 4.33.0, Python 3.10;
all runs were offline with fresh compilation outputs. This is not the full
closure engine's final input manifest or a two-identical-root build claim.

Observed results, rerun independently against E:

| Check | Result |
| --- | --- |
| General model and explicit axiom audits | 17 theorems; exit 0 |
| Actual Java observations and authority assertions | 272 rows; 757 assertions; exit 0 |
| Public verifier on untouched original certificates | All 8 VERIFIED, including all 6 original counterexamples |
| Strict compiler-resolved source census | 7 objects; exit 0 |
| Encoder/schema tests | 16 tests; exit 0 |
| Actual-observation kernel replay and audits | 272 theorems; exit 0; zero outcome/detail mismatches |
| Lean rejection controls | 11/11 exit 1 with registered false-decide diagnostics |
| Source rejection controls | 15/15 exit 1 with registered source-pin diagnostics; no accepted stale output |
| Configured output-path handoff | Java exit 0; exact TSV is a file and registered reader accepts it |

The final model is an exact Unicode-scalar-list model with a proved String
roundtrip, UTF-16 framing, five-field reconstruction and an exact version guard.
The final regression checks actual `isSourceCommandBound`,
`isAuthorizedAlloyProfile`, `isFixedCompatibilityProfile` and admissibility APIs;
these are not substitute verifier flags. Actual acceptance still comes from
public `IndependentVerifier.verify`, and clone rejection still comes from the
public writer/authority APIs. The explicit hash, parser, JVM, encoder/extractor
and environment trust listed above is unchanged. No hash injectivity or
whole-Java/parser refinement is claimed.

The main-owned flat-splice and dependent-fold-type repairs were reviewed here
only for their impact on the pinned profile path. The profile scalar emission
sites are unchanged. Their container/chain semantics are excluded from this
review. No general discovery expansion or new theory obligation was introduced.

The handoff at `/tmp/acgn-profile-handoff-final/profile-handoff.json` has the
user-supplied SHA-256
`75a5472afccf953d830e9b195ad9262b58280c232eaa743c3ac7d1ffec1dbcce`.
`final-handoff-binding/binding.json` mechanically matches its executable and
production-source hashes against both E and the final live files. The only
area-file difference after E's snapshot is the notes' closing development
evidence section, reviewed separately at its final hash below. That does not
rewrite E's execution root or turn it into full final closure evidence.
Unrelated container replay/test changes are retained in E's
`live-input-changes.json` and are outside this review.

Final handoff hashes (SHA-256):

| Input | Hash |
| --- | --- |
| `SemanticProfileWire.lean` | `9dfdba739dc2196d9a4f411fd2dc7a68bef3f47c70a664c76f27329bd5f7085e` |
| `SemanticProfileWireRegressionTest.java` | `6f0c272a46f1aa545d59573af2b26da2f62902c3288f9c8c4a40c45a8af6d4cc` |
| `SemanticProfileWireExtractor.java` | `44aecf329cd3fdaee863cb174f70a8466a95e4eaf01ccb7a5d740137a2c976d8` |
| `fourth_profile_replays.py` | `0fdff3097788e84732613b6f322c0db8f59f8595d87122af91880e98e7df9f31` |
| `test_fourth_profile_replays.py` | `69115c1966309828845082e96c15d50bae7c3246be4849aaadf36bb9fc8b4a2c` |
| Final `profile-notes.md` | `ce420dee19878451b3d3beff4978d3cfd626d47aae91fa49792e958d78272f7e` |
| `CertificateBundleWriter.java` | `0bfba740161bb7495aae130b25999b5255662a3aef0f1fae3b0751b3d42636c5` |
| `SemanticEvidenceVerifier.java` | `ec63aab42919edce0483dd93b50b755da5cedcb8dedc184291f3dfd08a238037` |

Final evidence hashes (paths relative to E):

| Evidence | SHA-256 |
| --- | --- |
| `review-results.json` | `b0159c5803d29d09683a59d4179972eba5a2c1166ceee27f56e0c76f6350ea69` |
| `commands.json` | `b72d61a6289c06a27c49a9cae6333400b5f5959958ddc4dd73950245541c5a62` |
| `final-handoff-binding/binding.json` | `418752e7fb3c5121fd4e7066506a40366386be023eaf24a03fc13368c5303c56` |
| `observed/semantic-profile-wire.tsv` | `3a8c31ae86135775f65d57a05e6cfbc80c8d4084d4388410698ffc518785cfa7` |
| `observed/semantic-profile-wire-source.tsv` | `5914004e7bde24f2829e142823548de27341b9fae25f15177c6927d7e71dd539` |
| `formal/SemanticProfileWireReplay.lean` | `305a8ec58fff72bee0222f08e0d6b4d64ae573f0a6e2f87dad951f84fb3ea31b` |
| `general-proof.log` | `440af12e4a8a41990629a4334a5e720496bf1927e74dec8826d0a1819e9cc747` |
| `observed-replay.log` | `f0c90bb534525293a855bb24e8a4e53630da2f31601ea665b50cf6bf4b595f14` |
| `public-original-certificates.log` | `f3ba4704f9f4b538eec7a0729ba328a56c9aa5e266c0d472829cf3bbd963d2af` |

Initial C evidence and the intervening D evidence remain preserved. All
concrete profile findings are resolved on the final bounded reviewed surface;
unlisted claims, whole-corpus behavior, release readiness, and full fourth-five
closure remain outside this disposition.
