# Phase 5 Audit: Source-Rule Guards

> Historical review verdicts and check counts below are retained as repair
> provenance only. They are not current evidence or ballots under the
> closed-world gate in `adversarial-review-protocol.md`.

## Status

PRELIMINARY MAXIMUM-ADVERSARIAL REVIEW: **FAIL**. THE ABLATION-BINDING AND
COMPOUND-EMPTY COUNTEREXAMPLES ARE REPAIRED, AND THE CERTIFICATE CLAIM IS NOW
EXPLICITLY NORMALIZED-IR-ONLY. COMPLETE PARSER/IR/IMPLEMENTATION REFINEMENT AND
AN IMMUTABLE FIVE-REVIEW BALLOT REMAIN BLOCKED.

## Implemented Obligations

- `set none` and `lone none` retain one quantified relation binding, whose
  value may be the empty relation. They are not treated as empty admissible
  binding sets.
- Bare Alloy declaration domains default to multiplicity `one`; explicit
  `set`, `lone`, `some`, `one`, and exact wrappers override that default in the
  `QuantiVar` tuple.
- Empty-domain elimination fires only for a statically empty positive
  multiplicity such as `one none` or `some none`.
- Relational subset uses `none in none = true`. An arbitrary value in `none`
  remains unreduced unless the value has a static nonemptiness proof.
- Guarded subset and empty-cardinality smart rules run before the
  certificate-facing matrix snapshot. A/C/I normalization remains after that
  boundary and still requires independent law evidence.
- Four set-difference factorizations, relational JOIN distribution over union,
  and `some`/`no` distribution over union are oriented before the normalized-IR
  snapshot. Every rule requires exact relation-family evidence; no analogous
  JOIN/cardinality rule is admitted for intersection.
- Relation-valued binder profiles retain every parser-derived column. Their
  certified descriptor and repair tuple therefore distinguish unary from
  higher-arity relation parameters even when their primitive labels coincide.
- An enclosing complement or self-difference rule may compare its operands in
  that certified ACI quotient before the snapshot. The comparison is read-only,
  composes slot maps, and cannot cross an operator, type, profile, or law-policy
  boundary.
- `AlloyRewriteSystem` reconstructs a scoped binding environment only from
  actual predicate/quantifier declaration nodes. It honors declaration order
  and shadowing; bare/`one`/`some` bindings authorize guarded membership while
  `set`/`lone` and synthetic unary labels do not.
- Standalone certificate replay begins at the normalized typed IR supplied to
  the exact graph. Raw Alloy identifiers and hashes are provenance metadata;
  they do not certify the parser-to-normal-form rewrite.

## Alloy-Backed Regression Boundary

`AlloySourceRuleRegressionTest` compiles selected higher-order declarations,
asks Alloy/Kodkod to prove `not (none in none)` unsatisfiable, and checks the
resulting MASG, `QuantiVar`, fast matrix, and certified observation. Kodkod
support is command-specific: the adversarial census observed eight translated
higher-order commands and four unsupported commands. Each result must retain
its own solver or parser/type/lowering authority; a uniform-refusal statement
is false.

## Fault And Contradiction Log

| ID | Located fault | Repair | Regression |
| --- | --- | --- | --- |
| P5-F01 | Prenexing treated every syntactic `none` domain as empty, including `set none` and `lone none`. | Distinguish the admissible binding set using the encoded cardinality. | `setSome` and `loneSome` retain `SET`/`LONE` bindings and do not collapse to false. |
| P5-F02 | `x in none` collapsed to false without proving `x` nonempty, and `none in none` was consequently wrong. | Require an explicit nonempty wrapper for the false rule and handle the empty subset first. | Core and ablation saturation tests cover unknown, empty, and statically nonempty left operands. |
| P5-F03 | Local smart rules ran only after `certificationMatrixEGraphRoot` was cloned, so the fast observation said `true` while the certified observation retained raw `IN`. | Add a narrow guarded source-rule pass before the certificate snapshot; leave algebraic normalization after it. | Certified observations agree with truth/false fixtures for all source-rule cases. |
| P5-F04 | A bare declaration domain was assigned `Cardinality.SET`, contradicting Alloy's default `one` multiplicity and making `some x: none` appear inhabited. | Default bare declaration domains to `Cardinality.ONE`; preserve explicit `set`. | `one Person` equals bare `Person`, differs from `set Person`, and bare `none` has an empty binding set. |
| P5-F05 | The first regression attempted to use Kodkod as a higher-order relation-quantifier evaluator and failed with `HigherOrderDeclException`. | Keep direct Alloy solving for the first-order subset fact; use Alloy parsing/typechecking plus lowering assertions for higher-order declarations. | The regression reports only the authority each check actually has. |
| P5-F06 | Guarded membership ignored the `QuantiVar` cardinality tuple and therefore could not distinguish certified positive multiplicity from nullable bindings. | Resolve variables through alpha, De Bruijn, and source aliases and accept only `ONE` or `SOME` binding evidence. | Parsed bare/one/some bindings fold; set/lone controls remain relational membership. |
| P5-F07 | `NOT_IN` did not mirror the independently justified `IN` empty/universal rules. | Add the exact relational duals with the same nonemptiness guard. | `none`, `univ`, and typed/nonempty controls agree across fast, certified, and ablation paths. |
| P5-F08 | A child rewrite could expose `NOT true/false` after the certification snapshot, leaving certified and fast matrices inconsistent. | Close Boolean negation after guarded child rewriting and before the snapshot. | `not(lone none)` and `not(one none)` have equal certified/fast constants and zero repair distance. |
| P5-F09 | Generic unary opcode labels such as synthetic `SOME(S)` could counterfeit a static nonemptiness proof. | Limit nonemptiness authority to authenticated positive binding tuples; remove opcode-label inference. | Manually typed synthetic labels retain `IN`/`NOT_IN` and do not become constants. |
| P5-F10 | `EGraphSaturationTest` still required an unbacked synthetic `ONE(S)` label to prove nonemptiness. | Change the test to require the term to remain `IN`. | The core saturation suite passes on the repaired authority boundary. |
| P5-F11 | `EGraphAblationTest` repeated the same label-as-proof assumption in all arms. | Require the synthetic term to remain distinct from false. | All five ablation arms pass the corrected source-rule regression. |
| P5-F12 | The audit claimed a green bounded gate before the two stale assertions were corrected. | Preserve the failed review here and report the gate only after remediation. | The complete bounded Java gate now passes, including 61 source-rule checks. |
| P5-F13 | `AlloyRewriteSystem` had no authenticated binding environment, so all five ablation engines failed `x in none`/`x not in none` for bare-`ONE` and explicit-`SOME` parameters. | Reconstruct lexical binding authority from declaration structure on every pass, preserving declaration order and nested shadowing; never infer authority from an operand label. | The original external probe now passes all 60 checks; permanent five-arm tests cover positive, nullable, synthetic, and shadowed bindings. |
| P5-F14 | `some x: one (none & A) | no none` is valid Alloy and Kodkod reports UNSAT, but the certified adapter rejected the unreduced intersection as unsupported associativity. | Prove a positive-cardinality binding domain empty when its relation expression has an explicitly empty intersection operand, before creating the binding or matrix constraint. | Alloy/Kodkod and the certified pipeline now agree; the source-rule suite includes the exact predicate. |
| P5-F15 | The audit described higher-order Kodkod support as a uniform refusal. | Correct authority per command and never promote parser/type evidence to SAT evidence. | Independent census observed `translated=8`, `unsupported=4`; public audit text is command-specific. |
| P5-F16 | Export provenance can hash raw Alloy, but certificate events begin at normalized IR and contain zero source-rewrite derivations. | Narrow certificate claims and the verification API contract to normalized typed IR; classify caller-supplied raw bytes only as provenance. Raw-source conformance remains a separate formal/implementation obligation. | Boundary probe records one `INSERT_FRESH` and zero source-rewrite events; the API and public matrix now state that exact boundary. |
| P5-B17 | Deterministic formal/refinement mappings are incomplete for nested alias resolution, the quantifier truth table, pipeline ordering, and cross-engine authority propagation. | **Unrepaired blocker:** complete claim-by-claim Lean and Java/provenance conformance obligations. | Independent Lean proves the finite semantic core but intentionally does not certify Java refinement. |
| P5-F18 | Parser-derived `none`/`univ` reach baseline terms as `SIG` atoms, and unary `some/one/no/lone none` had no baseline reductions; the original binding probe therefore remained false even after scoped authority was added. | Normalize only the fixed built-in `none`/`univ` atoms and apply the four exact empty-relation cardinality facts. Arbitrary signatures remain untouched. | Parser-backed probe moves from 20 failures to 0; `AlloySourceRuleRegressionTest=95` and `EGraphAblationTest` pass. |
| P5-F19 | Certified ACI equality reordered temporal siblings, but the repair projection numbered phases in source order; the guarded kernel therefore rejected an equivalent swapped conjunction at distance 6. | Derive one phase order from expanded certified matrix keys, preserve ordered and binary-temporal roles, and apply the same reindexing to temporal references and binder owners. | The original two-phase probe now reports SAT counterexample `false`, certified equality `true`, and guarded distance `0`; permanent two- and three-phase regressions raise `AlloySourceRuleRegressionTest` to 234 checks. |
| P5-F20 | The fixed `univ` atom was treated as unconditional evidence that its carrier is inhabited. At bitwidth zero, the universe can be empty, so that authority made `univ in none` collapse unsoundly. | Remove `univ` from the static-nonemptiness authority set. Retain only authenticated `ONE` or `SOME` binding tuples; membership *in* `univ` remains the independently valid subset law. | `Phase5SourceRules.lean` proves the empty-universe countermodel, and the parser-backed bitwidth-zero regression leaves `univ in none` guarded. |
| P5-F21 | `EXACTLYOF` was modeled as a positive multiplicity. Alloy's translator instead checks equality between the bound relation and its declared domain, so `exactly none` admits the binding `none` and proves no nonemptiness. | Model EXACTLY-of as domain equality, exclude it from empty-domain and nonemptiness rules in Fast Rewrite and all ablation paths, and retain its tuple tag without interpreting it as `some`. | The independent Lean model counts one EXACTLY-of binding over the empty relation; a direct Fast Rewrite fixture and all five ablation arms reject the forged nonemptiness fold, raising the source-rule gate to 253 checks. |
| P5-F22 | Higher-order source-rule checks had no per-command authority ledger, so mixed translator support was collapsed into prose and one aggregate pass count. | Record every selected parser command in source order as translated SAT, translated UNSAT, or parser/type/lowering-only for the exact higher-order-skolemization limitation; propagate every unrelated translator error. | Twelve command records are emitted and checked: eight translated results and four unsupported results. The expanded source-rule gate passes 299 checks, and Lean proves classification and ledger cardinality. |
| P5-F23 | Repair projection indexed predicate parameters and prenexed quantifiers by their raw source spelling as well as their scope-qualified identities. A parameter `x:A` and nested `some x:B` therefore collided after implication expansion, even though Alloy proved the source equivalent to `no B`. Same-carrier shadowing could instead have silently selected the wrong binding. | Treat original spellings as presentation metadata only. Projection authority is indexed by the alpha-renamed identity and rooted De Bruijn key; local binders retain their separate certified occurrence maps. The regression backtranslator derives predicate parameter names from the same prepared object rather than rewriting alpha text. | Parser-backed implication, IFF, outer-use, and same-carrier shadowing predicates all prepare and remain source-equivalent through scope 3. `PrenexAciScheduling.lean` proves that different scope paths separate equal spellings, and the source-rule gate passes 332 checks. Full parser-to-Java refinement remains part of P5-14. |
| P5-F24 | The pre-snapshot complement and self-difference checks could not consume an inner certified ACI equality. Thus `some (S + S) or not (some S)` retained a different producer observation even though post-snapshot repair saturation reduced it to `true`; a Boolean ACI singleton over bound slots had the same fault. | Compare operands through a read-only typed/profile-indexed ACI quotient, composing invocation maps and exposing a certified singleton before duality. Keep distinct operators and non-ACI containers explicit. | Parser-backed associativity, commutativity, idempotence, self-difference, bound-slot, and operator-near-miss cases pass in `CanonicalAlloyPipelineTest`; `Phase5SourceRules.lean` proves relation ACI and equality transport into the two enclosing laws. |
| P5-F25 | An initial probe treated exact `INT` result carrier as proof that parser `PLUS`/`MINUS` meant integer arithmetic. | Superseded by P5-F30 after direct solver reconstruction showed that source `+`/`-` remain relational union/difference over the unary `Int` relation. | The transient result-sort relabeling was removed; this row records the refuted diagnosis rather than an active rule. |
| P5-F26 | Nested same-operator binary children rewound a shared MASG visit counter, producing an empty or conflated later occurrence. | Keep the saved parent visit only for parent edges and retain monotonically allocated visits for every nested source occurrence. | Repeated and distinct nested-union equality plus distinct-operand near-miss checks pass. |
| P5-F27 | Complement comparison honored dual opcodes but not the certified commutative quotient of `EQUALS`/`NOT_EQUALS`. | Compare composed child keys as a multiset only when both dual policies certify commutativity. | Swapped equality, bound-slot, and near-miss parser fixtures pass. |
| P5-F28 | Relational union flattening stopped at a nested parser result carrier even when its alternatives were a proved subfamily of the outer carrier. | Permit only parser-authenticated, same-module, same-arity `PLUS` widening and keep explicit coercions to the outer carrier. | Heterogeneous nested grouping canonicalizes identically while a missing-operand near miss remains distinct. |
| P5-F29 | Nested formula or expression ITE children rewound the shared ITE visit counter, publishing an empty or conflated occurrence that failed fixed arity admission. | Keep the saved parent visit for parent edges without changing any nested occurrence's monotonically allocated visit. | A nested Boolean ITE equals its direct branch expansion at distance zero; a nested expression ITE prepares successfully; Lean proves all three nested branch visits distinct. |
| P5-F30 | Exact carrier `INT` was used to relabel valid parser `PLUS`/`MINUS` as arithmetic, although Alloy assigns those source forms relational set semantics. The first correction left the expanded trusted carrier policy under the prior digest. | Select relational versus arithmetic policy from the parser opcode identity. Permit `INT` as the element carrier only inside the certified relational Set schema for `PLUS`/`INTERSECT`; do not globally classify it as a relation family. Bind that policy to registry v3 in producer and independent verifier. | Direct Alloy SAT checks distinguish union from arithmetic, parser-backed producer/repair checks retain idempotence and self-difference, an independent-verifier bundle reconstructs Set-over-One(Int) PLUS authority, and Lean pins identity-directed lowering independently of result carrier. |
| P5-F31 | Distinct equal-valued literal nodes shared one structural-equality visit counter, so the second literal requested nonexistent exact-type visit 2. | Index lowering occurrence visits by MASG node identity; continue advancing only when traversal revisits the same shared parser node. | Genuine repeated and nested `fun/add`/`fun/sub` sources prepare with exact Int literals and the correct non-idempotent arithmetic policy; Lean proves distinct occurrence identities do not share the first occurrence's visit. |
| P5-F32 | `Type.is_int()` was used as an exclusivity test, so valid `Int + S` lost its signature alternative and crossed the public certificate boundary as Int-only relational union. | Classify exact Int only when the complete parser product family is the singleton unary built-in Int product; otherwise preserve all same-arity relation alternatives. | Parser-backed exact-type and pipeline checks distinguish `Int + S = Int` from `Int = Int`, direct Alloy SAT supplies a nonempty-`S` witness, a fresh exported bundle retains the heterogeneous carrier, and Lean proves the bounded exclusivity predicate. |
| P5-F33 | Nested PLUS widening compared exact alternatives literally, so `C extends P` blocked flattening of `(Int + C) + P` while the opposite grouping flattened. | For the identical parser module, cover every correlated nested product by one outer product column-by-column through authenticated ancestry; retain all other barriers. | Alloy proves the source pair associative; producer observations and repair distance now agree at zero. Reverse ancestry, siblings, arity mismatch, and unrelated types reject, and Lean checks the same bounded coverage relation. |
| P5-F34 | Authenticated `univ` identities and three remaining difference identities were visible only to post-snapshot saturation, so certified equality disagreed with zero-cost repair normalization. | Apply `R & univ -> R`, `R + univ -> univ`, `R - none -> R`, `R - univ -> none`, and `none - R -> none` in the guarded pre-snapshot pass and mirror them during saturation, using only the internal built-in authority. | Direct Alloy checks prove all five identities; parser-backed producer observations now compare equal at repair distance zero; forged-name controls retain the existing fail-closed boundary; Lean proves the five relation equations. |
| P5-F35 | Parser-certified subrelations were not absorbed by a union containing their full parent signature. The initial repair checked the wrong signature-leaf metatype and then failed to commit same-length contextual replacements across association. | Apply `R subset C => R + C = C` only when `C` is the actual parser-authenticated full user-signature leaf. Recurse through the same relational `PLUS` region to derive `(X + R) + C = X + C` without flattening unrelated source structure. Never treat an arbitrary expression whose result type is `C` as the full carrier. | Alloy proves `extends`, subset-`in`, composite typed-subrelation, and reassociated witnesses. Producer equality and repair distance are zero for those laws; sibling-only, unrelated, and typed-field-as-carrier controls remain distinct. Lean proves direct and contextual subset absorption. |
| P5-F36 | Exact primitive types erased the declaration boundary needed to prove `Y subset X` when both `Y` and `X` were subset signatures below `P`. | Carry live parser signature authority on actual nullary signature leaves. Use declaration-DAG ancestry for a named subset carrier and reserve exact-family containment for a primitive carrier whose source name matches its exact type. | Alloy proves `Y + X = X`; producer equality and distance are zero through transitive nesting and grouping. Sibling and typed-expression controls remain distinct, and Lean proves subset transitivity. |
| P5-F37 | `none in R` and its negation were reduced only when `R` was syntactically `none`, omitting the general empty-subset law. | Recognize only authority-bearing `none`, require exact relation arity agreement, then derive `none in R -> true` and `none not in R -> false` before certification and in saturation. | Unary and binary source witnesses agree with Boolean constants at distance zero; direct Alloy and Lean checks prove both polarities. |
| P5-F38 | Empty expressions headed by plain relational `ARROW` or `JOIN` were not first reduced to an exact-arity empty relation, blocking downstream empty-subset normalization. | For an exact relation-valued plain `ARROW` or `JOIN`, an authenticated empty operand annihilates the expression and preserves the result's exact arity. | `(none -> none) in R`, `none not in R`, and `some none.(A -> B)` regressions match Alloy; Lean proves product and composition annihilation. |
| P5-F39 | The first named-carrier repair delegated to `Sig.isSameOrDescendentOf`, which treats `X in A+B` as a descendant of either branch and is not sufficient evidence for `X subset A`. | Reconstruct declaration containment: unique primitive-parent recursion, all-parent recursion for subset signatures, same-module identity, and cycle rejection. | `X` is absorbed under the common ancestor of `A` and `B`, remains distinct from `A` alone, Alloy finds the latter countermodel, and Lean proves the all-branches condition plus its single-branch counterexample. |
| P5-F40 | Complete direct children of an Alloy `abstract` signature were retained as a union rather than identified with their declared carrier. | Recursively discharge every direct parser-owned `extends` branch, reject empty or partial covers and subset-`in` substitutes, require every extra operand below the carrier, and select the highest covered abstract ancestor. | Direct, nested, singleton, and contextual-cover pairs have certified equality and zero distance. Missing branches, ordinary parents, and subset-only abstract declarations remain distinct; Alloy SAT/UNSAT witnesses and Lean proofs cover each boundary. |
| P5-F41 | Alloy's reserved relational identity was represented as ordinary constant metadata, so JOIN could neither consume it safely nor distinguish it from a forged spelling. | Give parser-produced `iden` a non-public authority kind; preserve it only across trusted exact clones; eliminate only that authority-bearing leaf from exact relational JOIN chains. | Left, right, middle, and all-identity pipeline pairs compare at zero; a non-identity JOIN remains distinct; Alloy and Lean prove the two-sided law; a public `ConstSymbol` cannot mint the authority. |
| P5-F42 | Exact relation transpose remained structural across involution and binary Cartesian product. | Reduce double transpose only on exact binary relations and reverse the two children of a plain binary ARROW while retaining the parser-derived exact output type. | Parser-backed producer equality and distance agree; wrong product order remains distinct; Alloy and Lean independently prove involution and product reversal. |
| P5-F43 | Nested closure operators retained redundant layers and therefore missed four fixed-point laws. | Collapse exact binary CLOSURE/RCLOSURE pairs; use RCLOSURE if either layer is reflexive, otherwise CLOSURE. | All four closure combinations compare at zero, `^R` versus `*R` remains nonzero, direct Alloy finds the negative witness, and Lean derives the laws from finite positive paths. |
| P5-F44 | Enum cover behavior was inherited indirectly from abstract-signature handling but absent from explicit end-to-end coverage. | Add a parser enum, compare its complete atom union with the enum carrier, and pin the schema in solver and Lean checks. | The producer reports certified equality and zero distance; Alloy has no counterexample; no broader implementation rule was introduced. |
| P5-F45 | The all-operands-below-carrier barrier for abstract-cover reconstruction was implemented but had no direct unrelated-member regression, allowing a helper-local review to mistake a proposed carrier for an admitted rewrite. | Keep the existing terminal containment check and add an end-to-end negative pair with a complete abstract cover plus an unrelated signature. | Production retains positive distance, direct Alloy exhibits an outside atom, and Lean proves the general outside-member obstruction. |
| P5-F46 | Complete abstract covers did not compose through Cartesian product, leaving a union of product branches distinct from the equivalent product carrier. | Lift exactly one complete cover coordinate across plain binary ARROW terms with identical slot-free remaining coordinates; derive the result type from parser-authenticated factor products. | Left/right-coordinate pairs compare at zero; diagonal and unrelated alternatives remain nonzero; Alloy and Lean prove the positive laws and full-grid boundary. |
| P5-F47 | Parser-authorized `iden` was consumed as JOIN identity but not as a fixed point of transpose or either closure. | Reduce `~iden`, `^iden`, and `*iden` only when reserved identity authority and identical exact occurrence types agree. | All three parser pairs compare at zero; direct Alloy and Lean prove the fixed points. |
| P5-F48 | Abstract-cover lifting stopped at binary products and could not reconstruct a carrier across more than one varying product coordinate. | Flatten plain binary ARROW trees only inside the proof analysis, reduce a complete cover within one fixed-coordinate subgroup, recursively normalize the strictly smaller residual union, then rebuild binary dependent-chain syntax with an exact parser product proof at every node. | Ternary and complete two-coordinate-grid pairs compare at zero. Diagonal, three-cell partial-grid, unrelated, slot-bearing, and modified-ARROW barriers remain. Alloy proves the positive laws and supplies the missing-cell witnesses; Lean explicitly constructs all four grid cells and refutes the partial grid. |
| P5-F49 | A valid product cover with fixed `Int` threw because the exact product constructor treated scalar `Kind.INT` as never denoting Alloy's unary integer set. | Preserve `Kind.INT` for operator overloading, attach transient module authority only in `fromParser`, and admit that authenticated occurrence as exactly one `Int` product column. | `(A->Int)+(B->Int)` equals `Parent->Int` at zero distance. Synthetic, serialized, and foreign-module Int factors reject; relational `PLUS` and arithmetic `IPLUS` remain separate; Alloy and Lean prove the set law. |
| P5-F50 | Relational converse did not distribute through union, intersection, or difference before the certified snapshot. | For exact parser-authenticated binary relations, derive each operand's reversed correlated type and preserve the original relational container while pushing converse to its operands; recurse through nested admitted containers and plain products. | All three parser-backed pairs compare at zero, as do composition through a product union and a quantified relation slot. The container operators remain distinct, synthetic/serialized type authority rejects, direct Alloy supplies UNSAT laws and SAT witnesses, and Lean proves the homomorphisms. |
| P5-F51 | Cartesian-product factoring required an abstract-cover carrier, omitting ordinary distributivity already proved by the formal model. | Retain an authenticated coordinate `PLUS` when no smaller subtype/abstract carrier exists; factor one fixed-coordinate subgroup at a time and require a complete Cartesian grid for simultaneous coordinate reduction. | Left/right binary, ternary, and complete-grid pairs compare at zero. Partial and diagonal grids remain positive; exact union proofs reject mixed modules, missing authority, and arity mismatch; Alloy and Lean cover both sides. |
| P5-F52 | Product factoring rejected all bound-slot factors, despite the distribution law being parametric in arbitrary relation values. | Carry composed `EClassRef` invocation maps through product flattening, coordinate comparison, subgroup factoring, residual union construction, and ordered binary ARROW rebuilding. | Bound-set fixed-left, fixed-right, and complete-grid pairs compare at certified distance zero; a partial grid remains positive; Alloy and Lean check the parametric law and boundary. |
| P5-F53 | Boolean and relational lattice absorption/distribution were absent; the first repair also copied the outer exact type onto derived intermediates. | Factor expanded dual containers and project absorption only under certified operator authority. Derive every relational intermediate's exact union/intersection type from its own parser-authenticated operands, preserving correlated ancestry and authenticated empty arity. | All eight dual laws pass over overlapping set parameters, exact subtype/disjoint/empty cases agree, synthetic and cross-module proofs reject, direct Alloy is UNSAT for every law, and Lean proves the pointwise lattice schema. |
| P5-F54 | Exact intersection returned immediately after an empty prefix and skipped authority checks on trailing operands. | Validate module authority, kind, and arity for every operand before using the final overlap, even when an earlier intersection is already empty. | Empty-then-foreign and serialized-tail regressions reject; valid authenticated empty overlap remains admitted; Lean separates semantic emptiness from the all-operands authority predicate. |
| P5-F55 | The quotient knew `R + C = C` only for unary signature carriers and omitted the dual `R & C = R`, even when parser declaration or correlated exact type proved `R in C`. The first generalized repair also let two mutually containing composite operands erase each other before ACI deduplication. | Admit both directions only for a parser-authenticated full signature or a closed primitive-signature union/product. Use declaration-DAG evidence for subset-signature carriers, exact correlated ancestry for primitive/composite carriers, and a stable tie-break that retains one representative under mutual containment. | `A+B+P` equals `P`; `A&P`, `X&P`, `H.f&P`, and `H.r&(P->P)` equal their subrelations. Same-typed fields, sibling subset signatures, and proper sub-products remain distinct. Repeated `(S+T)` operands remain complete. Alloy provides five UNSAT laws and four SAT/nonvacuity controls; Lean proves both carrier laws and the same-static-type countermodel. |
| P5-F56 / DISCOVERY | Round-65 review exposed five omitted semantic families: four common-operand difference factorizations, two-sided JOIN distribution over union, and the `some`/`no` union laws. | Orient the four difference identities toward one common occurrence, factor JOIN only when exact adjacent boundary types agree, and expand union cardinality into Boolean ACI form. Keep JOIN and cardinality intersection analogues outside the rule set. | Eight parser pairs compare at certified distance zero; eight Alloy checks are UNSAT, two near-miss witnesses are SAT, and `Phase5SourceRules.lean` proves every admitted schema. |
| P5-F57 | A valid predicate parameter `r: U -> U` retained only the unary primitive carrier in its `QuantiVar`, while its parser occurrence had exact binary type. Certified binder construction therefore rejected valid JOIN-distribution input as a stored/parser family mismatch. | Preserve every nonempty parser relation arity in binding evidence, include that exact relation profile in slot identity, and use the same profile in certified binder lowering and repair projection. | Binary relation parameters now prepare and compare through both JOIN laws. Lean proves unary/binary profile separation, and the pipeline zero-kernel guard remains green. |
| P5-F58 | The first cardinality implementation oriented Boolean disjunction/conjunction back into `some/no` over union. That direction concealed Boolean operands from existing absorption and complement rules. The replacement expansion initially retained duplicate union terms and installed derived Boolean authority before child insertion revoked it. | Orient only `some/no` union toward Boolean ACI, deduplicate relation invocations before expansion, install authority after all child mutations, and recognize a finite covered opposite-polarity branch under the same certified Boolean container policy. | Existing Boolean absorption, direct complement, ACI complement, duplicate-union, and bound-slot tests pass together with the new cardinality laws; Lean proves arbitrary finite covered-dual absorption. |
| P5-F59 | The old nested-union test used `some ((S+T)+C)` both to test cardinality semantics and to require a surviving normalized-IR `PLUS` splice. Once the independently valid cardinality expansion erased that normalized operator, the provenance assertion became inapplicable. | Separate the obligations: cardinality has dedicated semantic pairs, while the exact associative-splice fixture places the nested union in equality where `PLUS` remains at the normalized-IR certificate boundary. Do not weaken flat-construction validation. | `CanonicalAlloyPipelineTest` again requires an exact nested `PLUS` splice and separately proves the `some/no` laws; the complete suite passes 1,202 checks. |
| P5-F60 | The content-sensitive traceability gate found that broad P5-09 still named the removed `removeSubrelationsCoveredByFullSignatures` helper after carrier normalization was generalized. | Map P5-09 to the live `removeSubrelationsCoveredByFullCarriers` implementation boundary without changing the claim or its incomplete refinement status. | The current traceability census has 173 exact rows and no missing Java declaration; its remaining diagnostics are explicit partial/refuted statuses. |
| P5-F61 / DISCOVERY | Valid Alloy source gives `((A-B)-C)=A-(B+C)`, but the production quotient retained distance 5; left and right nesting are not interchangeable. | Repeatedly orient only a left-nested `MINUS` into one `MINUS` whose removal side is a parser-certified relational union. Preserve composed slot invocations, exact source-module authority, and the outer result type; do not apply this schema to a right child. | Binary and four-level pairs compare at certified distance zero, left and right nesting remain distinct, Alloy reports UNSAT for the law and SAT for the nesting distinction and nonvacuity control, synthetic exact evidence is rejected, and Lean proves both boundaries. |
| P5-F62 / DISCOVERY | Round-67 falsification found the separate sound law `A-(B-C)=(A-B)+(A&C)` at production distance 9. | Expand only a right-nested `MINUS` into the union of the retained difference and restored intersection. Derive every relation intermediate from live parser evidence and normalize the generated branches recursively so overlap with the left-chain rule converges. | The parser pair compares at certified distance zero, Alloy reports UNSAT for the law, left/right nesting remains distinct, synthetic exact evidence rejects, and Lean proves the expansion. |
| P5-F63 / DISCOVERY | Round-67 falsification found `(A-B)&C=(A&C)-B` at production distance 4; the same omission covered intersections of multiple differences. | In one certified `INTERSECT`, collect every difference branch's kept operand with all ordinary operands and collect every removed operand by union, yielding `(intersection kept)-(union removed)`. Preserve composed slot invocations and exact parser type evidence. | Single- and two-difference parser pairs compare at certified distance zero, Alloy reports UNSAT for both laws, synthetic evidence rejects, and Lean proves both schemas. |
| P5-F64 / DISCOVERY | A Round-68 Luna reviewer found that Cartesian product difference was omitted: `(A->C)-(B->C)=(A-B)->C` and `A->B-A->C=A->(B-C)`. Independent minimization generalized the omission to any one changing coordinate in an equal-length product chain; changing two coordinates at once is unsound. The same audit found missing permanent coverage for overlapping and three-branch difference normalization. | Factor a `MINUS` of two parser-certified plain products only when exactly one composed coordinate invocation differs. Build that coordinate's relational difference from live same-module, same-arity exact evidence, then rebuild the ordered ARROW chain. Keep every multi-coordinate case explicit. Extend difference coverage through overlapping right/left nesting and three extracted branches. | Left, right, and ternary-middle product pairs compare at certified distance zero; the multi-coordinate near miss remains distinct and SAT. Five admitted Alloy assertions are UNSAT, exact-type authority barriers reject, and Lean proves all three positive coordinate forms, the two-coordinate countermodel, and three-branch extraction. |
| P5-F65 / DISCOVERY | Round-69 falsification found `(A->C)&(B->D)=(A&B)->(C&D)` and its fixed-coordinate, n-ary, and ternary forms absent from the quotient. | In an authenticated relational `INTERSECT`, collect every compatible equal-length plain-product subgroup, intersect every coordinate across all subgroup members, rebuild the ordered ARROW product, and leave nonproduct residual operands in the outer intersection. | Five parser pairs compare at certified distance zero, five Alloy assertions are UNSAT, an authenticated-empty factor retains product arity, synthetic/cross-module/arity barriers reject, and Lean proves binary, three-source, and ternary coordinatewise schemas. |
| P5-F66 / FAIL | The generalized n-ary regression exposed a pre-existing supported-path crash: an ARROW operand such as `a&b&c` was nested in the retained pre-ACI source but flat in the frozen repair tree, so exact dependent-chain commitment transfer rejected semantically certified ACI normalization. | Retain the certificate's exact pre-ACI occurrence commitment. Bind the frozen repair occurrence with a second exact commitment only after matching lineage, path, ordered chain structure, exact types, composed slot maps, and a parser-authorized ACI quotient key for operand containers. Mutation checks replay the exact repair commitment; no general structural weakening is admitted. | The formerly crashing n-ary ARROW pair now prepares, projects, and compares at zero distance inside the full 1,300-check pipeline suite. Same-typed source swaps and post-certification mutation still reject. Lean proves product/JOIN congruence from the independently proved ACI operand equalities. |
| P5-F67 / DISCOVERY | The first post-repair audit found that permanent transfer coverage exercised only n-ary intersection under ARROW; union order, idempotent duplicates, and the JOIN context named by P5-30 were not pinned end to end. | Add parser-backed ARROW duplicate elimination plus JOIN union reordering, union duplicate elimination, and intersection reordering. Each case must prepare both source forms, compare at certified distance zero, and pass a direct Alloy assertion. | All four pairs and four Alloy checks pass in the 1,300-check pipeline suite. The temporary source probe additionally covered guarded JOIN reassociation. No implementation broadening was needed; this closes the located coverage opportunity on changed bytes pending fresh review. |
| P5-F68 / DISCOVERY | Both Round-71 Luna reviewers found that converse of relational composition was omitted: `~(r.s)=(~s).(~r)`. Preserving the source operand order is unsound. | Reverse the complete ordered JOIN operand sequence and transpose every operand only when live parser evidence proves exact binary relation boundaries. Preserve JOIN as a sequence and derive the exact result type independently. | The parser pair compares at certified distance zero; the direct Alloy assertion is UNSAT; wrong-order Alloy and Lean witnesses remain distinct; synthetic exact types cannot authorize the rule. |
| P5-F69 / DISCOVERY | Both Round-71 Luna reviewers found omitted domain/range restriction algebra, including distribution in either coordinate, nested restriction intersection, opposite-side commutation, and complete union-grid factoring. A first subgroup-only implementation did not converge on a four-cell grid because generated relation-lattice nodes lacked the parser operator identity used by parsed nodes. | Treat a restriction as two authenticated coordinates. Apply union/intersection coordinate algebra only to complete coordinate covers, factor difference only when one coordinate changes, normalize repeated and opposite-side restrictions, flatten only parser-certified same lattice invocations, and assign every derived relation operator its canonical source identity. Preserve correlated exact types and composed slots throughout. | Seventeen parser pairs and Alloy assertions close; a complete grid reaches one representative; diagonal union, two-coordinate difference, and wrong converse order remain SAT/distinct; synthetic, serialized, cross-module, and nonunary evidence rejects. Standalone Lean proves every admitted schema and all three decisive countermodels. |
| P5-F70 / FAIL | Round-72 review found that union distribution in a flattened JOIN chain worked only at the terminal coordinate. Valid middle and initial-coordinate inputs such as `r.s.t + r.u.t` and `r.s.t + u.s.t` threw while transferring the dependent source certificate after the first factorization. | Close the guarded JOIN rewrite recursively to a fixed point before the certificate-facing matrix is cloned. Every step still requires parser-authenticated exact boundary types and preserves the ordered JOIN sequence and composed slot invocations. | Right, middle, and initial-coordinate parser pairs all compare at certified distance zero; three direct Alloy assertions are UNSAT; the existing intersection and synthetic-authority barriers remain distinct; Lean proves associativity and arbitrary-middle-context distribution. |
| P5-F71 / DISCOVERY | Round-72 review found that converse over transitive and reflexive-transitive closure remained structural even though `~(^r)=^(~r)` and `~(*r)=*(~r)`. Reflexive closure exposes a wider exact `univ->univ` result than its operand and therefore cannot reuse the operand's profile as the result proof. | Reverse finite positive paths extensionally and retain the original closure opcode. Derive the converse result from the closure occurrence's exact profile, then build the transposed operand under the same parser authority. | Both parser pairs compare at certified distance zero; both Alloy assertions are UNSAT; a closure-kind-changing witness is SAT/distinct; synthetic exact types do not authorize the rule; Lean proves positive-path reversal and both closure theorems. |
| P5-F72 / DISCOVERY | Round-72 review found the omitted converse/restriction coordinate permutation: `~(A<:r)=(~r):>A` and `~(r:>A)=A<:(~r)`. | Decompose only parser-authenticated restrictions, transpose the relation coordinate, swap DOMAIN and RANGE, retain the restrictor invocation, and derive the exact result independently. | Both parser pairs compare at certified distance zero; both Alloy assertions are UNSAT; an unswapped restriction witness is SAT/distinct; synthetic exact types do not authorize the rule; Lean proves both transpose/restriction identities. |
| P5-F73 / DISCOVERY | Main-agent Round-73 falsification found that restriction omitted its full-carrier identities and empty zeros. `univ<:r`, `U<:r`, `r:>univ`, and `r:>U` retained positive distance from `r`; an authenticated empty restrictor or empty relation coordinate likewise remained structural. | Recognize built-in `univ` or a closed parser-certified primitive carrier only when restriction typing leaves the relation occurrence unchanged. If either authenticated coordinate is empty, derive an arity-preserving empty result. Do not treat a relation-valued variable, subset-erased carrier, or synthetic spelling as full. | Ten parser pairs, including quantified relation-slot forms, compare at certified distance zero and ten Alloy assertions are UNSAT; a variable-carrier inequality is SAT; synthetic and forged authority controls remain explicit; Lean proves both identities and all four zero schemas. |
| P5-F74 / DISCOVERY | Inspection of an interrupted Round-73 review exposed a typed-empty lead. Independent probes confirmed that converse and transitive closure of `none->none` are empty but retained positive distance; reflexive-transitive closure must instead be `iden`. | Give exact arity-bearing empty relations a dedicated unary normalization: TRANSPOSE and CLOSURE derive authenticated empty, while RCLOSURE derives authenticated `iden` with the source result profile. | Three parser pairs compare at certified distance zero and three Alloy assertions are UNSAT; `*empty != empty` is SAT; a forged `none` spelling cannot authorize the rule; Lean proves empty converse and both closure results from the absence of positive paths. |
| P5-F75 / DISCOVERY | Main-agent Round-74 falsification found that sound restrictions remained structural across composition: `(D<:R).S`, `R.(S:>C)`, and `(R:>M).S` had distances 6, 6, and 5 from their equivalent endpoint-lifted or boundary-transferred forms. | Orient endpoint guards outward and an internal guard onto the right adjacent operand. Admit a step only after independently deriving the source JOIN, every constructed restriction/JOIN type, the exact result occurrence, and composed slot invocation under one parser authority. | Eight parser-backed pairs close at certified distance zero across long chains, ternary relations, combined restrictions, adjacent guard intersection, and quantified relation slots; seven Alloy assertions are UNSAT; wrong-side and wrong-guard witnesses are SAT/distinct; synthetic exact types do not authorize the rule; Lean proves the three local schemas and existing associativity lifts them through a chain. |
| P5-F76 / FAIL | Adversarial self-review of the first P5-F75 repair found that it treated a restriction on a unary JOIN operand as an output endpoint guard. That coordinate is instead the existentially eliminated JOIN boundary; two SAT-inequivalent pairs were falsely assigned distance zero. | Require arity at least two before lifting a left-domain or right-range endpoint. For a unary operand, preserve the guard on the shared boundary and orient it as a domain restriction on the right adjacent operand, just like any internal boundary guard. | Both false zeroes reopen at distances 4 and 5; the two sound unary-boundary pairs close at zero; two direct Alloy equalities are UNSAT and two wrong endpoint-lift witnesses are SAT; Lean models unary-left and unary-right composition separately and proves guard placement. |
| P5-F77 / DISCOVERY | Main-agent Round-75 review found that direct domain and range restriction remained distinct for a unary relation even though both are pointwise intersection. The exact producer reported distance 3 for `A<:r` versus `r:>A`. | Canonically orient an authenticated unary RANGE restriction to DOMAIN after exact restriction-type derivation. Do not apply the orientation to arity two or greater, and do not accept synthetic exact-type labels as parser authority. | The unary parser pair closes at distance zero and its Alloy assertion is UNSAT; a binary-side inequality is SAT/distinct; a synthetic unary RANGE remains structural; Lean proves unary equality and a binary countermodel. |
| P5-F78 / DISCOVERY | Main-agent Round-75 review found that `r in r`, `r = r`, `r not in r`, and `r != r` remained structural despite being respectively true, true, false, and false. The exact producer assigned distance 3 to every minimized pair. | Fold only parser-authenticated relation comparisons whose two invocations are identical in the certified operand quotient. Accept ACI-derived identity only through its existing certified container key; keep distinct invocations, ordered-container changes, and synthetic relation labels explicit. | Four parser pairs close at distance zero and four direct Alloy assertions are UNSAT; a distinct-subset case remains nonzero and has a SAT negation witness; synthetic authority cannot trigger the fold; Lean proves reflexivity and transports all four results across certified relation equality. |
| P5-F79 / DISCOVERY | Main-agent Round-77 review found five missing subset/lattice schemas: union injection, intersection and difference projection, union-on-the-left subset expansion, and intersection-on-the-right subset expansion. Exact distances were 5, 5, 5, 6, and 6 although direct Alloy checks were UNSAT. | Add a parser-authenticated, equal-arity structural subset proof closed recursively through union, intersection, difference, and restriction, plus complete Cartesian expansion of left unions and right intersections. Explicit NOT_IN uses the De Morgan dual. Do not distribute subset over a union on the right or an intersection on the left. | Eleven parser pairs, including restrictions, explicit negations, and n-ary containers, close at zero; eleven Alloy assertions are UNSAT; two opposite-disjunction cases remain nonzero with SAT witnesses; synthetic exact types cannot authorize either proof or expansion; Lean proves the schemas and a decisive disjunction countermodel. |

## Current Bounded Re-review

A fresh read-only review of the pre-P5-F22 candidate returned
`PASS-for-bounded-scope` for five explicitly bounded obligations. Its report is
`/tmp/acgn-p5-rereview-20260821.md`, SHA-256
`b73d29b11bf43eea10ec67608436ffdc9c0e57134f1d02e0e12b87519a54dcbd`.
It passed 253 source-rule checks, nine SAT falsification queries, schema-v8
writer/replay, and 76 metric-refinement checks over 11 Lean vectors. This is
not an immutable phase ballot. P5-F22 changed the candidate afterward and
therefore requires fresh review; complete alias/De Bruijn and cross-engine
refinement remain open under P5-14 and P5-11.

A later replacement review returned **FAIL** on the P5-F23 counterexample and
also identified a stale generated assurance catalog. Its report is
`/tmp/acgn-phase5-replacement-review-20260821.md`, SHA-256
`0effc5df4d35b810ddf3a612b58d4d9665cd8706c3bae6bf3360242398aca0d9e`.
That verdict remains part of the audit record. The exact counterexample is now
permanent, the focused suite passes 332 checks, and the scope-separation Lean
theorems compile; a new independent replacement review is required before any
bounded PASS can be recorded. The generated assurance catalog must also be
regenerated only after the traceability matrix stabilizes.

The fresh scope-shadow replacement review subsequently returned **PASS for the
explicitly exercised bounded Phase 5 scope**. Its report is
`/tmp/acgn-phase5-shadow-replacement-review-20260821.md`, SHA-256
`28f1003f59a5a1d367d86c4ad0a11f5d33c1c2db11a54a8c67f3836993184c1d`.
It compiled 296 producer sources, passed the 332-check source suite, 182
instrumented saturation assertions, and 264 ablation assertions. An independent
matrix covered 11 shadowing forms at scopes 1 through 4 with 44 UNSAT
source-versus-normalized checks and 60 projection checks. Four mutants were
killed, including source-first same-carrier capture and incorrect implication
and IFF polarity. The review explicitly excludes universal JVM refinement,
malformed internal IR, exhaustive scopes, corpus reruns, and artifact closure.

The independent semantic core is recorded in
`formal/Phase5SourceRules.lean`. It proves the finite relation, multiplicity,
empty-domain, subset, duality, compound-empty, and authority rules without
importing producer code. It also proves that the existing graph trace-kind
vocabulary cannot encode a raw-source rewrite. This is partial formal evidence,
not a Java/parser refinement proof.

## Independent Review 2

Verdict: **FAIL**. The reviewer confirmed P5-F06 through P5-F09 were repaired
and found no remaining label-based inference in `NormalForm`, `EGraphNode`, or
`AlloyRewriteSystem`. It correctly rejected closure because two repository
tests still demanded the removed behavior and this document therefore stated
a stale gate result. P5-F10 through P5-F12 record those contradictions.

## Remediation After Review 2

Both stale assertions now express the fail-closed rule. The complete bounded
gate passes `AlloySourceRuleRegressionTest=61`,
`CanonicalAlloyPipelineTest=251`, core saturation, all ablation arms, and
backtranslation. A fresh independent review remains pending.

## Independent Review 3

Historical verdict: **PASS under the superseded protocol**. The fresh read-only reviewer passed 61 source-rule checks,
34 saturation cases, 229 ablation assertions across five engines, 251 pipeline
checks, four backtranslation cases, and independent `123 + 40` check probes.
Synthetic `ONE`/`SOME` labels cannot prove nonemptiness; authenticated
`QuantiVar` cardinalities `ONE` and `SOME` can, while `SET`, `LONE`, and
`EXACTLY` remain guarded. No repository file was changed by that reviewer. Its
closure statement is invalid under the controlling protocol and is refuted by
P5-B13 through P5-B17.

That closure statement records the earlier review protocol. It is superseded
by `adversarial-review-protocol.md`; current closure requires five fresh
unanimous reviews of one unchanged snapshot.

## Review Gate

No five-review ballot may start until every blocker above is repaired on one
immutable snapshot. Each fresh reviewer must independently probe all five source forms, negated
variants, bare versus explicit multiplicities, the pre-certificate boundary,
and consistency between the fast, certified, and ablation rewrite paths.
