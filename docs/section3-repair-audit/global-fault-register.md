# Global Maximum-Adversarial Fault Register

## Status

`INCOMPLETE`. This register preserves findings from preliminary reviews over a
moving worktree. It is not a phase review, a requirements traceability matrix,
or evidence that a repair refines its formal contract. The former unbounded
review protocol was superseded by `do178c-assurance-plan.md` after its reviews
failed to terminate. The source census frozen under
`/tmp/acgn-global-claim-census` enumerated 112,314 over-approximated claim
records in 598 files and assigned no `PASS`. Its report and contradiction
digests were respectively
`1de5875ab4e6a94d2b6657c2873227ac04bd002b052c33b1bff4dbd9bf99ed3f`
and
`d3f62c5ae5609a5c826d1e5362eb359c9f0fd5231e1f1e6f2968de49053982e8`.

Every listed repair requires a fresh immutable-snapshot retest. `REPAIRED`
below means only that the concrete counterexample no longer reproduces on the
current moving worktree.

## Located Global Contradictions

| ID | Fault or contradiction | Current disposition |
| --- | --- | --- |
| GC-F01 | The worktree changed while preliminary evidence was collected. | Expected during repair; blocks every ballot until an immutable snapshot is minted. |
| GC-F02 | `docs/theory-artifact-matrix.md` claimed all 125 rows were `EXACT` while the controlling ledger and live counterexamples said otherwise. | `BLOCKED`; the public claim must be narrowed or regenerated only from closed obligations. |
| GC-F03 | Green producer-authored tests were cited as completeness evidence although independent counterexamples existed outside their fixtures. | `BLOCKED`; tests are bounded conformance evidence only. |
| GC-F04 | Compound-empty quantified domains were mishandled during the review. | Repaired on moving bytes; complete parser/IR/refinement evidence remains blocked. |
| GC-F05 | Five ablation engines lost declaration-derived nonemptiness authority. | Repaired on moving bytes; complete cross-engine refinement remains blocked. |
| GC-F06 | Raw Alloy source-to-normalized-kernel rewrites have no independently replayable semantic derivation; a source hash is only provenance. | `BLOCKED`; certificate claims must stay at normalized typed IR unless a source proof family is added. |
| GC-F07 | A missing local declaration domain was fabricated as `univ` with multiplicity `SET`; parser `let y = none | y` could also become `univ`. | Repaired on moving bytes: malformed local domains reject and a statically empty `let` remains `none`. Formal-to-Java refinement remains blocked. |
| GC-F08 | Local binder and typed symmetry groups eagerly retained all elements, including 5,040 elements for `S7`, despite streaming claims. | `REPAIRED/PENDING FRESH REVIEW`: production local groups now use deterministic stabilizer-chain traversal. Ninety-six `S4` subgroup differentials agree with an independent materialized oracle and `S7` emits exactly 5,040 elements with retained stabilizer state smaller than the orbit. Complete Java heap/refinement proof remains open. |
| GC-F09 | Canonical-orbit wire evidence omitted the selected witness used to break equal-shape ties. | The selected witness is now serialized and checked, but producer/verifier candidate-set and ordering equivalence (P6-12) and PAIR observation ownership (P6-13) remain blocked. |
| GC-F10 | The production writer rejects real collision and nontrivial rebuild histories while public text described standalone DTO fixtures as end-to-end producer coverage. | `BLOCKED`; either implement the producer path or narrow the claim. |
| GC-F11 | CALL edge validators compared source spelling while certificates claimed complete qualified callee identity; a later audit found the direct code-generator validator still made that comparison after MASG/IR had been repaired. | Repaired on moving bytes at all three boundaries: callee targets carry and compare source spelling, qualified identity, kind, arity, and authority, and both canonical preparation and direct generation reject the same-spelling/different-identity mutation. Full boundary refinement remains blocked. |
| GC-F12 | Two protected reproducibility terminal scripts differed from the repair base. | Repaired: both scripts are byte-identical to `8ec25613`; future audit wiring must use a new nonprotected entry point. |
| GC-F13 | Historical publication aggregates were presented near current implementation claims without a current immutable compatible run. | `BLOCKED`; historical results may be labeled only as historical until hash-bound recomputation. |
| GC-F14 | Compiled abstract Lean contracts were used as if they established Java, wire, verifier, or provenance refinement. | `BLOCKED`; every implementation claim needs its own formal contract and independent conformance relation. |
| GC-F15 | A java.base-only standalone verifier and positive fixtures were treated as complete semantic independence evidence. | `BLOCKED`; dependency isolation is necessary but not sufficient. |
| GC-F16 | Root-qualified binder identity was introduced under schema v5, silently changing v5 meaning. | Repaired on moving bytes by exclusive schema v6 admission and v5 rejection; fresh wire review remains required. |
| GC-F17 | The semantic-evidence mutation harness described argument four as a generic binder bundle although it requires exactly one binder-occurrence record; valid law-free binder fixtures therefore failed inside the mutation helper. | Repaired on moving bytes by naming the one-record precondition in the CLI contract and reporting the section tag and observed cardinality on misuse. The intended `bind-block-symmetric-a.acgncert` invocation passes 60 checks; this remains bounded harness evidence, not a ballot. |
| GC-F18 | The earlier semantic-profile review observed different certificate bytes across two JVM exports of the same width-specific source. | The exact probe no longer reproduces on current moving bytes: two fresh JVM runs produced width-4 SHA-256 `ea66193ced257d250a97a56d300e1044b60616a435af85fea0851963696a7cc7` and width-5 SHA-256 `140957055303112d0941ff3707fd4e6b0fffbd1de7d0aad59a005e992e3635e2` in both runs, and both `cmp` checks passed. This is one bounded nonreproduction, not the general P6-08 serialization proof. |
| GC-F19 | The universal claim census plus repeated exhaustive-review protocol had no finite stopping rule; four fresh reviewers remained running without completing their assigned whole-phase searches. | `CLOSED AS PROCESS DEFECT`: all four reviews were terminated and the protocol was superseded by the finite DO-178C Level A-inspired assurance plan. Lean remains mandatory for every scoped claim; executable testing and independent review now have explicit finite bounds and `PASS`/`FAIL`/`INCOMPLETE` outcomes. |
| GC-F20 | Existing prose and abstract proof names did not provide a mechanically checked claim-to-Lean/code/test mapping. | `OPEN`: the first matrix found 142 Section 3 requirements; self-audit then added the 12 assurance-process claims, yielding exactly 154 rows. An ordered scope manifest now binds every ID, class, and claim hash, and anti-counterfeit tests reject comment/string-only declarations and malformed scope substitutions. No existing proof or test is grandfathered; formal-type binding, execution reachability, coverage, and complete evidence mapping remain open. |
| GC-F21 | One bounded review evidence file appended pseudo-path records such as `TREE compiled-source-classes files=630` to `sha256sum` syntax, so `sha256sum -c` rejected the manifest; the same report also recorded a 142-requirement runtime result while later hashing a 154-row matrix. | `OPEN REVIEW-EVIDENCE DEFECT`: ordinary file hashes were independently checked and the findings remain preliminary, but this report cannot be promoted to A-12 evidence. The bounded runner must use a versioned manifest grammar and bind runtime logs to the exact input digest. |
| GC-F22 | P3-01 universally demanded recoverable ordered columns for statically empty relations, contradicting P3-15's correct rule that only positive arity is recoverable without inventing erased columns. | `FIXED-AWAITING-RETEST`: P3-01 now requires preservation of all parser-proved information and explicitly distinguishes recoverable columns from arity-only empty evidence. Its claim hash and formal mapping are invalidated pending fresh proof review. |
| GC-F23 | M-05 claimed Zhang-Shasha-style ordered tree edit distance, but the temporal recurrence deleted/inserted whole subtrees. The bounded counterexample `R(A(X))` versus `R(X)` costs 1 with internal-node deletion and child promotion, while the implementation returned 2. | `REPAIRED/PENDING ASSURANCE`: both metric paths now share a Zhang-Shasha postorder/leftmost/keyroot implementation. Lean proves the ordered-forest recurrence and unit promotion witnesses; 49 bounded pairs agree with an independent exhaustive ordered-mapping oracle. Structural coverage and immutable-snapshot review remain open. |
| GC-F24 | P4-01 claimed a nonempty collision bucket for every valid canonical shape, including shapes not stored by any live leader. A valid nullary shape queried in a quiescent empty graph had no owner or bucket. | `FORMAL REPAIR AWAITING RETEST/IMPLEMENTATION PARTIAL`: P4-01 now quantifies over supported-operation-reachable quiescent observations of stored live-leader shapes. Review `ca9b9e84b4210749e5ea448d4c26997947b06499c58bf6667a20fad1466f6f67` exposed a reflexive list-order determinism theorem. That theorem has been replaced by extensional duplicate-insensitive membership, permutation-independent canonical enumeration, supported mutation/rebuild closure, and exact public-observation proofs. Fresh independent proof review, Java transition refinement, structural coverage, and immutable rerun remain mandatory. |
| GC-F25 | P5 built-ins were recognized by case-insensitive source spelling, so legal user signatures `None` and `Univ` were treated as Alloy `none` and `univ`; solver-backed probes produced unsound equivalences in fast and certified paths. | `REPAIRED/PENDING ASSURANCE`: raw AST terms now recognize only exact reserved spellings, while Fast Rewrite IR carries a parser-derived `SigSymbol` identity through projection and theory adaptation. The original solver-backed probe now reports `rawEquivalent=false`, `canonicalEquivalent=false`, and all formerly unsound equality flags false. Lean proves exact-reserved/nonreserved disjointness; structural coverage and immutable review remain open. |
| GC-F26 | P5-12 did not recognize the statically empty domain `one (A - A)`; the Alloy predicate is unsatisfiable, but canonicalization retained one binding and remained distance 6 from false. | `REPAIRED/PENDING ASSURANCE`: self-difference now reduces to authenticated `none` in raw rewriting, Fast Rewrite saturation, and pre-prenex domain analysis. Lean proves `R - R = none` and absence of a positive-one binding. The original probe now reports `equivalentToFalse=true`, distance 0, and zero bindings. The finite static proof family and structural coverage remain open. |
| GC-F27 | P3-16's fixed semantic profile aliases source commands and execution options: different integer widths and modular versus overflow-forbidding semantics can share a profile. | `REFUTED/PARTIALLY REPAIRED`: fresh bounded review `d8949a6320ef75b44c261797f5124093d862a62c62f966e85a404e442f1c44bf` found explicit width 0 aliased to 4 and caller-forgeable authority; both are repaired with parser-module ownership, package-private authority, exact omitted-versus-zero width handling, Lean obligations, and 31 parser-backed checks. Production defaults still bypass selection, the A4Options semantic partition/cache inventory are open, and source-bound bundle profiles cannot be replayed by the standalone verifier. |
| GC-F28 | P6-02 and P6-10 claimed non-retaining streamed symmetry traversal, but local binder and typed symmetry groups retained complete closure lists and the verifier retained all candidates. | `REPAIRED/PENDING FRESH REVIEW`: the claims now exclude the deliberately exhaustive differential oracle; production local traversal uses a stabilizer chain and verifier replay keeps one running minimum. Universal heap/refinement evidence remains open. |
| GC-F29 | P5-10 requires command-specific translated-versus-parser-only authority, but the source-rule regression emitted only one aggregate result and no unsupported-command records. | `REPAIRED/PENDING IMMUTABLE REVIEW`: the regression now emits and checks one source-ordered record for each of 12 commands, distinguishing eight translated SAT/UNSAT results from four exact higher-order-skolemization limitations; unexpected translation errors propagate. Lean proves outcome classification and one-record-per-command cardinality. |
| GC-F30 | A generic inhabited-carrier escape allowed quantifiers to cross a relational-ITE condition, and a later prenex pass interpreted an earlier generated implication's antecedent with negated polarity. SAT4J found both semantic counterexamples; the latter also made conjunction order change the canonical observation. | `REPAIRED/PENDING IMMUTABLE REVIEW`: witness lifting now requires an admissible connective boundary, and every generated branch connective is eliminated to NNF before another prenex pass. The permanent suite checks both counterexamples, branch order, and bounded SAT equivalence (186 checks). |
| GC-F31 | `MASGVisitorTypeRegressionTest` demanded a matrix binding with synthetic carrier `univ` even though the binder correctly remained local below an unsafe conjunction and unconditional lifting is false over Alloy's admitted empty universe. Documentation repeated the unsafe premise. | `REPAIRED/PENDING IMMUTABLE REVIEW`: the stale assertion now separates directly liftable and locally retained obligations, checks exact `File` type provenance, proves bounded source equivalence, and requires both proposed unconditional lifts to have zero-scope counterexamples. `GuardedPrenex.lean` proves the required inhabitation premise and empty-carrier obstruction; the dead synthetic-`univ` producer switch was removed. |
| GC-F32 | Prenex slot minimization observed the parser's binary association and branch order before ACI normalization. In a three-branch conjunction or disjunction, an empty-domain-enabling prefix could be discovered only after a reusable sibling had remained local, so equivalent permutations acquired different slot counts and distances 2 or 3. | `REPAIRED/PENDING IMMUTABLE REVIEW`: maximal same-headed `AND`/`OR` regions are association-flattened without reordering or idempotence; allocation visits same-carrier empty-domain-enabling branches first while retaining source operand positions. The expanded permanent suite passes 332 checks, the six-pair adversary reports equality and distance zero, and `PrenexAciScheduling.lean` proves the two prefix-order laws plus schedule membership/length conservation. This is partial evidence for P5-14, not closure of that broader claim. |
| GC-F33 | The certified observation quotiented conjunction order across temporal siblings, while repair projection assigned phase numbers from source traversal. Swapping `always (all x:S | no x.r)` and `eventually (some y:S | some y.r)` therefore preserved semantics but produced temporal, quantifier, and matrix costs of 2 each and triggered the certified-kernel guard. | `REPAIRED/PENDING IMMUTABLE REVIEW`: repair projection now orders temporal occurrences by expanded certified matrix keys and applies one coherent reindexing to topology, references, and binding owners; ordered containers and binary temporal branch roles are not sorted. The original SAT-backed probe reports no counterexample and guarded distance zero, permanent two-/three-phase tests pass within 234 source-rule checks, and `TemporalAciPhasePresentation.lean` proves the exact bounded permutation obligation. General Java refinement, equal-key cases, larger phase sets, and fresh independent review remain open. |
| GC-F34 | Phase 4 producer snapshots accepted absent or contradictory intermediate parent paths, hidden empty-ledger mutations, orphan histories/retirements, and live invocations of absent classes; the trace sink and standalone verifier also failed to bind union revision effects and rebuild events to one closed interval. | `REPAIRED/PENDING FRESH REVIEW`: snapshot construction now cross-validates complete topology and ledgers, producer and standalone trace machines require one closed rebuild interval, twelve new producer/verifier attacks are permanent, and the schema-v8 protected harness remains at census `1/2/0`. |
| GC-F35 | The Phase 4 Lean wire relation accepted arbitrary dirty queues and projected away revision, status, parent paths, reverse uses, histories, and rebuild boundaries. | `FORMAL MODEL EXTENDED/PENDING REFINEMENT REVIEW`: exact bounded transition frames and a deterministic interval automaton now reject the known countermodels and compile without admissions. No mechanized Java/JVM refinement is claimed; executable conformance and a fresh statement review remain mandatory. |
| GC-F36 | A2-08 and two phase reports still claimed a name-authenticated `PARAMETER_SINGLETON` typing rule after the v4 producer and verifier had removed it and explicitly rejected name-based authority. | `REPAIRED/PENDING FRESH REVIEW`: the live claim, formal enumeration, and phase reports now admit only exact-relation and primitive-singleton rules; the failed v3 approach remains labeled as historical fault provenance. |
| GC-F37 | The Phase 4 rebuild regression survived a mutant in which `rebuildHashConsExactly()` performed no rebuild, so the cited test did not establish that the required transition occurred. | `REPAIRED/PASSED BOUNDED REPLACEMENT REVIEW`: rebuild now clears the derived index, reconstructs it from authoritative live records, and checks exact owner buckets before returning. A fresh read-only review killed both counter-only and empty-body mutants on the full and isolated interface-change paths. Universal Java/JVM refinement and immutable phase closure remain open. |
| GC-F38 | Three Phase 5 traceability rows cited Java string literals or test descriptions as declarations, and one row encoded two Lean paths in a field that admits exactly one file. The semantic tests passed, but those rows could not be mechanically linked to their evidence. | `REPAIRED/PENDING FRESH REVIEW`: every Java anchor now names a real declaration; the EXACTLY-none binding witness lives in the scheduling Lean module; the global checker reports no malformed P5 reference. This repairs evidence integrity only and does not strengthen the declared partial semantic refinements. |
| GC-F39 | Repair projection treated raw source aliases as global binding/type authority after implication or IFF expansion and prenexing. A predicate parameter `x:A` and a nested `x:B` either threw a cross-type collision or, for equal carriers, risked silent capture. | `REPAIRED/PASSED BOUNDED REPLACEMENT REVIEW`: parameter, matrix, and inherited bindings are indexed only by alpha-renamed and rooted De Bruijn identities; source spellings remain presentation metadata. A fresh review passed 11 shadowing variants, 44 bounded solver comparisons, 60 projection checks, and killed four alias/polarity mutants including same-carrier silent capture. Complete parser/JVM refinement remains open under P5-14. |
| GC-F40 | The assurance matrix left global and metric claims as generated `MISSING` rows even though bounded formal, implementation, and falsification evidence existed, while two populated rows cited nonexistent Java declarations. | `REPAIRED AS EVIDENCE-INTEGRITY DEFECT`: all 154 rows now resolve to real files and declarations. Global/metric rows retain `PARTIAL`, `DIRECT-BOUNDED`, or `REFUTED` status where their semantic obligations remain open; mapping completion is not claim closure. |
| GC-F41 | The audit overview presented obsolete current-facing ready-row and diagnostic counts without binding them to the matrix snapshot that produced them. | `REPAIRED`: the overview no longer duplicates volatile counts; only generated, input-bound reports may state them. Historical counts remain permissible when explicitly labeled and hash-bound. |
| GC-F42 | `run_section3_assurance.sh` invoked schema-v8 mutation fixtures at obsolete root paths and omitted the dedicated Phase 6 producer/verifier ordering regression, making the claimed assurance entry point fail before those checks. | `REPAIRED/BOUNDED RERUN COMPLETE`: fixture paths now name `schema-v8-coverage`, the parent-path fixture is explicit, and both Phase 6 order tests plus concrete metric refinement are registered. Development Run E executed all 57 steps with zero executable failures. Its `INCOMPLETE` result and 168 open traceability diagnostics remain binding; runner repair is not claim closure. |
| GC-F43 | P6-10 claimed that certificate export and verifier replay retained no complete orbit, but the wire intentionally carried a complete free-renaming proof ledger and standalone replay retained bounded completeness/group ledgers. | `CLAIM REPAIRED/PENDING FRESH REVIEW`: P6-10 now states only the production candidate-term retention property. Complete certificate proof ledgers, verifier completeness ledgers, and the exhaustive oracle are explicit exclusions rather than hidden counterexamples. Production traversal still retains only one candidate minimum and bounded traversal state. |
| GC-F44 | Permanent Phase 6 tests did not kill removal of the equal-shape witness tie-break, and a first-candidate verifier was exposed only by a temporary six-permutation `S3` family. | `REPAIRED/PENDING FRESH REVIEW`: the six producer bundles are permanent and verifier tests now exercise them all; a direct equal-term identity-versus-swap comparison covers both directions of the witness tie. Complete producer/verifier candidate-set refinement remains partial. |
| GC-F45 | The repair metric discarded minimum quantifier-edit correspondences and then guessed unmatched variables by equal coordinate. Parameter declarations were not charged, so a real `S`-to-`T` parameter change produced false zero and 210 of 1,521 bounded typed cases disagreed with an independent oracle. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: the scalar quantifier result is replaced by streamed enumeration of all minimum edit plans; only an injective paid modification or explicit positional-parameter diagonal selected by that plan may pre-seed matrix alignment; parameters are compared as one ordered declaration sequence; the unconditional coordinate fallback is removed. Source-level distances are now `1` for type-only and `2` for type-plus-body witnesses. |
| GC-F46 | Hungarian `int` potentials wrapped an exact assignment cost of `4,294,967,294` to `-2`; sequence and dimension additions also lacked checked boundaries. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: both certified and Fast Rewrite assignment implementations use checked `long` internals and one `Math.toIntExact` API boundary; malformed/negative matrices reject and unsupported totals fail closed. A permanent 210-matrix exhaustive differential and the overflow witness pass. |
| GC-F47 | M-06 described ordinary node insertion/deletion with child promotion, while the established Fast Rewrite matrix recurrence inserts or deletes complete operands; `A(X)` to `X` therefore costs two, not one. | `REQUIREMENT CONTRADICTION REPAIRED`: the claim now names complete operand-subtree edits and preserves the established metric semantics. Lean and Java pin the wrapper/child witness at cost two. General recurrence refinement remains partial. |
| GC-F48 | Exact scope/alpha searches materialized scope-alignment products and exposed unbounded factorial work without cancellation or a declared failure contract. | `PARTIALLY REPAIRED`: scope products and quantifier edit-plan products stream; configurable checked bounds on quantifier, scope, and alpha alignments throw `ResourceLimitException` before a partial minimum can be returned. Algorithmic improvement and complete supported-domain analysis remain open. |
| GC-F49 | The first correspondence repair carried only paid tuple modifications. For `[T] -> [S,T]`, the unchanged `T` parameter moved from ordinal zero to one but had no selected correspondence, so both insertion and deletion charged an erroneous matrix edit. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: every diagonal for the ordered positional-parameter component now carries its exact binding pair, including zero-cost diagonals; quantified declarations remain governed by paid changes plus certified scope alignment. A separate oracle enumerates all tied minimum edit plans for 1,156 `S`/`T` parameter-reference cases through length three. |
| GC-F50 | Matrix variable updates inspected only the left binding status. A same-spelled free variable compared with a bound variable could cost zero in one direction and one in the reverse direction. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: bound/free status is compared before either alpha mapping or lexical spelling; both directions cost one matrix update, and Lean pins the decisive bound/free case. |
| GC-F51 | The first post-repair Phase 6 mutation run still failed to detect producer witness-tie removal, standalone verifier first-candidate retention, and complete candidate-term materialization. | `TEST REPAIRED/PENDING FRESH REVIEW`: production and verifier expose package-scoped comparator/minimum probes; tests exercise both candidate orders and both tie directions. `BestCandidate` is structurally required to retain exactly one `LeastOption` field, which detects the concrete complete-list mutant. General candidate-set refinement and PAIR ownership remain explicitly partial. |
| GC-F52 / P0 | The full-corpus augmenter reached the legal subtype-overlap JOIN `Product.(Component->Position)`, but the adapter promoted the dependent theory's literal boundary mismatch into a fatal malformed-source error. The bounded smoke sample contained no such source. An initial fixed-binary fallback removed the crash but failed the Phase-A2 requirement to retain a dependent `Seq` when exact subtype evidence exists. The first five-reviewer round then found an underconstrained Lean wire predicate, an ancestry-forging public factory, unbound rule/endpoints, admitted `univ`, contradictory parent-ledger possibilities, an unauthenticated standalone hierarchy claim, incompatible v8 wire reuse, and an undocumented serialized-cache break. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: theory v5 retains live parser-positioned direct-parent ancestry and certificate-binds a closed endpoint correspondence; schema v9 replays one acyclic single-parent ledger and rejects all earlier schemas. The production case is `RIGHT_SUBTYPE_OF_LEFT` with witness `Component -> Product`; no endpoint is widened to `univ`, unrelated signatures remain unsupported, and ancestry is isolated from general canonical carrier identity. Lean now binds the rule, direction, endpoints, every direct-parent step, ordered `Seq`, and exact fold. Nonexact subtype bundles are structurally checked but remain `UNCHECKABLE` without an external source-hierarchy authority; malformed evidence rejects. `ExactAlloyType` stream v3 invalidates ancestry-free caches, which must be reparsed. |
| GC-F53 / P0 | A fresh authority review showed that `ExactAlloyType.from(Type)` accepted a parent hierarchy built with public synthetic `PrimSig` constructors, and Java serialization preserved the same nontrivial ancestry metadata. The producer-side extraction route therefore did not itself distinguish a live parser declaration from caller-fabricated type objects. | `SUPERSEDED BY GC-F54`: source-position and transient-authority checks closed the original constructor and serialization witnesses, but transferable parser attributes exposed a stronger forgery. |
| GC-F54 / P0 | Parser `Attr` values are publicly readable. A caller could transplant a genuine positioned `SUBSIG` attribute onto a synthetic `PrimSig`, defeating the first source-position-only repair while remaining inside ordinary Java APIs. | `IMPLEMENTATION REPAIRED/PENDING WHOLLY FRESH REVIEW`: every nontrivial producer path now requires identity membership in the exact final `CompModule` supplied to `MASGVisitor` plus source-positioned parent edges. Module-free conversion, synthetic objects, transferred attributes, and deserialized ancestry reject; self-only relation construction remains available. Lean models the independent module-object ledger and serialization loss. Standalone nonexact subtype evidence remains `UNCHECKABLE`, and complete parser/JVM refinement remains open. |
| GC-F55 / P0 | The first fresh module-identity review failed because the Lean Phase-A2 fold admitted unary dependent JOINs, did not connect the unary-interior JOIN guard or parser authority to the fold, assumed rather than executable-checked the encoded result, and omitted schema-v9 standalone outcomes. Its object ledger also did not bind identities to nominal labels, and its JOIN step could derive the unsupported nullary relation. | `SUPERSEDED BY GC-F56`: executable producer/structural folds and identity-to-label checks repaired those witnesses, but the first replacement still represented source positions as self-supplied flags and omitted fixed metadata from the slice outcome. |
| GC-F56 / P0 | The second fresh formal review found that the bounded exact-chain outcome ignored the fixed theory digest, module object IDs were not required to be injective, source positions were self-supplied Boolean flags rather than module-ledger declarations, and wire replay assumed whole-record equality. The theorem name also risked conflating acceptance of the dependent-chain evidence slice with whole-bundle `VERIFIED`. | `FORMAL MODEL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the module snapshot now has unique identity-to-label entries and positioned child/parent declarations; path admission replays both ledgers and rejects duplicate IDs, swapped labels, missing declarations, unknown positions, transfer, and serialization. Producer evidence no longer supplies position flags. The exact theory version and digest are executable checks. Chain evidence outcomes are explicitly slice-local, and full field commitment replay is an executable field-by-field Boolean checker rather than an assumed record equality. The finite Alloy witness model and Java/parser refinement remain explicitly partial and cannot support a general whole-verifier claim. |
| GC-F57 / P0 | A fresh formal preflight found that the module snapshot checked ownership and parent functionality but not global acyclicity, so a selected valid edge could coexist with a reverse edge. It also found that each variadic JOIN boundary carried its own snapshot and the fold did not require one common authority ledger. | `FORMAL MODEL REPAIRED/PENDING WHOLLY FRESH REVIEW`: snapshot well-formedness now follows every module-owned functional parent chain with bounded fuel and rejects cycles, including disconnected ones. The variadic producer fold requires structural equality of every boundary authority snapshot before consuming any path. Permanent Lean witnesses reject the two-node cycle and the three-operand mixed-module JOIN. Java already receives one final `CompModule` per `MASGVisitor`; the formal model now expresses that same chain-level invariant. |
| GC-F61 / P0 | The authenticated single-product subtype stack could not preserve correlation in relation-family unions and did not expose the complete Cartesian alternative-pair obligation for JOIN or ARROW. Reusing schema v9 would also have reinterpreted old dependent-chain bytes. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: dependent theory v7 uses a normalized finite DAG of correlated ordered products, complete row-major pair decisions, ordered duplicate-preserving `Seq`, and parser-authenticated nominal paths. UNION absorbs only authenticated componentwise subtypes; INTERSECTION and JOIN omit only authenticated disjoint branches; `univ` is terminal but grants no overlap authority. Schema v10 commits every DAG and pair decision, while v9 rejects. Standalone Lean and bounded producer/verifier mutation tests pass; arbitrary Java/parser refinement remains explicitly partial. |
| GC-F58 / P0 | The first full five-reviewer Ballot 3 review found that legal Alloy sequence types were rejected because the built-in `seq/Int -> Int` edge has no user-source filename. | `IMPLEMENTATION REPAIRED/PENDING WHOLLY FRESH REVIEW`: parser authority admits exactly the identity pair `Sig.SEQIDX -> Sig.SIGINT` through a closed built-in rule; every user-declared non-`univ` parent edge still requires module membership and a known parser position. Same-label synthetic objects reject. Lean models source and sequence-index provenance as disjoint constructors. |
| GC-F59 / P0 | Ballot 3 also found that module identity was checked while extracting each exact type and then discarded, allowing separately parsed modules with identical labels and parent paths to be mixed in one chain. Pairwise checks could additionally forget an earlier authority after JOIN consumed its last surviving column. | `IMPLEMENTATION REPAIRED/PENDING WHOLLY FRESH REVIEW`: `ExactAlloyType` retains the originating `CompModule` only as a transient identity capability. It is excluded from equality, stable keys, hashes, serialization, and wire data. Nonexact boundaries require both endpoints to share it; every live capability in all original variadic leaves must identify the same module before folding. Identical foreign modules and the consumed-authority three-operand witness reject. |
| GC-F60 / P0 | Ballot 3's formal reviewer reconstructed a supposedly serialized authority by setting the public `live` Boolean back to true, so the theorem proved only one record update rather than capability loss. | `FORMAL MODEL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the Boolean is removed. A live authority carries a proposition indexed by its immutable module snapshot, while serialization returns a distinct snapshot-only state that is ill-typed as producer authority. Snapshot identity, object ownership, provenance, functionality, and acyclicity remain independently executable checks. The capability proposition is an explicit bounded-model trust boundary, not a proof of JVM access control. |
| GC-F62 / P0 | Binary dependent-DAG combination checked shared module identity but did not explicitly validate the merged hierarchy before JOIN or intersection could consume contradictory columns. Valid parser output cannot construct the state, but the fold boundary did not independently maintain the invariant. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: every binary combination now checks hierarchy functionality and acyclicity over both complete input DAGs before making pair decisions. A bounded internal-state mutation with one carrier under two parents rejects before its two boundary occurrences can be consumed. |
| GC-F63 / P0 | Direct dependent-DAG combination treated exact `univ` boundaries as JOIN overlap while the then-current source-chain guard and standalone verifier denied all explicit `univ`. | `SUPERSEDED BY GC-F69`: blanket rejection aligned the layers but encoded the wrong Alloy semantics. |
| GC-F64 / P0 | Public dependent-column construction accepted arbitrary nullary graph constructors outside the Alloy carrier vocabulary, while wire verification accepted only `Int` and `AlloySig:*`. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: dependent columns admit only `Int` or nonempty `AlloySig:*` atoms; theory fixtures now use explicit Alloy signature identities, and non-Alloy/empty-label mutations reject. |
| GC-F65 | The generated assurance prose said Lean 4.33.0 was pinned, but the repository had no `lean-toolchain`; the current environment therefore selected Lean 4.33.1. | `PARTIALLY REPAIRED/RELEASE BLOCKED`: root `lean-toolchain` selects `leanprover/lean4:v4.33.0`, and bounded assurance records the resolved executable version. The protected assurance runner does not yet include the pin itself in `input_roots`, so its manifest cannot establish toolchain-pin byte identity. |
| GC-F66 | The Phase 6 report's current focused counts predated schema-v10 DAG tests. | `REPAIRED/PENDING STAGED FRESH REVIEW`: current counts are 445 theory-certificate checks, 179 verifier checks, 109 writer checks, and 113 semantic-evidence mutation checks. |
| GC-F67 / P0 | The DAG fold checked module authority only pairwise. A first exact JOIN could consume the last capability from module A, allowing a later exact boundary from module B to enter after the evidence had disappeared. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: the fold validates common parser-module identity and the complete hierarchy over every original correlated alternative before the first combination. The three-operand consumed-authority witness now rejects through both fold APIs. |
| GC-F68 / P0 | Java rejected direct `univ` boundary authority after GC-F63, but the standalone Lean raw boundary still admitted `.univ/.univ` as exact and relied on a later wrapper for rejection. | `SUPERSEDED BY GC-F69`: the temporary model alignment preserved an invalid blanket exclusion. |
| GC-F69 / P0 | The blanket `univ` exclusion confused explicit parser type data with an absent-type fallback and blocked legal associative JOIN chains. | `IMPLEMENTATION REPAIRED/PENDING FRESH STAGED REVIEW`: the v8 repair, retained by current dependent theory v10, admits explicit `univ` by exact identity or authenticated subtype-to-`univ` paths, keeps absent/unresolved typing fail-closed, preserves ordered duplicate-retaining `Seq`, and refuses JOIN reassociation only when the relational shape fails the existing interior-arity guard. Parser-level regressions cover `(x.trans).univ`, `(univ.trans).x`, both associations, both boundary directions, and zero repair distance. |
| GC-F70 | The standalone correlated-family Lean model exposed binary JOIN certification but did not place the Java interior-arity guard on an executable variadic certification function; the Phase A2 report also cited a stale mutation-test count and v6 evidence run. | `FORMAL/DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: `certifiedFamilyJoinChain` now applies the interior-alternative guard before an ordered fold and proves both explicit-`univ` endpoint chains plus rejection of a unary interior. The current report names the live mutation count and dependent theory v10; the full Java refinement remains explicitly partial. |
| GC-F71 | `scripts/build_certificate_verifier.sh` writes current ZIP timestamps, so two source-identical builds can produce byte-different tracked JARs. | `OPEN/RELEASE BLOCKED`: class payloads match, but byte-for-byte JAR reproducibility is not established. Reproducibility programs are protected in this phase, so this review records the fault without changing the script. |
| GC-F72 / P0 | A parser-valid JOIN with a complete all-disjoint boundary matrix, for example `(A->B).(C->D)`, derived zero result alternatives and was then treated as unsupported. That discarded a valid positive-arity empty relation and violated the requirement that only a genuinely non-associative JOIN shape block flattening. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: dependent theory v10 has a positive-arity typed empty family with zero alternatives, complete disjoint evidence, and ordered duplicate-preserving source `Seq`. Producer, verifier, parser-pipeline, mutation, and standalone Lean checks cover binary and variadic empty results; computed nullary relations remain outside Alloy's supported relation slice. |
| GC-F73 / P0 | The producer admitted a positive-arity typed-empty relation as the interior source operand of a variadic JOIN, but standalone replay called the nonempty-product decoder while checking the interior guard and rejected the same certificate. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: producer and verifier now apply the identical positive relation-arity predicate. A three-leaf exact fixture with an arity-two empty interior verifies independently and retains all three ordered source operands. |
| GC-F74 / P0 | `TheoryAlloyAdapter.dependentTypeDagForSource` returned immediately whenever the parser result type was empty, bypassing recursive proof of a `PLUS` or `INTERSECT` expression's empty result. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: operator structure is inspected before accepting an empty parser result. Parser-backed empty INTERSECT and UNION leaves are recursively derived, compared to the parser type, and then consumed by dependent JOIN fixtures. |
| GC-F75 / P0 | The first correlated-family authority model paired columns with a live module capability but no parser object or nominal path. A forged column could therefore borrow the production module ID without proving which parser object it denoted, and pure family semantics remained disconnected from authority. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: every formal correlated column now carries one module capability, object path, and nominal path checked by `parserAuthorizesPath`. Executable ARROW and JOIN functions consume those paired products, enumerate the Cartesian matrix, retain output proofs, represent all-disjoint results, and reject mixed-module or unbound-column witnesses. Arbitrary Java/parser refinement remains explicitly partial. |
| GC-F76 / P0 | The binary application constructor enforced the variadic JOIN interior-arity guard, but the public `DependentChainKind.fold`, package fold-columns path, and `DependentTypeDag.fold` APIs could be called directly with a unary interior operand and return a flattened result. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: all three variadic entry points invoke the same theory guard before combination. Direct witnesses for `[A->B, B, A->A]` now throw `UnsupportedFlattening`; typed-empty arity-two interiors remain admitted. |
| GC-F77 / P0 | The standalone Lean `TypedFamily` record was public, and raw JOIN accepted a forged arity-zero empty operand or a product whose width disagreed with its declared arity. The older authority abstraction also filtered absent column authorities before checking the common module. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: every standalone typed-family operator checks positive arity and exact product widths, while certified variadic JOIN validates every operand before folding. The parser-correlated fold requires an authority value at every modeled column and proves absent and mixed bound/unbound examples reject. |
| GC-F78 | The current test census still said 398 after the staged suite reached 405, and the format grammar required one or more dependent products even though typed-empty families intentionally have zero. | `DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: the current suite reports 442 theory-certificate checks, and the grammar permits zero products only for a positive-arity `AlloyEmptyRelation$arity=n` with matching arity and no common ancestor. |
| GC-F79 | Producer construction recursively derives source UNION/INTERSECTION DAGs, but the standalone wire replay serializes and checks the resulting leaf DAG rather than independently replaying the raw set-operation child DAG. | `OPEN/RELEASE BLOCKED`: this cannot authorize a production nonexact chain as `VERIFIED`, because every parser ancestry path is already `UNCHECKABLE / MISSING_EVIDENCE` without pinned source-hierarchy authority. Exact-only replay establishes equality relative to the kernel's exact stored leaf type, not independent Alloy parser correctness. A future schema must carry a source-derivation tree or an independently pinned parser/type authority before claiming raw-source replay. |
| GC-F80 | One Luna report treated every exact singleton column without a parser-module token as unauthenticated. Applying that rule rejected valid quantified primitive-set operands, whose exact relation view is already proved by the leaf's `PRIMITIVE_SET_SINGLETON` rule. | `CLAIM CORRECTED/PENDING FRESH REVIEW`: A2-27 now distinguishes parser-derived nontrivial ancestry, which requires object/path/module authority, from exact primitive singleton columns, which require the separately replayed leaf type proof. Neither source can invent a subtype edge. |
| GC-F81 / P0 | Verifier replay admitted any nullary constructor as an exact dependent column, while the producer admitted only `Int` or a nonempty `AlloySig:*` identity. A forged singleton `Bogus` path used no subtype ancestry, so it could avoid the `UNCHECKABLE` hierarchy boundary and reach `VERIFIED`. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: verifier and producer now share the exact atomic vocabulary. Focused verifier checks admit `Int` and `AlloySig:S` and reject `Bogus`, empty `AlloySig:`, and constructor applications before chain combination. |
| GC-F82 | The standalone certified JOIN wrapper validated every source operand, but its public recursive fold returned an invalid current family unchanged at the empty-rest base case. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: the base case independently checks positive arity and every product width. Explicit arity-zero and wrong-width base witnesses reduce to `none`. |
| GC-F83 | Review prose implied the scalar `foldColumns` compatibility helper could represent a positive-arity typed-empty family. Its value is only a nonempty scalar product, so an empty list loses the arity required to distinguish typed empties. | `CLAIM QUALIFIED/PENDING FRESH REVIEW`: production and family-level APIs use `DependentTypeDag`, where typed-empty arity is retained and admitted. The scalar helper remains restricted to nonempty single-product inputs and now enforces the variadic guard for every representable operand. |
| GC-F84 | The certified variadic JOIN wrapper validated every operand, but the separately callable `joinFlatGuard` inspected only interior declared arity and returned true for malformed products. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: the guard itself validates the first, last, and every interior family; an interior with declared arity two and an empty product now returns false. |
| GC-F85 | An early standalone helper named `certifiedFamilyArrow` accepted raw untyped product families and could return mixed-width outputs. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: raw `familyArrow` remains the mathematical Cartesian operation. The certification theorem now calls `typedFamilyArrow`, which checks both input families and returns an arity-indexed typed result. |
| GC-F86 | One format paragraph said typed-empty relations could not participate in JOIN/ARROW evidence, contradicting the v10 grammar, verifier, and exact empty-interior fixture. | `DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: positive-arity typed empties are explicitly admitted as source or result families; they retain only arity, contribute no alternatives or nominal boundaries, and must satisfy the ordinary result equation and interior guard. |
| GC-F87 / P0 | Producer primitive-singleton conversion accepted `AlloyCarrier(AlloySig:)`, while the dependent DAG and verifier reject an empty Alloy signature identity. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: primitive relation conversion now rejects an empty prefixed identity and a blank unprefixed carrier before constructing a relation view. Both direct conversion and leaf-proof admission have permanent negative tests. |
| GC-F88 / P0 | Producer, dependent DAG, boundary correspondence, and verifier accepted the whitespace-only identity `AlloySig: ` because their atomic-carrier checks used prefix length rather than the identity token. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: one producer predicate and its verifier mirror require a nonblank, whitespace-free suffix, including Unicode space separators. Primitive conversion and both standalone admission paths have permanent ordinary-space and `U+00A0` probes. The Round 12 snapshot is invalidated. |
| GC-F89 / P0 | Verifier replay rejected the producer's legal built-in `seq/Int -> Int -> univ` ancestry because a redundant nontrivial-path check excluded `Int` after atomic admission had accepted it. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: replay uses the closed atomic predicate for every step and retains the nontrivial-hierarchy `UNCHECKABLE` fence. A parser-backed sequence path is exported in the typed-empty fixture and replayed by the mutation harness. |
| GC-F90 / P0 | A broad adapter catch downgraded malformed dependent evidence to ordinary binary JOIN/ARROW fallback, and binding normalization could erase whitespace before checking its identity. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: malformed exact columns, ancestries, and binding identities fail at ingress. Only explicit unsupported-flattening or unsupported-correspondence markers authorize fallback; loss of live parser authority has its own unsupported marker. |
| GC-F91 / P0 | Public exact-leaf proof admission returned `EXACT_RELATION` for a generic `Rel(Bogus)` carrier before the dependent DAG rejected it. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: exact leaf admission validates the complete relation family through the same DAG atomic vocabulary. The minimum direct witness now rejects. |
| GC-F92 | Package-level producer conversion still normalized `" S "` to `AlloySig:S`, contradicting the fail-closed whitespace-free identity rule at an alternate ingress. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: both Alloy-column conversion helpers reject whitespace and Unicode space separators before construction. A direct package regression retains the exact witness. |
| GC-F93 / P0 | Producer and verifier identity admission still accepted C0 controls, Unicode format characters, unpaired surrogates, private-use code points, and unassigned code points after the whitespace repair. An unpaired surrogate could therefore change under UTF-8 certificate serialization. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: producer and verifier mirror one scalar-category admission policy at exact-type, dependent-column, binding, package-conversion, deserialization, and wire-replay ingress. Permanent probes reject NUL, `U+200B`, and unpaired `U+D800`; legal visible parser identities remain admitted. |
| GC-F94 / P0 | Replacement-prone producer encoding and polymorphic-key decoding could collapse malformed surrogate bytes into a valid `U+FFFD` identity before the scalar validator or digest comparison. The category test matrix also omitted private-use, unassigned, and valid supplementary-plane witnesses. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: both canonical encoders use reporting UTF-8 encoders, polymorphic-key replay uses a reporting decoder, and direct tests reject malformed surrogate bytes, private-use, and unassigned identities while preserving valid `U+10400`. |
| GC-F95 / P0 | General exact-type wire symbols, public in-memory wire nodes, and `SigSymbol` remained alternate identity ingresses after strict codec repair. They could admit forbidden categories or a lone surrogate before the protected encoder ran. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: verifier exact types use the same scalar policy, every `Wire.Node` tag and scalar is well-formed at construction, and `SigSymbol` reuses the exact Alloy identity guard. Direct wire, exact-type, signature, and supplementary-plane probes are permanent. |
| GC-F96 / P0 | Generic producer `GraphType` and writer exact-type emission admitted symbols rejected by verifier exact-type replay. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: generic graph symbols use the same visible-identity policy and writer emission rechecks it. Direct format, private-use, unassigned, and supplementary witnesses are permanent. |
| GC-F97 | Public exact-type constructors and parser extraction disagreed on leading `this/`; the review scope also left the nested structural-key length unit unstated. | `IMPLEMENTATION/DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: all exact columns and ancestry paths normalize `this/`. `FORMAT.md` now distinguishes UTF-16-code-unit lengths inside structural keys from strict UTF-8 byte lengths in the enclosing wire; both are injective and producer/verifier-identical. |
| GC-F98 / P0 | Local preflight found remaining `this/` aliases through `SigSymbol`, package conversion, and direct `AlloySig:` graph construction after exact-type normalization. Empty or repeated special prefixes also lacked one common rejection rule. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: all producer Alloy identity constructors normalize exactly one prefix and reject empty/repeated spellings; verifier exact-type and atomic replay reject the noncanonical wire forms. Direct cross-constructor equality and negative probes are permanent. |
| GC-F99 / P0 | Polymorphic operator and type-parameter declaration names admitted forbidden identity categories and could carry them into a structural/polymorphic wire key. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: `OperatorDeclaration` now uses the common visible-identity predicate. Eight direct operator/parameter probes cover control, format, private-use, and unassigned categories. |
| GC-F100 / P0 | Standalone polymorphic-key replay and the operator model used a broader text guard than producer declaration identity admission. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: verifier parameter replay and operator semantic identities now use an independent mirror of the visible-identity predicate. Twelve direct rejecting/admitting guard probes supplement the end-to-end verifier suite. |
| GC-F101 / P0 | Exact-type references shared one string namespace for content IDs and human-readable displays. A display equal to another type's content ID could therefore resolve to the wrong type through the verifier's test-only display fallback. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: exact-type parsing rejects every cross-type ID/display collision before any reference is resolved. A publication-profile verifier fixture constructs the minimum collision and requires `INVALID_TYPE`. |
| GC-F102 / P0 | CALL metadata and `CallSymbol` admitted embedded controls and other producer-forbidden identity categories that later operator construction or standalone replay rejected. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: CALL source names, qualified callees, and occurrence paths use the same well-formed visible-identity predicate at symbol, normalized metadata, certificate, and verifier ingress. Direct category probes retain the minimum NUL counterexample. |
| GC-F103 / P0 | A concurrent unstaged rewrite of the protected assurance runner and traceability matrix blanket-promoted every row to `PROVED/DIRECT`, producing a false `PASS` while row notes and this register still named open obligations. | `QUARANTINED/NOT ADMITTED`: the exact bytes are retained in the named Git stash `quarantine concurrent unreviewed assurance closure rewrite`; the reviewed matrix and protected runner were restored from the index. The next stable run returned `INCOMPLETE` with 182 diagnostics. |
| GC-F104 / P0 | The adapter recursively derived a `PLUS` or `INTERSECT` dependent DAG but compared it with the parser result only after erasing each column's parser-module authority to a plain relation type. Equal-looking ancestry from a foreign parsed module could therefore satisfy the comparison. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: dependent columns and DAGs now compare occurrence evidence, including exact paths and one shared live parser-module capability. Same-module equal evidence is admitted; identical labels and paths from a separately parsed module reject. Lean independently pins both outcomes, and direct dependent-chain tests retain the foreign-module witness. |
| GC-F105 / P0 | CALL occurrence rows were individually checked but not proved complete. A producer could omit a valid row, and the standalone verifier had no closed comparison between required CALL operators and the supplied occurrence ledger. | `SUPERSEDED BY GC-F106`: producer-ledger equality and total-section omission rejection were added, but standalone operator-set coverage still collapsed multiple occurrences of one callee. |
| GC-F106 / P0 | The first CALL completeness repair reduced standalone coverage to a set of semantic operator identities. Nested or repeated occurrences of one callee therefore collapsed, so deleting one row from `f[f[a]]` left the required operator set unchanged. | `IMPLEMENTATION/FORMAL PARTIALLY REPAIRED; SUPERSEDED IN AUTHORITY BY GC-F108`: each supplied wire occurrence has one distinct isolated anchor, so unpaired one-row omission rejects without breaking hash-consing or canonical CALL equality. The repair does not establish that the supplied occurrence set is complete relative to raw source. |
| GC-F107 / P0 | Local preflight found that the first occurrence anchors were complete but not explicitly isolated from semantic use. A crafted bundle could attempt to reference a provenance marker from a term, witness, proof, or canonical record. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: every anchor must be a unique nullary term, have exactly the source CALL's context and sort, and occur as a scalar exactly once in the complete bundle (its own content-addressed term ID). One bounded iterative scan checks all anchors together. Lean pins the valid isolated case plus extra-reference, wrong-context, and wrong-sort rejection. The interrupted Round 26 produced no reusable votes. |
| GC-F108 / P0 | Both Round 28 Luna reviewers removed one nested CALL row together with its unused marker term/operator. The remaining row/marker and semantic-operator sets still matched. More generally, a standalone bundle verifier that receives no raw Alloy source cannot distinguish a complete source-occurrence ledger from a coordinated self-consistent omission. | `OPEN/THEORY BLOCKER`: anchors retain value against unpaired omission, insertion, substitution, and semantic reuse, but they are not an external source authority. Raw-source occurrence completeness requires either source bytes plus an independently specified parser/normalizer replay, or a separately pinned occurrence commitment supplied outside the producer bundle. No such authority is present in schema v10. The focused ladder stops at Luna and the overall assurance result remains `INCOMPLETE`; no Terra or Sol vote is admissible. |
| GC-F109 / P0 | A capitalized quantified local whose spelling matched a signature was a parser-resolved variable leaf, but source graph resolution consulted the global AAME before lexical scope. The body therefore referenced the signature, the apparently unused quantifier was removed, and both Fast rewrite IR and certified equality merged `all Trash: File | File in Trash` with `File in Trash`. Successive review also found normalized explicit-global capture, signature/field overwrite in the single AAME map, and omission of imported signatures such as `time/Time` from a local-declaration-only index. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: parser leaf authority selects lexical, signature, or field namespace over the complete parser-reachable module environment. `VarExpr` resolves only through lexical scope; `SigExpr` uses a dedicated reachable-signature map; `FieldExpr` uses name plus parser-certified exact relation type, preserving overloaded fields from distinct owners. Regressions cover local/signature, explicit qualification, signature/field, overloaded-field, and imported-signature cases. `PrenexAciScheduling.lean` pins the authority branches, missing-lexical rejection, reachable indexing, and exact-typed field separation; complete parser-to-Java refinement remains open. See `p0-lexical-signature-shadow-incident.md`. |
| GC-F110 / P0 | Import indexing registered every explicitly aliased parameterized module instance under both its declared alias and the shared raw filename. Thus `open util/ordering[A] as o1` followed by `open util/ordering[B] as o2` failed before graph construction with a false ambiguous-alias error at `util/ordering`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: explicitly aliased instances are indexed only by the client-visible declared alias, while unaliased imports retain their derived alias and raw-path lookup. Reusing one declared alias for distinct instances still fails closed. A parser-backed regression calls `o1/first` and `o2/first` and requires distinct argument-qualified semantic identities; Lean pins distinct-alias admission and same-alias conflict rejection. Complete parser import-resolution refinement remains open. See `p0-imported-module-instance-incident.md`. |
| GC-F111 / P0 | Nested same-spelled quantified and `let` bindings were distinct in `ScopeTreeNode` but value-equal in `VarSymbol` or `RefSymbol`. Inserting the inner symbol into the global `uniqueNode` map replaced the outer lookup, so a later outer occurrence could point at the inner node. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: parser-created variables carry their unique lexical scope coordinate in map identity, and `LetSymbol` carries a unique lexical identity while preserving its readable source spelling. Parser-backed nested quantifier and nested-let pairs must equal their alpha-renamed controls at both Fast rewrite and certified boundaries. Lean separately pins nested scope-path inequality. Complete parser-to-De-Bruijn refinement remains open. See `p0-lexical-binding-node-identity-incident.md`. |
| GC-F112 | Reachable-signature preindexing marked every signature node with maximum arity zero. Local `SigDecl` traversal then reused that leaf-capped node and attached field-declaration groups plus `END`, leaving contradictory node metadata consumed by generation code. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: local declaration traversal replaces the provisional leaf cap with exactly `fieldDeclarationCount + 1`; imported signatures without attached local declaration structure remain leaves. A one-field parser regression requires arity two, and Lean pins the field-declaration-plus-terminator equation. Broader generation refinement remains open. See `p0-preindexed-signature-arity-incident.md`. |
| GC-F113 / P0 | The first GC-F111 repair encoded variable node identity by underscore-concatenating type and spelling before appending scope. In one scope, `(type=A_B,name=C)` and `(type=A,name=B_C)` both produced `VAR_A_B_C@scope:2`, so the second binding replaced the first. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: variable map identity is now the presentation-independent lexical coordinate `(scope id, slot)`. Readable names and exact types remain separate metadata. The exact delimiter-collision predicate must equal its alpha-renamed control at Fast rewrite and certified boundaries, and Lean pins distinct slots in one scope regardless of presentation. Complete parser-to-coordinate refinement remains open. See `p0-lexical-coordinate-injectivity-incident.md`. |
| GC-F114 / P0 | The first `(scope id, slot)` implementation derived `slot` from the size of a name-keyed scope map. Alloy accepts repeated binder spellings; inserting the second `x` overwrote the first without increasing map size, so the following `y` reused the second `x` slot and collapsed its node. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every scope owns a monotonic allocation counter independent of name-map cardinality. Repeated names may affect name lookup according to parser-visible shadowing but can never reuse an allocated coordinate. The exact `all x,x,y` witness retains distinct parser-exposed `x` and `y` variable nodes and equals an alpha-renamed unused-binder control. Lean pins advancing consecutive allocation. Complete parser binding-resolution refinement remains open. See `p0-repeated-binder-slot-allocation-incident.md`. |

## Preliminary Review-Gate Counterexamples

The independent preliminary gate review at
`/tmp/acgn-review-gate-falsification/report.md` (SHA-256
`2f06b8469ceb0c16abf0fe9c65256117c59214f8c4672fdf2a0a633b77e6cb1f`)
returned `FAIL / BLOCKED`; it is not a ballot.

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| RG-F01 | `HEAD` plus an ordinary dirty-diff hash omits untracked correctness-bearing files, permitting stale snapshot substitution. | `BLOCKED`; every ballot must bind one canonical full-input manifest including tracked, untracked, ignored, mode, symlink, dependency, toolchain, configuration, governing-prompt, and generated-evidence bytes. |
| RG-F02 | The closed-world census has no independently fixed file universe, so a producer can omit an uninspected file and still claim zero uncovered files. | `BLOCKED`; the immutable manifest, not a producer-selected inspected set, must define the census denominator. |
| RG-F03 | Many rows called atomic contain independent conjunctions, allowing one tested conjunct to stand for the whole row. | `BLOCKED`; split every row into one proposition with a per-claim evidence/result record before ballots. |
| RG-F04 | Compiling theorem names can discharge stronger English claims: the reviewed Phase 1 model permitted duplicate occurrence entries, and the metric model accepted an uncertified empty alignment as admissible. | The two concrete formal counterexamples are repaired on moving bytes: occurrence uniqueness now includes at-most-one consumption, and alpha/ACI candidates carry proved compatibility, maximality, and partition obligations. `BLOCKED`; checked claim-to-formula and Java refinement mappings remain absent. Falsifiers are retained under the report directory. |
| RG-F05 | Evidence reuse is prohibited in prose but free-form ledger cells cannot enforce evidence identity, assumptions, one-to-one use, or proved implication. | `BLOCKED`; evidence must be content-addressed and reuse must cite a separately proved implication record. |
| RG-F06 | Reviewer identities and independence are unauthenticated; copied prose records can be relabeled as distinct reviewers. | `BLOCKED`; no available ballot is admissible until distinct reviewer provenance and tamper-evident records are defined and independently checked. |
| RG-F07 | Per-phase votes may refer to different snapshots, and “relevant change” has no executable conservative invalidation rule. | `BLOCKED`; all phase and holistic ballots must bind one common artifact digest, and any digest change voids all votes. |
| RG-F08 | Historical and live claims share the same Markdown vocabulary, allowing current assertions to be laundered as historical and naive tools to count stale `PASS` text. | `BLOCKED`; archive historical claims under a separately hashed schema and prohibit live `PASS` outside computed ballot records. |

## Preliminary Claim-Census Counterexamples

The independent preliminary census review at
`/tmp/acgn-claim-census-falsification/report.md` (SHA-256
`31bc69db9fefc5e4df00dba4a7ec9c498a053d3155f3fe2622a21e0e6a4d174c`)
returned `BLOCKED`; it is not a ballot.

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| CC-F01 | None of the 156 changed correctness-bearing paths had a complete file-and-atomic-claim mapping; 170 of 243 named formal declarations were absent from the ledger. | `BLOCKED`; build the manifest-derived atomic census and map every formal statement explicitly. |
| CC-F02 | Coalesced rows and overlapping ownership violate one-proposition atomicity and exactly-once authority. | `BLOCKED`; split the compound rows and add explicit checked implication/ownership records. |
| CC-F03 | Five focused suites were absent from the bounded CI runner and GitHub Actions registration, despite prompt and phase-document claims. | `BLOCKED`; protected runners remain unchanged, so any future registration requires a new nonprotected entry point and independently checked CI wiring. |
| CC-F04 | The ledger described semantic `disjoint` arguments as a commutative bag while separately declaring `DISJOINT_LIST` ordered; the reviewed implementation used `DISJOINT_LIST` as the commutative semantic operator. | Repaired on moving bytes by distinct `DISJOINT` (nonflat C bag) and `DISJOINT_LIST` (ordered, law-free) identities across IR, policy, law-theory v2, verifier, projection, backtranslation, formal model, and tests. Full parser/wire refinement and immutable re-review remain blocked. |
| CC-F05 | Three public documents named canonicalizer v3 while code named v4. | Repaired on moving documentation bytes by naming `canon-g-production-v4` and `canon-g-exhaustive-v4`; immutable re-review required. |
| CC-F06 | Public/wire, metric, provenance, serialization, GUI, experiment, manifest, comparison, semantic-checker, and deliverable claims have no atomic `PUB-*`/`EMP-*` census. | `BLOCKED`; broad global rows cannot substitute for per-surface claims and hash-bound empirical derivations. |
| CC-F07 | Current public state text mixes stale run/HEAD identifiers and `EXACT`/numeric claims with a blocked moving implementation. | `BLOCKED`; historical claims require separately hashed provenance and current claims require recomputation on the final manifest. |

## Preliminary Phase 3 Counterexamples

The independent report at
`/tmp/acgn-phase3-max-review/PRELIMINARY-FAIL.md` (SHA-256
`1e30171e0117c01503aed4b97b51615d226e8dae1885f009fb1cea5f7c1624a8`)
records:

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| P3-R01 | Unary `none`, binary `none -> none`, and ternary empty relations all map to one arity-free `AlloyEmptyRelation`. | Repaired on moving bytes using only parser-proved positive arity: `ExactAlloyType`, graph keys, the standalone verifier, visualization, and public wire grammar distinguish canonical arities; arityless/malformed encodings reject; Java deserialization revalidates and reconstructs an immutable value; no erased parent column is invented. The preliminary report is `/tmp/acgn-empty-relation-review/report.md` (SHA-256 `2a49cc406764a589c37f1529f7413222ddbc6d389422127183651b09ed01721f`). Full boundary refinement and immutable re-review remain blocked. |
| P3-R02 | Commands with `4 Int` and `5 Int` receive the same fixed four-bit semantic profile and fingerprint. The default Alloy execution options are modular while the canonical default claims overflow-forbidding; trace bounds and effective max sequence are absent; profile version labels do not name the running adapter/signature versions. | `REFUTED/BLOCKED`; the preliminary report at `/tmp/acgn-profile-fingerprint-review/report.md` (SHA-256 `2d37f739c2bda0e852a9d86859683f7303c95efa577aac53e54d7b1643b11f1c`) requires one source/command/options-derived context before canonicalization, propagation through every cache/comparison/export boundary, and independent verifier policy. A scalar width patch would leave the other contradictions intact. |
| P3-R03 | Repair projection accepts a certified artifact paired with a post-certification source/type mutation. | Repaired on moving bytes: adaptation freezes the complete normalized source, retains exact phase identities, and projection derives and checks the result digest. The original one-node `true` to `false` attack now rejects at mutation; foreign-source and forged-digest probes reject. Complete field-family, concurrency, and formal-to-Java refinement review remains blocked. |
| P3-R04 | Public binder grammar listed six fields while writer/parser require eight fields plus a dependency child. | Repaired in the format document; fresh grammar/parser/writer review required. |
| P3-R05 | Historical prose ended with “Phase 3 is closed” while the current header and ledger said reopened/blocked. | Reframed explicitly as invalidated historical review; Phase 3 remains blocked. |

## Preliminary A2 Counterexamples

The independent A2 report at `/tmp/acgn-a2-pre-review-alloy2/report.md`
(SHA-256
`9bc60270b65f87c306d92b59df7806fb18027ecd5a25c07a88e28c4fa1733d41`)
recorded these additional findings:

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| A2-PF01 | Unguarded flattened JOIN certified a unary-interior association that Alloy separates in a two-atom model. | Repaired before this register; immutable re-review required. |
| A2-PF02 | `A + B` for unrelated unary signatures threw while attempting an ACI law instead of accepting the validated same-arity relation family. | Repaired on moving bytes; parser-backed source-rule suite passes the focused case and the abstract finite type-admission model compiles. Refinement remains blocked. |
| A2-PF03 | `let y = none | y` used as a declaration domain was recorded as `univ`. | Repaired with GC-F07; immutable re-review required. |
| A2-PF04 | A transient schema-v6 verifier snapshot referenced an undefined `OrbitCandidate`. | Repaired on later bytes; it remains evidence that moving-snapshot test results cannot be reused. |
| A2-PF05 | Public A2 evidence named stale schema-v5 results after schema v6 became active. | `BLOCKED`; public evidence must be regenerated from the immutable reviewed snapshot. |

The report also treated absent exact Boolean provenance as a universal failure.
That proposed claim is rejected: a Boolean result can be derived from an
independently validated Boolean syntax/metatype without inventing a relational
carrier. The fail-closed requirement applies to missing relational and
dependent-chain type evidence and to any path that would fabricate `univ`.

## Gate Consequence

No row in this register can authorize a phase `PASS`. The superseding finite
plan in `do178c-assurance-plan.md` requires every scoped claim to have a
compiled Lean obligation and bounded direct conformance evidence, zero open
faults, one immutable evidence manifest, at least two fresh independent phase
reviews, and at least two fresh independent integrated reviews. Any finding is
`FAIL`; exhausted bounds or missing evidence are `INCOMPLETE`, never `PASS`.
