# Global Maximum-Adversarial Fault Register

## Status

`INCOMPLETE`. This register preserves findings from preliminary reviews over a
moving worktree. It is not a phase review, a requirements traceability matrix,
or evidence that a repair refines its formal contract. The former unbounded
review protocol was superseded by `do178c-assurance-plan.md` after its reviews
failed to terminate. The source census frozen under
`/tmp/acgn-global-claim-census` enumerated 112,314 over-approximated claim
records in 598 files and assigned no `PASS`. Its report and contradiction
digests were respectively
`1de5875ab4e6a94d2b6657c2873227ac04bd002b052c33b1bff4dbd9bf99ed3f`
and
`d3f62c5ae5609a5c826d1e5362eb359c9f0fd5231e1f1e6f2968de49053982e8`.

Every listed repair requires a fresh immutable-snapshot retest. `REPAIRED`
below means only that the concrete counterexample no longer reproduces on the
current moving worktree.

## Located Global Contradictions

| ID | Fault or contradiction | Current disposition |
| --- | --- | --- |
| GC-F01 | The worktree changed while preliminary evidence was collected. | Expected during repair; blocks every ballot until an immutable snapshot is minted. |
| GC-F02 | `docs/theory-artifact-matrix.md` claimed all 125 rows were `EXACT` while the controlling ledger and live counterexamples said otherwise. | `BLOCKED`; the public claim must be narrowed or regenerated only from closed obligations. |
| GC-F03 | Green producer-authored tests were cited as completeness evidence although independent counterexamples existed outside their fixtures. | `BLOCKED`; tests are bounded conformance evidence only. |
| GC-F04 | Compound-empty quantified domains were mishandled during the review. | Repaired on moving bytes; complete parser/IR/refinement evidence remains blocked. |
| GC-F05 | Five ablation engines lost declaration-derived nonemptiness authority. | Repaired on moving bytes; complete cross-engine refinement remains blocked. |
| GC-F06 | Raw Alloy source-to-normalized-kernel rewrites have no independently replayable semantic derivation; a source hash is only provenance. | `BLOCKED`; certificate claims must stay at normalized typed IR unless a source proof family is added. |
| GC-F07 | A missing local declaration domain was fabricated as `univ` with multiplicity `SET`; parser `let y = none | y` could also become `univ`. | Repaired on moving bytes: malformed local domains reject and a statically empty `let` remains `none`. Formal-to-Java refinement remains blocked. |
| GC-F08 | Local binder and typed symmetry groups eagerly retained all elements, including 5,040 elements for `S7`, despite streaming claims. | `REPAIRED/PENDING FRESH REVIEW`: production local groups now use deterministic stabilizer-chain traversal. Ninety-six `S4` subgroup differentials agree with an independent materialized oracle and `S7` emits exactly 5,040 elements with retained stabilizer state smaller than the orbit. Complete Java heap/refinement proof remains open. |
| GC-F09 | Canonical-orbit wire evidence omitted the selected witness used to break equal-shape ties. | The selected witness is now serialized and checked, but producer/verifier candidate-set and ordering equivalence (P6-12) and PAIR observation ownership (P6-13) remain blocked. |
| GC-F10 | The production writer rejects real collision and nontrivial rebuild histories while public text described standalone DTO fixtures as end-to-end producer coverage. | `BLOCKED`; either implement the producer path or narrow the claim. |
| GC-F11 | CALL edge validators compared source spelling while certificates claimed complete qualified callee identity; a later audit found the direct code-generator validator still made that comparison after MASG/IR had been repaired. | Repaired on moving bytes at all three boundaries: callee targets carry and compare source spelling, qualified identity, kind, arity, and authority, and both canonical preparation and direct generation reject the same-spelling/different-identity mutation. Full boundary refinement remains blocked. |
| GC-F12 | Two protected reproducibility terminal scripts differed from the repair base. | Repaired: both scripts are byte-identical to `8ec25613`; future audit wiring must use a new nonprotected entry point. |
| GC-F13 | Historical publication aggregates were presented near current implementation claims without a current immutable compatible run. | `BLOCKED`; historical results may be labeled only as historical until hash-bound recomputation. |
| GC-F14 | Compiled abstract Lean contracts were used as if they established Java, wire, verifier, or provenance refinement. | `BLOCKED`; every implementation claim needs its own formal contract and independent conformance relation. |
| GC-F15 | A java.base-only standalone verifier and positive fixtures were treated as complete semantic independence evidence. | `BLOCKED`; dependency isolation is necessary but not sufficient. |
| GC-F16 | Root-qualified binder identity was introduced under schema v5, silently changing v5 meaning. | Repaired on moving bytes by exclusive schema v6 admission and v5 rejection; fresh wire review remains required. |
| GC-F17 | The semantic-evidence mutation harness described argument four as a generic binder bundle although it requires exactly one binder-occurrence record; valid law-free binder fixtures therefore failed inside the mutation helper. | Repaired on moving bytes by naming the one-record precondition in the CLI contract and reporting the section tag and observed cardinality on misuse. The intended `bind-block-symmetric-a.acgncert` invocation passes 60 checks; this remains bounded harness evidence, not a ballot. |
| GC-F18 | The earlier semantic-profile review observed different certificate bytes across two JVM exports of the same width-specific source. | The exact probe no longer reproduces on current moving bytes: two fresh JVM runs produced width-4 SHA-256 `ea66193ced257d250a97a56d300e1044b60616a435af85fea0851963696a7cc7` and width-5 SHA-256 `140957055303112d0941ff3707fd4e6b0fffbd1de7d0aad59a005e992e3635e2` in both runs, and both `cmp` checks passed. This is one bounded nonreproduction, not the general P6-08 serialization proof. |
| GC-F19 | The universal claim census plus repeated exhaustive-review protocol had no finite stopping rule; four fresh reviewers remained running without completing their assigned whole-phase searches. | `CLOSED AS PROCESS DEFECT`: all four reviews were terminated and the protocol was superseded by the finite DO-178C Level A-inspired assurance plan. Lean remains mandatory for every scoped claim; executable testing and independent review now have explicit finite bounds and `PASS`/`FAIL`/`INCOMPLETE` outcomes. |
| GC-F20 | Existing prose and abstract proof names did not provide a mechanically checked claim-to-Lean/code/test mapping. | `OPEN`: the first matrix found 142 Section 3 requirements and successive scoped repairs expanded the closed manifest to 191 rows. An ordered scope manifest now binds every ID, class, and claim hash, and anti-counterfeit tests reject comment/string-only declarations and malformed scope substitutions. No existing proof or test is grandfathered; structural coverage, immutable review, and the remaining explicitly partial evidence mappings remain open. |
| GC-F21 | One bounded review evidence file appended pseudo-path records such as `TREE compiled-source-classes files=630` to `sha256sum` syntax, so `sha256sum -c` rejected the manifest; the same report also recorded a 142-requirement runtime result while later hashing a 154-row matrix. | `OPEN REVIEW-EVIDENCE DEFECT`: ordinary file hashes were independently checked and the findings remain preliminary, but this report cannot be promoted to A-12 evidence. The bounded runner must use a versioned manifest grammar and bind runtime logs to the exact input digest. |
| GC-F22 | P3-01 universally demanded recoverable ordered columns for statically empty relations, contradicting P3-15's correct rule that only positive arity is recoverable without inventing erased columns. | `FIXED-AWAITING-RETEST`: P3-01 now requires preservation of all parser-proved information and explicitly distinguishes recoverable columns from arity-only empty evidence. Its claim hash and formal mapping are invalidated pending fresh proof review. |
| GC-F23 | M-05 claimed Zhang-Shasha-style ordered tree edit distance, but the temporal recurrence deleted/inserted whole subtrees. The bounded counterexample `R(A(X))` versus `R(X)` costs 1 with internal-node deletion and child promotion, while the implementation returned 2. | `REPAIRED/PENDING ASSURANCE`: both metric paths now share a Zhang-Shasha postorder/leftmost/keyroot implementation. Lean proves the ordered-forest recurrence and unit promotion witnesses; 49 bounded pairs agree with an independent exhaustive ordered-mapping oracle. Structural coverage and immutable-snapshot review remain open. |
| GC-F24 | P4-01 claimed a nonempty collision bucket for every valid canonical shape, including shapes not stored by any live leader. A valid nullary shape queried in a quiescent empty graph had no owner or bucket. | `FORMAL REPAIR AWAITING RETEST/IMPLEMENTATION PARTIAL`: P4-01 now quantifies over supported-operation-reachable quiescent observations of stored live-leader shapes. Review `ca9b9e84b4210749e5ea448d4c26997947b06499c58bf6667a20fad1466f6f67` exposed a reflexive list-order determinism theorem. That theorem has been replaced by extensional duplicate-insensitive membership, permutation-independent canonical enumeration, supported mutation/rebuild closure, and exact public-observation proofs. Fresh independent proof review, Java transition refinement, structural coverage, and immutable rerun remain mandatory. |
| GC-F25 | P5 built-ins were recognized by case-insensitive source spelling, so legal user signatures `None` and `Univ` were treated as Alloy `none` and `univ`; solver-backed probes produced unsound equivalences in fast and certified paths. | `REPAIRED/PENDING ASSURANCE`: raw AST terms now recognize only exact reserved spellings, while Fast Rewrite IR carries a parser-derived `SigSymbol` identity through projection and theory adaptation. The original solver-backed probe now reports `rawEquivalent=false`, `canonicalEquivalent=false`, and all formerly unsound equality flags false. Lean proves exact-reserved/nonreserved disjointness; structural coverage and immutable review remain open. |
| GC-F26 | P5-12 did not recognize the statically empty domain `one (A - A)`; the Alloy predicate is unsatisfiable, but canonicalization retained one binding and remained distance 6 from false. | `REPAIRED/PENDING ASSURANCE`: self-difference now reduces to authenticated `none` in raw rewriting, Fast Rewrite saturation, and pre-prenex domain analysis. Lean proves `R - R = none` and absence of a positive-one binding. The original probe now reports `equivalentToFalse=true`, distance 0, and zero bindings. The finite static proof family and structural coverage remain open. |
| GC-F27 | P3-16's fixed semantic profile aliases source commands and execution options: different integer widths and modular versus overflow-forbidding semantics can share a profile. | `REFUTED/PARTIALLY REPAIRED`: fresh bounded review `d8949a6320ef75b44c261797f5124093d862a62c62f966e85a404e442f1c44bf` found explicit width 0 aliased to 4 and caller-forgeable authority; both are repaired with parser-module ownership, package-private authority, exact omitted-versus-zero width handling, Lean obligations, and 31 parser-backed checks. Production defaults still bypass selection, the A4Options semantic partition/cache inventory are open, and source-bound bundle profiles cannot be replayed by the standalone verifier. |
| GC-F28 | P6-02 and P6-10 claimed non-retaining streamed symmetry traversal, but local binder and typed symmetry groups retained complete closure lists and the verifier retained all candidates. | `REPAIRED/PENDING FRESH REVIEW`: the claims now exclude the deliberately exhaustive differential oracle; production local traversal uses a stabilizer chain and verifier replay keeps one running minimum. Universal heap/refinement evidence remains open. |
| GC-F29 | P5-10 requires command-specific translated-versus-parser-only authority, but the source-rule regression emitted only one aggregate result and no unsupported-command records. | `REPAIRED/PENDING IMMUTABLE REVIEW`: the regression now emits and checks one source-ordered record for each of 12 commands, distinguishing eight translated SAT/UNSAT results from four exact higher-order-skolemization limitations; unexpected translation errors propagate. Lean proves outcome classification and one-record-per-command cardinality. |
| GC-F30 | A generic inhabited-carrier escape allowed quantifiers to cross a relational-ITE condition, and a later prenex pass interpreted an earlier generated implication's antecedent with negated polarity. SAT4J found both semantic counterexamples; the latter also made conjunction order change the canonical observation. | `REPAIRED/PENDING IMMUTABLE REVIEW`: witness lifting now requires an admissible connective boundary, and every generated branch connective is eliminated to NNF before another prenex pass. The permanent suite checks both counterexamples, branch order, and bounded SAT equivalence (186 checks). |
| GC-F31 | `MASGVisitorTypeRegressionTest` demanded a matrix binding with synthetic carrier `univ` even though the binder correctly remained local below an unsafe conjunction and unconditional lifting is false over Alloy's admitted empty universe. Documentation repeated the unsafe premise. | `REPAIRED/PENDING IMMUTABLE REVIEW`: the stale assertion now separates directly liftable and locally retained obligations, checks exact `File` type provenance, proves bounded source equivalence, and requires both proposed unconditional lifts to have zero-scope counterexamples. `GuardedPrenex.lean` proves the required inhabitation premise and empty-carrier obstruction; the dead synthetic-`univ` producer switch was removed. |
| GC-F32 | Prenex slot minimization observed the parser's binary association and branch order before ACI normalization. In a three-branch conjunction or disjunction, an empty-domain-enabling prefix could be discovered only after a reusable sibling had remained local, so equivalent permutations acquired different slot counts and distances 2 or 3. | `REPAIRED/PENDING IMMUTABLE REVIEW`: maximal same-headed `AND`/`OR` regions are association-flattened without reordering or idempotence; allocation visits same-carrier empty-domain-enabling branches first while retaining source operand positions. The expanded permanent suite passes 332 checks, the six-pair adversary reports equality and distance zero, and `PrenexAciScheduling.lean` proves the two prefix-order laws plus schedule membership/length conservation. This is partial evidence for P5-14, not closure of that broader claim. |
| GC-F33 | The certified observation quotiented conjunction order across temporal siblings, while repair projection assigned phase numbers from source traversal. Swapping `always (all x:S | no x.r)` and `eventually (some y:S | some y.r)` therefore preserved semantics but produced temporal, quantifier, and matrix costs of 2 each and triggered the certified-kernel guard. | `REPAIRED/PENDING IMMUTABLE REVIEW`: repair projection now orders temporal occurrences by expanded certified matrix keys and applies one coherent reindexing to topology, references, and binding owners; ordered containers and binary temporal branch roles are not sorted. The original SAT-backed probe reports no counterexample and guarded distance zero, permanent two-/three-phase tests pass within 234 source-rule checks, and `TemporalAciPhasePresentation.lean` proves the exact bounded permutation obligation. General Java refinement, equal-key cases, larger phase sets, and fresh independent review remain open. |
| GC-F34 | Phase 4 producer snapshots accepted absent or contradictory intermediate parent paths, hidden empty-ledger mutations, orphan histories/retirements, and live invocations of absent classes; the trace sink and standalone verifier also failed to bind union revision effects and rebuild events to one closed interval. | `REPAIRED/PENDING FRESH REVIEW`: snapshot construction now cross-validates complete topology and ledgers, producer and standalone trace machines require one closed rebuild interval, twelve new producer/verifier attacks are permanent, and the schema-v8 protected harness remains at census `1/2/0`. |
| GC-F35 | The Phase 4 Lean wire relation accepted arbitrary dirty queues and projected away revision, status, parent paths, reverse uses, histories, and rebuild boundaries. | `FORMAL MODEL EXTENDED/PENDING REFINEMENT REVIEW`: exact bounded transition frames and a deterministic interval automaton now reject the known countermodels and compile without admissions. No mechanized Java/JVM refinement is claimed; executable conformance and a fresh statement review remain mandatory. |
| GC-F36 | A2-08 and two phase reports still claimed a name-authenticated `PARAMETER_SINGLETON` typing rule after the v4 producer and verifier had removed it and explicitly rejected name-based authority. | `REPAIRED/PENDING FRESH REVIEW`: the live claim, formal enumeration, and phase reports now admit only exact-relation and primitive-singleton rules; the failed v3 approach remains labeled as historical fault provenance. |
| GC-F37 | The Phase 4 rebuild regression survived a mutant in which `rebuildHashConsExactly()` performed no rebuild, so the cited test did not establish that the required transition occurred. | `REPAIRED/PASSED BOUNDED REPLACEMENT REVIEW`: rebuild now clears the derived index, reconstructs it from authoritative live records, and checks exact owner buckets before returning. A fresh read-only review killed both counter-only and empty-body mutants on the full and isolated interface-change paths. Universal Java/JVM refinement and immutable phase closure remain open. |
| GC-F38 | Three Phase 5 traceability rows cited Java string literals or test descriptions as declarations, and one row encoded two Lean paths in a field that admits exactly one file. The semantic tests passed, but those rows could not be mechanically linked to their evidence. | `REPAIRED/PENDING FRESH REVIEW`: every Java anchor now names a real declaration; the EXACTLY-none binding witness lives in the scheduling Lean module; the global checker reports no malformed P5 reference. This repairs evidence integrity only and does not strengthen the declared partial semantic refinements. |
| GC-F39 | Repair projection treated raw source aliases as global binding/type authority after implication or IFF expansion and prenexing. A predicate parameter `x:A` and a nested `x:B` either threw a cross-type collision or, for equal carriers, risked silent capture. | `REPAIRED/PASSED BOUNDED REPLACEMENT REVIEW`: parameter, matrix, and inherited bindings are indexed only by alpha-renamed and rooted De Bruijn identities; source spellings remain presentation metadata. A fresh review passed 11 shadowing variants, 44 bounded solver comparisons, 60 projection checks, and killed four alias/polarity mutants including same-carrier silent capture. Complete parser/JVM refinement remains open under P5-14. |
| GC-F40 | The assurance matrix left global and metric claims as generated `MISSING` rows even though bounded formal, implementation, and falsification evidence existed, while two populated rows cited nonexistent Java declarations. | `REPAIRED AS EVIDENCE-INTEGRITY DEFECT`: all 191 current rows resolve to real files and declarations. Global/metric rows retain `PARTIAL`, `DIRECT`, or `REFUTED` status where their semantic obligations remain open; mapping completion is not claim closure. |
| GC-F41 | The audit overview presented obsolete current-facing ready-row and diagnostic counts without binding them to the matrix snapshot that produced them. | `REPAIRED`: the overview no longer duplicates volatile counts; only generated, input-bound reports may state them. Historical counts remain permissible when explicitly labeled and hash-bound. |
| GC-F42 | `run_section3_assurance.sh` invoked schema-v8 mutation fixtures at obsolete root paths and omitted the dedicated Phase 6 producer/verifier ordering regression, making the claimed assurance entry point fail before those checks. | `REPAIRED/BOUNDED RERUN COMPLETE`: fixture paths now name `schema-v8-coverage`, the parent-path fixture is explicit, and both Phase 6 order tests plus concrete metric refinement are registered. Development Run E executed all 57 steps with zero executable failures. Its `INCOMPLETE` result and 168 open traceability diagnostics remain binding; runner repair is not claim closure. |
| GC-F43 | P6-10 claimed that certificate export and verifier replay retained no complete orbit, but the wire intentionally carried a complete free-renaming proof ledger and standalone replay retained bounded completeness/group ledgers. | `CLAIM REPAIRED/PENDING FRESH REVIEW`: P6-10 now states only the production candidate-term retention property. Complete certificate proof ledgers, verifier completeness ledgers, and the exhaustive oracle are explicit exclusions rather than hidden counterexamples. Production traversal still retains only one candidate minimum and bounded traversal state. |
| GC-F44 | Permanent Phase 6 tests did not kill removal of the equal-shape witness tie-break, and a first-candidate verifier was exposed only by a temporary six-permutation `S3` family. | `REPAIRED/PENDING FRESH REVIEW`: the six producer bundles are permanent and verifier tests now exercise them all; a direct equal-term identity-versus-swap comparison covers both directions of the witness tie. Complete producer/verifier candidate-set refinement remains partial. |
| GC-F45 | The repair metric discarded minimum quantifier-edit correspondences and then guessed unmatched variables by equal coordinate. Parameter declarations were not charged, so a real `S`-to-`T` parameter change produced false zero and 210 of 1,521 bounded typed cases disagreed with an independent oracle. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: the scalar quantifier result is replaced by streamed enumeration of all minimum edit plans; only an injective paid modification or explicit positional-parameter diagonal selected by that plan may pre-seed matrix alignment; parameters are compared as one ordered declaration sequence; the unconditional coordinate fallback is removed. Source-level distances are now `1` for type-only and `2` for type-plus-body witnesses. |
| GC-F46 | Hungarian `int` potentials wrapped an exact assignment cost of `4,294,967,294` to `-2`; sequence and dimension additions also lacked checked boundaries. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: both certified and Fast Rewrite assignment implementations use checked `long` internals and one `Math.toIntExact` API boundary; malformed/negative matrices reject and unsupported totals fail closed. A permanent 210-matrix exhaustive differential and the overflow witness pass. |
| GC-F47 | M-06 described ordinary node insertion/deletion with child promotion, while the established Fast Rewrite matrix recurrence inserts or deletes complete operands; `A(X)` to `X` therefore costs two, not one. | `REQUIREMENT CONTRADICTION REPAIRED`: the claim now names complete operand-subtree edits and preserves the established metric semantics. Lean and Java pin the wrapper/child witness at cost two. General recurrence refinement remains partial. |
| GC-F48 | Exact scope/alpha searches materialized scope-alignment products and exposed unbounded factorial work without cancellation or a declared failure contract. | `PARTIALLY REPAIRED`: scope products and quantifier edit-plan products stream; configurable checked bounds on quantifier, scope, and alpha alignments throw `ResourceLimitException` before a partial minimum can be returned. Algorithmic improvement and complete supported-domain analysis remain open. |
| GC-F49 | The first correspondence repair carried only paid tuple modifications. For `[T] -> [S,T]`, the unchanged `T` parameter moved from ordinal zero to one but had no selected correspondence, so both insertion and deletion charged an erroneous matrix edit. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: every diagonal for the ordered positional-parameter component now carries its exact binding pair, including zero-cost diagonals; quantified declarations remain governed by paid changes plus certified scope alignment. A separate oracle enumerates all tied minimum edit plans for 1,156 `S`/`T` parameter-reference cases through length three. |
| GC-F50 | Matrix variable updates inspected only the left binding status. A same-spelled free variable compared with a bound variable could cost zero in one direction and one in the reverse direction. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: bound/free status is compared before either alpha mapping or lexical spelling; both directions cost one matrix update, and Lean pins the decisive bound/free case. |
| GC-F51 | The first post-repair Phase 6 mutation run still failed to detect producer witness-tie removal, standalone verifier first-candidate retention, and complete candidate-term materialization. | `TEST REPAIRED/PENDING FRESH REVIEW`: production and verifier expose package-scoped comparator/minimum probes; tests exercise both candidate orders and both tie directions. `BestCandidate` is structurally required to retain exactly one `LeastOption` field, which detects the concrete complete-list mutant. General candidate-set refinement and PAIR ownership remain explicitly partial. |
| GC-F52 / P0 | The full-corpus augmenter reached the legal subtype-overlap JOIN `Product.(Component->Position)`, but the adapter promoted the dependent theory's literal boundary mismatch into a fatal malformed-source error. The bounded smoke sample contained no such source. An initial fixed-binary fallback removed the crash but failed the Phase-A2 requirement to retain a dependent `Seq` when exact subtype evidence exists. The first five-reviewer round then found an underconstrained Lean wire predicate, an ancestry-forging public factory, unbound rule/endpoints, admitted `univ`, contradictory parent-ledger possibilities, an unauthenticated standalone hierarchy claim, incompatible v8 wire reuse, and an undocumented serialized-cache break. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: theory v5 retains live parser-positioned direct-parent ancestry and certificate-binds a closed endpoint correspondence; schema v9 replays one acyclic single-parent ledger and rejects all earlier schemas. The production case is `RIGHT_SUBTYPE_OF_LEFT` with witness `Component -> Product`; no endpoint is widened to `univ`, unrelated signatures remain unsupported, and ancestry is isolated from general canonical carrier identity. Lean now binds the rule, direction, endpoints, every direct-parent step, ordered `Seq`, and exact fold. Nonexact subtype bundles are structurally checked but remain `UNCHECKABLE` without an external source-hierarchy authority; malformed evidence rejects. `ExactAlloyType` stream v3 invalidates ancestry-free caches, which must be reparsed. |
| GC-F53 / P0 | A fresh authority review showed that `ExactAlloyType.from(Type)` accepted a parent hierarchy built with public synthetic `PrimSig` constructors, and Java serialization preserved the same nontrivial ancestry metadata. The producer-side extraction route therefore did not itself distinguish a live parser declaration from caller-fabricated type objects. | `SUPERSEDED BY GC-F54`: source-position and transient-authority checks closed the original constructor and serialization witnesses, but transferable parser attributes exposed a stronger forgery. |
| GC-F54 / P0 | Parser `Attr` values are publicly readable. A caller could transplant a genuine positioned `SUBSIG` attribute onto a synthetic `PrimSig`, defeating the first source-position-only repair while remaining inside ordinary Java APIs. | `IMPLEMENTATION REPAIRED/PENDING WHOLLY FRESH REVIEW`: every nontrivial producer path now requires identity membership in the exact final `CompModule` supplied to `MASGVisitor` plus source-positioned parent edges. Module-free conversion, synthetic objects, transferred attributes, and deserialized ancestry reject; self-only relation construction remains available. Lean models the independent module-object ledger and serialization loss. Standalone nonexact subtype evidence remains `UNCHECKABLE`, and complete parser/JVM refinement remains open. |
| GC-F55 / P0 | The first fresh module-identity review failed because the Lean Phase-A2 fold admitted unary dependent JOINs, did not connect the unary-interior JOIN guard or parser authority to the fold, assumed rather than executable-checked the encoded result, and omitted schema-v9 standalone outcomes. Its object ledger also did not bind identities to nominal labels, and its JOIN step could derive the unsupported nullary relation. | `SUPERSEDED BY GC-F56`: executable producer/structural folds and identity-to-label checks repaired those witnesses, but the first replacement still represented source positions as self-supplied flags and omitted fixed metadata from the slice outcome. |
| GC-F56 / P0 | The second fresh formal review found that the bounded exact-chain outcome ignored the fixed theory digest, module object IDs were not required to be injective, source positions were self-supplied Boolean flags rather than module-ledger declarations, and wire replay assumed whole-record equality. The theorem name also risked conflating acceptance of the dependent-chain evidence slice with whole-bundle `VERIFIED`. | `FORMAL MODEL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the module snapshot now has unique identity-to-label entries and positioned child/parent declarations; path admission replays both ledgers and rejects duplicate IDs, swapped labels, missing declarations, unknown positions, transfer, and serialization. Producer evidence no longer supplies position flags. The exact theory version and digest are executable checks. Chain evidence outcomes are explicitly slice-local, and full field commitment replay is an executable field-by-field Boolean checker rather than an assumed record equality. The finite Alloy witness model and Java/parser refinement remain explicitly partial and cannot support a general whole-verifier claim. |
| GC-F57 / P0 | A fresh formal preflight found that the module snapshot checked ownership and parent functionality but not global acyclicity, so a selected valid edge could coexist with a reverse edge. It also found that each variadic JOIN boundary carried its own snapshot and the fold did not require one common authority ledger. | `FORMAL MODEL REPAIRED/PENDING WHOLLY FRESH REVIEW`: snapshot well-formedness now follows every module-owned functional parent chain with bounded fuel and rejects cycles, including disconnected ones. The variadic producer fold requires structural equality of every boundary authority snapshot before consuming any path. Permanent Lean witnesses reject the two-node cycle and the three-operand mixed-module JOIN. Java already receives one final `CompModule` per `MASGVisitor`; the formal model now expresses that same chain-level invariant. |
| GC-F61 / P0 | The authenticated single-product subtype stack could not preserve correlation in relation-family unions and did not expose the complete Cartesian alternative-pair obligation for JOIN or ARROW. Reusing schema v9 would also have reinterpreted old dependent-chain bytes. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: dependent theory v7 uses a normalized finite DAG of correlated ordered products, complete row-major pair decisions, ordered duplicate-preserving `Seq`, and parser-authenticated nominal paths. UNION absorbs only authenticated componentwise subtypes; INTERSECTION and JOIN omit only authenticated disjoint branches; `univ` is terminal but grants no overlap authority. Schema v10 commits every DAG and pair decision, while v9 rejects. Standalone Lean and bounded producer/verifier mutation tests pass; arbitrary Java/parser refinement remains explicitly partial. |
| GC-F58 / P0 | The first full five-reviewer Ballot 3 review found that legal Alloy sequence types were rejected because the built-in `seq/Int -> Int` edge has no user-source filename. | `IMPLEMENTATION REPAIRED/PENDING WHOLLY FRESH REVIEW`: parser authority admits exactly the identity pair `Sig.SEQIDX -> Sig.SIGINT` through a closed built-in rule; every user-declared non-`univ` parent edge still requires module membership and a known parser position. Same-label synthetic objects reject. Lean models source and sequence-index provenance as disjoint constructors. |
| GC-F59 / P0 | Ballot 3 also found that module identity was checked while extracting each exact type and then discarded, allowing separately parsed modules with identical labels and parent paths to be mixed in one chain. Pairwise checks could additionally forget an earlier authority after JOIN consumed its last surviving column. | `IMPLEMENTATION REPAIRED/PENDING WHOLLY FRESH REVIEW`: `ExactAlloyType` retains the originating `CompModule` only as a transient identity capability. It is excluded from equality, stable keys, hashes, serialization, and wire data. Nonexact boundaries require both endpoints to share it; every live capability in all original variadic leaves must identify the same module before folding. Identical foreign modules and the consumed-authority three-operand witness reject. |
| GC-F60 / P0 | Ballot 3's formal reviewer reconstructed a supposedly serialized authority by setting the public `live` Boolean back to true, so the theorem proved only one record update rather than capability loss. | `FORMAL MODEL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the Boolean is removed. A live authority carries a proposition indexed by its immutable module snapshot, while serialization returns a distinct snapshot-only state that is ill-typed as producer authority. Snapshot identity, object ownership, provenance, functionality, and acyclicity remain independently executable checks. The capability proposition is an explicit bounded-model trust boundary, not a proof of JVM access control. |
| GC-F62 / P0 | Binary dependent-DAG combination checked shared module identity but did not explicitly validate the merged hierarchy before JOIN or intersection could consume contradictory columns. Valid parser output cannot construct the state, but the fold boundary did not independently maintain the invariant. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: every binary combination now checks hierarchy functionality and acyclicity over both complete input DAGs before making pair decisions. A bounded internal-state mutation with one carrier under two parents rejects before its two boundary occurrences can be consumed. |
| GC-F63 / P0 | Direct dependent-DAG combination treated exact `univ` boundaries as JOIN overlap while the then-current source-chain guard and standalone verifier denied all explicit `univ`. | `SUPERSEDED BY GC-F69`: blanket rejection aligned the layers but encoded the wrong Alloy semantics. |
| GC-F64 / P0 | Public dependent-column construction accepted arbitrary nullary graph constructors outside the Alloy carrier vocabulary, while wire verification accepted only `Int` and `AlloySig:*`. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: dependent columns admit only `Int` or nonempty `AlloySig:*` atoms; theory fixtures now use explicit Alloy signature identities, and non-Alloy/empty-label mutations reject. |
| GC-F65 | The generated assurance prose said Lean 4.33.0 was pinned, but the repository had no `lean-toolchain`; the current environment therefore selected Lean 4.33.1. | `IMPLEMENTATION REPAIRED/BOUNDED GOVERNED RUN EXECUTED`: root `lean-toolchain` selects `leanprover/lean4:v4.33.0`; the assurance input manifest hashes that pin, records the resolved executable version, compiles every mapped file, and emits `#check` plus `#print axioms` evidence for all 888 mapped declarations. The 77-step governed run had zero executable failures. Immutable review and the broader release gate remain open. |
| GC-F66 | The Phase 6 report's current focused counts predated schema-v10 DAG tests. | `REPAIRED/PENDING STAGED FRESH REVIEW`: current counts are 445 theory-certificate checks, 179 verifier checks, 109 writer checks, and 113 semantic-evidence mutation checks. |
| GC-F67 / P0 | The DAG fold checked module authority only pairwise. A first exact JOIN could consume the last capability from module A, allowing a later exact boundary from module B to enter after the evidence had disappeared. | `IMPLEMENTATION REPAIRED/PENDING STAGED FRESH REVIEW`: the fold validates common parser-module identity and the complete hierarchy over every original correlated alternative before the first combination. The three-operand consumed-authority witness now rejects through both fold APIs. |
| GC-F68 / P0 | Java rejected direct `univ` boundary authority after GC-F63, but the standalone Lean raw boundary still admitted `.univ/.univ` as exact and relied on a later wrapper for rejection. | `SUPERSEDED BY GC-F69`: the temporary model alignment preserved an invalid blanket exclusion. |
| GC-F69 / P0 | The blanket `univ` exclusion confused explicit parser type data with an absent-type fallback and blocked legal associative JOIN chains. | `IMPLEMENTATION REPAIRED/PENDING FRESH STAGED REVIEW`: the v8 repair, retained by current dependent theory v10, admits explicit `univ` by exact identity or authenticated subtype-to-`univ` paths, keeps absent/unresolved typing fail-closed, preserves ordered duplicate-retaining `Seq`, and refuses JOIN reassociation only when the relational shape fails the existing interior-arity guard. Parser-level regressions cover `(x.trans).univ`, `(univ.trans).x`, both associations, both boundary directions, and zero repair distance. |
| GC-F70 | The standalone correlated-family Lean model exposed binary JOIN certification but did not place the Java interior-arity guard on an executable variadic certification function; the Phase A2 report also cited a stale mutation-test count and v6 evidence run. | `FORMAL/DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: `certifiedFamilyJoinChain` now applies the interior-alternative guard before an ordered fold and proves both explicit-`univ` endpoint chains plus rejection of a unary interior. The current report names the live mutation count and dependent theory v10; the full Java refinement remains explicitly partial. |
| GC-F71 | `scripts/build_certificate_verifier.sh` writes current ZIP timestamps, so two source-identical builds can produce byte-different tracked JARs. | `OPEN/RELEASE BLOCKED`: class payloads match, but byte-for-byte JAR reproducibility is not established. Reproducibility programs are protected in this phase, so this review records the fault without changing the script. |
| GC-F72 / P0 | A parser-valid JOIN with a complete all-disjoint boundary matrix, for example `(A->B).(C->D)`, derived zero result alternatives and was then treated as unsupported. That discarded a valid positive-arity empty relation and violated the requirement that only a genuinely non-associative JOIN shape block flattening. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: dependent theory v10 has a positive-arity typed empty family with zero alternatives, complete disjoint evidence, and ordered duplicate-preserving source `Seq`. Producer, verifier, parser-pipeline, mutation, and standalone Lean checks cover binary and variadic empty results; computed nullary relations remain outside Alloy's supported relation slice. |
| GC-F73 / P0 | The producer admitted a positive-arity typed-empty relation as the interior source operand of a variadic JOIN, but standalone replay called the nonempty-product decoder while checking the interior guard and rejected the same certificate. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: producer and verifier now apply the identical positive relation-arity predicate. A three-leaf exact fixture with an arity-two empty interior verifies independently and retains all three ordered source operands. |
| GC-F74 / P0 | `TheoryAlloyAdapter.dependentTypeDagForSource` returned immediately whenever the parser result type was empty, bypassing recursive proof of a `PLUS` or `INTERSECT` expression's empty result. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: operator structure is inspected before accepting an empty parser result. Parser-backed empty INTERSECT and UNION leaves are recursively derived, compared to the parser type, and then consumed by dependent JOIN fixtures. |
| GC-F75 / P0 | The first correlated-family authority model paired columns with a live module capability but no parser object or nominal path. A forged column could therefore borrow the production module ID without proving which parser object it denoted, and pure family semantics remained disconnected from authority. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: every formal correlated column now carries one module capability, object path, and nominal path checked by `parserAuthorizesPath`. Executable ARROW and JOIN functions consume those paired products, enumerate the Cartesian matrix, retain output proofs, represent all-disjoint results, and reject mixed-module or unbound-column witnesses. Arbitrary Java/parser refinement remains explicitly partial. |
| GC-F76 / P0 | The binary application constructor enforced the variadic JOIN interior-arity guard, but the public `DependentChainKind.fold`, package fold-columns path, and `DependentTypeDag.fold` APIs could be called directly with a unary interior operand and return a flattened result. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: all three variadic entry points invoke the same theory guard before combination. Direct witnesses for `[A->B, B, A->A]` now throw `UnsupportedFlattening`; typed-empty arity-two interiors remain admitted. |
| GC-F77 / P0 | The standalone Lean `TypedFamily` record was public, and raw JOIN accepted a forged arity-zero empty operand or a product whose width disagreed with its declared arity. The older authority abstraction also filtered absent column authorities before checking the common module. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: every standalone typed-family operator checks positive arity and exact product widths, while certified variadic JOIN validates every operand before folding. The parser-correlated fold requires an authority value at every modeled column and proves absent and mixed bound/unbound examples reject. |
| GC-F78 | The current test census still said 398 after the staged suite reached 405, and the format grammar required one or more dependent products even though typed-empty families intentionally have zero. | `DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: the current suite reports 442 theory-certificate checks, and the grammar permits zero products only for a positive-arity `AlloyEmptyRelation$arity=n` with matching arity and no common ancestor. |
| GC-F79 | Producer construction recursively derives source UNION/INTERSECTION DAGs, but the standalone wire replay serializes and checks the resulting leaf DAG rather than independently replaying the raw set-operation child DAG. | `OPEN/RELEASE BLOCKED`: this cannot authorize a production nonexact chain as `VERIFIED`, because every parser ancestry path is already `UNCHECKABLE / MISSING_EVIDENCE` without pinned source-hierarchy authority. Exact-only replay establishes equality relative to the kernel's exact stored leaf type, not independent Alloy parser correctness. A future schema must carry a source-derivation tree or an independently pinned parser/type authority before claiming raw-source replay. |
| GC-F80 | One Luna report treated every exact singleton column without a parser-module token as unauthenticated. Applying that rule rejected valid quantified primitive-set operands, whose exact relation view is already proved by the leaf's `PRIMITIVE_SET_SINGLETON` rule. | `CLAIM CORRECTED/PENDING FRESH REVIEW`: A2-27 now distinguishes parser-derived nontrivial ancestry, which requires object/path/module authority, from exact primitive singleton columns, which require the separately replayed leaf type proof. Neither source can invent a subtype edge. |
| GC-F81 / P0 | Verifier replay admitted any nullary constructor as an exact dependent column, while the producer admitted only `Int` or a nonempty `AlloySig:*` identity. A forged singleton `Bogus` path used no subtype ancestry, so it could avoid the `UNCHECKABLE` hierarchy boundary and reach `VERIFIED`. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: verifier and producer now share the exact atomic vocabulary. Focused verifier checks admit `Int` and `AlloySig:S` and reject `Bogus`, empty `AlloySig:`, and constructor applications before chain combination. |
| GC-F82 | The standalone certified JOIN wrapper validated every source operand, but its public recursive fold returned an invalid current family unchanged at the empty-rest base case. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: the base case independently checks positive arity and every product width. Explicit arity-zero and wrong-width base witnesses reduce to `none`. |
| GC-F83 | Review prose implied the scalar `foldColumns` compatibility helper could represent a positive-arity typed-empty family. Its value is only a nonempty scalar product, so an empty list loses the arity required to distinguish typed empties. | `CLAIM QUALIFIED/PENDING FRESH REVIEW`: production and family-level APIs use `DependentTypeDag`, where typed-empty arity is retained and admitted. The scalar helper remains restricted to nonempty single-product inputs and now enforces the variadic guard for every representable operand. |
| GC-F84 | The certified variadic JOIN wrapper validated every operand, but the separately callable `joinFlatGuard` inspected only interior declared arity and returned true for malformed products. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: the guard itself validates the first, last, and every interior family; an interior with declared arity two and an empty product now returns false. |
| GC-F85 | An early standalone helper named `certifiedFamilyArrow` accepted raw untyped product families and could return mixed-width outputs. | `FORMAL REPAIRED/PENDING FRESH REVIEW`: raw `familyArrow` remains the mathematical Cartesian operation. The certification theorem now calls `typedFamilyArrow`, which checks both input families and returns an arity-indexed typed result. |
| GC-F86 | One format paragraph said typed-empty relations could not participate in JOIN/ARROW evidence, contradicting the v10 grammar, verifier, and exact empty-interior fixture. | `DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: positive-arity typed empties are explicitly admitted as source or result families; they retain only arity, contribute no alternatives or nominal boundaries, and must satisfy the ordinary result equation and interior guard. |
| GC-F87 / P0 | Producer primitive-singleton conversion accepted `AlloyCarrier(AlloySig:)`, while the dependent DAG and verifier reject an empty Alloy signature identity. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: primitive relation conversion now rejects an empty prefixed identity and a blank unprefixed carrier before constructing a relation view. Both direct conversion and leaf-proof admission have permanent negative tests. |
| GC-F88 / P0 | Producer, dependent DAG, boundary correspondence, and verifier accepted the whitespace-only identity `AlloySig: ` because their atomic-carrier checks used prefix length rather than the identity token. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: one producer predicate and its verifier mirror require a nonblank, whitespace-free suffix, including Unicode space separators. Primitive conversion and both standalone admission paths have permanent ordinary-space and `U+00A0` probes. The Round 12 snapshot is invalidated. |
| GC-F89 / P0 | Verifier replay rejected the producer's legal built-in `seq/Int -> Int -> univ` ancestry because a redundant nontrivial-path check excluded `Int` after atomic admission had accepted it. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: replay uses the closed atomic predicate for every step and retains the nontrivial-hierarchy `UNCHECKABLE` fence. A parser-backed sequence path is exported in the typed-empty fixture and replayed by the mutation harness. |
| GC-F90 / P0 | A broad adapter catch downgraded malformed dependent evidence to ordinary binary JOIN/ARROW fallback, and binding normalization could erase whitespace before checking its identity. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: malformed exact columns, ancestries, and binding identities fail at ingress. Only explicit unsupported-flattening or unsupported-correspondence markers authorize fallback; loss of live parser authority has its own unsupported marker. |
| GC-F91 / P0 | Public exact-leaf proof admission returned `EXACT_RELATION` for a generic `Rel(Bogus)` carrier before the dependent DAG rejected it. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: exact leaf admission validates the complete relation family through the same DAG atomic vocabulary. The minimum direct witness now rejects. |
| GC-F92 | Package-level producer conversion still normalized `" S "` to `AlloySig:S`, contradicting the fail-closed whitespace-free identity rule at an alternate ingress. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: both Alloy-column conversion helpers reject whitespace and Unicode space separators before construction. A direct package regression retains the exact witness. |
| GC-F93 / P0 | Producer and verifier identity admission still accepted C0 controls, Unicode format characters, unpaired surrogates, private-use code points, and unassigned code points after the whitespace repair. An unpaired surrogate could therefore change under UTF-8 certificate serialization. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: producer and verifier mirror one scalar-category admission policy at exact-type, dependent-column, binding, package-conversion, deserialization, and wire-replay ingress. Permanent probes reject NUL, `U+200B`, and unpaired `U+D800`; legal visible parser identities remain admitted. |
| GC-F94 / P0 | Replacement-prone producer encoding and polymorphic-key decoding could collapse malformed surrogate bytes into a valid `U+FFFD` identity before the scalar validator or digest comparison. The category test matrix also omitted private-use, unassigned, and valid supplementary-plane witnesses. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: both canonical encoders use reporting UTF-8 encoders, polymorphic-key replay uses a reporting decoder, and direct tests reject malformed surrogate bytes, private-use, and unassigned identities while preserving valid `U+10400`. |
| GC-F95 / P0 | General exact-type wire symbols, public in-memory wire nodes, and `SigSymbol` remained alternate identity ingresses after strict codec repair. They could admit forbidden categories or a lone surrogate before the protected encoder ran. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: verifier exact types use the same scalar policy, every `Wire.Node` tag and scalar is well-formed at construction, and `SigSymbol` reuses the exact Alloy identity guard. Direct wire, exact-type, signature, and supplementary-plane probes are permanent. |
| GC-F96 / P0 | Generic producer `GraphType` and writer exact-type emission admitted symbols rejected by verifier exact-type replay. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: generic graph symbols use the same visible-identity policy and writer emission rechecks it. Direct format, private-use, unassigned, and supplementary witnesses are permanent. |
| GC-F97 | Public exact-type constructors and parser extraction disagreed on leading `this/`; the review scope also left the nested structural-key length unit unstated. | `IMPLEMENTATION/DOCUMENTATION REPAIRED/PENDING FRESH REVIEW`: all exact columns and ancestry paths normalize `this/`. `FORMAT.md` now distinguishes UTF-16-code-unit lengths inside structural keys from strict UTF-8 byte lengths in the enclosing wire; both are injective and producer/verifier-identical. |
| GC-F98 / P0 | Local preflight found remaining `this/` aliases through `SigSymbol`, package conversion, and direct `AlloySig:` graph construction after exact-type normalization. Empty or repeated special prefixes also lacked one common rejection rule. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: all producer Alloy identity constructors normalize exactly one prefix and reject empty/repeated spellings; verifier exact-type and atomic replay reject the noncanonical wire forms. Direct cross-constructor equality and negative probes are permanent. |
| GC-F99 / P0 | Polymorphic operator and type-parameter declaration names admitted forbidden identity categories and could carry them into a structural/polymorphic wire key. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: `OperatorDeclaration` now uses the common visible-identity predicate. Eight direct operator/parameter probes cover control, format, private-use, and unassigned categories. |
| GC-F100 / P0 | Standalone polymorphic-key replay and the operator model used a broader text guard than producer declaration identity admission. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: verifier parameter replay and operator semantic identities now use an independent mirror of the visible-identity predicate. Twelve direct rejecting/admitting guard probes supplement the end-to-end verifier suite. |
| GC-F101 / P0 | Exact-type references shared one string namespace for content IDs and human-readable displays. A display equal to another type's content ID could therefore resolve to the wrong type through the verifier's test-only display fallback. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: exact-type parsing rejects every cross-type ID/display collision before any reference is resolved. A publication-profile verifier fixture constructs the minimum collision and requires `INVALID_TYPE`. |
| GC-F102 / P0 | CALL metadata and `CallSymbol` admitted embedded controls and other producer-forbidden identity categories that later operator construction or standalone replay rejected. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: CALL source names, qualified callees, and occurrence paths use the same well-formed visible-identity predicate at symbol, normalized metadata, certificate, and verifier ingress. Direct category probes retain the minimum NUL counterexample. |
| GC-F103 / P0 | A concurrent unstaged rewrite of the protected assurance runner and traceability matrix blanket-promoted every row to `PROVED/DIRECT`, producing a false `PASS` while row notes and this register still named open obligations. | `QUARANTINED/NOT ADMITTED`: the exact bytes are retained in the named Git stash `quarantine concurrent unreviewed assurance closure rewrite`; the reviewed matrix and protected runner were restored from the index. The next stable run returned `INCOMPLETE` with 182 diagnostics. |
| GC-F104 / P0 | The adapter recursively derived a `PLUS` or `INTERSECT` dependent DAG but compared it with the parser result only after erasing each column's parser-module authority to a plain relation type. Equal-looking ancestry from a foreign parsed module could therefore satisfy the comparison. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: dependent columns and DAGs now compare occurrence evidence, including exact paths and one shared live parser-module capability. Same-module equal evidence is admitted; identical labels and paths from a separately parsed module reject. Lean independently pins both outcomes, and direct dependent-chain tests retain the foreign-module witness. |
| GC-F105 / P0 | CALL occurrence rows were individually checked but not proved complete. A producer could omit a valid row, and the standalone verifier had no closed comparison between required CALL operators and the supplied occurrence ledger. | `SUPERSEDED BY GC-F106`: producer-ledger equality and total-section omission rejection were added, but standalone operator-set coverage still collapsed multiple occurrences of one callee. |
| GC-F106 / P0 | The first CALL completeness repair reduced standalone coverage to a set of semantic operator identities. Nested or repeated occurrences of one callee therefore collapsed, so deleting one row from `f[f[a]]` left the required operator set unchanged. | `IMPLEMENTATION/FORMAL PARTIALLY REPAIRED; SUPERSEDED IN AUTHORITY BY GC-F108`: each supplied wire occurrence has one distinct isolated anchor, so unpaired one-row omission rejects without breaking hash-consing or canonical CALL equality. The repair does not establish that the supplied occurrence set is complete relative to raw source. |
| GC-F107 / P0 | Local preflight found that the first occurrence anchors were complete but not explicitly isolated from semantic use. A crafted bundle could attempt to reference a provenance marker from a term, witness, proof, or canonical record. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: every anchor must be a unique nullary term, have exactly the source CALL's context and sort, and occur as a scalar exactly once in the complete bundle (its own content-addressed term ID). One bounded iterative scan checks all anchors together. Lean pins the valid isolated case plus extra-reference, wrong-context, and wrong-sort rejection. The interrupted Round 26 produced no reusable votes. |
| GC-F108 / P0 | Both Round 28 Luna reviewers removed one nested CALL row together with its unused marker term/operator. The remaining row/marker and semantic-operator sets still matched. More generally, a standalone bundle verifier that receives no raw Alloy source cannot distinguish a complete source-occurrence ledger from a coordinated self-consistent omission. | `MVP REPAIRED UNDER EXTERNAL AUTHORITY`: every non-fixture semantic verification now requires a caller-pinned, input-scope-specific occurrence commitment, including for an empty set. Absence is `UNCHECKABLE / MISSING_EVIDENCE`; coordinated omission against the retained full set is `REJECTED / MISSING_EVIDENCE`. Internal anchors remain integrity evidence, never completeness authority. `--inspect-call-occurrences` emits only an untrusted candidate. Independent raw-Alloy parser replay/refinement remains incomplete, so verification is unblocked only when the caller supplies the external pin and no broader source-completeness claim is made. |
| GC-F109 / P0 | A capitalized quantified local whose spelling matched a signature was a parser-resolved variable leaf, but source graph resolution consulted the global AAME before lexical scope. The body therefore referenced the signature, the apparently unused quantifier was removed, and both Fast rewrite IR and certified equality merged `all Trash: File | File in Trash` with `File in Trash`. Successive review also found normalized explicit-global capture, signature/field overwrite in the single AAME map, and omission of imported signatures such as `time/Time` from a local-declaration-only index. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: parser leaf authority selects lexical, signature, or field namespace over the complete parser-reachable module environment. `VarExpr` resolves only through lexical scope; `SigExpr` uses a dedicated reachable-signature map; `FieldExpr` uses name plus parser-certified exact relation type, preserving overloaded fields from distinct owners. Regressions cover local/signature, explicit qualification, signature/field, overloaded-field, and imported-signature cases. `PrenexAciScheduling.lean` pins the authority branches, missing-lexical rejection, reachable indexing, and exact-typed field separation; complete parser-to-Java refinement remains open. See `p0-lexical-signature-shadow-incident.md`. |
| GC-F110 / P0 | Import indexing registered every explicitly aliased parameterized module instance under both its declared alias and the shared raw filename. Thus `open util/ordering[A] as o1` followed by `open util/ordering[B] as o2` failed before graph construction with a false ambiguous-alias error at `util/ordering`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: explicitly aliased instances are indexed only by the client-visible declared alias, while unaliased imports retain their derived alias and raw-path lookup. Reusing one declared alias for distinct instances still fails closed. A parser-backed regression calls `o1/first` and `o2/first` and requires distinct argument-qualified semantic identities; Lean pins distinct-alias admission and same-alias conflict rejection. Complete parser import-resolution refinement remains open. See `p0-imported-module-instance-incident.md`. |
| GC-F111 / P0 | Nested same-spelled quantified and `let` bindings were distinct in `ScopeTreeNode` but value-equal in `VarSymbol` or `RefSymbol`. Inserting the inner symbol into the global `uniqueNode` map replaced the outer lookup, so a later outer occurrence could point at the inner node. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: parser-created variables carry their unique lexical scope coordinate in map identity, and `LetSymbol` carries a unique lexical identity while preserving its readable source spelling. Parser-backed nested quantifier and nested-let pairs must equal their alpha-renamed controls at both Fast rewrite and certified boundaries. Lean separately pins nested scope-path inequality. Complete parser-to-De-Bruijn refinement remains open. See `p0-lexical-binding-node-identity-incident.md`. |
| GC-F112 | Reachable-signature preindexing marked every signature node with maximum arity zero. Local `SigDecl` traversal then reused that leaf-capped node and attached field-declaration groups plus `END`, leaving contradictory node metadata consumed by generation code. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: local declaration traversal replaces the provisional leaf cap with exactly `fieldDeclarationCount + 1`; imported signatures without attached local declaration structure remain leaves. A one-field parser regression requires arity two, and Lean pins the field-declaration-plus-terminator equation. Broader generation refinement remains open. See `p0-preindexed-signature-arity-incident.md`. |
| GC-F113 / P0 | The first GC-F111 repair encoded variable node identity by underscore-concatenating type and spelling before appending scope. In one scope, `(type=A_B,name=C)` and `(type=A,name=B_C)` both produced `VAR_A_B_C@scope:2`, so the second binding replaced the first. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: variable map identity is now the presentation-independent lexical coordinate `(scope id, slot)`. Readable names and exact types remain separate metadata. The exact delimiter-collision predicate must equal its alpha-renamed control at Fast rewrite and certified boundaries, and Lean pins distinct slots in one scope regardless of presentation. Complete parser-to-coordinate refinement remains open. See `p0-lexical-coordinate-injectivity-incident.md`. |
| GC-F114 / P0 | The first `(scope id, slot)` implementation derived `slot` from the size of a name-keyed scope map. Alloy accepts repeated binder spellings; inserting the second `x` overwrote the first without increasing map size, so the following `y` reused the second `x` slot and collapsed its node. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every scope owns a monotonic allocation counter independent of name-map cardinality. Repeated names may affect name lookup according to parser-visible shadowing but can never reuse an allocated coordinate. The exact `all x,x,y` witness retains distinct parser-exposed `x` and `y` variable nodes and equals an alpha-renamed unused-binder control. Lean pins advancing consecutive allocation. Complete parser binding-resolution refinement remains open. See `p0-repeated-binder-slot-allocation-incident.md`. |
| GC-F115 / P0 | `MASGVisitor.END_NODE` and `SHADOW_NODE` were static mutable `AugmentedNode` instances. Their graph-keyed occurrence maps and edge lists accumulated links from every visited module, keeping completed multigraphs, exact parser types, `GlobalVariables`, and `CompModule` trees strongly reachable. A live 8 GiB batch near item 35,000 retained about 81,600 modules, 199,000 multigraphs, 6.6 million edges, and 12.6 million unary AST nodes and spent most of its time in GC. | `IMPLEMENTATION REPAIRED/PENDING FULL-CORPUS RETEST`: sentinel symbols remain shared identities, but mutable sentinel nodes are now visitor-local. A regression requires distinct END/SHADOW node identities and proves by mutation observation that a later visit cannot alter an earlier visitor's END links. Focused semantic tests pass, and a 500-file, eight-worker, 1 GiB batch completes at 15.73 files/s. The sorted corpus also enters the intrinsically slower `socialMedia` class at item 34,774; that workload boundary is separate from the repaired retention fault. See `p0-masg-sentinel-retention-incident.md`. |
| GC-F116 / P0 | Fourteen correct-pool comparisons exposed both directions of a strict-kernel contradiction. Every source carried the corpus label `CORRECT`, which identified the affected reference-pool stage but supplied no proof authority. Prenexed non-`ALL` coordinates were vetoed by obsolete lexical paths despite identical certified owner/exchange/dependency payloads, while `false or A` was reduced only after the certificate-facing matrix snapshot. Follow-up rounds found name-only Boolean literals, missing container/arena guards, cross-arena snapshots/comparisons, malformed branch rewrites, and Lean exchange/matching abstractions that were broader or narrower than Java. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FULL-CORPUS RETEST`: source paths remain presentation metadata only after explicit certified projection. Boolean laws require concordant literal/container/arena/profile evidence; malformed branch connectives remain explicit; snapshots preserve and freeze their source ownership; cross-arena comparisons reject. Lean exchange maps are owner-scoped, injective, monotone, parameter-aware, connected to class allocation, and model fixed pairs plus minimum cost among maximum matchings. Parser-backed, forged-type/provenance, concrete-pair, and fresh 11-file reproducer checks pass. See `p0-correct-pool-kernel-divergence-incident.md`. |
| GC-F117 / P0 | The first GC-F116 fail-closed branch guard classified relation-valued ITE as a malformed Boolean connective and returned before recursively alpha-renaming its condition. A quantified relational ITE backtranslated with an unbound `_bind_0`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: relation-valued ITE is retained but its children are traversed; only an exact Boolean ITE may expand. The parser-backed source-rule suite reparses and solver-checks the quantified witness, and `Phase5SourceRules.lean` pins the two normalization actions. |
| GC-F118 / P0 | The first malformed-Boolean guard could return before a descendant metadata-incomplete CALL reached the incidental sort-key consumer that validated its semantic identity. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every CALL validates complete metadata at node admission before any rewrite guard. The existing incomplete-CALL-under-parent regression passes, and `Phase5SourceRules.lean` pins rejection before rewrite traversal. |
| GC-F119 / P0 | Boolean rewrite guards validated the outer operator but not its operand types. Forged relation operands could therefore be erased by double negation or complement elimination, or be redistributed by IMPLIES, IFF, and Boolean ITE expansion. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every Boolean rewrite requires exact Boolean evidence for each immediate source operand; the double-negation and De Morgan paths additionally validate the inner connective. Explicit relation operands remain observable. Internal untyped pattern slots are admitted only when both exact and source type are absent. Java counterexamples and the standalone operand-concordance theorem pass. |
| GC-F120 / P0 | The first path repair inferred safe prenexing merely from entry into the repair metric, so a hand-built non-prenex binding could erase a distinct lexical scope. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: `RepairView.Binding` carries explicit prenex path-erasure authority supplied only by certified matrix/inherited projection and preserved through coherent phase reindexing. Parameters cannot claim it. Without authority on both sides, paths must match. Java positive/negative witnesses and Lean certified/uncertified theorems pass. |
| GC-F121 / P0 | Certified orbit lists were checked only for repeated owner coordinates. A malformed split partition could enter pairwise alpha search, and the formal candidate relation did not require preservation of orbit equivalence or one global owner identity across temporal occurrences. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: each owner/payload/exchange orbit must be the complete sorted partition; dynamic zero-cost mappings preserve same-orbit equivalence pairwise; inherited temporal occurrences collapse to one global identity; canonical phase reindexing preserves that identity. Java malformed-ledger and temporal-consistency controls pass; Lean proves complete/split ledger examples, orbit-preserving candidates, repeated-owner mapping, and injective matrix-owner reindexing. |
| GC-F122 / P0 | CALL validation attached to local operator admission could still be bypassed when a Boolean absorber discarded an enclosing wrapper before recursive admission reached a malformed descendant CALL. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: `NormalForm.normalize` now starts with a read-only whole-graph admission traversal. Every reachable CALL is checked before alpha-renaming, beta reduction, branch elimination, NNF, neutral/absorbing rewrites, or END cleanup. Arity-zero LET references remain an explicitly validated source placeholder rather than a lowered operator. The exact `false AND wrapper(malformed CALL)` Java witness rejects, and Lean models the recursive rewrite gate. |
| GC-F123 / P0 | Exact `Bool` metadata alone could authorize relation-only opcodes such as JOIN or GLOBALBINDING as Boolean operands, allowing `A OR NOT A` to erase a relational term. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: Boolean operands must now belong to an admitted formula-opcode family in addition to carrying concordant metatype, exact type, and source provenance. Exact forged JOIN and GLOBALBINDING witnesses remain observable; Lean independently separates formula and relation-only operand shapes. |
| GC-F124 / P0 | The package-visible raw `RepairView` constructor accepted a caller-supplied producer observation key. A hand-built view could therefore impersonate certified producer equality at the public kernel-checked metric entry point. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: only `RepairProjection` can mint a sealed view from a matching frozen `TheoryAlloyAdapter.Result`; the private constructor receiving the seal is unreachable to fixture builders. Public kernel checking rejects every unsealed view. Bounded metric fixtures use explicitly test-only unchecked or claimed-observation authorities, and Lean proves fixture equality is insufficient authority. |
| GC-F125 / P0 | A matrix variable could carry a bound index absent from its phase binding ledger, reaching an array lookup and throwing `ArrayIndexOutOfBoundsException` instead of failing at the repair-view boundary. | `IMPLEMENTATION REPAIRED/PENDING FRESH REVIEW`: `RepairView.Node` rejects indices below `-1`, and `RepairView.Phase` recursively proves that every nonnegative matrix and alpha-alternative index is present in the phase ledger. The exact absent-index and negative-index constructors reject deterministically. |
| GC-F126 | The formal maximum/optimal predicates quantify over supplied candidates and therefore do not alone prove that Java enumerates every admissible alpha mapping. | `BOUNDED DIRECT EVIDENCE/PENDING FRESH REVIEW`: an independent exhaustive Java oracle now checks all source/target words of length three over every one-, two-, and three-coordinate complete orbit, comparing the implementation against every permutation. Lean defines all six three-coordinate permutations and proves its finite enumerator complete. The metric suite passes 2,266 checks. Unbounded all-and-only Java candidate-generation refinement remains explicitly open and cannot support an unrestricted completeness claim. |
| GC-F127 / P0 | Whole-graph admission traversed every e-node in a structural e-class but not every separately allocated e-class in the same union-find component. A valid visible CALL could therefore be unioned with a malformed hidden CALL alternative and reach an absorbing rewrite. The first corrective traversal also scanned every registered class once per visited class, introducing a quadratic full-corpus risk. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: admission and certification freezing traverse every member of each reachable union component. Union roots maintain merged member sets, so component expansion is proportional to the members actually returned rather than to all registered classes per visit. The hidden malformed-CALL witness rejects, the hidden alternative freezes, and Lean quantifies admission over every alternative of every e-class in a component. |
| GC-F128 / P0 | Parent temporal normalization and `pushTemporalNegations` could rewrite or discard a temporal reference before admission had visited the referenced child phase. A malformed descendant CALL could therefore escape the source-admission gate through temporal dualization. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: `NormalForm` admits the complete acyclic temporal tree, including live and certification matrices, before any phase normalization or temporal-negation rewrite. The exact malformed-child witness rejects through both public entry points, and Lean proves that one malformed descendant phase rejects the parent temporal rewrite gate. |
| GC-F129 / P0 | Generic exact `Bool` type metadata was treated as provenance for Boolean operators. A hand-built AND or ITE with no parser operator identity could therefore invoke absorbers or branch expansion and erase source structure. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: operator rewrite authority is now either a concordant parser `MIDDLENODE_<operator>` identity or an internal exact-opcode derivation token minted only by the normalization package. Generic Boolean type evidence remains valid formula/literal typing but grants no operator law. Authority is preserved only by exact-opcode clones, reset on constants and relations, and reissued on trusted derived opcode changes. Java retains forged AND/ITE witnesses; Lean separates generic type, parser authority, and internal derivation. |
| GC-F130 / P0 | The first internal Boolean authority token remained live after public mutation of source type, exact type, or children. Restoring generic `Bool` metadata could therefore reuse stale derivation evidence and apply an absorber to a term that trusted normalization had never derived. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every public semantic and child mutation revokes both internal Boolean authority and temporal-reference provenance. A package-private NormalForm construction path alone preserves exact-opcode authority while assembling trusted normalized children. The exact type-away/type-back/child-replacement witness remains an unreduced OR, and Lean separates public mutation from trusted normalized construction. |
| GC-F131 / P0 | Recursive temporal admission checked child phases but accepted any exact-Boolean `REF` whose source spelling parsed as `temporal[index:arity]`. A public producer could manufacture that spelling, trigger temporal dualization, and pass direct theory adaptation without IRAgent provenance. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: each temporal reference is issued by its owning NormalForm against the exact source opcode and already attached child identities. Live and certification matrices require an owner-bound authority record before normalization, dualization, freezing, or direct theory adaptation. Public mutation revokes the record; exact-opcode clones retain it. Direct forged-reference and wrong-owner finite models reject, while the parser-backed temporal fixtures and 11-file reproducer pass. |
| GC-F132 / P0 | The first owner ledger still exposed a public metadata-only factory. It accepted an unreachable temporal source with no children because it compared only the caller-supplied arity with the child-phase group. Two fresh Round-31 Luna probes passed such an unbound zero-child source through admission and direct adaptation. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: the metadata-only overload is removed. `NormalForm` accepts only a single-use `IRAgent.TemporalReferenceEvidence` whose constructor is private to the MASG traversal. Issuance binds the exact parser node identity, graph, visit, child-edge occurrences and positions, source opcode/type/lineage, owner, child range, and arity; every item is rechecked at consumption. Parser-backed unary and binary temporal fixtures pass, while public API reflection confirms that no metadata-only issuance path remains. |
| GC-F133 / P0 | Temporal authority identifiers restarted at one for every NormalForm owner. A reference transplanted between structurally similar owners could collide with the destination's local ledger key. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: authority identifiers now come from one process-wide monotonic allocator, and each ledger record retains the parser occurrence and source lineage as provenance-only evidence. A parser-backed two-owner regression proves positive distinct identifiers and rejection after transplant. Lean independently pins globally distinct successive authorities and one-use parser evidence. |
| GC-F134 / P0 | Round 32 found that parser-issued temporal evidence checked each child edge against the bucket named by that edge instead of the ticket's `sourceVisit`. The existing binary-operator fallback could therefore lower a malformed visit-1 occurrence using a complete visit-2 child bucket. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: issuance and every later owner admission require one exact source-visit bucket, exact identity coverage in both directions, edge visit equality, unique contiguous positions, and a matching live graph-edge occurrence. The public malformed-visit-1/complete-visit-2 IRAgent regression now rejects. Lean exposes visit, bucket, and position premises separately. |
| GC-F135 / P0 | The first consumed temporal claim retained only numeric lineage/occurrence IDs. Removing the source graph's public vertex and edge sets after issuance did not revoke admission or direct adaptation, so the owner ledger no longer had recheckable source provenance. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: before preparation, the owner ledger retains the consumed occurrence claim and revalidates graph root, identity reachability path, parser node, source-visit edge bucket, graph-edge counterparts, exact parser type/metadata, semantic profile, owner, and child range on every admission. Clearing either source graph ledger therefore rejects through both NormalForm admission and direct TheoryAlloyAdapter. `CanonicalDistance.prepare` and certification freezing perform one final complete admission and then seal the owner-bound immutable claim, dropping the parser graph and source-node references so a prepared corpus does not retain every MASG. Lean distinguishes invalid live evidence from a valid owner-bound sealed snapshot. |
| GC-F136 / P0 | Round 33 found that `NormalForm.freezeForCertification` published its frozen flag before freezing the reachable e-graph arena. A concurrent public e-node mutation could therefore occur after adapter admission but before the arena freeze, and the adapter certified the changed source. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: each live and certification matrix now performs admission, temporal-reference validation, and an enforced reachable-component freeze under one arena monitor. The trusted path uses direct arena dispatch, preventing the former cross-thread mutation window. The NormalForm publishes its frozen flag only after parameters, descendants, both matrices, and temporal occurrence seals are frozen. The final EGraphNode boundary from GC-F137 also removes reentrant producer overrides; post-freeze mutation rejects. Fast metric preparation uses the same atomic freeze instead of retaining a mutable shallow snapshot. Lean excludes a successful mutable arena state and a published-frozen/mutable-arena state. |
| GC-F137 / P0 | Round 34 found that direct arena dispatch still called overridable e-node admission methods. A hostile EGraphNode subclass reported `CONSTANT` through `getOpcode` and made `requireAdmittedArity` a no-op, so a metadata-less underlying CALL froze and adapted successfully. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: `EGraphNode` is now a final concrete trusted-representation class. No producer subclass can override opcode, source metadata, arity admission, child access, or the freeze boundary. The regression mechanically requires the final class boundary and verifies post-adaptation mutation rejection. Lean distinguishes sealed base dispatch from an inadmissible producer hook. Instrumentation/Unsafe mutation remains outside the Java language-level threat model. |
| GC-F138 / P0 | Round 35 found that `retainReachable` marked every registered e-class reachable. A disconnected two-class union component therefore remained strongly referenced by the arena after the retained normal-form roots were known, defeating the bounded corpus-retention repair. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: cleanup now traverses child edges and expands only union components reached from retained roots, then removes every other registered class. It returns the deterministic removal count for regression evidence. A direct fixture removes exactly two disconnected classes, preserves both members of a reachable union component, and is stable on a second cleanup. Lean independently distinguishes child/union reachability from registration alone. |
| GC-F139 / P0 | Round 36 found that live temporal evidence checked the source vertex and exact edge occurrence but not membership of each downlink target in the Multigraph vertex set. Removing only a temporal child vertex while leaving its edge intact therefore preserved admission. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every recorded temporal downlink now requires its exact target identity to remain in the live graph vertex set until sealing. The regression removes only the child vertex and requires both NormalForm admission and direct adaptation to reject, restores it to prove the check is specific, and then independently checks edge removal. Lean exposes child-target liveness as a separate premise. |
| GC-F140 / P0 | Round 37 found that a non-temporal captured graph root could be removed from the Multigraph vertex set while its retained root-to-temporal-source edge and all downstream vertices remained. Path continuity and target checks therefore still passed even though the recorded traversal root was no longer live. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: pre-seal temporal evidence now requires both graph-root identity stability and exact root membership in the live vertex set. A nested `NOT(AFTER true)` fixture removes only the captured root and requires NormalForm admission and direct adaptation to reject, then restores it and proves the valid graph remains admissible. Lean exposes root liveness separately from source and child liveness. |
| GC-F141 / P0 | Round 38 passed both Luna reviews, but Terra found that `saturate()` checked mutability only for its entry root. An unfrozen second parent could recursively rewrite a shared descendant that had already been frozen through a certified parent; a later frozen sibling could also be detected only after an earlier mutable sibling had been partially rewritten. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: saturation now performs a whole reachable graph/component mutability preflight under the arena monitor before its first rewrite and rechecks each recursively visited node. A shared frozen `IN(none, none)` descendant blocks saturation from another parent, both the frozen descendant and an earlier mutable sibling remain unchanged, and Lean models all-reachable mutability plus absence of a partial result on failure. |
| GC-F142 / P0 | Round 39 found that temporal-reference completeness quantified only over issued authorities. A manually attached temporal child with no authority and no matrix REF therefore made the empty authority subset check pass vacuously and crossed admission/freezing as an unreferenced phase. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every temporal child index must now occur in exactly one owner-issued authority range; ranges must be in bounds, identity-exact, nonoverlapping, and collectively exhaustive. Temporal children require a live matrix and nonempty authority ledger, while an empty child list forbids residual authorities. Direct admission and freeze both reject the one-child/zero-reference fixture. Lean models missing, exact, and overlapping coverage. |
| GC-F143 / P0 | The other Round-39 Luna reviewer found that rewrite-created nodes used the executing thread's `CURRENT_ARENA`. A valid IMPLIES graph built on one thread and saturated on another therefore constructed its derived NOT in a foreign arena and failed child attachment. Snapshot creation also transiently consulted the wrong thread-local arena. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: the node's arena is final, public construction alone consults the thread-local graph builder, and every node synthesized from an existing graph receives that graph's arena explicitly. Snapshot construction does the same. A cross-thread implication regression completes to OR without foreign-arena failure, and Lean distinguishes owner-arena inheritance from executing-thread selection. |
| GC-F144 / P0 | Round 40 found that fixed-arity child replacement and append operations validated arity after mutating the child list. A rejected second child on `NOT` therefore left the source malformed; invalid CALL occurrence/arity setters likewise revoked provenance before validating their input. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: replacement and append paths now construct and validate the prospective child state before committing it, invocation append follows the same transaction boundary, and CALL metadata validates before provenance changes. Direct regressions require every rejected mutation to preserve the original child identity, arity, metadata, and admitted state. Lean models rejected-state preservation and one-step commit independently. |
| GC-F145 / P0 | The empty-root branch of retained-arena cleanup returned before pruning, so an arena with no retained NormalForm root kept every registered class and its union-find component ledger. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: an empty retained-root set now means an empty closure, atomically clearing both registered classes and union-find state unless the arena is already certification-frozen. A two-class union fixture reports two removals and zero on repetition; Lean proves that the empty-root closure retains no registered class. |
| GC-F146 / P0 | Round 41 found that pruning removed arena and union-find entries but left each escaped `EClass` handle marked registered. Reusing such a handle therefore looked up an erased leader, produced a null canonical class, and failed with `NullPointerException` or could re-enter erased state. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every pruned class is permanently retired at the same cleanup boundary. Canonicalization, invocation, mutation, union, admission, and retained-root traversal reject retired handles explicitly; no retired class can re-register itself. The original two-class witness now raises the documented lifecycle error, and Lean separates live from retired reuse authority. |
| GC-F147 / P0 | The other Round-41 reviewer found that NormalForm clone and synthetic factories still selected the executing thread's arena. Cross-thread temporal negation changed a child's temporal opcode before foreign-arena child attachment failed, leaving an operation/matrix mismatch. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every NormalForm-derived node now inherits its source e-node arena and semantic profile. Temporal negation stages all child duals, child matrices, parent matrices, and saturation before committing only nonthrowing field assignments. A parser-backed `NOT ALWAYS` fixture delegates the rewrite to another thread and requires a coherent `EVENTUALLY` child plus rewritten parent; Lean models all-or-nothing operation/matrix commit. |
| GC-F148 / P0 | Round 42 found that the central admitted-graph traversal read a retired root's residual node list without checking its lifecycle, so `requireAdmittedGraph()` still accepted a pruned handle despite the stated retirement boundary. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: admission and union-component expansion now require a live class before visiting any node. The stale two-class fixture requires direct admission to raise the lifecycle error, and Lean distinguishes reuse/admission authority for retired state. |
| GC-F149 / P0 | The other Round-42 reviewer found that retirement removed arena-table entries but retained the e-class node list and its child graph, shape cache, symmetry group, and slots through an escaped public class handle. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: retirement now clears all semantic payload after recording the permanent tombstone state. An escaped class retains only identity/lifecycle data needed for deterministic rejection; Lean models zero node, shape, symmetry, and slot payload after release. JVM garbage collection timing is not claimed. |
| GC-F150 / P0 | Round 43 passed Luna, but both Terra reviewers found a check-then-act race between temporal dualization and certification freeze: rewriting checked a child as mutable before staging, while another thread could freeze the child before the later field commit. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: every NormalForm in one temporal tree shares one lifecycle monitor. Construction, normalization, temporal staging/commit, admission, and recursive certification freeze execute under that monitor; derived e-node creation separately checks owner mutability under the owning arena monitor. The parser-backed race accepts only rewrite-then-freeze or freeze-then-reject, never a partially rewritten frozen phase. Lean enumerates exactly those two atomic outcomes. |
| GC-F151 / P0 | Round 43 also found that a nonempty Java list containing only null roots bypassed the explicit empty-list branch and returned without pruning the current arena. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: root filtering now treats an all-null list as the same empty closure as an empty list and clears both registered-class and union-find state. The direct fixture retires a two-class component, and Lean separately maps a finite all-null root list to no concrete roots. |
| GC-F152 / P0 | Round 44 Luna found that ordinary public metadata could assign the reserved `alloy/builtin/univ` identity to a user-created set node. The guarded `x in univ` rule then treated that forged node as the Alloy built-in and collapsed the formula to `true`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: reserved `none` and `univ` identities can no longer be installed through the public identity setter. Parser extraction uses a dedicated constructor carrying an internal built-in kind, derivation preserves that kind internally, and set-constant recognition requires the kind, exact identity, canonical source spelling/type, SET metatype, and nullary shape. Trusted normalization preserves the kind only across an unchanged nullary rebuild; every public metadata/child mutation and every nonnullary rebuild revokes it. Direct tests retain the exact forgery and authentic parser/factory cases; Lean separates public metadata, parser-factory, and derived authority. |
| GC-F153 / P0 | The other Round 44 Luna reviewer retained an `EGraphNode` removed by reachability pruning and could still read its opcode, source metadata, and children even though the owning e-class was retired. Clearing only the e-class list therefore left an escaped semantic payload and a stale child-list view. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: retirement now clears every node semantic field and child reference before discarding the class payload, and every public semantic accessor, including an already escaped child-list view, checks the e-class tombstone first. The e-class identity remains only for deterministic lifecycle rejection. Direct tests retain both the node and view and require reads to fail; Lean models live-versus-retired read authority without making a garbage-collection claim. |
| GC-F154 / P0 | Round 45 Luna supplied the valid Alloy pair `some (A + none)` and `some A`. Post-snapshot saturation removed the relational union unit, but the certificate-facing matrix retained it, so producer observations differed while repair distance was zero and the public kernel check aborted. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: authenticated `R + none -> R` and `R & none -> none` now close in the existing guarded source-rule pass before the certificate snapshot. The rule recognizes only internally authenticated Alloy `none`; ACI reassociation, commutation, and idempotence remain certificate-governed after the snapshot. Parser-backed union-unit and intersection-absorber pairs now have equal certified observations and zero repair distance. Lean proves both relation laws and equal certified/fast values after a pre-snapshot source rewrite. |
| GC-F155 / P0 | Bounded follow-up over the same production boundary found two more saturation-only smart-rule families with the same scheduling risk: `R - R -> none` and Boolean complement/contradiction. Parser duals such as SOME/NO also carried different operator source labels, while complement comparison incorrectly required those labels to match. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: self-difference now creates an arena-owned, typed, derived `none` before the snapshot. Boolean complement comparison uses the dual opcode plus identical child invocations rather than requiring contradictory source-operator labels, and complement/contradiction close in the guarded pre-snapshot pass. Parser-backed self-difference, `A or not A`, `A and not A`, and ACI idempotence controls all agree across certified equality and repair distance. Lean proves the relation, Boolean, and pre-snapshot agreement obligations. |
| GC-F156 / P0 | Round 46 Luna supplied the valid Alloy pair `some (S + S) or not (some S)` and `no none`. The enclosing complement rule compared operands before certified ACI idempotence was visible; post-snapshot repair saturation then reduced both predicates to `true`, while the producer observation retained different normalized source terms and the public zero-kernel check aborted. A bound-slot variant also showed that collapsing an entire Boolean ACI parent had to precede dual-opcode comparison. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: source smart rules now use a read-only, typed/profile-indexed certified-container quotient key that flattens only same-operator licensed ACI regions, composes every slotted invocation, sorts and deduplicates only certified Set operands, and collapses an ACI singleton before duality. Distinct operators remain barriers. Parser-backed A, C, I, self-difference, bound-slot, and operator-near-miss cases now agree across producer equality and repair distance. Lean proves relation union/intersection ACI and transports established equality into complement and self-difference; complete Java-to-Lean refinement remains open. |
| GC-F157 / P0 | An initial probe observed valid `#S + #T` as parser `PLUS` with exact result carrier `INT` and incorrectly inferred that the source denoted integer arithmetic. | `REFUTED BY GC-F162`: direct Alloy SAT checks prove that source `+` and `-` retain relational union/difference over the unary `Int` relation; exact result carrier alone cannot choose the operator family. The transient result-sort relabeling was removed before review closure. |
| GC-F158 / P0 | `some ((S + T) + (S + T))` is valid Alloy, but MASGVisitor rewound the shared `PLUS` node's visit counter after each nested same-operator child. Both children then occupied one visit while the advertised final visit had no edges; IRAgent constructed an empty `PLUS`, which failed admission before any certified observation. Distinct nested operands could also be conflated in the shared edge bucket. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: a binary parent retains its saved visit for parent edges while every nested same-symbol source occurrence keeps its later monotone visit. Repeated and distinct two-child nesting now produce complete, separate occurrence buckets; no semantic SHADOW or source operand is invented. Parser-backed equality and near-miss controls pass, and Lean proves consecutive nested visits are distinct. |
| GC-F159 / P0 | Round 47 Luna supplied `(S = Protected) or not (Protected = S)` versus `no none`. Equality and inequality have certified commutative sibling quotients, but dual-opcode complement comparison remained positional, yielding unequal producer observations and a zero repair projection. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: dual child comparison now composes invocation maps, derives certified child keys, and compares them as a multiset only when both operator policies certify commutativity. Ordered duals remain positional. Parser-backed swapped equality, bound-slot, and operand-near-miss cases pass; Lean proves the swapped equality complement law. |
| GC-F160 / P0 | The occurrence repair exposed a second valid relational witness: `(S + T) + (Product + Position)` and `S + T + Product + Position` retained different certified groupings because each parser-inferred intermediate union carrier was treated as a different monomorphic operator instance. This contradicted the advertised ACI semantics of relational union. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: only parser-authenticated relational `PLUS` may splice across a result-carrier widening, and only when the nested alternatives form a same-arity subfamily of the outer exact carrier. The adapter uses the outer operator instance and retains explicit relational coercions for every widened leaf. Unrelated profiles, operators, arities, modules, and non-subfamily types remain barriers. Distinct-nesting equality and near-miss fixtures pass; Lean proves union associativity and that restriction to an authenticated super-carrier preserves a subrelation. |
| GC-F161 / P0 | Round 48 exercised a valid nested formula ITE and reached `ITE has arity 0 outside K[3]`. `visitITE` rewound the shared ITE symbol after each nested branch, so later source occurrences occupied erased or conflated visit buckets and the production pipeline rejected before certification. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the parent keeps its saved visit solely for its three outgoing edges, while every condition, then branch, and else branch retains the monotonically allocated occurrence visit produced by traversal. No semantic SHADOW or branch is invented. Parser-backed nested formula ITE equals its direct branch expansion at certified distance zero, a nested expression ITE reaches a certified artifact with all three branches admitted, and Lean proves the three later occurrence visits pairwise distinct. |
| GC-F162 / P0 | The Round-49 candidate selected additive semantics from exact result carrier. Valid `#S + #T`, `Int + Int`, and their `-` counterparts all have exact carrier `INT` but parser identities `PLUS`/`MINUS`; relabeling them `IPLUS`/`IMINUS` changed relational set semantics into arithmetic or rejected the supported source. The first carrier repair also changed trusted law admission without changing the public registry digest. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: parser operator identity is preserved. Relational `PLUS`/`MINUS` over the unary `Int` relation keep exact `INT` carrier, while their certified Set container schema supplies the relational ACI distinction; genuine parser `IPLUS`/`IMINUS` retain integer policy. Registry v3 names this carrier rule in both producer and independent verifier, and the verifier reconstructs a positive `ALLOY/PLUS` Set-over-One(Int) bundle. Direct SAT checks establish `Int + Int = Int`, `no (Int - Int)`, and, under singleton signature counts, `#Left + #Right = 1` rather than arithmetic `2`. Parser-backed producer equality and repair distance agree, and Lean makes operator identity independent of carrier. |
| GC-F163 / P0 | Round 50 supplied valid `open util/integer; pred p { 1 fun/add 1 = 2 }`. MASG extraction correctly produced `IPLUS`, but IRAgent counted distinct literal nodes through structural `AugmentedNode.equals`; the second textual `1` requested visit 2 although that literal object stored its exact type at visit 1. Production adaptation aborted on a CONSTANT without exact occurrence type. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the lowering traversal's visit ledger is identity-keyed. Shared parser nodes still advance because they are the same object, while separately allocated equal-valued literals each begin at their own occurrence visit. Genuine repeated/nested `fun/add`, `fun/sub`, cardinality, and negative-literal probes now prepare; overflow-forbidding IPLUS retains fixed C-Bag arity two and no idempotence. The focused pipeline passes 679 checks, the exact Round-50 counterexample probe completes, and Lean separates equal literal value from occurrence identity. |
| GC-F164 / P0 | Round 51 supplied valid `sig S {}; pred mixed { some (Int + S) }`. Alloy reports `Type.is_int()` when Int participates in a heterogeneous union; `ExactAlloyType.from` treated that predicate as proof of the exclusive Int carrier, erased the `S` alternative, and exported a bundle that the independent verifier accepted under Int-only relational PLUS authority. Alloy directly satisfies `some S and (Int + S) != Int`, so the verified observation was false. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: exact Int now requires the complete nonempty product family to be exactly one unary product headed by the parser's built-in `Sig.SIGINT`. Heterogeneous Int/signature families continue through relation-alternative extraction and receive an `AlloyRelationUnion` carrier. Parser-backed extraction, producer equality/distance, direct SAT, export/verifier, and Lean classifier obligations cover the witness and exact-Int controls. Round 51 remains failed and cannot contribute review credit. |
| GC-F165 / P0 | Round 52 supplied valid `C extends P` and the associative pair `some ((Int + C) + P)` / `some (Int + (C + P))`. Alloy found no associativity counterexample, but the production observations differed at distance 4 because nested PLUS widening required literal alternative containment and ignored authenticated `C -> P` ancestry. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: a nested relation family now widens only when both exact types come from the identical parser module and every correlated candidate product is covered column-by-column by one carrier product through the candidate's authenticated ancestry paths. Reverse, sibling, cross-module, arity-changing, non-PLUS, and unproved paths remain barriers. The exact Round-52 probe, parser-backed producer/distance checks, direct SAT, and Lean positive/negative coverage obligations pass on moving bytes. Round 52 remains failed; all prior tier results are void for closure. |
| GC-F166 / P0 | Round 53 supplied the valid pair `some (P & univ)` / `some P`. Alloy found no counterexample, but the certified producer observations differed at distance 3 because `R & univ -> R` existed only in post-snapshot saturation. The same scheduling gap affected `R + univ`, `R - none`, `R - univ`, and `none - R`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: all five authenticated built-in set identities now close in the guarded source pass before the certificate snapshot and remain mirrored in saturation. Recognition still requires parser-factory or trusted-derived built-in authority; spelling and public metadata are insufficient. Parser-backed producer equality/distance checks, direct Alloy SAT checks, and standalone Lean relation proofs cover each identity. Round 53 remains failed; no review result from that snapshot contributes closure credit. |
| GC-F167 / P0 | Round 54 reviewers returned `PASS`, but a latent discovery in the review context supplied `sig A, B extends P {}` with `some (A + B + P)` versus `some P`. Alloy proves the union equal to `P`; production retained distance 4 because no guarded rule consumed parser-certified subtype containment. The first repair also required the impossible `SET` metatype from signature leaves, and a size-only child-list comparison failed to publish a same-length contextual replacement such as `(Int + A) + P -> Int + P`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: relational `PLUS` now removes a candidate only when an actual nullary user-signature leaf names the full carrier and live same-module parser ancestry proves every candidate product lies below it. The guarded pass prunes covered operands through associative union context without flattening unrelated source grouping; saturation mirrors the rule. `extends` and `in` subsignatures and arbitrary typed subrelations are absorbed only in the presence of the full carrier. Siblings alone, unrelated signatures, and merely carrier-typed field expressions do not act as full carriers. Direct Alloy, producer equality/distance, exact negative controls, and Lean subset/association theorems cover the schema. Round 54 is permanently invalid despite its two Luna `PASS` reports. |
| GC-F168 / P0 | Both Round 55 Luna reviewers supplied the valid declaration chain `sig X in P {}; sig Y in X {}` and the law `Y + X = X`. Alloy found no counterexample, but production retained distance 3 because exact Alloy `Type` records only the primitive carrier `P` and had erased the named subset-carrier boundary `X`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: each actual signature leaf now carries runtime-only parser declaration authority. Named carriers use a same-module declaration-DAG proof with unique-parent primitive recursion and all-parent subset recursion; exact-type family containment remains available only when the carrier leaf is the primitive signature named by that type. Evidence survives only trusted exact clones/snapshots and is cleared by semantic mutation, constant collapse, and retirement. Transitive subset, grouping, sibling, multi-parent, and merely typed-expression controls pass; Lean proves subset transitivity and nested-carrier absorption. Round 55 is permanently `FAIL`. |
| GC-F169 / P0 | A Round 55 reviewer contained the latent discovery `none in R = true` for every same-arity Alloy relation. The minimized unary source `none in X` had no Alloy counterexample but remained distance 3 from `no none`; `none not in X` had the dual omission. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the guarded source pass and saturation derive `true`/`false` only when the authenticated `none` operand and the other exact relation have equal arity. Unary and binary parser-backed producer/distance checks pass, direct Alloy checks establish both polarities, and Lean proves empty subset and its negation. Round 55 remains invalid because a reviewer `PASS` could not suppress this discovery. |
| GC-F170 / P0 | Generalizing GC-F169 exposed valid empty relation expressions that were not represented by a `none` leaf: `(none -> none) in R` and `some none.(A -> B)`. Plain relational `ARROW` and `JOIN` had no empty-operand annihilator before the certified snapshot. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: an exact relation-valued plain `ARROW` or `JOIN` with an authenticated empty operand now derives an empty relation carrying the operator result's exact arity, before certification and in mirrored saturation. This enables the general same-arity subset law without syntax exceptions. Parser-backed unary/binary witnesses, direct Alloy checks, and independent Lean product/composition proofs pass. |
| GC-F171 / P0 | Independent validation of the Round-55 declaration-DAG repair found that Alloy's convenience predicate `Sig.isSameOrDescendentOf` is too broad as proof authority for `sig X in A + B`: it reports `X` below either parent, although Alloy permits `X - A` to be nonempty. Using it directly could unsoundly absorb `X` from `X + A`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the producer reconstructs the declaration proof. A primitive signature follows its unique `extends` parent; a subset signature is below a proposed carrier only when every parent branch is recursively below that carrier. Identity cycles fail closed. `X in A+B` is absorbed by a common ancestor of both branches but not by `A` alone. Direct Alloy supplies the branch countermodel, parser-backed positive/negative observations pass, and Lean proves both the all-branches rule and the one-branch counterexample. |
| GC-F172 / P0 | Main-agent falsification after Round 55 found that the declaration-DAG normalization still omitted Alloy's abstract-signature cover fact. For `abstract sig P {}; sig A, B extends P {}`, Alloy proves `P = A+B`, while production retained distance 3 between `some P` and `some (A+B)`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: parser-owned direct `extends` edges now prove a complete abstract cover recursively. A cover is admitted only when every direct extension branch is represented, every additional union operand is declaration- or exact-type-certified below the carrier, and the carrier has at least one direct extension. The deterministic representative is the highest covered abstract ancestor, which includes the sound singleton-child corollary. Subset-`in` declarations never discharge cover branches. Direct Alloy, parser-backed equality/distance, missing-branch, non-abstract, subset-only, nested, singleton, and extra-subrelation controls pass; Lean proves the cover, nesting, absorption, and missing-branch countermodel. Round 57 is invalidated before reviewer closure. |
| GC-F173 / DISCOVERY | Round-58 review noted that enum atoms were not named explicitly in the abstract-cover regression ledger, even though Alloy enums are represented by an abstract carrier and complete extension branches. | `COVERAGE REPAIRED/PENDING WHOLLY FRESH REVIEW`: a parser-backed enum pair now requires the atom union and enum carrier to have certified equality and zero distance. A direct Alloy check proves the cover, and Lean instantiates the abstract-cover theorem as an enum schema. No implementation broadening was needed. |
| GC-F174 / DISCOVERY | Both Round-58 Luna reviewers found the omitted two-sided relational identity law for Alloy's reserved `iden`: `iden.R = R`, `R.iden = R`, including identity operands inside a flattened JOIN chain. Generic constant spelling could not safely authorize this rule. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: parser extraction now creates `iden` through a dedicated internal constant kind, and public metadata cannot mint that authority. The guarded source pass and saturation remove only authority-bearing `iden` operands from exact relational JOIN chains, retaining one identity when every operand is `iden`. Left, right, middle-chain, all-identity, and ordinary-JOIN near-miss cases pass; direct Alloy checks and standalone Lean prove both composition identities and the authority boundary. |
| GC-F175 / DISCOVERY | Round-58 review found that double transpose and transpose over a binary Cartesian product remained structural: `~~R = R` and `~(A -> B) = B -> A`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: exact binary relation evidence gates involution; a plain binary ARROW is rebuilt with reversed operands and the parser's exact transpose result. No general operand permutation is admitted. Parser-backed equalities have zero distance, wrong-order products remain distinct, Alloy supplies both UNSAT laws and a SAT near miss, and Lean proves pair-swap involution and product reversal. |
| GC-F176 / DISCOVERY | Round-58 review found that nested transitive and reflexive-transitive closure fixed points were omitted: `^(^R)=^R`, `*(*R)=*R`, `^(*R)=*R`, and `*(^R)=*R`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the guarded source pass and saturation collapse only exact binary nested closure occurrences, selecting reflexive closure whenever either layer is reflexive. Transitive closure remains distinct from reflexive-transitive closure. All four positive pairs, a concrete negative pair, direct Alloy SAT/UNSAT checks, and a standalone Lean finite-path construction cover the complete schema. |
| GC-F177 / DISCOVERY | A Round-59 Luna reviewer alleged that an unrelated union member could ride a valid abstract branch cover into the carrier. Independent production-path validation refuted the defect: `some (B+C)` remained distinct from `some A` at distance 3 for unrelated `C`, because `parserCertifiedAbstractUnionCarrier` checks every terminal against the proposed carrier. The review nevertheless exposed that this decisive downstream guard lacked an explicit unrelated-member regression. | `COVERAGE/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: a parser-backed pair now requires `some (AbstractA+AbstractB+S)` to remain non-equivalent and at positive distance from `some AbstractParent`; a direct Alloy run supplies a nonempty unrelated witness; standalone Lean proves that any operand with one member outside the carrier prevents equality. The runtime rule was already correctly bounded and was not broadened. The Round-59 ballot is invalid as a PASS and the repaired snapshot requires a fresh review ladder. |
| GC-F178 / DISCOVERY | Round-60 Terra review found that abstract-cover reconstruction stopped at unary signatures. Alloy proves `(B->B)+(B->C)=B->P` when `abstract sig P` is completely covered by `B,C extends P`, but both certified preparation paths retained distance 7. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: one complete parser-owned abstract cover now lifts through either coordinate of a plain binary Cartesian product when every other coordinate is the same slot-free invocation and every source alternative is parser-certified below the derived product. The derived exact type is the Cartesian product of its factor proofs, never the stale union type. Diagonal products and unrelated alternatives remain distinct. Parser-backed equality/distance, direct Alloy UNSAT/SAT controls, and Lean distributivity, lifting, and diagonal countermodel theorems cover the schema. |
| GC-F179 / DISCOVERY | Round-60 Terra review found three omitted fixed points for Alloy's reserved identity relation: `~iden=iden`, `^iden=iden`, and `*iden=iden`. Alloy found no counterexample, while the certificate-facing pipeline retained each unary node at distance 2. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the guarded source pass and mirrored saturation adopt only the parser-authorized identity child when the unary result has the identical exact relation occurrence type. Public spelling still cannot mint identity authority. All three parser pairs now have certified equality and zero distance; direct Alloy checks and standalone transpose/path proofs establish the laws. |
| GC-F180 / DISCOVERY | Round-61 Luna review and an independent main-agent probe found that the first product-cover repair admitted only binary source products. Alloy proves `(B->B->B)+(B->B->C)=B->B->P`, but the pipeline retained distance 10. The main audit generalized the witness further: the complete four-cell union `(B->B)+(B->C)+(C->B)+(C->C)` equals `P->P`, while either diagonal cells or any three-cell partial grid do not. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: plain binary ARROW source trees are flattened only for proof analysis, preserving their binary dependent-chain commitments. A complete cover can reduce any coordinate of a slot-free product subgroup; strictly smaller residual unions are normalized recursively, so complete multi-coordinate Cartesian grids close by repeated certified reductions. Derived products remain binary source trees and carry a parser-authenticated exact product proof at every node. Ternary and full-grid pairs now have certified equality and zero distance; diagonal, partial-grid, and unrelated controls remain positive. Direct Alloy checks and standalone Lean variadic-distributivity, four-cell construction, and missing-cell proofs cover the schema. |
| GC-F181 / FAIL | A Round-62 Luna reviewer identified `Int` as an omitted fixed product coordinate. Independent validation promoted the finding from discovery to a supported-path defect: Alloy proves `(A->Int)+(B->Int)=Parent->Int` for a complete abstract cover and has a nonvacuous model, but production threw `IllegalArgumentException` because its exact Cartesian constructor accepted only `Kind.RELATION`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: parser-produced exact `Int` retains scalar `Kind.INT` and its stable wire representation but now carries transient same-module authority. Only that live occurrence may contribute the unary set column `Int` to an exact Cartesian product. Synthetic `intType()`, serialized evidence, mixed parser modules, Boolean values, and arithmetic operator policy remain rejected. The parser pair now has certified equality and distance zero; direct Alloy, exact-type boundary tests, and a Lean set-coordinate theorem cover the rule. |
| GC-F182 / DISCOVERY | After two Luna and two Terra passes on Round 63, a Sol reviewer found that relational converse did not distribute through source containers. Alloy proves `~(r+s)=~r+~s`, `~(r&s)=~r&~s`, and `~(r-s)=~r-~s` nonvacuously, while production retained distance 5 for all three. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the guarded source pass and mirrored saturation push converse through exact parser-authenticated binary union, intersection, and difference while preserving the container operator and recursively reversing plain Cartesian-product operands. `ExactAlloyType.parserCertifiedTranspose` reverses correlated type columns only with live parser authority; synthetic, serialized, wrong-arity, and cross-module evidence reject. Parser pairs now compare at zero, operator near misses remain distinct, composition through a product union and a quantified binary-relation slot both compare at zero, direct Alloy checks the laws and witnesses, and Lean proves involution plus all three homomorphism laws. Round 63 is invalid. |
| GC-F183 / DISCOVERY | The other Round-63 Sol reviewer found that ordinary Cartesian-product distribution was recognized only when coordinate operands reconstructed an abstract carrier. Alloy proves `(A->A)+(A->B)=A->(A+B)` nonvacuously, while production retained distance 5 despite already formalizing binary distribution. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: product-union factoring now treats any parser-authenticated, same-profile, slot-free coordinate union as a retained relational `PLUS`; subtype and abstract covers remain optional smaller certified representatives. One-coordinate subgroups factor directly, residual unions shrink recursively, and simultaneous reductions still require the complete Cartesian grid. Binary left/right, ternary, and four-cell grids compare at zero; diagonal and three-cell grids remain positive. The exact union proof preserves correlated alternatives and rejects synthetic, serialized, mixed-module, and arity-mismatched evidence. Direct Alloy and Lean prove the positive laws and missing-cell boundary. Round 63 is invalid. |
| GC-F184 / FAIL | The first post-repair assurance run executed the semantic and Lean checks successfully but its traceability test still pinned the governed requirement count at 161 after P5-16 and P5-17 extended the scope to 163. Both invocations of that test failed before a review snapshot could be admitted. | `HARNESS REPAIRED/PENDING WHOLLY FRESH REVIEW`: the explicit anti-shrinkage pin now requires exactly 163 requirement and matrix rows. The traceability engine independently enforces one-to-one claim, scope, and matrix correspondence and the catalog reports 163 requirements with 184 honestly open diagnostics. No semantic rule or proof status changed. |
| GC-F185 / DISCOVERY | Round 64 probing found that ordinary Cartesian-product factoring discarded every product containing a bound-slot invocation. Alloy proves `(x->A)+(x->B)=x->(A+B)` for arbitrary set-valued `x`, but production retained distance 6 because the proof analysis required slot-free factors. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: product analysis now composes and retains each `EClassRef` invocation map through flattening, coordinate covers, subgroup replacement, residual unions, and binary ARROW rebuilding. Arbitrary bound-set fixed-left, fixed-right, and complete-grid pairs compare at certified distance zero; a partial grid remains positive. Direct Alloy checks the universal laws and missing-cell witness, and Lean states the product laws parametrically over arbitrary relation values. Round 64 is invalid. |
| GC-F186 / DISCOVERY | A Round 64 Luna reviewer found that the source quotient omitted both absorption and both distributive laws for Boolean `AND`/`OR` and relational `INTERSECT`/`PLUS`. The initial generalized repair then assigned every derived intermediate the outer expression's exact type, causing one false inequality and one unsound subrelation absorption on disjoint carriers. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: one guarded lattice normalizer projects direct absorption and factors expanded forms while preserving slot invocations. Boolean authority remains explicit. Relational intermediates independently derive parser-authenticated union or column-wise ancestry intersection types, including an authenticated arity-bearing empty carrier; synthetic, serialized, cross-module, and arity-mismatched evidence reject. All eight laws compare at certified distance zero over overlapping bound sets, direct Alloy proves them, exact-type boundaries are permanent regressions, and Lean proves the complete dual schema. Round 64 is invalid. |
| GC-F187 / FAIL | The first post-Round-64 traceability run found that broad claim P5-09 still named the removed slot-free product helper after product factoring was generalized to preserve invocation maps. The content-sensitive gate therefore reported one stale implementation reference. | `HARNESS REPAIRED/PENDING WHOLLY FRESH REVIEW`: P5-09 now names `plainProductFactorInvocations`, the actual invocation-preserving helper. Claim hashes and semantic statuses are unchanged; the generated catalog is refreshed only after the corrected matrix validates. |
| GC-F188 / FAIL | A fail-closed audit of the new exact intersection proof found that an already-empty prefix returned before validating later operands. A public call containing an authenticated empty operand followed by a foreign-module or serialized operand could therefore obtain an authenticated empty result without discharging every authority obligation. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: intersection now traverses and validates the complete operand list even after the semantic overlap is empty, then returns the empty result. Foreign-module and serialized operands reject in both leading and trailing positions. The Java regression fixes the hostile order, and Lean proves that an empty semantic prefix cannot erase a foreign authority obligation. |
| GC-F189 / DISCOVERY | Main-agent falsification of Round 65 found that certified containment was not used as a full-carrier intersection identity: Alloy proves `A & P = A`, `X & P = X`, `H.f & P = H.f`, and `H.r & (P->P) = H.r`, but production distances were 3, 3, 5, and 7. The first generalized implementation exposed a second fault by allowing two mutually containing composite carriers to delete each other, reducing `((S+T)+(S+T))` to an empty matrix. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: union and intersection now share a parser-authorized full-carrier containment proof, with declaration-DAG handling for subset signatures and closed primitive-signature union/product proofs for composites. A stable mutual-containment tie retains one ACI representative, and recursive mixed-union pruning remains restricted to signature carriers. Positive pairs have certified equality and zero distance; same-typed fields, sibling subset signatures, proper sub-products, and repeated nested ACI operands retain their distinctions/completeness. Direct Alloy and Lean prove the generalized laws and boundary. Round 65 is invalid. |
| GC-F190 / DISCOVERY | One Round-65 Luna reviewer supplied sound candidates for common-operand set-difference factoring, relational JOIN distribution over union, and `some`/`no` over union. Independent production probes returned positive distances although Alloy found no counterexample. The proposed JOIN/cardinality intersection analogues were independently false. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the guarded source pass and saturation mirror orient exactly four difference schemas, factor JOIN only with exact parser-derived adjacent boundary types, and expand union cardinality into Boolean ACI. Eight positive pairs now have certified equality and distance zero; eight Alloy assertions are UNSAT, two operator-boundary witnesses are SAT, and standalone Lean proves every admitted law. The obsolete Round-65 snapshot is invalid. |
| GC-F191 / FAIL | Exercising JOIN distribution through parameters `r,s,t: U->U` exposed a certified-binder mismatch: Fast Rewrite retained only unary primitive carrier evidence in each `QuantiVar`, while the parser occurrence had exact binary relation type. Valid Alloy source therefore failed before observation. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: relation-valued bindings retain every nonempty parser-derived arity, include the exact relation profile in slot identity, and use the same profile in the faithful binder adapter and repair projection. The parameterized JOIN laws now prepare and close at distance zero; Lean proves unary and binary binding profiles cannot coincide. |
| GC-F192 / FAIL | The first implementation of union cardinality normalization oriented Boolean terms back into `some/no`, hiding them from existing absorption. Reversing the orientation initially left duplicate relation terms and installed derived Boolean authority before child insertion revoked it, so existing Boolean complement and duplicate-ACI regressions failed. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: normalization now expands `some/no` over union, deduplicates the certified relation invocation set, installs derived authority only after child construction, and closes any finite fully covered opposite-polarity branch under the same Boolean container policy. Existing Boolean, ACI, bound-slot, and new cardinality regressions pass together; Lean proves the finite covered-dual schema. |
| GC-F193 / HARNESS | The nested-union splice test used `some ((S+T)+C)` and simultaneously required the normalized-IR artifact to retain that `PLUS`. The sound cardinality expansion removes the operator before the certificate boundary, making the provenance assertion inapplicable even though cardinality semantics were correct. | `HARNESS REPAIRED/PENDING WHOLLY FRESH REVIEW`: cardinality equivalence has dedicated source/solver tests, while nested relational splice evidence is exercised in an equality context where the operator survives normalization. Exact flat-construction validation remains unchanged and the focused pipeline passes 1,202 checks. |
| GC-F194 / HARNESS | The post-repair traceability census found that P5-09 still named `removeSubrelationsCoveredByFullSignatures`, which no longer existed after full-carrier generalization. | `HARNESS REPAIRED/PENDING WHOLLY FRESH REVIEW`: the matrix now names `removeSubrelationsCoveredByFullCarriers`. The atomic claim and its honestly partial refinement status are unchanged; content-sensitive validation finds no missing implementation symbol. |
| GC-F195 / DISCOVERY | Main-agent Round-66 falsification found that Alloy proves `((A-B)-C)=A-(B+C)` and admits a nonempty witness, while the production quotient retained distance 5. Left and right nesting are not interchangeable. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the guarded source pass and saturation mirror repeatedly accumulate only a left-nested difference chain into one parser-certified removal union, preserving composed slot invocations and exact result evidence. Binary and four-level parser pairs now have certified equality and distance zero; the distinct nesting control has a solver counterexample; synthetic exact types cannot authorize the rule; Lean proves the schema and nesting distinction. Round 66 is invalid. |
| GC-F196 / DISCOVERY | Main-agent Round-67 falsification proved the separate right-nested identity `A-(B-C)=(A-B)+(A&C)` with no Alloy counterexample, while production retained distance 9. Both Luna reviewers returned PASS, but those reports cannot suppress the independently validated discovery. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: right-nested difference expands under exact parser relation authority into one retained difference and one restored intersection, then recursively normalizes generated branches. The parser pair now has certified equality and distance zero; left/right nesting remains distinct; synthetic evidence rejects; Lean proves the law. Round 67 is invalid. |
| GC-F197 / DISCOVERY | Main-agent Round-67 falsification proved `(A-B)&C=(A&C)-B` with no Alloy counterexample at production distance 4 and generalized it to intersections containing multiple differences. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: one guarded extraction collects all kept operands by intersection and all removed operands by union, retaining exact parser evidence and composed slots. Single- and two-difference parser pairs now have certified equality and distance zero; synthetic evidence rejects; Lean proves both forms. Round 67 is invalid. |
| GC-F198 / DISCOVERY | One Round-68 Luna reviewer found the omitted product-difference laws `(A->C)-(B->C)=(A-B)->C` and `A->B-A->C=A->(B-C)`. Independent Alloy checks were UNSAT and production distances were 6. The audit also identified absent permanent overlap/three-branch coverage for the preceding difference repairs. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the generalized rule factors equal-length parser-certified product chains only when exactly one coordinate differs, preserves composed slots, and derives the coordinate difference from live same-module, same-arity exact evidence. A ternary middle coordinate closes at zero; a two-coordinate proposal remains distinct with an Alloy/Lean countermodel. Overlapping nesting and three extracted differences are now permanent solver/zero-distance regressions. Round 68 is invalid. |
| GC-F199 / DISCOVERY | Main-agent Round-69 falsification found the omitted coordinatewise Cartesian-product intersection law, minimized as `(A->C)&(B->D)=(A&B)->(C&D)`. Fixed-coordinate, three-product, ternary-product, and mixed-residual forms were likewise absent. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: every compatible equal-length product in an authenticated intersection subgroup contributes one operand to every coordinate intersection; the ordered product is rebuilt and residual operands stay outside. Five parser/solver pairs close at certified distance zero, authority barriers reject, and Lean proves binary, three-source, and ternary schemas. Round 69 is invalid. |
| GC-F200 / P0 | The new three-product regression reached a valid ARROW whose ACI operand was nested in the retained source and flat in the repair tree. Dependent-chain transfer required byte-identical raw content and threw before producing a certified observation, even though the changed operand shape was exactly an admitted ACI normalization. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the dependent certificate keeps its exact raw occurrence commitment. A separate frozen-repair commitment is minted only after same-lineage/path, ordered-chain, exact-type, slot-map, and certified-ACI quotient agreement; later mutation is checked against that exact repair commitment. The 1,300-check pipeline suite now covers the former crash, while certificate swaps and mutation still reject. Product/JOIN congruence is proved in Lean. |
| GC-F201 / DISCOVERY | Main-agent Round-70 review found a coverage contradiction in P5-30: the claim named both JOIN and ARROW plus all certified ACI laws, but the new permanent transfer regression exercised only n-ary intersection beneath ARROW. | `TEST/ASSURANCE REPAIRED/PENDING WHOLLY FRESH REVIEW`: add parser-backed ARROW union-duplicate elimination and JOIN union reorder, union duplicate elimination, and intersection reorder pairs. Every pair reaches the certificate-integrated pipeline at distance zero and its direct Alloy assertion is UNSAT. The production rule was already general enough; no semantic implementation was widened. Round 70 is invalid. |
| GC-F202 / DISCOVERY | Both Round-71 Luna reviewers independently found the omitted converse-composition law `~(r.s)=(~s).(~r)`, including the requirement to reverse rather than preserve JOIN order. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: exact parser-authenticated binary JOIN sequences now reverse and transpose every operand before certification. The positive source pair closes at certified distance zero; direct Alloy and Lean prove the law, wrong-order witnesses remain distinct, and synthetic authority rejects. Round 71 is invalid. |
| GC-F203 / DISCOVERY | Both Round-71 Luna reviewers independently found omitted domain/range restriction homomorphisms, nested laws, commutation, and complete-grid factoring. The generalized implementation then exposed a convergence defect caused by inconsistent source identity on generated relation-lattice nodes. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: parser-authenticated restrictions are represented as two coordinates, complete lattice covers factor without diagonal invention, one-coordinate differences factor, repeated restrictions intersect, opposite sides commute, and every derived relational operator has canonical source identity. Seventeen parser/solver pairs close; exact correlated type derivation and authority-loss barriers pass; Alloy and Lean preserve diagonal, multi-coordinate-difference, and order counterexamples. Round 71 is invalid. |
| GC-F204 / P0 | Round-72 Luna review reached valid four-operand JOIN expressions whose middle or initial coordinate was a union. The first sound factorization exposed another factorizable JOIN but the source pass cloned its certificate matrix before closing the rewrite; transfer then threw `A dependent source changed outside its certified ACI operands`. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: recursively close parser-certified JOIN factoring before cloning the certificate-facing matrix. Three n-ary coordinate laws are UNSAT in Alloy and now compare at certified distance zero; exact boundary, slot, intersection, and synthetic-authority controls remain active; Lean proves the contextual schema. Round 72 is invalid. |
| GC-F205 / DISCOVERY | Round-72 Luna review found that converse was not normalized through transitive or reflexive-transitive closure. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: commute converse with the exact parser-authenticated closure while preserving CLOSURE versus RCLOSURE and deriving the result from the closure occurrence profile. Two Alloy laws close at certified distance zero, a closure-kind witness remains distinct, synthetic authority rejects, and Lean proves path reversal. Round 72 is invalid. |
| GC-F206 / DISCOVERY | Round-72 Luna review found that converse did not swap parser-authenticated domain and range restriction. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: transpose the relation coordinate and swap DOMAIN/RANGE while preserving the restrictor and exact derived result. Two Alloy laws close at certified distance zero, an unswapped witness remains distinct, synthetic authority rejects, and Lean proves both identities. Round 72 is invalid. |
| GC-F207 / DISCOVERY | Main-agent Round-73 falsification found missing restriction identities for authenticated full endpoint carriers and missing zeros when either restriction coordinate is empty. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: built-in univ and closed primitive carrier proofs may eliminate a restriction only when exact occurrence typing proves the relation unchanged; either authenticated empty coordinate yields arity-preserving none. Ten parser/solver pairs, including quantified relation slots, close; variable-carrier and authority boundaries remain distinct, and Lean proves the complete identity/zero family. Round 73 is invalid. |
| GC-F208 / DISCOVERY | A latent lead in the interrupted Round-73 review became a reproduced omission: typed empty binary relations remained structural under converse and transitive closure, while reflexive-transitive closure lacked its `iden` result. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: authenticated arity-two empty operands now normalize under TRANSPOSE/CLOSURE to empty and under RCLOSURE to internally authenticated iden. Three parser/solver pairs close, reflexive-versus-empty and forged-name controls remain distinct, and Lean proves all three laws. Round 73 is invalid. |
| GC-F209 / DISCOVERY | Main-agent Round-74 falsification found omitted contextual restriction laws across relational JOIN. Alloy proved `(D<:R).S=D<:(R.S)`, `R.(S:>C)=(R.S):>C`, and `(R:>M).S=R.(M<:S)`, while the production distances were 6, 6, and 5. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: normalize endpoint and internal guards under exact parser occurrence evidence, ordered JOIN structure, and composed slot maps. Eight parser-backed pairs close across arbitrary chain positions, higher arity, intersected adjacent guards, and quantified slots; wrong-side, wrong-guard, and synthetic-authority barriers remain distinct; seven direct Alloy laws are UNSAT and Lean proves the three schemas. Round 74 is invalid. |
| GC-F210 / P0 | Adversarial review of the initial contextual-restriction repair found an arity-role error: a restriction on a unary JOIN operand guards the eliminated middle coordinate, but the first patch lifted it to the surviving output endpoint and falsely certified two SAT-inequivalent pairs at distance zero. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: endpoint lifting now requires the underlying restricted operand to retain that coordinate (`arity >= 2`). Unary and internal guards remain on the existential boundary and are oriented as a domain restriction on the right operand. The false pairs reopen at distances 4/5, the sound boundary pairs close at zero, Alloy proves both distinctions and both equalities, and Lean proves separate arbitrary-prefix/suffix and unary cases. Round 74 remains invalid. |
| GC-F211 / DISCOVERY | Main-agent Round-75 review found that unary domain and range restriction remained different canonical operators despite being the same pointwise intersection; the exact producer assigned distance 3 to an Alloy-UNSAT equality. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: under live parser occurrence evidence and exact arity one, orient RANGE to DOMAIN before certification. The unary pair closes at zero, binary roles remain SAT-distinct, synthetic evidence cannot authorize the rule, and Lean proves both the equality and a higher-arity countermodel. Round 75 is invalid. |
| GC-F212 / DISCOVERY | Main-agent Round-75 review found that reflexive relation comparisons remained structural: `r in r` and `r = r` had positive distance from true, while `r not in r` and `r != r` had positive distance from false. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: under parser-authenticated set-family evidence, compare both operands in the already-certified ACI invocation quotient and fold the four reflexive atoms. Four parser/solver pairs close, distinct invocations and synthetic authority remain explicit, and Lean proves both direct reflexivity and equality-premise transport. Round 75 is invalid. |
| GC-F213 / DISCOVERY | Main-agent Round-77 review found that elementary subset/lattice contexts remained outside the quotient: `r in r+s`, `r&s in r`, and `r-s in r` had distance 5 from true, while `(r+s) in t` and `r in (s&t)` had distance 6 from their exact conjunctions. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: use only parser-authenticated equal-arity relation evidence to prove recursive structural containment and to expand left union/right intersection coordinates, with De Morgan duals for NOT_IN. Eleven positive pairs close; two unsound opposite disjunctions and synthetic authority remain barriers; Alloy and Lean pin the schemas. Round 77 is invalid. |
| GC-F214 / P0 | Final-tier review of adaptive source-command replay found that a generated equality command inherited the selected Alloy follow-up command's parent. Alloy could therefore return an ancestor command's UNSAT result without executing the equality, producing a false `VERIFIED_LOCAL` zero with a replay-valid certificate. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: source profiles reject non-null command parents until complete chain semantics are represented, and every generated validation command is explicitly parentless. The minimized valid Alloy witness rejects before authority exists; the independently executed false equality remains SAT. Dedicated adaptive/profile regressions and standalone Lean command-result lemmas cover the boundary. Snapshot v16 and every ballot over it are invalid. |
| GC-F215 / DISCOVERY | A v17 Terra reviewer found that source-command replay committed the command formula in its profile but executed equality under only the copied bounds/options. A valid `run { one A }` therefore falsified the sound contextual equality `some A iff one A` using a two-atom model excluded by the command. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: the parser-owned command formula is treated as its already-lowered executable search domain and conjoined with the generated equality counterexample. This handles `run C` as `C and not E` and `check C` as `not C and not E`; the actual executed-formula digest enters Alloy evidence. Run/check contextual equalities now verify locally, their unbound forms remain SAT-distinct, and standalone Lean proves the search-domain implication. Snapshot v17 and its ballots are invalid. |
| GC-F216 / DISCOVERY | A v18 Luna reviewer found that source-bound context identity included the ignored request convenience scope. Requests at scopes 1 and 2 executed the same parser-owned scope-2 query but created two verified local records and certificates. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: normalize identity to the effective execution context. Request scope remains part of request-scoped validation; source-bound identity uses only the authenticated command index/options and source bytes. The witness now reuses one record/context/certificate, while ordinary scope-sensitive local evidence remains distinct. Lean proves both cases. Snapshot v18 and its ballots are invalid. |
| GC-F217 / DISCOVERY | A v19 Luna reviewer found that raw endpoint spelling remained in local context identity. `left` and `this/left` resolved to the same root declaration and prepared pair but created two verified local records. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: use the source-correspondence digest, not selector text, as endpoint context identity. That digest binds source bytes, parser declaration identity, canonical observations, and profiles. Qualified/unqualified witnesses now share one record/context/certificate; imported or wrong endpoints still reject. Lean proves spelling erasure after parser resolution. Snapshot v19 and its ballots are invalid. |
| GC-F218 / P0 | A v20 Luna reviewer found that generalized-schema application accepted a request command/options context without matching it to the prepared endpoints' certified semantic profile. Endpoint reconstruction still succeeded, so a zero certificate and application record could name an unauthenticated execution context. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING WHOLLY FRESH REVIEW`: authenticate every application request with `requireSemanticContext` before correspondence, cache lookup, or ledger mutation. A regression compiled against the frozen v20 classes fails because the request is accepted (`/tmp/acgn-ea-f38-pre-fix-witness.log`, SHA-256 `f3f050d5af91bb07edc7f9ea893520a5ba3b972a272c74f9206ec005fa17a098`); repaired classes reject it and keep the ledger unchanged. Lean proves that mismatched contexts cannot construct application authority. Snapshot v20 and all ballots over it are invalid. |
| GC-D219 / NONBLOCKING DISCOVERY | A v21 Luna reviewer found four source-reachable cardinality/empty-relation identities at positive `R0` distance, minimized by `no R iff R = none`. The main agent independently reproduced distances 2/2/10/10 and Alloy `NO_COUNTEREXAMPLE` for the no/some/lone/one family. | `LOGGED FOR FUTURE ADAPTIVE WORK`: this is incompleteness, not a false equality or certificate. The static inventory remains frozen. Exact observations may use instance-local augmentation; reusable admission requires parser-authenticated relational-cardinality semantics plus a generated Lean proof. Probe log SHA-256: `7ddffddc8ac7aa2d92278836114939dde6cce7af6ebb3a85470c092f919df6f6`. |
| GC-D220 / NONBLOCKING DISCOVERY | A second v21 Luna reviewer noted that direct Alloy validation accepts term equality and universal binders while automatic Boolean schema correspondence is narrower. Such verified claims can remain local and cannot participate in generalized admission. | `LOGGED FOR FUTURE ADAPTIVE WORK`: the current path fails closed and does not issue a generalized zero. Future support must retain exact term/binder semantics and produce an independently checked Lean obligation; it must not broaden static `R0`. |
| GC-F221 / P0 | A v22 Terra reviewer found that adaptive provenance committed the root source but not an explicitly opened module. Replacing a valid filesystem dependency after endpoint preparation changed the exact Alloy comparison from `COUNTEREXAMPLE` to `NO_COUNTEREXAMPLE`; the semantic profile remained equal and source correspondence accepted the stale prepared pair. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING FRESH REVIEW`: adaptive validation and source correspondence now require a closed root module until exact parser-resolved dependency bytes are committed. Ordinary imported-module parsing and canonicalization remain supported; only certificate authority fails closed. The frozen transition log is `/tmp/acgn-main-import-correspondence.log` (SHA-256 `e6b1b227e38b87649686fcbe3724745104c17f3caf3bb2decc6ee9d2120bb9b3`); repaired output is `/tmp/acgn-main-import-correspondence-v23.log` (SHA-256 `333c68ee26425ce4f61b2a39a55d5aac4d3642439efc44d2d9eb0b97f2ac998c`). Lean proves that an explicit-open state cannot construct the admitted closed-root authority. Snapshot v22 and every ballot over it are invalid. |
| GC-F222 / P0 | The bounded full-corpus preflight reached a CORRECT `trash_rl/inv1` pair whose R0 observations were equal while the repair projection charged matrix distance 5. Its certified flat AND construction had one idempotent output fiber and a singleton endpoint, but projection compared only repaired source syntax. | `IMPLEMENTATION/FORMAL REPAIRED/BOUNDED REPLAY PASSED`: the adapter transfers exact `ContainerApplicationTrace` fibers to the repaired occurrence by preserved lineage. Projection keeps every certified fiber representative for pairwise metric minimization and replays the certified singleton endpoint. Bag and sequence multiplicity remain untouched, occurrence/operator/arity mismatches reject, no adaptive or static equality is added, and standalone Lean proves idempotence plus pairwise minimization. The complete 587-file replay produced 383 distances, 204 AST skips, and 0 failures; PF-F08 records all gates. |
| GC-F223 / P0 | The supervised 66,080-file run reported 11 unreachable repair phases. Exact Set fibers were recorded against certification-tree input indices, but prenex/ACI normalization could reorder the repaired operands before projection. In a second valid case, exact idempotence removed one of two equivalent temporal operands while the metric retained its now-unreferenced phase. | `IMPLEMENTATION/FORMAL REPAIRED/PENDING BOUNDED REPLAY`: each certified flat input now records its positive source lineage, and fiber indices are remapped to the repaired occurrence through an exact one-to-one lineage multimap before projection; missing or extra occurrences reject. Repair phases are emitted only from the representative selected by already-certified alternatives, so an idempotently redundant temporal phase is not charged. Parser-backed regressions cover both child reordering and temporal duplicate elimination, and `FullCorpusPreflight.lean` proves fiber-membership preservation and duplicate temporal Set collapse. No equality or rewrite family was added. |
| GC-F224 / P0 | After the non-temporal repairs, six valid `trainStationOld/inv3` and `trash_ltl/inv12` sources failed as unbound variables inside temporal phases. Their lexical quantifiers dominated the temporal operands in Alloy, but strict phase extraction detached each child `NormalForm` without carrying the active local binder. The first complete replay then found five IFF-normalized sources where the same temporal child was visited twice under one exact scope. | `IMPLEMENTATION/FORMAL REPAIRED/FULL CORPUS PASSED`: each temporal reference now snapshots active local binders and imports them into the target phase by exact source-binder lineage, owner/target phase paths, binder context, coordinate, slot, and graph type. The child is resolved only under the certified owner frame, and repair projection uses a separate `LOCAL_INHERITED` role. Exact repeated snapshots are idempotent; any changed provenance field rejects. Branches remain phase-distinct; name-only, lineage-only, wrong-context, and wrong-coordinate matches reject. The first object-identity check was corrected to exact normalized owner occurrence lineage because normalization legitimately clones binders. Lean covers every supported unary operator and both roles of every supported binary operator, exact-snapshot repetition, and conflicting-context rejection. The six-file and five-file replays passed independently, the historical replay produced 241/241 successes, and the complete 66,080-file corpus produced 61,598 distances, 4,482 AST skips, and zero failures or incorrect zero-distance merges. |
| GC-F225 / P0 | A rewarded 32-worker, 4 GiB full-corpus run reached 62,144/66,080 with old generation at 99.95-100%. Across 212 seconds, 380 full GCs consumed 208.524 seconds while only 17 files completed. The dump showed 18 Rewarder/SAT workers and 13 certificate-preparation workers, not a one-task tail. Luna deliberately killed the JVM and its JSON remained unterminated. | `OPERATIONAL REPAIRED/PENDING FULL REPLAY`: use one heap-derived permit budget across certified construction and Rewarder, and end the prepared-graph helper scope before reward allocation. At 4 GiB the 32-thread scheduler admits nine simultaneous memory-heavy phases. The augmenter's later reward-only executor uses the same bound. Reports expose requested and effective concurrency. A deterministic Java test pins the resource calculation; semantic Lean is intentionally inapplicable because no equality, certificate, distance, or reward query changed. The failed run is documented in `p0-canonical-reward-gc-collapse-incident.md` and cannot be resumed. |
| GC-F226 / P0 | ACI-reordered Boolean branches containing retained local quantifiers could receive different pre-ACI local alpha numbers and therefore a nonzero Fast Rewrite matrix distance despite certified equality. A post-ACI renaming experiment fixed the number but broke the certificate-to-repair binding identity. | `METRIC REPAIRED/BOUNDED REGRESSION PASSED`: leave both operational and certification trees unchanged and expose retained local `QuantiVar` descriptors to the established pairwise alpha-minimization space under a distinct `LOCAL` role. The ACI permutation regression now has distance zero, while certificate projection continues to resolve the original local binding identities. |
| GC-F227 / P0 | The producer emitted semantic adapter/signature versions v13/v8 and dependent-chain theory/leaf versions v11/v4 while standalone replay still reconstructed older versions. Otherwise valid producer evidence therefore failed at the public verifier boundary before its intended verified/uncheckable classification. | `PRODUCER/VERIFIER CONTRACT REPAIRED/PENDING BOUNDED ASSURANCE REPLAY`: align replay and fixture expectations to v13/v8 and dependent theory v11/leaf v4. Exact and primitive leaves retain their v1 proof; the new parser-authenticated subfamily v2 proof is structurally replayed but remains `UNCHECKABLE` without independently pinned hierarchy authority. `ProducerSemanticEvidenceMutationTest` passes 116 checks, and `PhaseA2DependentChains.lean` pins the v11 digest. |
| GC-F228 / REGRESSION | Empty-right subset expansion rewrote `x in none` to `no x` before the guarded comparison could use an existing `ONE` or `SOME` parameter cardinality. A first closure over arbitrary unary `NO`/`SOME` then erased typed predicate interfaces such as `pred p[x:S] { some x }`. | `IMPLEMENTATION REPAIRED/BOUNDED REGRESSION PASSED`: discharge only `IN`/`NOT_IN` with an authenticated `none` right operand and existing certified `QuantiVar` nonemptiness evidence before structural subset expansion. Bare, `one`, and `some` parameters close in those comparisons; `set` and `lone` remain structural, and standalone cardinality formulas retain their typed slots. `AlloySourceRuleRegressionTest` passes 332 checks and the parameter-type repair regression remains one edit. |
| GC-F229 / P0 | Postflight soundness checking of the otherwise complete `acgn-luna-full-20260827T095404Z` run found that eager LET substitution followed by spelling-based alpha renaming captured an outer `y` beneath a same-spelled inner quantifier. `all y:S | let x=y | some y:S | x!=y` was falsely merged with `all y:S | some y:S | y!=y`, although Alloy produced a counterexample. | `IMPLEMENTATION/FORMAL REPAIRED/BOUNDED REPLAY PASSED`: parser `VarSymbol` lexical identities now survive into variable e-nodes, declaration lookup, `QuantiVar` aliases, and temporal/projection binding, while canonical keys continue to observe renamed alpha identities. The witness, an alpha-renamed positive control, and the captured negative control pass; all retained 4,088 natural zero claims have no bounded counterexample and every targeted probe remains unmerged. Lean proves identity-keyed shadow preservation and substitution; complete parser-to-Lean refinement remains open. The old run is rejected and must be regenerated. |
| GC-F230 / P0 | The replacement clean run had zero paired-file incorrect merges but augmented ranking found 18 certified zeroes between `all t:Teacher | some c:Class | P` and `all p:Person | some c:Class | p in Teacher implies P`. Primitive-domain reduction inserted the outer implication only after lifting the inner existential and silently assumed `Class` inhabited. | `IMPLEMENTATION/FORMAL REPAIRED/BOUNDED REPLAY PASSED`: a guarded universal disables an unwitnessed existential lift, with the dual restriction for conjunctive guards. Existing scoped nonemptiness evidence still licenses the movement, unsafe binders remain local, and no `univ` carrier is invented. Alloy supplies the empty-Class counterexample and `GuardedPrenex.lean` proves the exact inhabited/empty boundary. |
| GC-F231 / P0 | One residual augmented zero merged `always all t:Trash | after no t` with `always all t:Trash | after no (File&t)` for mutable `File`. The imported singleton retained exact `File` type shape, which was incorrectly reused as current-phase membership evidence. | `IMPLEMENTATION/FORMAL REPAIRED/BOUNDED REPLAY PASSED`: cross-phase variables carry snapshot provenance; exact type shape remains available, but a snapshot cannot discharge full-carrier absorption against a parser-authenticated `var` signature. Static carriers and same-phase values retain the law. Alloy supplies the temporal counterexample, Lean proves the admission boundary, and all 29 prior zero candidates replay with zero certified false merges. The rejected run and gate are documented in `p0-cross-pool-zero-distance-incident.md`. |
| GC-F232 | The traceability schema had accumulated a non-schema `DIRECT-BOUNDED` spelling even though `DIRECT` already means bounded implementation conformance. The duplicate state inflated diagnostics and contradicted the checker vocabulary. | `BOOKKEEPING REPAIRED`: matrix and explanatory documents use only `DIRECT`; the schema now states the bounded meaning explicitly. Formal limitations remain in `formal_status` and row notes rather than inventing a second conformance state. |
| GC-F233 | A syntactically valid Java test reference could name a field or local variable, and a `DIRECT` row did not prove that its test owner or named method ran from a governed entry point. | `IMPLEMENTATION REPAIRED/BOUNDED GOVERNED RUN EXECUTED`: test references must resolve to callable declarations. A separate execution-coverage gate requires every `DIRECT` Java owner in a governed runner, every named non-`main` method to be called by that owner's `main`, and every script test to be invoked by a governed runner or workflow. Counterfeit-field and omitted-execution regressions pass, and the complete governed run had zero executable failures. |
| GC-F234 | Phase 4 rebuild had a union bound but no implementation-enforced processing rank, so repeated stale dirty work was not tied to a finite measure. | `IMPLEMENTATION/FORMAL REPAIRED/BOUNDED GOVERNED RUN EXECUTED`: a checked fixed-batch budget covers the initial dirty records plus every possible leader-decreasing union epoch. Lean proves strict rank decrease for record and union steps; 522 bounded rebuild checks pass in the governed run. |
| GC-F235 | P3-16 documentation called a fixed compatibility profile production authority and one traceability row cited a Java local variable as its test symbol. | `IMPLEMENTATION/BOOKKEEPING REPAIRED/BOUNDED GOVERNED RUN EXECUTED`: production preparation retains the parser-owned complete source-command profile, cross-command comparison rejects, fixed compatibility remains internal/test-only, and the matrix names executable methods only. The complete governed run had zero executable failures. |
| GC-F236 | Mapped Lean declarations compiled, but the governed run neither hashed `lean-toolchain` nor emitted an explicit assumption inventory for each declaration. | `ASSURANCE IMPLEMENTATION REPAIRED/BOUNDED GOVERNED RUN EXECUTED`: the runner hashes the pin and a deterministic extractor emits `#check` and `#print axioms` logs for every distinct mapped declaration, failing on any unresolved name. The complete run covered 888 declarations in 24 generated logs with no executable failure. |
| GC-F237 | G-02 was incorrectly mapped to fixture theory pins rather than the fixed Alloy law registry, while G-03 lacked a complete wire-field mutation sweep. | `IMPLEMENTATION/FORMAL/BOOKKEEPING REPAIRED/BOUNDED GOVERNED RUN EXECUTED`: G-02 maps the producer registry and independently reconstructed verifier registry. The formal evidence index includes every authority/profile/operator/path/carrier/arity/law/digest/schema/parameter/endpoint coordinate, and all seventeen serialized law fields reject one-at-a-time mutation; the governed verifier suite passes 135 checks. |
| GC-F238 | CALL visit validation occurred only after graph construction; the formal “immediate stable capture” had no immutable implementation object linking creation to final validation. | `IMPLEMENTATION REPAIRED/BOUNDED GOVERNED RUN EXECUTED`: each CALL node is created inside an immutable `CallVisitCapture` before graph or symbol registration, and completion rechecks object, symbol, and occurrence identity against that same capture. Parser-backed extraction reports equal occurrence, capture, and validated-visit counts, passes 161 checks, and ran in the complete governed suite. |
| GC-F239 | `claim-ledger.md` retained historical `BLOCKED`, `FORMAL-PARTIAL`, and custom state labels after the authoritative matrix changed; seven rows also carried two adjacent state cells. The checker ignored that display field, so public bookkeeping could contradict the gate. | `BOOKKEEPING/GATE REPAIRED`: all 191 ledger rows now carry exactly one state equal to `formal_status/conformance_status`, and the traceability checker rejects disagreement or an extra cell. Synthetic state and duplicate-cell mutations are permanent; the focused checker passes 35 tests and reports 87 ready rows with 132 honestly open diagnostics. |
| GC-F240 | Fast Rewrite reported zero for nine pairs that differed only by the owner of an overloaded field and one pair whose inherited binder was independently remapped in a nested temporal phase. The field comparator ignored exact type evidence already retained by the IR, and matrix minimization chose alpha maps independently per phase. | `FAST METRIC REPAIRED/BOUNDED REGRESSION PASSED/PENDING FULL CORPUS REPLAY`: field atoms now use source spelling plus exact owner-bearing relation type when no stronger semantic identity exists; repeated temporal uses consume one jointly minimized owner mapping while phase-local variables remain local. All ten archived witnesses now cost one, positive alpha/permutation controls remain zero, and Certificate-Integrated distances/digests plus all fourteen certified-only zeroes are unchanged. The publication outputs remain historical until a full rerun. |

## Preliminary Review-Gate Counterexamples

The independent preliminary gate review at
`/tmp/acgn-review-gate-falsification/report.md` (SHA-256
`2f06b8469ceb0c16abf0fe9c65256117c59214f8c4672fdf2a0a633b77e6cb1f`)
returned `FAIL / BLOCKED`; it is not a ballot.

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| RG-F01 | `HEAD` plus an ordinary dirty-diff hash omits untracked correctness-bearing files, permitting stale snapshot substitution. | `BLOCKED`; every ballot must bind one canonical full-input manifest including tracked, untracked, ignored, mode, symlink, dependency, toolchain, configuration, governing-prompt, and generated-evidence bytes. |
| RG-F02 | The closed-world census has no independently fixed file universe, so a producer can omit an uninspected file and still claim zero uncovered files. | `BLOCKED`; the immutable manifest, not a producer-selected inspected set, must define the census denominator. |
| RG-F03 | Many rows called atomic contain independent conjunctions, allowing one tested conjunct to stand for the whole row. | `BLOCKED`; split every row into one proposition with a per-claim evidence/result record before ballots. |
| RG-F04 | Compiling theorem names can discharge stronger English claims: the reviewed Phase 1 model permitted duplicate occurrence entries, and the metric model accepted an uncertified empty alignment as admissible. | The two concrete formal counterexamples are repaired on moving bytes: occurrence uniqueness now includes at-most-one consumption, and alpha/ACI candidates carry proved compatibility, maximality, and partition obligations. `BLOCKED`; checked claim-to-formula and Java refinement mappings remain absent. Falsifiers are retained under the report directory. |
| RG-F05 | Evidence reuse is prohibited in prose but free-form ledger cells cannot enforce evidence identity, assumptions, one-to-one use, or proved implication. | `BLOCKED`; evidence must be content-addressed and reuse must cite a separately proved implication record. |
| RG-F06 | Reviewer identities and independence are unauthenticated; copied prose records can be relabeled as distinct reviewers. | `BLOCKED`; no available ballot is admissible until distinct reviewer provenance and tamper-evident records are defined and independently checked. |
| RG-F07 | Per-phase votes may refer to different snapshots, and “relevant change” has no executable conservative invalidation rule. | `BLOCKED`; all phase and holistic ballots must bind one common artifact digest, and any digest change voids all votes. |
| RG-F08 | Historical and live claims share the same Markdown vocabulary, allowing current assertions to be laundered as historical and naive tools to count stale `PASS` text. | `BLOCKED`; archive historical claims under a separately hashed schema and prohibit live `PASS` outside computed ballot records. |

## Preliminary Claim-Census Counterexamples

The independent preliminary census review at
`/tmp/acgn-claim-census-falsification/report.md` (SHA-256
`31bc69db9fefc5e4df00dba4a7ec9c498a053d3155f3fe2622a21e0e6a4d174c`)
returned `BLOCKED`; it is not a ballot.

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| CC-F01 | None of the 156 changed correctness-bearing paths had a complete file-and-atomic-claim mapping; 170 of 243 named formal declarations were absent from the ledger. | `BLOCKED`; build the manifest-derived atomic census and map every formal statement explicitly. |
| CC-F02 | Coalesced rows and overlapping ownership violate one-proposition atomicity and exactly-once authority. | `BLOCKED`; split the compound rows and add explicit checked implication/ownership records. |
| CC-F03 | Five focused suites were absent from the bounded CI runner and GitHub Actions registration, despite prompt and phase-document claims. | `BLOCKED`; protected runners remain unchanged, so any future registration requires a new nonprotected entry point and independently checked CI wiring. |
| CC-F04 | The ledger described semantic `disjoint` arguments as a commutative bag while separately declaring `DISJOINT_LIST` ordered; the reviewed implementation used `DISJOINT_LIST` as the commutative semantic operator. | Repaired on moving bytes by distinct `DISJOINT` (nonflat C bag) and `DISJOINT_LIST` (ordered, law-free) identities across IR, policy, law-theory v2, verifier, projection, backtranslation, formal model, and tests. Full parser/wire refinement and immutable re-review remain blocked. |
| CC-F05 | Three public documents named canonicalizer v3 while code named v4. | Repaired on moving documentation bytes by naming `canon-g-production-v4` and `canon-g-exhaustive-v4`; immutable re-review required. |
| CC-F06 | Public/wire, metric, provenance, serialization, GUI, experiment, manifest, comparison, semantic-checker, and deliverable claims have no atomic `PUB-*`/`EMP-*` census. | `BLOCKED`; broad global rows cannot substitute for per-surface claims and hash-bound empirical derivations. |
| CC-F07 | Current public state text mixes stale run/HEAD identifiers and `EXACT`/numeric claims with a blocked moving implementation. | `BLOCKED`; historical claims require separately hashed provenance and current claims require recomputation on the final manifest. |

## Preliminary Phase 3 Counterexamples

The independent report at
`/tmp/acgn-phase3-max-review/PRELIMINARY-FAIL.md` (SHA-256
`1e30171e0117c01503aed4b97b51615d226e8dae1885f009fb1cea5f7c1624a8`)
records:

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| P3-R01 | Unary `none`, binary `none -> none`, and ternary empty relations all map to one arity-free `AlloyEmptyRelation`. | Repaired on moving bytes using only parser-proved positive arity: `ExactAlloyType`, graph keys, the standalone verifier, visualization, and public wire grammar distinguish canonical arities; arityless/malformed encodings reject; Java deserialization revalidates and reconstructs an immutable value; no erased parent column is invented. The preliminary report is `/tmp/acgn-empty-relation-review/report.md` (SHA-256 `2a49cc406764a589c37f1529f7413222ddbc6d389422127183651b09ed01721f`). Full boundary refinement and immutable re-review remain blocked. |
| P3-R02 | Commands with `4 Int` and `5 Int` receive the same fixed four-bit semantic profile and fingerprint. The default Alloy execution options are modular while the canonical default claims overflow-forbidding; trace bounds and effective max sequence are absent; profile version labels do not name the running adapter/signature versions. | `REFUTED/BLOCKED`; the preliminary report at `/tmp/acgn-profile-fingerprint-review/report.md` (SHA-256 `2d37f739c2bda0e852a9d86859683f7303c95efa577aac53e54d7b1643b11f1c`) requires one source/command/options-derived context before canonicalization, propagation through every cache/comparison/export boundary, and independent verifier policy. A scalar width patch would leave the other contradictions intact. |
| P3-R03 | Repair projection accepts a certified artifact paired with a post-certification source/type mutation. | Repaired on moving bytes: adaptation freezes the complete normalized source, retains exact phase identities, and projection derives and checks the result digest. The original one-node `true` to `false` attack now rejects at mutation; foreign-source and forged-digest probes reject. Complete field-family, concurrency, and formal-to-Java refinement review remains blocked. |
| P3-R04 | Public binder grammar listed six fields while writer/parser require eight fields plus a dependency child. | Repaired in the format document; fresh grammar/parser/writer review required. |
| P3-R05 | Historical prose ended with “Phase 3 is closed” while the current header and ledger said reopened/blocked. | Reframed explicitly as invalidated historical review; Phase 3 remains blocked. |

## Preliminary A2 Counterexamples

The independent A2 report at `/tmp/acgn-a2-pre-review-alloy2/report.md`
(SHA-256
`9bc60270b65f87c306d92b59df7806fb18027ecd5a25c07a88e28c4fa1733d41`)
recorded these additional findings:

| ID | Counterexample | Current disposition |
| --- | --- | --- |
| A2-PF01 | Unguarded flattened JOIN certified a unary-interior association that Alloy separates in a two-atom model. | Repaired before this register; immutable re-review required. |
| A2-PF02 | `A + B` for unrelated unary signatures threw while attempting an ACI law instead of accepting the validated same-arity relation family. | Repaired on moving bytes; parser-backed source-rule suite passes the focused case and the abstract finite type-admission model compiles. Refinement remains blocked. |
| A2-PF03 | `let y = none | y` used as a declaration domain was recorded as `univ`. | Repaired with GC-F07; immutable re-review required. |
| A2-PF04 | A transient schema-v6 verifier snapshot referenced an undefined `OrbitCandidate`. | Repaired on later bytes; it remains evidence that moving-snapshot test results cannot be reused. |
| A2-PF05 | Public A2 evidence named stale schema-v5 results after schema v6 became active. | `BLOCKED`; public evidence must be regenerated from the immutable reviewed snapshot. |

The report also treated absent exact Boolean provenance as a universal failure.
That broad claim remains rejected: a derived Boolean operator result can be
justified by its validated declaration and Boolean metatype without inventing
a relational carrier. GC-F116 establishes the narrower boundary that a source
`CONSTANT` spelling may authorize a `true` or `false` rewrite only with exact
Alloy `BOOL` evidence. Relational and dependent-chain paths remain fail-closed
when their own type evidence is missing, and no path may fabricate `univ`.

## Gate Consequence

No row in this register can authorize a phase `PASS`. The superseding finite
plan in `do178c-assurance-plan.md` requires every scoped claim to have a
compiled Lean obligation and bounded direct conformance evidence, zero open
faults, one immutable evidence manifest, at least two fresh independent phase
reviews, and at least two fresh independent integrated reviews. Any finding is
`FAIL`; exhausted bounds or missing evidence are `INCOMPLETE`, never `PASS`.
