# Phase A2 Audit: Dependent JOIN and ARROW Chains

> Historical review verdicts and check counts below are retained as repair
> provenance only. They are not current evidence or ballots under the
> closed-world gate in `adversarial-review-protocol.md`.

## Status

P0 SUBTYPE REPAIR IMPLEMENTED. AN EARLIER REVIEW ROUND FOUND BLOCKERS AND WAS
VOIDED. THE PHASE REMAINS OPEN PENDING A CURRENT BOUNDED GATE AND THE STAGED
LUNA, TERRA, AND SOL REVIEW PAIRS ON ONE IMMUTABLE SNAPSHOT.

## Contract

Phase A2 extends Phase 2 without weakening its homogeneous-flat-port
restrictions. JOIN and ARROW remain ordered and multiplicity preserving. Their
binary source associations may reach one variadic `Seq` target only through a
separate dependent-chain certificate family whose proof is indexed by every
operand type and the exact result type.

For exact nonempty relation-column lists `cols(R)`:

```text
ARROW(R, S): cols(R) ++ cols(S)

JOIN(R, S):  init(cols(R)) ++ tail(cols(S))
             provided the boundary carriers are identical, or one exact
             PrimSig (including an explicit univ endpoint) is connected to
             the other by a recorded chain
             of direct PrimSig.parent edges

JOIN(R1,...,Rn) may be reassociated only if every interior operand
R2,...,R(n-1) has at least two columns. This keeps its left and right join
boundaries distinct. A unary interior operand remains ordinary binary syntax.
```

If every alternative pair in a well-typed JOIN is authenticated as disjoint,
the result is a typed empty family with arity
`arity(R) + arity(S) - 2`. It still retains the complete pair-decision matrix
and ordered source `Seq`; it is not an unsupported fallback. Alloy has no
nullary relation carrier, so a computed result arity of zero remains outside
this certified slice.

Repeated operands remain repeated. Source order remains observable. The
dependent `Seq` carrier grants no generic associativity, commutativity,
idempotency, permutation, or unit authority. Reassociation is justified only
by independently replaying the typed source chain against the equations and
the interior-arity guard above. ARROW needs no corresponding guard.

## Independent Leaf Typing

Every leaf contains its own stored port type, exact relation view, named typing
rule, structural proof, and source identity. The closed v5 rule family is:

- `EXACT_RELATION`: the stored type and relation view are identical;
- `PRIMITIVE_SET_SINGLETON`: `Int` or `AlloyCarrier(S)` is lifted to the unary
  relation view `Rel(Int)` or `Rel(AlloySig:S)`.

There is no name-based parameter rule. A source spelling such as `ParameterN`
has no independent typing authority; parameter occurrences must carry the same
exact relation or primitive carrier evidence as every other leaf.

No absent type is converted to `univ`. Missing source provenance rejects at
binding construction. A parser-authenticated explicit `univ` is an exact unary
relation carrier: `univ/univ` is an exact boundary and `S/univ` consumes the
authenticated `S -> ... -> univ` path. A genuinely unresolved expression whose
exact relation columns cannot be proved does not receive a dependent-chain
certificate and stays in the ordinary fixed-binary representation.

## Implemented Obligations

| Obligation | Implementation | Regression evidence |
| --- | --- | --- |
| Ordered heterogeneous carrier | `SeqPortSchema.dependent` stores one exact schema per position | role swaps reject; duplicate ARROW operands survive |
| Closed chain equations | `DependentChainKind` implements exact-column JOIN and ARROW folds; `DependentChainTheory.requireSoundFlattening` enforces the JOIN side condition | boundary mismatch and nonrelation operands reject; unary-interior JOIN stays binary |
| Explicit subtype correspondence | `ExactAlloyType` retains direct-parent paths only when every `PrimSig` is an identity member of the exact originating `CompModule`; user-declared edges below top level are source-positioned, while the closed built-in rules cover `Sig.SEQIDX -> Sig.SIGINT` and an authenticated top-level carrier's parser parent `Sig.UNIV`. A transient module capability is retained only for proof admission. `DependentColumnEvidence` and `DependentBoundaryCorrespondence` bind exact endpoints, direction, and the shortest endpoint path without changing canonical carrier identity | `Product.(Component->Position)` and both concrete/`univ` boundary directions remain ordered dependent `Seq`; legal `seq` fields are admitted; module-free, synthetic, same-label foreign-module, transferred-attribute, deserialized, sibling, and path mutations reject |
| Independent leaf proofs | `DependentChainLeaf` and `DependentChainTheory` bind stored type, relation view, rule, and proof | exact and primitive singleton rules replay independently |
| Exact certificate index | `DependentChainCertificate` binds profile, deterministic source occurrence/content, source association, all operand types, result, target, theory version, and digest | left/right associations have distinct source endpoints and one target only when the guard holds |
| Source authority | `ConstructionSourceLedger` records concrete source applications before certified construction | omitted, extra, or unreferenced evidence rejects |
| Independent wire replay | schema v10 retains source occurrence/content commitments, correlated type DAGs, source trees, leaf rules/proofs, exhaustive application pair matrices, ancestry paths, and dependent positional schemas | malformed commitments, omitted or reordered pairs, changed decisions, subtype directions/endpoints, conflicting parents, cycles, unary JOIN interiors, and arity mismatches reject; structurally valid nonexact subtype evidence remains `UNCHECKABLE` without an external hierarchy authority |
| Metric preservation | `RepairProjection` flattens only an immutable occurrence binding mapped to an artifact-admitted certificate, then rechecks lineage, source content, kind, output, leaf order, and every leaf type | licensed JOIN/ARROW reassociations have certified equality and repair distance zero; unlicensed JOIN remains nonzero |
| Fail-closed fallback | unresolved or unprovable typing, nullary-result, or non-associative JOIN chains remain fixed binary; authenticated disjointness produces a typed empty family | `x.*r` and a unary interior JOIN receive no dependent JOIN certificate, while `(A->B).(C->D)` retains a dependent empty `Seq` |

## Fault And Contradiction Log

| ID | Located fault | Repair | Regression |
| --- | --- | --- | --- |
| A2-F01 | Homogeneous flat-port evidence cannot express JOIN/ARROW's changing intermediate types. | Add a separate dependent ordered-Seq family and closed exact-column theory. | JOIN and ARROW association fixtures converge only through dependent evidence. |
| A2-F02 | Initial dependent leaves carried relation views without naming how a primitive stored slot justified that view. | Add a closed leaf typing-rule enum and proof indexed by stored and relation types. | Unsupported constructors and non-unary primitive views reject. |
| A2-F03 | The first wire shape did not expose enough leaf data for independent type replay. | Serialize term, relation type, type rule, source key, and type proof; independently reconstruct all five. | Scalar/rule/proof mutations reject. |
| A2-F04 | Generic flat/container verification paths could accidentally consume a dependent `Seq`. | Reject dependent schemas from homogeneous evidence and require the dedicated source-tree verifier. | Forged generic evidence for a dependent target rejects. |
| A2-F05 | Certified semantic equality initially reached one target while Layer 3 still compared the old binary source syntax, violating the metric kernel. | Transfer the concrete admitted source certificate to its repair occurrence and revalidate kind, output, leaf count, order, and every leaf type before metric flattening. | Both parsed associations have distance zero; unlicensed chains stay binary. |
| A2-F06 | Missing type provenance was normalized to `univ`, fabricating an apparent unary relation proof. | Remove the default and require a nonempty source type at binding boundaries. | A direct null binding-type attack fails closed; explicit parser-provided `univ` remains distinguishable and flattens when its chain equation is valid. |
| A2-F07 | Repair labels compared raw atom spelling, so `this/S` and `S` were certified equal but one edit apart; the same spelling at two exact relation types was certified unequal but zero edits apart. | Separate readable payload from a hidden certified semantic payload containing normalized identity and exact result type. | Both kernel directions now accept: spelling normalization costs zero and an exact type change costs one. |
| A2-F08 | Dependent certificates were transferred by reusable parser ID, opcode, result type, and FIFO traversal order. Two same-ID chains with different operand partitions cross-attached proofs. | Reseed a unique non-semantic occurrence lineage at the pre-ACI checkpoint, preserve it through the certification clone and same-opcode normalization, and transfer by lineage. | Two ARROW roots with ID `701` and result `A->B->C` retain distinct lineages and the correct positional proofs after ACI reorder. |
| A2-F09 | An unproved Fast Rewrite e-class union could select one representative for certification and another source spelling for repair projection. | Require every pre-certification source occurrence to be the sole member and canonical leader of its e-class. | A manually unioned `left`/`right` source rejects before any certificate is issued. |
| A2-F10 | A concrete JOIN/ARROW type disagreement could fall through to an unchecked ordinary binary operator. | Reject concrete chain-equation and declared-result mismatches; permit binary fallback only for a recognized unsupported correspondence, nonnullary-slice limitation, or non-associative JOIN shape. | A concrete `A -> B` node falsely declared unary `X` rejects; `x.*r` remains binary without a dependent certificate, while well-typed explicit-`univ` chains do not take fallback. |
| A2-F11 | The public projection API accepted an artifact and independently supplied evidence maps, permitting certificate-map substitution outside the adapter result. | Accept the privately constructed `TheoryAlloyAdapter.Result` as one evidence unit and enforce injective certificate use. | One certificate cannot be replayed over two roots through the production projection API. |
| A2-F12 | A preliminary reviewer swapped same-typed JOIN/ARROW certificates and mutated leaf names after adaptation; both attacks were accepted because the bridge checked only opcode, output, and positional types. | Bind each certificate endpoint to a deterministic source path and canonical source-content commitment; retain an immutable lineage binding and revalidate it during projection. | Same-typed swaps and post-certification mutations reject for both JOIN and ARROW. |
| A2-F13 | A preliminary reviewer found the minimum unary-middle counterexample to JOIN associativity: `R={(A,B)}`, `S={(B)}`, `T={(A,A)}` makes `(R.S).T={(A)}` but `R.(S.T)={}`. The old type-only proof certified both. | Version the fixed theory to v2 and require every interior JOIN operand in a variadic chain to have at least two columns. Unsafe but valid Alloy expressions fall back to fixed binary syntax. | The exact counterexample now has one inner certificate per side, distinct canonical observations, and repair distance `4`; the independent Lean model proves both the counterexample and guarded reassociation. |
| A2-F14 | The original Lean theorem proved only list-append equality after assuming a decomposition that excluded the unary-middle case. | Replace it with tuple/relation semantics whose dependent middle tuple has distinct left/right boundaries, plus an explicit unguarded countermodel. | Pinned Lean 4.33.0 compiles the guarded denotational theorem and counterexample with no admitted theorem. |
| A2-F15 | A parameterized, guarded JOIN was association-sensitive because an intermediate implementation wrapped a primitive carrier as `ParameterN(AlloyCarrier(S))`. | An attempted v3 repair added name-based `PARAMETER_SINGLETON`; A2-F18 later invalidated and removed that authority. The live v5 repair retains v4's exact type evidence independently of readable parameter spelling. | Current tests reject forged `ParameterN` typing authority and admit only exact relation or primitive singleton proofs. |
| A2-F16 | The schema-v5 documentation and the intermediate v3 implementation disagreed about whether `PARAMETER_SINGLETON` existed. | Superseded by A2-F18: v4 removes the rule from producer, verifier, formal contract, and live documentation. | The two-rule producer/verifier enum and fixed theory text agree; unsupported constructors reject. |
| A2-F17 | Public schema/verifier documentation described JOIN dependent reassociation without stating its required interior-arity guard, which could be read as authority for the falsified unguarded associativity claim. | State that ARROW is generally associative but a JOIN chain longer than two requires every interior operand to have at least two columns; otherwise it remains fixed binary. | `FORMAT.md`, verifier `README.md`, fixed theory source, verifier replay, Java regressions, and Lean counterexample now state the same boundary. |
| A2-F18 | The attempted v3 `PARAMETER_SINGLETON` rule derived semantic type authority from readable source spelling and contradicted the implementation's later two-rule fail-closed policy. | Version the dependent theory to v4, remove name-based authority, and prove that every successful relation view belongs to the exact-relation or primitive-singleton family. | `parameter_spelling_has_no_independent_authority` and `successful_relation_view_has_only_two_rule_families` compile; forged `Parameter0(...)` and unsupported constructors reject. |
| A2-F19 (P0) | A valid Alloy subtype-overlap JOIN, `p.position` for `p: Product` and `position: Component -> Position`, aborted full-corpus augmentation because the v4 proof family required literal boundary equality. The first incident patch avoided the crash by falling back to fixed binary syntax, but that discarded the requested dependent-chain authority and was therefore insufficient. The first review round then found that the Lean wire predicate failed to bind the encoded rule/endpoints, the hierarchy could be self-asserted to the standalone verifier, and the incompatible wire extension still used the v8 label. | Version the fixed theory to v5 and the wire contract to v9. Preserve each parser-provided `PrimSig.parent` path as proof-only metadata; derive an explicit `EXACT`, `LEFT_SUBTYPE_OF_RIGHT`, or `RIGHT_SUBTYPE_OF_LEFT` correspondence; bind it into source/application/theory/certificate keys; serialize it; and replay a single acyclic parent ledger. Keep JOIN as ordered `Seq`. That revision temporarily rejected `univ` endpoints; A2-F34 later superseded that restriction. Make the Lean wire predicate enforce the same closed rule, endpoint, direction, and direct-parent conditions. Because the standalone bundle does not authenticate parser hierarchy, return `UNCHECKABLE` after successful structural subtype replay; malformed evidence remains `REJECTED`. | The parsed production-line shape carries `RIGHT_SUBTYPE_OF_LEFT` with shortest witness `Component -> Product`; Lean proves subtype membership preservation, concrete wire-to-correspondence, exact resulting columns, and rejection of sibling, reversed, truncated, and unrelated paths; producer/verifier mutations reject, v8 relabeling rejects, and valid nonexact subtype wire evidence cannot become standalone `VERIFIED`. |
| A2-F20 (P0) | A fresh authority review constructed `new PrimSig("Child", parent)` and passed its `Type` to the public exact-type converter, minting a nontrivial path without a parser declaration; the same metadata also survived Java serialization. | Require every nontrivial producer ancestry edge to carry a live parser-assigned declaration position. Keep that authority marker transient, reject nontrivial synthetic and deserialized ancestry at the dependent bridge, and retain self-only public relation types. This is producer-side admission only and does not upgrade standalone verification. | Parsed `Component extends Product` remains admitted; a public synthetic child and a serialized parsed value both reject as subtype authority; stream version 3 preserves ancestry data but not authority. |
| A2-F21 (P0) | The source-position repair in A2-F20 was still forgeable: parser `Attr` objects are publicly readable and can be transplanted onto a synthetic `PrimSig`, carrying a genuine position without module ownership. | Bind nontrivial ancestry to identity membership in the exact final `CompModule` as well as source positions. Make the module an explicit `MASGVisitor` constructor dependency and make ordinary `from(Type)` non-authoritative for subtype evidence. | A transplanted parser attribute on a foreign `PrimSig`, a raw synthetic chain, module-free conversion, and deserialization all reject; the parsed production `Component` path remains admitted. Lean separately models live module-object membership and authority loss on serialization. |
| A2-F22 (P0) | The first fresh post-A2-F21 reviewer found that the Lean fold admitted unary chains and unary interior JOIN operands, did not consume parser authority, accepted an unsupported nullary result, encoded validity as an assumed equality, and omitted schema-v9 standalone outcomes. The first identity ledger also failed to bind each object ID to its nominal label. | Separate structural wire evidence from live producer evidence. Bind every path identity to its nominal carrier and every parent edge to a positioned declaration; feed that evidence into an executable producer fold. Enforce Java's minimum chain arity, nonempty operands, JOIN interior guard, non-nullary result, evidence count, and computed-result comparison. Require a checked index before granting equality authority. Add an independently executable structural wire fold and schema-v9 outcome function. | Unary JOIN/ARROW chains, unary-interior JOIN, nullary JOIN, transferred identity, swapped identity/label, missing position, and serialized authority all reject. The concrete production fold succeeds. Schema v8 and malformed v9 reject, exact v9 verifies, and structurally valid nonexact v9 is `UNCHECKABLE / MISSING_EVIDENCE`, never `VERIFIED`. The failed five-reviewer ballot was stopped and invalidated after its first `FAIL`. |
| A2-F23 (P0) | The second fresh formal reviewer showed that A2-F22's first authority model still accepted duplicate object IDs and self-supplied position flags. Its slice-level exact outcome ignored the fixed theory digest, was named like a whole-verifier result, and wire replay assumed the complete expected record. | Move concrete positioned child/parent declarations into the parser-module snapshot, require unique object IDs, and replay object/nominal membership plus every declared edge. Pin and check the exact v5 theory version and digest. Rename outcomes as dependent-chain-slice results. Replace whole-record equality with a field-by-field executable checker and retain complete parser/JVM and wire-parser refinement as explicit open work. | Duplicate IDs, swapped ID/label pairs, missing declarations, blank source positions, wrong digests, old schemas, malformed folds, and nonexact escalation reject. Exact v9 chain evidence is accepted only with fixed metadata; this is not a claim of whole-bundle `VERIFIED`. The second ballot was stopped and invalidated after its first `FAIL`. |
| A2-F24 (P0) | The pre-ballot formal review found that a functional, module-owned parent ledger could still contain a cycle and that adjacent boundaries in one variadic JOIN could consume distinct authority snapshots. | Require global acyclicity for every object in the module parent ledger. Require all producer boundary witnesses in one JOIN fold to carry one structurally identical module snapshot. | A two-node `Component <-> Product` ledger rejects even when the selected `Component -> Product` edge is valid. A guarded three-operand JOIN whose two boundaries use separately numbered but otherwise valid module ledgers rejects. A same-module concrete chain remains admitted. |
| A2-F28 (P0) | The single-product subtype stack could not represent Alloy's correlated union-of-products types and could therefore widen `(A->B) + (C->D)` into an unsound independent column product or omit alternative-pair JOIN obligations. | Replace the stack projection with a normalized finite dependent type DAG whose leaves are correlated ordered products. ARROW records the full Cartesian product; JOIN records every row-major alternative pair as exact/subtype overlap or authenticated disjointness. Synthetic union/common-ancestor nodes never become nominal authority, and sharing only `univ` never creates overlap. Bump dependent theory to v7 and wire schema to v10. | The standalone `DependentTypeDagStandalone.lean` module compiles without admitted axioms or unsafe declarations. Java exercises correlated UNION/INTERSECTION, four-product ARROW, complete two-by-two JOIN, Seq order/multiplicity, and parser-result agreement. Producer/verifier mutations reject omitted, reordered, and altered pair evidence; schema v9 rejects. |
| A2-F25 (P0) | Ballot 3 found that legal sequence fields were rejected because the Alloy-owned `seq/Int -> Int` edge lacks an ordinary source filename. | Add a closed identity-only built-in edge rule for `Sig.SEQIDX -> Sig.SIGINT`; retain positioned-declaration requirements for user edges. Model the two provenance families separately in Lean. | `sig B { f: seq A }` produces authenticated columns with path `seq/Int -> Int -> univ`; a same-label synthetic sequence index rejects. |
| A2-F26 (P0) | Ballot 3 found that `ExactAlloyType` discarded its source-module identity after extraction, so separately parsed modules with identical labels could share dependent evidence. A consumed JOIN boundary could also erase the only earlier authority before a later foreign operand. | Retain the exact `CompModule` as a transient proof-only identity; require both subtype endpoints to share it and precheck every live capability across all original variadic leaves. Keep it outside semantic equality, stable keys, serialization, and certificates. | Same-module subtype JOIN succeeds; conflicting-parent modules, textually identical foreign modules, and a three-operand consumed-authority attack reject. |
| A2-F27 (P0) | Ballot 3 found that Lean's caller-settable `live` Boolean could be restored after serialization. | Replace the Boolean with a capability proposition indexed by the exact immutable snapshot, and make serialization return a distinct snapshot-only state. | The formal model compiles with serialization rejection by construction; malformed snapshots, built-in-label substitution, cycles, and mixed module identities remain executable rejecting witnesses. |
| A2-F29 (P0) | A fresh review found that binary DAG combination checked common parser-module identity but did not explicitly validate the merged parent ledger until after JOIN or intersection could consume the columns carrying a contradiction. Valid parser modules cannot create this state, but the fold boundary did not itself preserve the invariant. | Validate hierarchy functionality and acyclicity over both complete input DAGs before any pair decision or column elimination. | A reflection-based internal-state attack gives the same exact carrier two parents under one live authority and arranges for JOIN to consume both boundary columns; the public combination rejects before producing a result. |
| A2-F30 (P0) | A direct DAG caller could combine two exact `AlloySig:univ` boundaries while the then-current source-chain guard and standalone verifier denied all explicit `univ`. | `SUPERSEDED BY A2-F34`: the attempted blanket rejection restored implementation/model agreement but encoded the wrong Alloy boundary. | Retained as provenance for the invalidated review round, not as a current invariant. |
| A2-F31 (P0) | The public exact-column constructor accepted arbitrary nullary `GraphType` constructors such as `BogusCarrier`, allowing theory-level certificates outside the Alloy carrier vocabulary that the standalone verifier rejects. | Restrict dependent columns to `Int` or a nonempty `AlloySig:*` identity. Update abstract fixtures to name explicit Alloy signatures. | Empty `AlloySig:` and non-Alloy constructors reject; all parser-backed, sequence, correlated-DAG, writer, and verifier fixtures remain admitted. |
| A2-F32 (P0) | Local preflight found that `DependentTypeDag.fold` validated each current pair but not all original operands before folding. An exact JOIN could consume module A's last live column, after which a later exact boundary from module B appeared compatible. | Flatten every original operand's correlated alternatives into one preflight ledger and validate common module authority, parent functionality, and acyclicity before the first pair is combined. | The minimum three-operand witness `[X] . [X,Y] . [Y,Z]`, with the first and last operands parsed in different modules, rejects in both the scalar-column and DAG-fold APIs. |
| A2-F33 (P0) | Local formal review found Java/Lean disagreement under the temporary blanket-`univ` exclusion. | `SUPERSEDED BY A2-F34`: matching the exclusion in both implementations removed the contradiction but preserved the incorrect exclusion. | Retained as provenance for the aborted review round. |
| A2-F34 (P0) | The blanket exclusion conflated an explicit parser-provided `univ` relation with absent or unresolved type information and prevented legal associative chains such as `(x.trans).univ` and `(univ.trans).x` from flattening. | Admit explicit `univ` as an exact carrier; derive exact or authenticated concrete-to-`univ` boundaries; remove all producer/verifier fallback special cases; retain the unary-interior JOIN guard as the only association-specific non-flattening condition; bump the fixed dependent theory to v8. | Java proves both boundary directions, exact `univ`, ARROW admission, and both associations of both three-operand examples converge to ordered three-element `Seq` nodes. The parser pipeline reports certified equality and distance zero. Both standalone Lean models compile, and absent type still cannot invent `univ`. |
| A2-F35 (P0) | A parser-valid positive-arity JOIN whose complete alternative matrix was disjoint, such as `(A->B).(C->D)`, was classified as unsupported after correctly deriving zero result alternatives. This discarded valid flattening and contradicted the rule that only a genuinely non-associative JOIN shape blocks reassociation. | Introduce a positive-arity typed empty dependent family, preserve zero alternatives together with the complete disjoint matrix and ordered source `Seq`, admit it through producer and verifier theory v10, and keep only computed nullary results outside the relation slice. | Parser-backed binary and variadic tests retain typed-empty arity and source order; the standalone Lean model proves all-disjoint JOIN and INTERSECTION results, empty UNION arity, and nullary rejection; verifier mutations of empty arity, carrier, and common ancestor reject. |
| A2-F36 (P0) | Fresh Luna review found that a typed-empty interior operand passed the producer's arity-two JOIN guard but the verifier attempted to decode it as a nonempty product family and rejected it. | Make verifier guard replay use the same positive relation-arity function as producer admission. | A three-leaf certificate with an exact arity-two empty interior is independently `VERIFIED`, preserves all three source positions, and would fail under the old decoder. |
| A2-F37 (P0) | The parser adapter accepted an empty result type before recursively proving that a source `PLUS` or `INTERSECT` expression derived that empty family. | Inspect set-operator structure before the empty-leaf base case; recursively derive every operand DAG and require exact equality with the parser result. | Parser-backed empty INTERSECT and nested UNION leaves enter dependent JOIN only after recursive derivation and retain arity one. |
| A2-F38 (P0) | The first formal correlated-family wrapper checked only shape and a shared module ID. It did not bind columns to parser objects or ancestry paths and did not compose JOIN family semantics with that authority. | Pair each correlated column with its live module capability, parser object path, and nominal path; check `parserAuthorizesPath`; execute ARROW/JOIN directly over paired products; retain paired proofs in results. | Lean rejects an arbitrary `univ` column carrying the Product path and mixed modules, proves complete correlated ARROW/JOIN matrices, proves retained path validity, and preserves a typed-empty all-disjoint JOIN. |
| A2-F39 (P0) | Direct variadic fold APIs bypassed the interior-arity guard enforced by `DependentChainApplication`. | Invoke `requireSoundFlattening` in graph-type, scalar-column, and DAG folds before any combination. | All three representable `[A->B, B, A->A]` witnesses reject. Graph-type and DAG family folds retain and admit an arity-two typed-empty interior; the scalar compatibility helper intentionally represents only one nonempty product and carries no empty-family arity. |
| A2-F40 (P0) | Public standalone `TypedFamily` values could claim arity zero or a product width different from their arity and still enter JOIN. | Validate positive arity and every product width at each UNION, INTERSECTION, ARROW, binary JOIN, and variadic JOIN boundary. | Forged nullary-empty and wrong-width JOIN operands reduce to `none`; legal typed-empty positive-arity families remain admitted. |
| A2-F41 | The older parser-correlated Lean fold discarded absent authority values before module comparison. | Require a present modeled authority for every parser-correlated column before checking one common module. Keep Java's separately replayed primitive singleton leaf rule distinct from parser ancestry authority. | All-absent and mixed present/absent parser-correlated folds reduce to `none`; primitive Java slots remain exact without inventing subtype paths. |
| A2-F42 | Wire replay does not independently reconstruct raw UNION/INTERSECTION child DAGs; it checks the producer-derived leaf DAG against the kernel's exact stored leaf type. | Keep raw-source derivation outside `VERIFIED` production claims until a future schema carries the derivation tree or pins parser/type authority. | Nontrivial parser ancestry remains `UNCHECKABLE / MISSING_EVIDENCE`; malformed DAG structure rejects; exact-only replay is explicitly relative to the kernel model rather than raw Alloy parser correctness. |
| A2-F43 (P0) | Standalone replay accepted a singleton exact relation column headed by any nullary constructor, including `Bogus`, although producer construction permits only `Int` or a nonempty `AlloySig:*` carrier. | Mirror the producer's atomic-column vocabulary before accepting any dependent product or ancestry step. | Direct verifier boundary tests cover the two admitted forms and reject a generic constructor, empty Alloy signature identity, and constructor application. |
| A2-F44 | The standalone Lean recursive JOIN fold returned its current family without validating it when no operands remained. | Validate the fold base case with the same positive-arity and product-width predicate used at operation boundaries. | Forged nullary and wrong-width base cases reduce to `none`; certified valid chains are unchanged. |
| A2-F45 | The separately callable variadic guard checked only interior declared arity, and a raw helper labeled ARROW as certified without typed-family validation. | Make the guard validate every family itself; reserve certification for `typedFamilyArrow` and keep raw `familyArrow` explicitly mathematical. | A malformed arity-two interior product fails the guard, and explicit-univ ARROW certification returns one valid arity-two typed family. |
| A2-F46 | The format prose denied typed-empty JOIN/ARROW evidence despite the schema and verifier admitting a positive-arity empty interior. | State the arity-only source/result rule and prohibit invented alternatives or boundaries rather than the family itself. | The prose now agrees with the exact empty-interior fixture and the zero-product grammar. |
| A2-F47 (P0) | Producer primitive conversion admitted `AlloyCarrier(AlloySig:)`, although no dependent column may use an empty Alloy signature identity. | Reject empty prefixed and blank unprefixed carriers before creating the unary relation view. | Direct relation-view conversion and primitive leaf-proof admission both reject the empty carrier. |
| A2-F48 (P0) | Both producer and verifier treated `AlloySig: ` as nonempty because they checked only prefix length. The whitespace-only identity could enter a primitive relation view and exact dependent column. | Use one producer atomic-column predicate and its verifier mirror to require a nonblank, whitespace-free identity, including Unicode space separators; apply it during primitive conversion, DAG admission, boundary derivation, and wire replay. | Direct relation-view and leaf-proof probes reject ordinary-space and `U+00A0` carriers; standalone atomic admission rejects both before any chain combination. |
| A2-F49 (P0) | Producer exported the legal built-in ancestry `seq/Int -> Int -> univ`, but verifier replay applied a second constructor-only check after admitting `Int` atomically and rejected the same path. | Remove the contradictory nominal-only replay check; every ancestry step uses the already closed atomic vocabulary, while nontrivial paths still set the untrusted-hierarchy fence. | The all-disjoint typed-empty fixture now contains parser-authenticated sequence ancestry and reaches `UNCHECKABLE / MISSING_EVIDENCE` rather than structural rejection. |
| A2-F50 (P0) | Adapter leaf construction caught every `IllegalArgumentException` as an unsupported dependent shape. A malformed exact type could therefore fall back to an ordinary binary JOIN/ARROW with no dependent construction evidence. Binding type normalization could also erase whitespace before validation. | Reject whitespace-bearing public `ExactAlloyType` columns/ancestries and binding identities at ingress; classify authority loss as `UnsupportedFlattening`; permit binary fallback only for explicit `UnsupportedFlattening` or `UnsupportedCorrespondence` markers. | Escaped `U+00A0` exact types reject at construction, type-name whitespace rejects before normalization, and valid unsupported associations retain binary fallback through typed marker exceptions. |
| A2-F51 (P0) | Public `requireLeafTypeProof` returned `EXACT_RELATION` for `Rel(Bogus)` when stored and relation types matched, even though later DAG construction rejected the generic carrier. | Validate the complete exact relation family through dependent DAG admission before issuing the exact leaf rule. | A direct `Rel(Bogus)` exact-leaf proof probe rejects while valid exact, typed-empty, primitive, sequence, and explicit-`univ` leaves remain admitted. |
| A2-F52 | Package-level Alloy-column converters still trimmed their input, so `" S "` became the distinct identity `AlloySig:S` instead of failing closed. | Make both conversion helpers reject every Java whitespace or Unicode space separator before constructing a column; never normalize an identity at this boundary. | A direct package-level `alloyColumn(" S ")` probe rejects, while parser-produced whitespace-free labels are unchanged. |
| A2-F53 (P0) | The whitespace-only repair still admitted C0 controls, Unicode format characters, private-use and unassigned code points, and unpaired UTF-16 surrogates. An unpaired surrogate was lossy under UTF-8 certificate encoding. | Replace whitespace-only checks with a scalar identity predicate mirrored by producer and verifier; reject controls, format, surrogate, private-use, and unassigned categories at every identity ingress. | Direct exact-type, package-converter, and verifier probes reject NUL, `U+200B`, and unpaired `U+D800`; legal parser labels, `seq/Int`, and explicit `univ` remain admitted. |
| A2-F54 (P0) | The scalar validator could be bypassed by replacement-prone byte conversion: the producer codec encoded an unpaired surrogate as `U+FFFD`, and polymorphic-key replay decoded malformed UTF-8 to the same replacement character before hashing. Tests also omitted private-use, unassigned, and valid supplementary-plane witnesses. | Make canonical producer and verifier encoding strict in both directions, route polymorphic-key bytes through the strict decoder, and exercise all excluded categories plus one admitted supplementary scalar. | Canonical wire and polymorphic-key probes reject malformed surrogate bytes; producer/verifier probes reject private-use and unassigned identities and preserve a valid `U+10400` identity byte-for-byte. |
| A2-F55 (P0) | Strict bytes did not close every in-memory ingress: general verifier exact-type records admitted forbidden constructor symbols, `Wire.Node` and `Bundle.parse` admitted lone surrogates before encoding, and public `SigSymbol` admitted the same forbidden identity categories. | Apply the scalar policy to every exact-type symbol, require every in-memory wire tag/scalar to be well-formed Unicode at construction, and reuse the producer exact-identity guard in `SigSymbol`. | Recomputed exact-type fixtures carrying format, control, private-use, or unassigned symbols reject; lone-surrogate wire construction rejects; `SigSymbol` rejects all excluded categories and preserves `U+10400`. |
| A2-F56 (P0) | Generic `GraphType` symbols and writer exact-type emission remained broader than verifier exact-type admission, so a format/private-use/unassigned constructor could be produced but not replayed. | Apply the visible-identity predicate in `GraphType` and independently recheck writer emission. | Direct generic-constructor probes reject each forbidden category; valid supplementary symbols remain admitted and the complete bounded producer/verifier suite passes. |
| A2-F57 | Public `ExactAlloyType` constructors retained a leading `this/` while parser ingestion removed it, yielding two dependent identities for the same Alloy signature. Review prose also conflated outer UTF-8 wire lengths with the injective inner structural-key grammar's UTF-16 code-unit lengths. | Normalize `this/` in every exact column and ancestry path, and specify the two nested length domains explicitly without changing their injective encoding. | `unaryRelation("this/A")` equals and bridges identically to `unaryRelation("A")`; supplementary scalars retain two inner UTF-16 units and four outer UTF-8 bytes. |
| A2-F58 (P0) | Local preflight found that the `this/` repair still left aliases in direct `SigSymbol`, package column conversion, and `AlloySig:` graph constructors; empty and repeated prefixes were also representable through generic exact types. | Canonicalize one leading `this/` at every producer Alloy identity constructor, reject repeated/empty prefixes, and make verifier exact-type and atomic-column replay reject all noncanonical wire spellings. | Exact type, graph type, package converter, and signature symbol agree on `this/A == A`; repeated prefixes and wire `AlloySig:`/`AlloySig:this/A` reject. |
| A2-F59 (P0) | Polymorphic `OperatorDeclaration` names and type-parameter identities still used `trim().isEmpty()`, so a control/format/private-use/unassigned parameter could enter the structural and polymorphic wire key despite graph-type and verifier restrictions. | Apply the same visible-identity predicate to operator and type-parameter declaration names. | `TheoryPortsTest` rejects each forbidden category independently for both operator and phantom type-parameter positions. |
| A2-F60 (P0) | Verifier polymorphic-key replay still used the broader canonical-text guard for type-parameter names, so producer-forbidden identities could be reconstructed in a committed declaration. Operator semantic identities had the same broader model ingress. | Add an independent verifier visible-identity guard for polymorphic parameters and operator semantic identities. | Direct verifier guards reject nonbreaking-space, control, format, private-use, and unassigned names and preserve `U+10400`; the standalone verifier suite passes 174 checks. |
| A2-F61 (P0) | Test-only exact-type display fallback and content-addressed type IDs occupied one unresolved reference namespace. A constructor display equal to the Boolean type ID could be interpreted as the Boolean ID instead of the constructor display. | Require global disjointness between every exact-type content ID and every other exact-type display before resolving references. | A publication-profile bundle containing the minimum cross-type collision rejects with `INVALID_TYPE`; the standalone verifier suite passes 175 checks. |
| A2-F62 (P0) | Recursive source derivation for `PLUS` and `INTERSECT` compared only the erased relation type with the parser result. A DAG carrying equal-looking nominal ancestry from a different parsed module could pass that comparison. | Compare complete occurrence evidence: ordered correlated alternatives, exact column paths, and one shared live parser-module capability. Do not use display or structural type equality as authority. | Same-module reconstruction is accepted; an independently parsed module with identical labels and paths rejects. `PhaseA2DependentChains.lean` proves both bounded outcomes and `TheoryDependentChainTest` retains all three direct authority probes. |

## Focused Evidence

- `TheoryDependentChainTest`: 149 checks pass in the current focused run.
- The bounded runner reaches these assertions through
  `TheoryCertificatesTest`; its post-repair aggregate is rechecked by the
  complete phase gate rather than copied from the pre-repair run.
- `CanonicalAlloyPipelineTest`: 514 checks pass in the current focused run,
  including live module ownership, rejection of public synthetic,
  transferred-attribute, module-free, and deserialized ancestry, and the v3
  subtype-ancestry serialization contract.
- `QuotientRepairDistanceTest`: 1,463 checks pass.
- `formal/PhaseA2DependentChains.lean` compiles under pinned Lean `4.33.0`
  and discharges guarded JOIN and ARROW denotation, the unguarded JOIN
  counterexample, ordered/multiplicity-preserving `Seq`, closed typing,
  no-invented-`univ`, exact occurrence binding, replay, repair-label,
  mismatch, identity-to-label module admission, transferred-position and
  swapped-identity rejection, serialization authority loss, executable
  producer and structural folds, schema-v10 outcome classification, subtype
  correspondence, and pristine-source claims.
- `TheoryCanonicalizationTest`: 14,481 checks pass with deterministic seed
  `55520260819`.
- `ProducerSemanticEvidenceMutationTest`: 113 checks pass when its fourth
  argument is the required single-occurrence
  `bind-block-symmetric-a.acgncert` fixture.
- `CertificateBundleWriterTest`: 109 checks pass in each deterministic run.
- The exact failing production-line file and a balanced 129-file production
  smoke complete augmentation; the latter considers 124 nonidentical models
  and executes 303 logical ranking comparisons without a chain exception.
- The schema-v10 producer/verifier harness passes with standalone census exactly
  `VERIFIED=1`, `UNCHECKABLE=2`, `REJECTED=0`; the separately pinned fixture
  theory digests remain unchanged. A dedicated subtype fixture is structurally
  replayed and then returns `UNCHECKABLE / MISSING_EVIDENCE`; its malformed
  variants reject.
- The latest recorded bounded gate at
  `/tmp/acgn-section3-release-final-v2`
  executes all 58 steps with zero executable failures. Its input-manifest
  SHA-256 is
  `cdd634bbbc792e1211c5eb4ddbf1ef14f62baa800fb782edca8fe30f0959312f`.
  It correctly returns `INCOMPLETE` because the repository-wide catalog
  retains 182 open traceability diagnostics; this is not phase closure.

The current fixed producer/verifier identity is
`alloy-dependent-chain-theory-v11` with digest
`3387749f582a53216caa599105386b710e9e5e748110c59e1b3bf3401ac03cf8`.
The formalization is deliberately split across two independently compiled
files. `DependentTypeDagStandalone.lean` covers normalized correlated
families, the complete Cartesian alternative matrix, and the guarded
variadic JOIN fold. `PhaseA2DependentChains.lean` covers parser/module
authority, boundary witnesses, certificate fields, and replay. Neither file
alone is claimed as a complete refinement proof of the Java producer; that
cross-language refinement remains an explicit open obligation.

These focused results are evidence, not closure. Phase A2 remains open until
the staged Luna, Terra, and Sol review pairs unanimously accept one unchanged
snapshot under `adversarial-review-protocol.md`.

## Review Gate

Reviewers must try to forge leaf type provenance, replace an explicit type by
invented `univ`, reorder or deduplicate operands, cross-use JOIN and ARROW
proofs, replay across profiles/types/source associations, smuggle a dependent
target through generic flat evidence, exploit hash-consed representatives or
source-occurrence transfer, and find any certified equality whose metric view
is nonzero or any zero metric result without certified equality.
