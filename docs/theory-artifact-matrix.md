# Theory to Artifact Obligation Matrix

> **Historical matrix, not a current conformance verdict.** The row statuses
> below record the earlier phased implementation review and are preserved for
> provenance. They have not passed the current closed-world formal/refinement
> gate and must not be cited as current `EXACT` claims. The controlling current
> status is `BLOCKED` in
> `docs/section3-repair-audit/claim-ledger.md`; located contradictions are in
> `docs/section3-repair-audit/global-fault-register.md`.

> **Publication superseding note (2026-08-19):** Dirty-worktree and earlier
> run identities below are retained as historical phase provenance. The
> archived publication snapshot is clean publication run
> `dc368829-9623-4856-8bf1-b655aeaf59e0` from source commit `f1bb1607`, verified
> by `scripts/verify_imported_publication_snapshot.sh`.

This matrix began as the Phase A audit gate for the Certificate-Integrated IR at
commit `a9261da4c096f6ba5fcb1a34bcac93cb1b1df23d`. Its historical review target was
HEAD `5f88982788c0070eddb0471c036d8b4ad53f596d` in a dirty worktree, against the
normative `E_Graphs_Draft_LNCS-3.pdf` (SHA-256
`6b128156008abe8065fe2cf3871950ff0206a47dcc263e3486e475053e4a0d33`).
The paper is the normative specification. Existing experimental behavior is
evidence about the current artifact, not a substitute for a formal object or
certificate. Its statuses are updated as later phase gates are completed; the
Phase A snapshot and original counts remain in `theory-phase-a-audit.md`.

Allowed status values are exactly `EXACT`, `PARTIAL`, `ABSENT`,
`CONTRADICTED`, and `UNCERTAIN`.

Gate rule: every row below is mandatory unless its formal object is explicitly
described as an integration or test obligation. Phase A is not complete-engine
acceptance: unresolved rows are expected now and must become `EXACT` before a
certificate-integrated implementation claim is made.

| ID | Formal object/invariant | Paper location | Existing code | Status | Required implementation | Runtime enforcement | Tests | Evidence |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| TY-01 | Type grammar and typed values | Def. 1, sec. 3.3 | `GraphType` represents variables, `Int`, `Bool`, arrows, relations, and constructors structurally | EXACT | Phase B carrier complete; later signatures and e-classes must consume it without conversion to strings | Factory validation and immutable structural values | Positive/malformed grammar and ordering | `theory/GraphType.java:9-181`; `TheoryFoundationsTest.java` |
| TY-02 | A slot carries exactly one type | Def. 1, sec. 3.3 | `TypedSlot` identity contains graph type, disjoint alphabet, and non-negative unbounded ordinal | EXACT | Phase B carrier complete | Constructor rejects absent type/alphabet and negative ordinal | Cross-type identity, alphabet separation, ordering | `theory/TypedSlot.java:7-103`; `TheoryFoundationsTest.java` |
| TY-03 | Ordered, disjoint canonical free and bound alphabets by type | Def. 1; Appendix C.1 | `SlotAlphabet` and `CanonicalSlotAlphabet` provide deterministic disjoint free/bound allocations per type | EXACT | Phase B allocator complete | Rejects `SOURCE` as canonical; least-fresh and first-count construction | Stable per-type counts, freshness, disjointness | `theory/SlotAlphabet.java`; `theory/CanonicalSlotAlphabet.java:10-53` |
| TY-04 | Finite typed slot context | Def. 1, sec. 3.3 | `TypedSlotContext` is an immutable finite sorted set with typed projections and context algebra | EXACT | Phase B carrier complete | Constructor rejects nulls and duplicate slot occurrences | Context laws, mixed types, immutable views | `theory/TypedSlotContext.java:15-180`; `TheoryFoundationsTest.java` |
| EMB-01 | Typed embedding is a type-preserving injection | Def. 2 | Sealed `TypedEmbedding` stores declared contexts and a total immutable source map | EXACT | Phase B carrier complete | Constructor checks exact domain, codomain membership, type preservation, and injection | Cross-type, duplicate image, missing/extra source, outside target rejection | `theory/TypedEmbedding.java:13-190`; `TheoryFoundationsTest.java` |
| EMB-02 | Typed renaming is onto its declared codomain | Def. 2 | Sealed `TypedRenaming` refines a validated embedding with surjectivity | EXACT | Phase B carrier complete | Constructor rejects every proper embedding | Proper injection cannot become renaming | `theory/TypedRenaming.java:8-67`; `TheoryFoundationsTest.java` |
| EMB-03 | Only renamings may be inverted | Def. 2, Prop. 1 | `inverse()` exists only on sealed `TypedRenaming` and `TypedPermutation`; proper embeddings can only request checked conversion | EXACT | Phase B API complete | Closed subtype hierarchy and onto check prevent unchecked inversion | Reflection boundary, non-surjective conversion rejection, inverse laws | `theory/TypedEmbedding.java:13,127-134`; `theory/TypedRenaming.java:35-44`; `TheoryFoundationsTest.java` |
| EMB-04 | Typed permutation is a context automorphism | Def. 2 | Final `TypedPermutation` has one declared context and inherits typed bijection validation | EXACT | Phase B carrier complete | Same source/codomain by construction | Cross-type rejection, identity, inverse, exhaustive small group laws | `theory/TypedPermutation.java:7-45`; `TheoryFoundationsTest.java` |
| EMB-05 | Identity and composition preserve declared contexts and types | Prop. 1 | `identity`, `andThen`, and formal-order `compose(after,before)` preserve exact endpoints | EXACT | Phase B algebra complete | Composition rejects a middle-context mismatch | Identity, associativity, direction, type preservation over seeded chains | `theory/TypedEmbedding.java:91-113`; `TheoryFoundationsTest.java` |
| EMB-06 | Executable embedding predicates | Mission secs. 3.2 and 27 | All five predicates are public and reused by the typed construction and invariant-checking boundaries | EXACT | Phase B carrier and Phase G checker reuse complete | Constructors plus direct predicates fail closed on malformed input | Predicate truth tables | `theory/TypedEmbedding.java:113-190`; `TheoryFoundationsTest.java` |
| INV-01 | Invocation is `m * a` with `m in TEmb(S_a,Gamma)` | Def. 2; Cor. 2 | Immutable `TypedInvocation` pairs `TypedEClassInterface` with a validated embedding | EXACT | Phase D class table must own the interface value | Constructor requires embedding source exactly equal to `S_a`; caller context is embedding codomain | Valid/invalid invocation construction | `theory/TypedInvocation.java:6-76`; `theory/TypedEClassInterface.java` |
| INV-02 | Invocation output is exactly `tau_a` | Cor. 2 | `TypedInvocation.outputType()` is derived only from its immutable e-class interface | EXACT | Phase D must preserve this when interfaces become table-owned | No caller-supplied invocation output field exists | Output preservation under action | `theory/TypedInvocation.java:37-39,46-54`; `TheoryFoundationsTest.java` |
| INV-03 | Identity invocation | Cor. 2 | `TypedInvocation.identity` uses the typed identity renaming on the exact exposed interface | EXACT | Phase B carrier complete | Constructor revalidates source context | Identity support and output | `theory/TypedInvocation.java:20-24`; `TheoryFoundationsTest.java` |
| INV-04 | Embedding action composes as `e . (m*a)=(e o m)*a` | Def. 4; Cor. 2 | `TypedInvocation`, all six sealed port variants, both binder forms, and `TypedENode` implement one typed structural action | EXACT | Phase C action algebra complete; graph-owned action in later phases must reuse it | Unary and block binders choose deterministic fresh bound coordinates and extend the one caller embedding disjointly | Generated support equivariance, invocation composition, unary/block capture avoidance, nested blocks, and node action | `theory/TypedInvocation.java`; `theory/BindPort.java`; `theory/BindBlockPort.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| PORT-01 | Explicit `One(tau)` port | Def. 3 | Sealed `OnePortSchema` and `OnePort` carry either a same-typed slot or typed opaque invocation | EXACT | Phase C carrier complete | Constructor checks schema type, slot membership, and invocation caller context | Valid/malformed slot and invocation leaves | `theory/OnePortSchema.java`; `theory/OnePort.java`; `TheoryPortsTest.java` |
| PORT-02 | Explicit `Seq(kappa)` preserving order and multiplicity | Def. 3; sec. 3.2 | Distinct immutable `SeqPort` stores its validated elements in source order | EXACT | Phase C carrier complete | Schema/context check per occurrence; no sorting or deduplication | Permutation differs; duplicates retained | `theory/SeqPortSchema.java`; `theory/SeqPort.java`; `TheoryPortsTest.java` |
| PORT-03 | Explicit `Bag(kappa)` discarding order, retaining multiplicity | Def. 3; sec. 3.2 | Distinct immutable `BagPort` sorts by complete structural keys and retains every occurrence | EXACT | Phase C carrier complete | Schema/context validation, deterministic sort, and unequal-key-collision rejection | Shuffled bags equal; changed multiplicity unequal; generated cases | `theory/BagPortSchema.java`; `theory/BagPort.java`; `theory/PortValues.java`; `TheoryPortsTest.java` |
| PORT-04 | Explicit `Set(kappa)` discarding order and duplicate structural classes | Def. 3; sec. 3.2 | Distinct immutable `SetPort` normalizes one shared-context value and deduplicates exact complete structural keys; Phase E supplies one global free renaming and locally quotient-normalized children | EXACT | Phase C carrier and structural Phase E consumption complete | Schema/context validation; no per-element free alpha normalization; key collision between unequal values fails closed | Shuffled/duplicate sets, global-renaming adversary, leader/block orbit collapse | `theory/SetPortSchema.java`; `theory/SetPort.java`; `theory/PortValues.java`; `TheoryPortsTest.java`; `TheoryCanonicalizationTest.java` |
| PORT-05 | Explicit `Bind(tau,kappa)` with fresh same-typed coordinate | Def. 3-4 | `BindPortSchema` and `BindPort` store bound type, fresh typed slot, and recursively typed body | EXACT | Phase C carrier complete | Constructor checks same type, freshness, body schema, and `Gamma+bound` context; action avoids capture | Capture avoidance, type/schema/context rejection, support subtraction | `theory/BindPortSchema.java`; `theory/BindPort.java`; `TheoryPortsTest.java` |
| PORT-06 | Port semantics are not one generic collection plus metadata | Def. 3; mission sec. 5 | The sealed schema/value hierarchies have distinct `One`, Seq, Bag, Set, unary `Bind`, and `BindBlock` classes; every container class stores its formal `epsilon` index | EXACT | Phase C constructor grammar complete | Closed permits clauses plus constructor-specific validation prevent metadata-tagged fallback values | Sealed boundary, schema/value mismatch, nested block, and container semantics | `theory/PortSchema.java`; `theory/PortValue.java`; `TheoryPortsTest.java` |
| PORT-07 | Container schemas distinguish nonempty `K+` from unit-admitting `K0` | Def. 3; sec. 4 | `ContainerEmptiness` is part of every Seq/Bag/Set schema and structural key; values reject empty `K+`, and strict use of a `K0` path requires a matching certified unit law | EXACT | Phase C carrier plus Phase F unit provenance complete | Empty `K+` fails at value construction; strict canonicalization rejects `K0` without its exact unit certificate | Top-level, nested, flat, certified, and uncertified cases for Seq, Bag, and Set | `theory/ContainerEmptiness.java`; all three container schemas/values; `ContainerLawDeclaration.java`; `TheoryPortsTest.java`; `TheoryCertificatesTest.java` |
| PORT-08 | `BindBlock(beta,kappa)` is a first-class port with an occurrence-local fresh bound context | Def. 3; Def. 6; sec. 3.6 | `BindBlockPortSchema` and `BindBlockPort` carry a complete immutable descriptor, a typed `Delta_beta`-to-occurrence renaming, a fresh disjoint bound context, a recursively typed body, and certificate-gated `Aut(beta)` | EXACT | Phase C carrier plus Phase F automorphism provenance complete | Constructor checks descriptor map endpoints, freshness, bound alphabet, body schema/context, and nested action; strict use verifies every closure derivation | Nested blocks, freshness, action composition, S2/S3 certified closure, descriptor mismatches, and incompatible permutations | `theory/BindBlockPortSchema.java`; `theory/BindBlockPort.java`; `theory/BinderBlockDescriptor.java`; `theory/BinderAutomorphismCertificate.java`; both Phase C/F tests |
| SIG-01 | Typed polymorphic signature `Sigma(f)` | Def. 4 | `OperatorDeclaration` stores quantified type variables, recursive schemas including blocks, output type, certificate-bearing law declarations, and optional flat port | EXACT | Phase C typed signature plus Phase F law provenance complete | Exact substitution domain, descriptor-slot substitution, recursive type instantiation, and strict certificate ownership are checked | Polymorphic relation/block instantiation and malformed substitutions/signatures/certificates | `theory/OperatorDeclaration.java`; `theory/InstantiatedOperator.java`; `theory/BinderBlockDescriptor.java`; `TheoryPortsTest.java`; `TheoryCertificatesTest.java` |
| SIG-02 | Node output type follows instantiated signature | Def. 4; Lemma 2 | Private `TypedENode` constructor derives output solely from `InstantiatedOperator` | EXACT | Phase C construction boundary complete | Port count, schema, context, and empty-unit obligations checked before construction | Wrong count/schema/context and flat bypass rejected | `theory/TypedENode.java`; `TheoryPortsTest.java` |
| LAW-01 | Schema laws are separately certified | Sec. 4; mission sec. 21 | `ContainerLawDeclaration.certified` binds one machine-checkable `ContainerLawCertificate` to each declared law at its exact schema path | EXACT | Phase F law-provenance gate complete | Strict graph canonicalization and leader extraction recursively reject raw law declarations | Certified/raw contrasts plus invalid top-level and nested schema/law combinations | `theory/ContainerLawDeclaration.java`; `theory/ContainerLawCertificate.java`; `theory/CertificateVerifier.java`; `TheoryCertificatesTest.java` |
| LAW-02 | Seq=A, Bag=AC, Set=ACI only when certified | Sec. 4; Appendix C.8 | Certificate schemas permit exactly Seq=A, Bag=AC, and Set=ACI; normalization consumes only certificate-checked declarations in strict graph mode | EXACT | Phase F container-law obligation complete | Each certificate records schema, path, law kind, and structured signature-axiom origin | A/AC/ACI behavior, absent/wrong law, and forbidden Bag idempotency rejection | `theory/ContainerLawCertificate.java`; `theory/ContainerLawDeclaration.java`; `TheoryCertificatesTest.java` |
| LAW-03 | `K+` excludes empty values; empty `K0` requires a certified unit | Def. 3; sec. 4 | Schema construction excludes empty `K+`; `K0` requires a declaration whose exact unit certificate is checked before strict use | EXACT | Phase C emptiness plus Phase F unit-law obligation complete | Raw/missing/wrong-path/wrong-schema unit declarations fail before exact canonicalization | Top-level, nested, flat, and certified/raw `K0` cases for all three containers | `theory/ContainerEmptiness.java`; `theory/ContainerLawCertificate.java`; `theory/TypedENode.java`; both Phase C/F tests |
| FLAT-01 | Every associative construction uses one smart path | Flat-construction contract; Appendix C.2 | `TypedENode` has only a private constructor; ordinary `construct` rejects flat operators and `flatConstruct` is the sole flat entry | EXACT | Phase I adapters must call this already-enforced boundary | Java visibility plus runtime rejection prevents public bypass | Constructor reflection and direct flat-construction bypass rejection | `theory/TypedENode.java`; `TheoryPortsTest.java` |
| FLAT-02 | Visible same-headed syntax is flattened | Flat-construction contract | `FlatApplication` retains visible source syntax until recursive same-operator splicing by `flatConstruct` | EXACT | Phase C implementation complete | Same instantiated operator is compared before child sealing | Left/right nesting equality for Seq plus AC/ACI examples | `theory/FlatApplication.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| FLAT-03 | Opaque invocation is never opened to find hidden same head | Flat-construction contract; sec. 3.7 | `FlatLeaf` can hold `One(invocation)` and exposes no child alternatives; `flatConstruct` copies it as one element | EXACT | Phase D-G graph operations must preserve this boundary | Flatten traversal branches only on visible `FlatApplication` values | Hidden same-head invocation stays one element; different visible head is sealed once | `theory/FlatLeaf.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| SUP-01 | Formal support for slot, invocation, containers, node, and both binders | Def. 4 | Every sealed Phase B-C carrier computes support structurally from slot leaves, invocation images, unions, unary subtraction, or complete block-context subtraction | EXACT | Phase C support algebra complete | No public support field or parser-variable fallback exists in the exact path | Every grammar variant, nested blocks, nodes, and both binder forms | `theory/HasSlotSupport.java`; `theory/PortValues.java`; `theory/BindPort.java`; `theory/BindBlockPort.java`; `TheoryPortsTest.java` |
| SUP-02 | Invocation support is embedding image | Def. 4 | `TypedInvocation.support()` returns the image of its validated total typed embedding | EXACT | Phase B carrier complete | Invocation construction guarantees a legal image | Weakening and exact-image cases | `theory/TypedInvocation.java:41-43`; `theory/TypedEmbedding.java:73-89` |
| SUP-03 | Support equivariance `slots(e.q)=e[slots(q)]` | Cor. 1 | Structural typed action is implemented for invocations, all six ports, both binders, and nodes | EXACT | Phase C algebra complete | Every action checks source context; block action extends it by one descriptor-indexed occurrence bijection | Generated bag/set equivariance, proper-embedding block equivariance, nested blocks, and node equivariance | `theory/TypedInvocation.java`; `theory/PortValues.java`; `theory/BindBlockPort.java`; `theory/TypedENode.java`; both theory tests |
| STATE-01 | Explicit state `G=(U,M,H)` | Def. 5 | `TypedSlottedPortEGraph` directly owns a typed renamed union-find, immutable class-record table, and typed canonical-shape hash-cons | EXACT | Phase D state carrier complete; later transitions must mutate it only through the graph boundary | Private state fields, package-private setup transitions, and graph-wide invariant checks | Empty, populated, dirty, and deterministic-state cases | `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| STATE-02 | `M(a)` stores output type `tau_a` | Def. 5 | Every immutable `TypedEClassRecord` owns a `TypedEClassInterface` whose output type is the sole `tau_a` | EXACT | Phase D field complete | Registration, parent links, stored children, and invariant audit reject output mismatches | Cross-output parent/link and invocation rejection | `theory/TypedEClassRecord.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| STATE-03 | `M(a)` stores finite exposed typed context `S_a` | Def. 5 | Every immutable class record owns the exact `TypedSlotContext`; `restrictInterfaceCertified` is the sole narrowing transaction | EXACT | Field and certified mutation boundary complete | Class/interface identity, every embedding endpoint, and restriction history are checked | Interface spoofing, malformed factorization, historical invocation replay, and proper contraction | `theory/TypedEClassRecord.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| STATE-04 | `B_a` stores canonical shapes and witnesses | Def. 5 | `TypedEClassRecord` owns an immutable ordered `CanonicalShape -> ShapeWitness` map | EXACT | Phase D dependent carrier complete; Phase E supplies `canon_G` results | Record construction checks shape output, exact context, exposed interface, and witness uniqueness | Witness round-trip, malformed record, key-collision, and bottom-up ownership cases | `theory/CanonicalShape.java`; `theory/ShapeWitness.java`; `theory/TypedEClassRecord.java`; `TheoryStateTest.java` |
| STATE-05 | Shape witness retains exact slots, ambient support, and exposed interface | Def. 5 | `ShapeWitness` stores all three contexts separately plus the exact-to-ambient typed renaming; restriction certificates carry exact transported copies | EXACT | Phase D witness carrier and Phase F transport evidence complete | Constructor enforces exposed subset of ambient and exact renaming endpoints; verifier preserves every field except the narrowed interface | Strict redundant ambient-coordinate, malformed-context, and factorized transport cases | `theory/ShapeWitness.java`; `theory/InterfaceRestrictionCertificate.java`; both Phase D/F tests |
| STATE-06 | `G_a` is a certified typed subgroup | Def. 5; Def. 7 SC | `TypedSymmetryGroup` stores an exact finite typed closure on `S_a`, reconstructs every element, and restriction/union transport only certified setwise stabilizers | EXACT | Current-interface carrier and Phase G transport complete | Strict invariants audit owner, carrier, generators, closure derivations, and transported endpoints | S2/S3 closure, restriction to S2, union stabilizer transport, and cross-context rejection | `theory/TypedSymmetryGroup.java`; `theory/TypedSlottedPortEGraph.java`; both Phase F/G tests |
| UF-01 | Parent edge is typed `U(a)=m*b`, `m:S_b -> S_a` | Def. 5 | `ParentStep` stores the formal parent-interface-to-child-interface embedding and, on the public exact path, its verified `ParentEdgeCertificate` | EXACT | Phase D edge carrier plus Phase F proof field complete | Raw construction is package-only; strict graph mode rejects every uncertified primitive edge | Direction-sensitive chains, reversed edges, exact endpoint proofs, and reflection boundary | `theory/ParentStep.java`; `TheoryStateTest.java`; `TheoryCertificatesTest.java` |
| UF-02 | Parent pointers form a forest; roots have identity invocation | Def. 5 | `TypedRenamedUnionFind` owns a total assignment; roots are explicit identity invocations and nonroots form a checked forest | EXACT | Phase D forest complete | Registration, leader-only links, cycle checks, and full invariant audit | Root, forest, cycle, generated chain, and branching cases | `theory/ParentAssignment.java`; `theory/TypedRenamedUnionFind.java`; `TheoryStateTest.java` |
| UF-03 | `find` returns leader plus composite typed embedding | Def. 5 | `TypedFindResult` returns the original invocation, leader invocation, and primitive parent path | EXACT | Phase D find result complete | Result recomputes and checks `m_original` after the formal parent-path composite | Identity, multi-edge composition, support image, generated forests | `theory/TypedFindResult.java`; `theory/ParentPath.java`; `TheoryStateTest.java` |
| UF-04 | Path compression composes embeddings in formal direction | Def. 5; Thm. 1 obligation 9 | Compression installs the leader-to-child composite while retaining the full primitive path | EXACT | Phase D embedding component complete | Pre/post find values and every path endpoint are checked | Direction-sensitive three-edge and generated-chain equivalence | `theory/TypedRenamedUnionFind.java`; `theory/ParentAssignment.java`; `TheoryStateTest.java` |
| UF-05 | Path compression preserves certificate derivation | Lemma 5; Thm. 1 obligation 9 | Every certified primitive `ParentStep` retains `ParentEdgeCertificate`; `ParentPath.composedCertificate()` transports and composes them while compression keeps the primitive path | EXACT | Phase F path-proof obligation complete | `TypedFindResult.parentCertificate()` checks exact original/leader invocation endpoints after caller transport | Proper two-edge contraction and 64 generated certified chains before/after compression | `theory/ParentStep.java`; `theory/ParentPath.java`; `theory/TypedFindResult.java`; `TheoryCertificatesTest.java` |
| HC-01 | `H(p)=a` iff leader `a` owns `p` | Def. 5 | At quiescence `TypedSlottedPortEGraph` enforces the exact bidirectional relation between typed canonical shapes and leader records | EXACT | Phase D hash-cons state and invariant complete | Atomic collision rejection plus graph-wide iff audit | Leader ownership, nonleader rejection, collision atomicity, deterministic state | `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| HC-02 | Explicit dirty versus quiescent state | Def. 5; sec. 3.7 | `GraphStatus`, the exact reverse-use index, a deduplicated dirty queue, and an in-flight rebuild marker distinguish stale administrative state from publishable quiescence | EXACT | Phase G lifecycle complete | Mutations mark affected records; only an empty queue plus exact collision-free H can publish `QUIESCENT` | Dirty query rejection, failed/requeued contraction, zero-dirty completion, and idempotent rebuild | `theory/GraphStatus.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| HC-03 | Equality/canonicalization/measurement never use stale H | Def. 5 | Hash-cons observations, graph-relative canonicalization, leader-kernel extraction, and the Phase H finite-unfolding measurement oracle all require one current coherent quiescent prefix | EXACT | Current exact-engine H-dependent clients are guarded; Phase I clients remain separately tracked by `INT-*` | Every public exact query either checks `QUIESCENT` directly or validates a revision-bound coherent family | Hash/kernel query during dirty mutation plus stale/dirty unfolding attempts | `theory/TypedSlottedPortEGraph.java`; `theory/BoundedFiniteUnfoldingOracle.java`; `TheoryFiniteUnfoldingTest.java` |
| ALPHA-01 | Indexed typed alpha-equivalence with one free-slot bijection | Def. 6 | `TypedAlphaEquivalence` implements the structural node/port judgement indexed by one `TypedRenaming` | EXACT | Phase E relation complete | Exact schema, operator, type, and context endpoints are checked recursively | Identity, inverse, composition, Seq/Bag/Set, and generated cases | `theory/TypedAlphaEquivalence.java`; `TheoryCanonicalizationTest.java` |
| ALPHA-02 | Binder alpha-renaming is fresh, capture-avoiding, and same-typed | Def. 6; Lemma 2 | Binder comparison extends the one free renaming with the two fresh same-typed bound coordinates; block automorphisms are independently certificate-gated | EXACT | Phase E binder alpha plus Phase F block-group admission complete | `BindPort` construction and checked disjoint extension reject capture/type mismatch | Differently named bound slots, canonical least-fresh binder, generated witness replay, and certified blocks | `theory/TypedAlphaEquivalence.java`; `theory/BindPort.java`; `TheoryCanonicalizationTest.java`; `TheoryCertificatesTest.java` |
| ALPHA-03 | Structural alpha relation preserves output type | Prop. 2 | Node relation requires the same instantiated operator and output type | EXACT | Phase E relation complete | Relation fails before descending on operator/output mismatch | Different-operator/output and witness replay cases | `theory/TypedAlphaEquivalence.java`; `TheoryCanonicalizationTest.java` |
| ALPHA-04 | Binder-block alpha-equivalence quantifies over exactly `Aut(beta)` after occurrence contexts are alpha-aligned | Def. 6; sec. 3.6 | The recursive block clause aligns occurrences through canonical `Delta_beta`; public structural and strict graph-relative entry points reject uncertified nonidentity descriptor groups | EXACT | Certified block relation complete | Every consumed group element is reconstructed from descriptor-preserving certified generators | Certified S2/S3 blocks, identity-only rejection, field mismatches, nested blocks, and capture adversaries | `theory/TypedAlphaEquivalence.java`; `theory/BinderAutomorphismGroup.java`; `TheoryPortsTest.java`; `TheoryCertificatesTest.java` |
| GEQ-01 | Graph-relative invocation relation uses same leader and certified symmetry | Sec. 3.6 graph-relative extension | Strict graph-relative comparison uses typed find and only the graph-owned certificate-checked leader group | EXACT | Phase F relation obligation complete | Quiescence, same leader, typed map equation, exact group membership, and generator-proof ownership are checked | Different IDs/same leader, identity-only rejection, certified S3 closure replay | `theory/TypedAlphaEquivalence.java`; `theory/TypedSymmetryGroup.java`; `TheoryCertificatesTest.java` |
| GEQ-02 | Structural alpha, leader equality, symmetry, and arbitrary e-class equality remain distinct | Sec. 3.6; mission sec. 22 | Structural alpha, graph-relative alpha, typed find/leader membership, and structural Java equality have separate APIs and tests | EXACT | Phase E distinction complete | No relation delegates to Java equality or infers invocation equality from class membership alone | Four-way distinction and same-class/different-map adversary | `theory/TypedAlphaEquivalence.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryCanonicalizationTest.java` |
| GEQ-03 | Java `equals` remains structural | Mission sec. 22 | Every exact immutable carrier keeps structural equality; graph-relative equality is only in `TypedAlphaEquivalence` | EXACT | Phase E separation complete | No graph lookup occurs from an `equals` implementation | Structural versus shared-leader relation | `theory/TypedENode.java`; `theory/PortValue.java`; `theory/TypedAlphaEquivalence.java`; `TheoryCanonicalizationTest.java` |
| CAN-01 | Canonicalizer first replaces invocations by leaders, composes path embeddings, and returns the effective-kernel transport | Sec. 3.6-3.7; Lemma 8; repaired Appendix C.1-C.2 | Both structural canonicalizers consume `LeaderKernelResult`, normalize only exact `K_G(n)`, and return `iota_n`, `omega_n`, and structural `xi_n`; `canonicalizeCertified` separately adds `d_n^w` | EXACT | Structural/dependent boundary complete; concrete replay is tracked by `CERT-05` | Extraction and canonicalization are quiescence- and exact-source-guarded; every transport and certificate endpoint is checked | Nonleader and two-edge paths, strict contraction, Seq/Bag/Set fibers, bind/block scopes, complete-result differential and coherent replay | Kernel, canonicalization, and coherent insertion tests |
| CAN-02 | One global canonical free-slot context by type | Sec. 3.6; Appendix C.1-C.2 | Both canonicalizers allocate one `canonicalFreeContext` from the exact effective kernel and share each candidate renaming across the complete node | EXACT | Phase E step complete | Recursive calls only extend the one global renaming with lexically scoped bound coordinates; they never allocate a free context | Coupled pair-set false-dedup adversary, nested blocks, and generated alpha variants | Both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-03 | Enumerate all and only type-preserving free-slot bijections | Sec. 3.7 step 2; Fig. 4 | `TypedRenamingEnumerator` deterministically streams every typed bijection without a cap | EXACT | Phase E enumeration complete | Type-count mismatch yields no map; every emitted value is a validated `TypedRenaming` | Seven-slot `7! = 5,040`, cross-type rejection, differential generated cases | `theory/TypedRenamingEnumerator.java`; `TheoryCanonicalizationTest.java` |
| CAN-04 | Use only certified leader symmetries | Sec. 3.7 step 2; Fig. 4 | Strict canonicalization obtains each leader group through `symmetryGroupForCanonicalization`, which rejects unproved or wrong-owner generators | EXACT | Certified leader-orbit gate complete | Every closure element has a reconstructed typed derivation; structural Phase E fixtures are package-private | Certified S2/S3 closure plus existing generated orbit differential tests | Both canonicalizers; `theory/TypedSymmetryGroup.java`; `TheoryCertificatesTest.java`; `TheoryCanonicalizationTest.java` |
| CAN-05 | Use only certified binder-block automorphisms | Sec. 3.6; Def. 6 | Strict canonicalization rejects raw nonidentity descriptors and minimizes only over certificate-bearing complete `Aut(beta)` groups | EXACT | Certified block-orbit gate complete | Descriptor payload and every closure-element derivation are checked before use | Certified/raw contrast, S3 closure, local Set collapse, Bag multiplicity, nesting | Both canonicalizers; `theory/BinderAutomorphismGroup.java`; `TheoryCertificatesTest.java`; `TheoryCanonicalizationTest.java` |
| CAN-06 | Seq/Bag/Set canonicalization follows exact container semantics after local quotient normalization | Sec. 3.6-3.7; Lemma 4 | Both independent recursions compute local invocation/block minima first, then preserve Seq order, sort Bag occurrences with multiplicity, and sort/deduplicate Set structural classes | EXACT | Structural Phase E container step complete | Container constructors receive only recursively quotient-normalized children | Flat ACI, bag multiplicity, local block-orbit Set collapse, nested blocks, and differential cases | Both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-07 | Set dedup uses one shared free-slot renaming, never local alpha per element | Sec. 3.6; Appendix C.2 | Set construction occurs only after every element receives the same whole-node free renaming and its local declared-group quotient | EXACT | Phase E set rule complete | No recursive call allocates an independent free context | `{(x,y),(y,x)}` retains two elements; leader- and binder-orbit duplicates collapse | Both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-08 | Deterministic least complete structural key | Sec. 3.6-3.7; Appendix C.1 | Both implementations compare complete Phase B-E `StructuralKey` values and break equal-shape witness ties by complete embedding key | EXACT | Phase E least-candidate selection complete; cross-process replay remains `ORD-02` | Unequal comparator-key collisions fail closed | Repetition, insertion-order foundations, idempotence, reference/production equality | `theory/StructuralKey.java`; both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-09 | Canonicalizer returns kernel, shape, effective-support renaming, ambient embedding, and source-to-kernel provenance | Sec. 3.6; Lemma 8; repaired Appendix C.1-C.2 | `CanonicalizationResult` is `(K,p,sigma,iota,omega,xi)`; `CertifiedCanonicalizationResult` adds only `d_n^w`; `omega=iota o sigma`, and only `p` is a hash key | EXACT | Complete structural record and coherent theorem-facing projection implemented | Constructors validate operator/type, exact contexts/transports, current coherent family, replay trace, and dependent endpoints | Strict contraction, deterministic result equality, Set replay, binder alpha replay, and retained insertion provenance | Both result classes and canonicalizers; leader, canonicalization, and coherent insertion tests |
| CAN-10 | Canonical-shape exactness is stated over the effective leader kernel | Cor. 3; Lemma 8; Appendix C.2 | Both canonicalizers enumerate `Can(Delta_n) -> Delta_n` only after exact Phase DA extraction and accept proper `Delta_n -> Gamma_0` inclusion | EXACT | Structural effective-support rule complete | A proper inclusion remains `TypedEmbedding`; it is never inverted or cast to `TypedRenaming` | Smallest proper-parent contraction, 32 extracted contractions, and complete-result differential case | `theory/ExactContextRestrictor.java`; `theory/CanonicalizationResult.java`; both canonicalizers; `TheoryLeaderKernelTest.java`; `TheoryCanonicalizationTest.java` |
| CAN-11 | Invocation and binder-block quotient orbits are minimized locally before Bag aggregation and Set deduplication | Sec. 3.6 `Q_G`; Lemma 4; Fig. 4 | The production recursion streams each local finite orbit; the independent reference materializes it. Both choose the local least before returning to the enclosing container | EXACT | Structural quotient-order rule complete relative to declared groups | Structural port order is used inside local orbits and shape order only for completed nodes | Set `{b,pi.b}` collapses to one; Bag retains two; nested and identity-only blocks; reference/production equality | Both canonicalizers; `TheoryCanonicalizationTest.java`; `theory-phase-e-canonicalizer.md` |
| ORD-01 | One deterministic total order covers every relevant field | Sec. 3.6; mission sec. 12 | `StructuralKey` covers all Phase B-F carriers, including typed endpoints, structured origins, derivation trees, descriptors, extensional groups, kernels, traces, and complete canonicalization results | EXACT | Structural-key grammar complete for implemented phases | Semantic group/law identity remains extensional while proof payload has its own deterministic key | Phase B-F order properties, certificate composition, descriptor relabeling, repeated extraction | `theory/StructuralKey.java`; `theory/TypedEqualityCertificate.java`; `TheoryCertificatesTest.java` |
| ORD-02 | Equality and comparison agree across JVM runs | Mission secs. 12 and 30 | Complete length-prefixed keys produce identical graph-state and canonical-shape SHA-256 digests across repetition, admissible insertion order, worker count, and fresh JVMs | EXACT | Exact-package deterministic replay gate complete; Phase I manifest recording is tracked by `INT-03` | Ordered carriers, deterministic quotient minimization, and digest comparison fail closed | 12 repeats, forward/reverse batch and Bag order, 1/available-core workers, and two child JVMs | `theory/StructuralKey.java`; `TheoryDeterminismTest.java` |
| SYM-01 | Every symmetry generator has typed witness-equation provenance | Def. 7 SC; Thm. 1 obligation 5; Appendix B | `SymmetryCertificate` records the originating derivation, endpoint invocations, induced typed permutation, endpoint type check, and provenance category | EXACT | Phase F generator provenance complete | `addSymmetryCertified` accepts only a verified certificate for the current leader | Rewrite-derived S2/S3, duplicate, nonbijective, wrong-owner, and fake cases | `theory/SymmetryCertificate.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryCertificatesTest.java` |
| SYM-02 | Same type is necessary but insufficient for symmetry | Sec. 3.6; Lemma 3 | Same-typed slots create no group element; admission additionally requires a checked witness equation, while binder swaps preserve every descriptor field | EXACT | Phase F admission distinction complete | There is no public raw e-class generator insertion path | Identity-only same-type case and descriptor-incompatible quantifier swap | `theory/TypedSymmetryGroup.java`; `theory/BinderAutomorphismCertificate.java`; `TheoryCertificatesTest.java` |
| SYM-03 | Symmetry group closure is finite and consistent on current interface | Def. 5; Prop. 3 | `TypedSymmetryGroup` computes uncapped finite closure on exactly `S_a` and reconstructs each element by certified generator, inverse, and composition rules | EXACT | Current-interface closure complete; restriction transport is isolated in `REST-02` | Strict invariant checking verifies owner, context, generator coverage, and all reconstructed endpoints | Certified S2/S3 exact closure, inverse/composition, context/type rejection | `theory/TypedSymmetryGroup.java`; `TheoryCertificatesTest.java` |
| BIND-01 | Complete binder descriptor | Sec. 3.6; sec. 4; Appendix C.1/C.7 | `BinderBlockDescriptor` owns ordered typed coordinates with exact domain keys, quantifier, multiplicity, normalized disjointness and exchange classes, and checked dependency order | EXACT | Phase C carrier is consumed by the Phase I `QuantiVar` adapter without weakening descriptor fields | Constructor rejects out-of-context/forward dependencies, noncanonical descriptor slots, malformed fields, and cross-exchange-class automorphisms | Field mismatches, exchange/disjointness relabeling and partition, dependency order, polymorphic substitution, and Alloy integration contrasts | `theory/BinderCoordinateDescriptor.java`; `theory/BinderBlockDescriptor.java`; `adapter/TheoryAlloyAdapter.java`; `TheoryPortsTest.java`; `CanonicalAlloyPipelineTest.java` |
| BIND-02 | `Aut(beta)` contains only descriptor-preserving permutations | Sec. 3.6; Lemma 3 | `BinderAutomorphismCertificate` checks the complete normalized descriptor; the group retains certified generators, compact ordinal closure actions, and reconstructs a requested closure proof on demand | EXACT | Phase F binder-group obligation complete without an eager all-elements proof DAG | Same type alone never admits a swap; strict canonicalization rejects raw nonidentity groups | Certified S2/S3; quantifier, domain, multiplicity, exchange-class, disjointness, and dependency mismatches | `theory/BinderAutomorphismCertificate.java`; `theory/BinderAutomorphismGroup.java`; `TheoryCertificatesTest.java` |
| BIND-03 | Binder consumer invariance is a declared proof obligation | Sec. 4, Lemma 3 | `TheoryAlloyAdapter` maps complete Alloy binder payloads to `BinderCoordinateDescriptor`s and issues structured signature-axiom certificates only for equal type/domain/quantifier/cardinality/disjointness payloads in one continuous exchange class | EXACT | Phase I binder-consumer certificate boundary complete | `BinderBlockDescriptor.certified` verifies every generator; incompatible coordinates remain outside the generated group | Same-descriptor and nested-block permutation, shadowing, exchange-class, and disjointness contrasts plus malformed certificate cases | `adapter/TheoryAlloyAdapter.java`; `CanonicalAlloyPipelineTest.java`; `TheoryCertificatesTest.java` |
| BIND-04 | Prenex coordinates reflect lexical liveness rather than the sum of source declarations | Sec. 3.6; scoped-slot construction | `NormalForm.PrenexSlotAllocator` shares typed ordinal frontiers only for sibling universal conjunctions or existential disjunctions, consumes nested lanes, closes at quantifier/connective barriers, and preserves all readable aliases | EXACT | Phase I normalization now feeds the exact binder block its maximum live coordinate set | Domain traversal is scope-neutral; mixed `ALL/SOME/ALL`, temporal alias, nested scope, and separate-disjointness adversaries fail closed | Five-scope 3/1/2/1/1 maximum=3, direct nesting=2, incompatible branch polarity, mixed barrier, temporal inheritance, and bounded backtranslation | `core/NormalForm.java`; `core/QuantiVar.java`; `ir/IRAgent.java`; `CanonicalAlloyPipelineTest.java`; `EGraphSaturationTest.java` |
| REST-01 | Interface restriction requires typed factorization certificate | Thm. 1 obligation 6 | `InterfaceRestrictionCertificate` checks a proper typed subcontext and exact factorization; `restrictInterfaceCertified` is the only publishing path | EXACT | Phase G certified restriction boundary complete | Unsupported rebuild contraction throws before mutation and remains dirty until an independent factorization is supplied | Valid factorization, no-inference rejection, state preservation, and successful retry | `theory/InterfaceRestrictionCertificate.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| REST-02 | Restriction transports shapes, parent edges, and certificates and dirties parents | Thm. 1 obligation 6; sec. 3.7 | One transaction transports shape witnesses/equations, flattens affected UF paths with replayed certificates, restricts the certified symmetry stabilizer, records historical metadata, and dirties all indexed parents | EXACT | Phase G transport transaction complete for represented fixed batches | Every historical invocation must normalize through a composable restriction chain; quiescence permits only current leader invocations | Incident parent contraction, historical find replay, S3-to-S2 transport, and descendant path checks | `theory/TypedRenamedUnionFind.java`; `theory/TypedFindResult.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| REST-03 | No other operation mutates class interface | Mission sec. 15 | Interfaces and records remain immutable; only graph-owned `restrictInterfaceCertified` replaces one interface after complete prevalidation | EXACT | Mutation boundary complete | No public setter/raw UF exposure; class ID/output type are preserved | Reflection boundary, rejected implicit contraction, and certified transaction | `theory/TypedEClassRecord.java`; `theory/TypedSlottedPortEGraph.java`; both Phase F/G tests |
| CERT-01 | Typed certificate hierarchy | Def. 7; Appendix B; mission sec. 16 | The sealed hierarchy includes input, direct congruence, rebuild congruence, effective-shape collision, parent, symmetry, restriction, container, and binder certificates plus derived rules | EXACT | Phase F/G hierarchy complete for represented transitions | Closed permits list and recursive verifier prevent generic-string or foreign proof objects | Every category, collision-generated union, restriction transport, and sealed-boundary audit | `theory/TypedEqualityCertificate.java`; all `*Certificate.java`; both Phase F/G tests |
| CERT-02 | Certificate composition validates typed endpoints | Lemma 5; Thm. 1 | `EqualityCertificates` implements reflexivity, symmetry, transitivity, typed renaming, and endpoint-factorized context restriction | EXACT | Proof algebra complete for Phase G transport | Invalid middle endpoint, context, sort, action source, or restriction factorization fails | Legal path/ambient transport, strict support restriction, and malformed composition | `theory/EqualityCertificates.java`; `theory/CertificateVerifier.java`; both Phase F/G tests |
| CERT-03 | Coherent family supports EC, PC, and SC reconstruction | Def. 7 | `CoherentWitnessFamily` captures every oriented EC, total current PC, and deterministic SC reconstruction at a strict quiescent semantic revision | EXACT | Coherent-family carrier and stale-snapshot boundary complete | Capture checks exact domains/endpoints; path compression preserves the revision while every semantic mutation invalidates it | Empty/nonempty family, EC/PC/SC coverage, path use, and stale-family rejection | `theory/CoherentWitnessFamily.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryCoherentInsertionTest.java` |
| CERT-04 | Rebuild collision records child equations/congruence proof | Lemma 4; Appendix B | `RebuildCongruenceCertificate` retains find/law/group provenance and `EffectiveShapeCollisionCertificate` composes both ECs before `ParentEdgeCertificate` admission | EXACT | Phase G collision obligation complete | Collision orientation is prevalidated before stale-key mutation; union still accepts only a permitted forward derivation | One-child collision, recursive fan-in, incompatible embedding rejection boundary, and noninjective-parent adversary | Both congruence certificate classes; `theory/TypedSlottedPortEGraph.java`; Phase F/G tests |
| CERT-05 | Leader-kernel extraction retains structural trace `xi_n` and replays it to typed source-to-kernel certificate `d_n^w` | Lemma 8; repaired Appendix C.1-C.2 | `KernelReplayCertificate` separately replays the current `xi_n` against an explicit coherent family, composing find, concrete Seq/Bag/Set normalization, forward congruence, and capture-safe structural alpha transport | EXACT | Repaired PF3 dependent replay contract complete | Factory recomputes the current structural trace and checks exact endpoints `floor(n)_w` and `iota_n.floor(K)_w`; `xi` remains certificate-free | Identity/path replay, graph-induced Set dedup, binder alpha widening, stale-family and malformed endpoint rejection | Replay, container, and alpha certificate classes; `TheoryCoherentInsertionTest.java` |
| UNION-01 | Distinct-leader union requires typed derivation | Thm. 1 obligation 4 | Exact `unionCertified` accepts only `ParentEdgeCertificate` whose oriented endpoint derivation originates in input/rewrite equality or forward congruence | EXACT | Phase F union proof boundary complete | Verifier checks type, context, exact endpoints, orientation, current distinct leaders, and allowed roots before mutation | Input, proper-embedding, congruence-derived, malformed, and nonleader cases | `theory/ParentEdgeCertificate.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryCertificatesTest.java` |
| UNION-02 | New parent edge stores embedding and parent-edge certificate | Thm. 1 obligation 4 | `ParentStep.certified` stores both the exact formal-direction embedding and immutable parent-edge proof | EXACT | Phase F parent installation complete | No public raw `ParentStep` constructor or union-find mutation path exists | Two-edge and 64 generated parent-proof replays | `theory/ParentStep.java`; `theory/ParentPath.java`; `TheoryCertificatesTest.java` |
| UNION-03 | Same-leader event does not infer symmetry | Thm. 1 obligation 5 | Exact union rejects same-ID endpoints; only separate `addSymmetryCertified` can mutate `G_a` | EXACT | Phase F separation complete | Union code has no group-inference branch | Union leaves identity group unchanged; same-ID edge rejected | `theory/TypedSlottedPortEGraph.java`; `TheoryCertificatesTest.java` |
| UNION-04 | Endpoint image mismatch infers neither symmetry nor redundant slot | Thm. 1 obligation 7 | Proper parent embeddings are admitted only with exact endpoint proof; no image difference mutates symmetry or interface | EXACT | Phase F non-inference obligation complete | Typed parent direction plus explicit proof; incomparable metadata fails | Proper contraction parent proof and unchanged group/interface | `theory/ParentEdgeCertificate.java`; `TheoryCertificatesTest.java` |
| CONG-01 | Forward congruence only | Sec. 3.2; Lemma 4; Thm. 1 | `CongruenceCertificate` lifts exact child proofs through one node/port constructor and exposes no inverse decomposition rule | EXACT | Phase F congruence direction complete | Distinct slot leaves, changed arity/head/schema, missing child proof, and parent-to-child use fail | Positive two-level lift and noninjective opaque-operator adversary | `theory/CongruenceCertificate.java`; `TheoryCertificatesTest.java` |
| CONG-02 | Different operators/types never structurally collide | Def. 6; Appendix C.2 | `CanonicalShape` and certificate congruence endpoints retain complete instantiated operator, schema, output type, and context | EXACT | Typed collision tags complete | Constructors and structural keys reject head/type/schema mismatch | Existing shape noncollision plus congruence mismatch cases | `theory/CanonicalShape.java`; `theory/CongruenceCertificate.java`; both theory tests |
| REB-01 | First-class deduplicated dirty-parent rebuild | Sec. 3.7 | Exact graph owns an immutable record-key reverse index and a `TreeSet` dirty queue; child/group/interface mutations enqueue affected parents once | EXACT | Phase G queue complete | Checker recomputes the entire reverse index and rejects missing, extra, or forgotten stale records | Queue dedup, recursive fan-in, in-flight tracking, zero-dirty completion | `theory/ParentRecordKey.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| REB-02 | Rebuild removes stale key, finds children, composes embeddings, canonicalizes, normalizes ports, and reinserts | Sec. 3.7 | `rebuildRecord` executes a transactional equivalent of the leader-kernel/`canon_G` pipeline: the in-flight key is logically stale while replacement and collision evidence are prevalidated, then H is physically removed/reinserted | EXACT | Phase G record rebuild complete | Public canonicalization remains quiescent-only; one private nonreentrant rebuild epoch permits historical leaderization without path-compression mutation | Key change, proper support contraction, failed/retried restriction, and Seq/Bag/Set traces | `theory/TypedSlottedPortEGraph.java`; `theory/RebuildCongruenceCertificate.java`; `TheoryRebuildTest.java` |
| REB-03 | Collision union uses congruence certificate after child derivations exist | Sec. 3.7; Thm. 1 obligation 8 | Both colliding ECs and the generated rebuild derivation are composed into a prevalidated forward collision certificate before graph mutation | EXACT | Phase G collision handling complete | No hash collision directly invokes raw union; typed orientation must exist and `ParentEdgeCertificate` rechecks provenance | Certified wrapper collision, recursive fan-in, deterministic owner orientation | `theory/EffectiveShapeCollisionCertificate.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| REB-04 | Rebuild does not open opaque child classes | Sec. 3.7 | Leader-kernel traversal calls `find` only on invocation leaves and never enumerates a referenced class's shapes | EXACT | Phase G opacity contract complete | Rebuild APIs have no representative-unfolding callback | Hidden same-headed Set remains one opaque invocation after leaderization | `theory/LeaderKernelExtractor.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| REB-05 | Fixed finite rebuild reaches quiescence; rewrite saturation termination is separate | Prop. 3 | `rebuild` has no pass cap or rewrite insertion and exits only after the finite deduplicated queue is empty and H is exact | EXACT | Proposition 3's stated fixed-batch operational assumptions are realized | Rebuild reports administrative work; saturation budgets remain solely in Fast Rewrite and retained comparison drivers | 48 generated fan-in traces, forward/reverse order, repeated quiescent rebuild | `theory/RebuildReport.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| FIN-01 | Indexed finite-unfolding representation relation | Def. 8 after EC/PC/SC; Lemma 7 | `FiniteUnfoldingTree` retains every selected shape, exact witness, oriented EC, complete invocation subtree, and an explicit indexed weakening/extension trace satisfying the commuting square with fresh same-typed redundant coordinates | EXACT | Bounded complete-tree representation implemented over one current coherent prefix | Each `FiniteUnfoldingStepIndex` checks `mbar o inclusion = iota o m`, freshness outside `iota[Gamma]`, scoped binders, and final weakening; pairwise final contexts retain a compatible common weakening | Acyclic, productive cyclic, redundant-support, unary binder, Bag symmetry, common-context, and malformed child cases | `theory/FiniteUnfoldingTree.java`; `FiniteUnfoldingIndexTrace.java`; `FiniteUnfoldingCommonWeakening.java`; `TheoryFiniteUnfoldingTest.java` |
| FIN-02 | Finite-unfolding validation harness with positive and malformed traces | Thm. 1; mission sec. 24 | `BoundedFiniteUnfoldingOracle` establishes the exact find/leader-symmetry premise, enumerates only complete bounded representations, and compares independent normalized or finite-model observations | EXACT | Phase H conformance oracle complete; it remains validation evidence, not a theorem substitute | Strict quiescence/current-family gate, finite depth/count bounds, retained certificates, and read-only failure paths | 48 deterministic generated graphs, cyclic/acyclic and fresh-coordinate agreement, plus stale, dirty, unknown, unequal, malformed-EC, malformed-tree, and limit rejection | `theory/BoundedFiniteUnfoldingOracle.java`; `FiniteUnfoldingEqualityWitness.java`; `FiniteUnfoldingConformanceReport.java`; `TheoryFiniteUnfoldingTest.java` |
| MUT-01 | Central mutation boundary for U, M, H, groups, interfaces, shapes | Mission sec. 25 | Exact state is private/immutable and graph-owned; fixed-batch admission, coherent `insertNode`, union, symmetry, restriction, historical find, and rebuild are centralized | EXACT | Isolated exact-engine mutation boundary complete before integration | Source insertion prevalidates children, coherent replay, orbit, witness definition, EC, and prospective collision before publishing U/M/H | Fresh insertion, stale family, missing child, collision, restriction rollback, and generated rebuild traces | `theory/TypedSlottedPortEGraph.java`; `theory/GraphCertificateMode.java`; Phase F/G and coherent insertion tests |
| MUT-02 | Source and rewrite insertion share controlled construction | Flat contract; mission sec. 25 | Mutable `EGraphNode` construction is confined to the pre-boundary normalization IR; `TheoryAlloyAdapter` converts every resulting source/rewrite term to typed ports and routes every exact-state insertion through one private `insert` method and graph-owned `insertNode` | EXACT | Phase I exact-state construction boundary complete; Fast Rewrite IR remains an explicit co-maintained frontend | Exact nodes are narrowed to structural support, inserted with a current coherent family, rebuilt to quiescence, and checked before return; no adapter can mutate U/M/H directly | Adapter support, ACI, binder, temporal, negative, and determinism checks; exact graph insertion adversarial suite | `adapter/TheoryAlloyAdapter.java`; `theory/TypedENode.java`; `theory/TypedSlottedPortEGraph.java`; `CanonicalAlloyPipelineTest.java`; `TheoryCoherentInsertionTest.java` |
| CHK-01 | Full runtime `checkInvariants()` | Mission sec. 26 | Checker recomputes U/M domains, leader ownership, EC/PC/SC proofs, restriction chains, exact reverse uses, dirty coverage, certified node theories, and quiescent H iff B | EXACT | Phase G checker complete for represented exact state | Strict and structural-fixture modes remain explicit; nonleaders cannot retain strict shapes | Random legal traces plus malformed admission/restriction/collision/query cases | `theory/TypedSlottedPortEGraph.java`; all theory state/certificate/rebuild tests |
| CHK-02 | Checker runs after every debug operation and rebuild | Mission sec. 26 | Setup gates and every public certified union, symmetry, restriction, admission, find, and successful rebuild boundary run the same checker | EXACT | Phase G mutation-check boundary complete | In-flight rebuild records remain logically dirty; failed contraction is requeued intact | Generated traces explicitly check after every transition | `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| CHK-03 | Dirty-state query rejection/rebuild | Def. 5; mission secs. 10 and 26 | H and public canonicalization reject dirty state; fixed-batch rebuild alone may enter the private canonicalization epoch and publishes only after exact H restoration | EXACT | Dirty/quiescent lifecycle complete | Failed restrictions preserve DIRTY state and the queued record | Stale hash query, failed contraction, successful retry, and idempotent quiescence | `theory/GraphStatus.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryRebuildTest.java` |
| TEST-01 | Foundational embedding/renaming/alpha property tests | Mission sec. 27 | Deterministic generated suites cover typed embedding/renaming/permutation laws and indexed structural alpha identity, inverse, composition, and binder cases | EXACT | Foundational and alpha property obligation complete | Seeds `55520260816` and `55520260819` are printed | 1,053 foundation checks plus Phase E alpha groupoid and malformed cases | `theory/TheoryFoundationsTest.java`; `TheoryCanonicalizationTest.java` |
| TEST-02 | Canonicalization/container/support properties | Mission sec. 27 | Generated Phase C-E tests cover indexed containers, first-class and nested blocks, action/support, exact leader kernels, proper and empty support contraction, structural traces, local quotient ordering, and complete results | EXACT | Structural Phase E property obligation complete | Constructors validate every carrier; canonicalization acts only on exact effective kernels | 1,006 Phase C checks, 233 Phase DA checks, and 11,560 Phase E checks | `theory/TheoryPortsTest.java`; `TheoryLeaderKernelTest.java`; `TheoryCanonicalizationTest.java` |
| TEST-03 | Mutation-sequence fuzzing with one-fault illegal traces | Mission sec. 28 | Seeded suites now include 48 random fixed-batch admission/union/find/collision rebuild traces, 24 random symmetry/restriction/history traces, reverse dirty order, and targeted illegal transitions | EXACT | Phase G mutation gate complete for represented operations | `checkInvariants()` runs after every generated transition; malformed transitions are prevalidated | Seeds `55520260818`, `55520260821`, and `55520260822`; all state/certificate/rebuild suites | `TheoryStateTest.java`; `TheoryCertificatesTest.java`; `TheoryRebuildTest.java` |
| TEST-04 | Differential exhaustive reference versus production canonicalizer | Mission sec. 29 | Separate local-orbit materializing reference and streaming production recursions return equal complete structural results on generated small states | EXACT | Structural Gate E passed; certificate replay is a distinct Phase F differential gate | Every case compares kernel, shape, sigma, iota, omega, xi, global free-renaming candidates, and local quotient work items; the two implementations share no quotient helper | 11,560 checks including K+/K0, invocation S3, block Set/Bag/nesting, proper/empty contraction, global-renaming, counter-unit, and tie cases | `theory/ExhaustiveGraphCanonicalizer.java`; `theory/ProductionGraphCanonicalizer.java`; `TheoryCanonicalizationTest.java` |
| TEST-05 | Deterministic replay across runs, workers, and insertion orders | Mission sec. 30 | The exact package records deterministic seeds and checks graph-state plus canonical-shape digests across repetitions, admissible order changes, worker counts, and fresh JVMs | EXACT | Executable deterministic replay complete; manifest publication remains `INT-03` | Every replay builds an independent strict graph and compares complete stable digests | 12 repeats, reversed fixed-batch/Bag order, 1/available-core workers, two child JVMs | `theory/TheoryDeterminismTest.java`; `theory/StructuralKey.java` |
| TEST-06 | Mandatory adversarial proof-hazard suite | Mission sec. 34 | All hazards representable before integration, including malformed finite unfolding, stale family, dirty H, wrong reachability, EC endpoint, scoped binder, collision, restriction, and flexible-container cases, fail before publication or preserve the required distinction | EXACT | Exact-package adversarial suite complete; future adapter-origin cases are obligations of `INT-02` | Controlled graph operations and read-only oracle checks fail closed | All Phase B-H theory gates, especially generated malformed Phase G/H traces | All theory tests; `TheoryFiniteUnfoldingTest.java` |
| INT-01 | Fast Rewrite, comparison, and certificate-integrated engines are explicitly distinguished | Paper sec. 5.2; mission sec. 31 | Exact engine reports `typed-slotted-port-egraph`, `canon-g-production-v4`, and `typed-finite-unfolding-oracle-v2`; retained comparison arms and the Fast Rewrite IR remain separately named and active | EXACT | Implementation-path and nomenclature distinction complete; adding the arm is separately `INT-03` | Isolated package boundary and distinct public version identifiers | Exact Phase B-H tests plus unchanged Fast Rewrite compatibility tests | `theory/TypedSlottedPortEGraph.java`; `ProductionGraphCanonicalizer.java`; `BoundedFiniteUnfoldingOracle.java`; `theory/package-info.java` |
| INT-02 | Certificate-integrated normalized-IR adapter path | Mission phase I | `TheoryAlloyAdapter` consumes normalized temporal phases, constructs typed signatures/ports/binder blocks, performs certified insertion and quiescent rebuilding, and publishes an immutable `CertifiedSemanticArtifact`, the phase coordinate maps, every local binder descriptor and source-coordinate map, and the signature-certified source container-law registry | NORMALIZED-IR EXACT; RAW REFINEMENT BLOCKED | The independent certificate begins at normalized typed IR. Raw Alloy identifiers and hashes are provenance metadata, not a replayed parser/normalization derivation | Every graph transition runs strict invariants; raw-source-to-normal-form conformance remains a separate Phase 1/3/5 refinement obligation | Focused pipeline and mutation checks exercise the normalized boundary; historical corpus figures are empirical evidence, not a proof of raw-source refinement | `theory/TheoryAlloyAdapter.java`; `theory/CertifiedSemanticArtifact.java`; `CanonicalAlloyPipeline.java`; `CanonicalAlloyPipelineTest.java`; `EGraphSemanticSoundnessCheck.java` |
| INT-03 | Additional evaluation arm and complete manifest fields | Mission sec. 32 | Batch and augmentation primary canonical metrics use the quotient-aware `CanonicalAlloyPipeline`; ablation/capability add `typed-slotted-port-egraph`; manifest v3 records engine, exact flag, invariant/rule/canonicalizer/certificate, repair-projection, quotient-metric and representative-TED versions, workers, seed, bounds, source/dataset/runtime identity, and output hashes | EXACT | Phase I experiment and manifest integration complete while retaining Fast Rewrite and representative-TED diagnostics | Combined report generation checks context compatibility and verifies every per-arm output hash before publication | Full seven-arm 61,598-pair run with 0 failures and certificate-integrated `+1/-0` over Fast Rewrite zeroes; full 5,500-pair capability run with all expected boundaries; hash-checked report-only regeneration; current run IDs `cfe55f5d-daf6-4ae1-808e-3eaa863015a8` and `dfd614b7-661d-4be6-8b46-6459e00809ad` | `CanonicalBatchTest.java`; `Alloy4FunAugmenter.java`; `EGraphAblationStudy.java`; `EGraphAblationSuite.java`; `CapabilityBenchmark.java`; `AblationRunManifest.java` |
| MET-01 | Canonical equality and repair geometry are separate consumers | Quotient metric contract | `CanonicalObservation` defines equality/hash/serialization; `CanonicalRepresentativeTreeDistance` is diagnostic; `RepairProjection` and `QuotientRepairDistance` do not depend on canonical tree positions | EXACT | Three-layer dependency direction implemented | Public zero distance is kernelled by certified equality in both directions; either disagreement fails closed with source pair, observation digests, and component distances in corpus clients | Real alpha/ACI discontinuity, fixed commutativity, named-reference discrimination, local-comprehension alpha with ordered-column discrimination, owner-consistent temporal inheritance, prefix-witnessed carriers, proof-wrapper erasure, and complete correct-pool/ranking audits | `canonical/*`; `metric/*`; `CanonicalAlloyPipeline.java`; both metric/pipeline tests; `Alloy4FunAugmenter.java` |
| MET-02 | Established structure-specific repair units | Fast Rewrite metric preservation contract | The certificate-integrated port preserves `D_temporal + D_quantifiers + D_matrix`; Seq uses ordered DP; commutative/Bag/Set children use minimum assignment with the declared multiplicity/idempotence; declaration tuple insertion/deletion/modification costs one. Proven corrections quotient adjacent non-disjoint equal-domain local declaration regrouping and require one owner-coordinate alpha mapping across every inherited temporal phase | EXACT | `CanonicalDistance` geometry is re-expressed without calling the Fast Rewrite implementation or representative TED; certified scope structure narrows only the Fast Rewrite mappings proved too broad, while same-coordinate fallback preserves a one-unit declaration modification and frontend/projected Set deduplication restores the required ACI quotient | Proof/schema/witness/rebuild wrappers cannot enter `RepairView`; certified laws select alignment rather than add wrapper costs; Set alone removes duplicate operands, Bag multiplicity and Seq order survive, and inherited occurrences cannot choose independent permutations | 13 focused metric checks, 185 pipeline checks, 100/100 corpus-smoke parity, a 784-file former-failure replay, a 61,598-model full augmentation with 1,424,852 deduplicated incorrect-to-truth comparisons and no strict-kernel failure, and a complete seven-arm corpus run with 2,317 exact `CORRECT` zeroes and 0 incorrect zeroes | `core/CanonicalDistance.java`; `metric/RepairView.java`; `metric/RepairProjection.java`; `metric/QuotientRepairDistance.java`; `QuotientRepairDistanceTest.java` |
| MET-03 | Pairwise alpha alignment uses certified legality | Sec. 3.6; Fast Rewrite metric preservation contract | Pairwise order-preserving scope-block matchings are enumerated; for each, the metric computes maximum compatible variable cardinality and minimizes over every maximum partial injection | EXACT | No binder-alignment cap or independently canonicalized coordinate order remains | Exchange IDs are local, continuously nested interchangeable declarations share one certified block, different blocks cannot cross-permute, descriptor domain/dependency payloads guard candidates, and projection proves that certified adjacent transpositions generate every Fast Rewrite declaration-class permutation before exposing that space | Scope barriers, shifted exchange IDs, unequal-arity best-nonfirst mapping, alpha invariance, and zero-merge smoke | `theory/BinderBlockDescriptor.java`; `metric/RepairProjection.java`; `metric/QuotientRepairDistance.java`; pipeline/certificate tests |
| T1-01 | Bottom-up well-typed insertion; children exist; fresh class exposes the effective kernel interface `Delta_n` | Thm. 1 obligation 1; repaired Appendix C.2 | `insertNode` validates every child, computes the complete structural/coherent record, allocates a fresh class with interface `Delta_n`, stores the EC, and returns the leader invocation with an exact source certificate | EXACT | Theorem insertion obligation 1 implemented in the isolated engine | Missing/current child metadata, exact support, law provenance, current witness family, replay endpoints, and fresh ID are checked before publication | Fresh and strict-interface insertion, stale family, missing child, exact return endpoint | Graph insertion and result classes; `TheoryCoherentInsertionTest.java` |
| T1-02 | Canonicalization uses only typed alpha, certified binder automorphisms, and certified container laws | Thm. 1 obligation 2 | Public exact canonicalization is typed/uncapped and rejects any nonidentity leader/block generator or Seq/Bag/Set law lacking structured certificate provenance | EXACT | Theorem obligation 2 implemented for the exact path | Strict graph mode validates every declaration and reconstructs every group element before use; structural gates are package-only | Certified/raw law and block contrasts, S3 proofs, full Phase E differential suite | Both canonicalizers; `CertificateVerifier.java`; `TheoryCertificatesTest.java` |
| T1-03 | Hash-cons only equal canonical typed shapes | Thm. 1 obligation 3 | Fixed-batch rebuild and coherent source insertion hash only `CanonicalShape`; source collisions construct both replay-bearing ECs and an `EffectiveShapeCollisionCertificate` before union | EXACT | Theorem hash-cons obligation implemented for the isolated engine | H is inaccessible while dirty; fresh/collision paths share exact key equality and the quiescent checker recomputes H iff B | Fresh noncollision, two-source collision retaining both `d_n^w`, stale query, rebuild, and exact H restoration | Canonical shape/result, insertion, collision, and graph classes; coherent insertion and rebuild tests |
| T1-04 | Every union has endpoint derivation from input equation or forward congruence | Thm. 1 obligation 4 | Exact union accepts only an oriented typed parent-edge certificate rooted in input/rewrite equality or forward congruence | EXACT | Theorem obligation 4 implemented for represented unions | Certificate verifier runs before any forest mutation | Input, rewrite, congruence-derived, wrong-root, and wrong-endpoint cases | `theory/ParentEdgeCertificate.java`; `TheoryCertificatesTest.java` |
| T1-05 | Same-leader symmetry requires separate witness equation | Thm. 1 obligation 5 | Same-leader parent edges are rejected; only `addSymmetryCertified` with the explicit SC equation can enlarge the group | EXACT | Theorem obligation 5 implemented | Union path cannot access group mutation | Same-ID rejection, unchanged identity group, certified swap | `theory/TypedSlottedPortEGraph.java`; `TheoryCertificatesTest.java` |
| T1-06 | Slot removal has restricted witness, inclusion equation, and transported certificates | Thm. 1 obligation 6 | Atomic restriction transports witnesses, ECs, UF parent proofs, the certified symmetry stabilizer, historical invocation evidence, and the reverse dirty relation | EXACT | Theorem obligation 6 implemented for exact fixed-batch state | Rebuild never treats a proper find embedding as the missing factorization | Valid/invalid factorization, rejected implicit contraction, incident-parent retry, and S3-to-S2 transport | Restriction, UF, graph classes; `TheoryRebuildTest.java` |
| T1-07 | Different endpoint images alone create no symmetry/redundancy | Thm. 1 obligation 7 | Exact union stores a proved proper embedding but performs no image-derived symmetry or interface change | EXACT | Theorem non-inference obligation implemented | Separate certificate types and graph methods are required for all three transitions | Proper image contraction with unchanged group/interface | `theory/TypedSlottedPortEGraph.java`; `TheoryCertificatesTest.java` |
| T1-08 | Rebuild congruence occurs only after child derivations exist | Thm. 1 obligation 8 | Rebuild retains every find proof and declared law/group premise, composes the two exact shape equations, and prevalidates a forward collision certificate before union | EXACT | Theorem obligation 8 implemented for fixed-batch rebuilding | Collision has no raw union path and parent equality is never inverted into child equality | Direct/noninjective congruence tests plus collision and recursive fan-in rebuilds | Congruence/rebuild collision certificates; both Phase F/G tests |
| T1-09 | Path compression composes typed embeddings and certificates | Thm. 1 obligation 9 | Primitive certified parent steps survive compression; find composes their embeddings and transports/transitively composes their proofs | EXACT | Theorem obligation 9 implemented | Replayed endpoints must equal the original and returned leader invocations | Proper two-edge and 64 generated compressed certificate paths | `theory/ParentPath.java`; `theory/TypedFindResult.java`; `TheoryCertificatesTest.java` |
| RW-01 | Ordered per-phase pipeline | Appendix C.3 | `NormalForm.normalize` follows alpha/beta/branch, NNF, prenex, second branch/NNF, ACI, saturation | EXACT | Preserve this order in Alloy adapter integration | Stage observer and regression tests | Stage-order regression | `NormalForm.java:247-299` |
| RW-02 | Parser markers, END cleanup, lexical alpha, capture-avoiding let beta | Appendix C.4 | `NormalForm` performs cleanup/alpha/beta before the typed boundary; `TheoryAlloyAdapter` rejects any surviving END marker and routes every resulting node through typed smart construction | EXACT | Phase I typed routing complete while retaining the established frontend semantics | Scope-aware substitution, arity checks, explicit END rejection, typed construction, and strict insertion checks | Existing END/let/shadow regressions plus shadow-safe exact-adapter integration | `NormalForm.java`; `AlloyRewriteSystem.java`; `adapter/TheoryAlloyAdapter.java`; `CanonicalAlloyPipelineTest.java` |
| RW-03 | Implication, iff, formula ITE elimination and Boolean NNF | Appendix C.5 | Implemented and regression-tested | EXACT | Adapt outputs to typed port constructors | Existing branch/negation checks | Implication/IFF/ITE polarity tests | `NormalForm.java:423-460`; `EGraphSaturationTest.java:679-750` |
| RW-04 | Temporal duals and no quantifier crossing temporal phases | Appendix C.6; sec. 5.1 | Separate normal-form phases and dualization exist | EXACT | Keep phase adapter outside exact graph or encode explicit temporal layer | Phase-local traversal | Existing temporal tests | `IRAgent.java:99-155`; `NormalForm.java:110-237` |
| RW-05 | Quantifier negation, empty/constant rules, guarded prenex, primitive domains, no temporal crossing | Appendix C.7 | The ordered normal-form pipeline eliminates branch connectives before movement, normalizes any generated guards before a later pass, permits carrier-witness lifting only at an admissible connective boundary, retains unsafe binders locally, and keeps complex domains as guards over the parser-derived primitive type | BOUNDED-CONFORMANCE; GENERAL REFINEMENT OPEN | The two previously missed relational-ITE and repeated-guard counterexamples are repaired, but a general source-to-normal-form proof is not claimed | Residual quantifiers decrease under a bounded fixed-point loop; no synthetic `univ` carrier is fabricated; temporal and relational-ITE boundaries reject generic witness lifting | Quantifier polarity, empty universe, guarded local/direct binders, repeated passes, relational ITE, branch order, shadowing, and temporal SAT regressions plus `GuardedPrenex.lean` | `NormalForm.java`; `QuantiVar.java`; `MASGVisitorTypeRegressionTest.java`; `AlloySourceRuleRegressionTest.java`; `formal/GuardedPrenex.lean` |
| RW-06 | Complete A/AC/ACI operator-law table | Appendix C.8 Table 11 | Frontend `FlexibleArityKind` is translated to typed `SeqPortSchema`, `BagPortSchema`, or `SetPortSchema` with structured A, AC, or ACI law certificates under `canonical-alloy-signature-v8`; fixed commutative nonassociative operators use an ordinary certified `BagPort`, and non-temporal references retain their normalized symbol identity | EXACT | Phase I certified signature translation complete | Strict node construction verifies the declaration at its exact schema path; Seq preserves order, Bag preserves multiplicity, Set alone deduplicates, fixed equality/IFF Bags exchange exactly two operands without flattening, and `ordering/first` cannot collide with `ordering/last` | Theory port/certificate suites, ACI exact-adapter case, fixed equality/inequality commutativity, named-reference contrast, nested/grouped subtype-binder case, and all generated capability families | `EGraphNode.java`; `AlloyRewriteSystem.java`; `adapter/TheoryAlloyAdapter.java`; `TheoryPortsTest.java`; `TheoryCertificatesTest.java`; `CanonicalAlloyPipelineTest.java` |
| RW-07 | Local Boolean and relational rules | Appendix C.9 | Rules run in the ordered normalization frontend; the normalized result is the semantic source term at the exact-graph boundary, while only explicitly certified container laws create exact graph equalities | NORMALIZED-IR EXACT; RAW REFINEMENT BLOCKED | Preprocessing does not issue exact graph-union certificates, but the standalone verifier does not replay raw Alloy normalization | The adapter has no raw union path; exact equality-producing graph transitions remain closed under the certificate verifier and graph-owned mutation API. This does not certify parser-to-normal-form correctness | Rule-by-rule saturation regressions and bounded differential checks; complete formal Java refinement remains open in the Section 3 repair ledger | `EGraphNode.java`; `AlloyRewriteSystem.java`; `theory/TheoryAlloyAdapter.java`; `theory/CertificateVerifier.java`; `EGraphSaturationTest.java`; `CanonicalAlloyPipelineTest.java` |
| RW-08 | Rewrite saturation bound is separate from exact administrative rebuild | Appendix C.10; Prop. 3 | Fast Rewrite and retained comparison arms use bounded saturation, while certificate-integrated `rebuild` inserts no rules, has no round cap, and drains its finite dirty set | EXACT | Exact administrative/rewrite separation complete; arbitrary saturation termination is not claimed | Separate package, API, state, and report counters | Generated fixed-batch quiescence and repeated zero-work rebuild | `theory/TypedSlottedPortEGraph.java`; `theory/RebuildReport.java`; `TheoryRebuildTest.java` |

## Gate Status

The historical review labeled all 125 rows `EXACT`. That historical verdict is
invalid under the current maximum-adversarial gate: abstract proofs, bounded
tests, Java behavior, wire replay, provenance, and empirical claims were not
mapped claim-completely, and current counterexamples remain open. No current
phase or matrix gate passes. The old rows remain useful as an implementation
inventory only. See
`theory-architecture-map.md` for mutation paths,
`theory-phase-e-canonicalizer.md` for the current canonicalization evidence,
`theory-phase-f-certificates.md` for the certificate gate and blockers,
`theory-phase-g-rebuild.md` for fixed-batch rebuild evidence,
`theory-pre-phase-h-unblocked.md` for coherent replay/insertion evidence,
`theory-phase-h-finite-unfolding.md` for bounded conformance evidence,
`theory-phase-i-artifact-integration.md` for the adapter, runner, manifest, and
targeted conformance evidence,
and `theory-phase-a-audit.md` for the baseline.
