# Theory to Artifact Obligation Matrix

This matrix began as the Phase A audit gate for the theory-faithful engine. It audits commit
`a9261da4c096f6ba5fcb1a34bcac93cb1b1df23d` against the current normative
`E_Graphs_Draft_LNCS-1.pdf` (SHA-256
`6e44bae88fcd82fd9cd26db54d52025affb9f7d21d5cc3a2165211ab52a9d42f`).
The paper is the normative specification. Existing experimental behavior is
evidence about the current artifact, not a substitute for a formal object or
certificate. Its statuses are updated as later phase gates are completed; the
Phase A snapshot and original counts remain in `theory-phase-a-audit.md`.

Allowed status values are exactly `EXACT`, `PARTIAL`, `ABSENT`,
`CONTRADICTED`, and `UNCERTAIN`.

Gate rule: every row below is mandatory unless its formal object is explicitly
described as an integration or test obligation. Phase A is not complete-engine
acceptance: unresolved rows are expected now and must become `EXACT` before a
theory-faithful implementation claim is made.

| ID | Formal object/invariant | Paper location | Existing code | Status | Required implementation | Runtime enforcement | Tests | Evidence |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| TY-01 | Type grammar and typed values | Def. 1, sec. 3.3 | `GraphType` represents variables, `Int`, `Bool`, arrows, relations, and constructors structurally | EXACT | Phase B carrier complete; later signatures and e-classes must consume it without conversion to strings | Factory validation and immutable structural values | Positive/malformed grammar and ordering | `theory/GraphType.java:9-181`; `TheoryFoundationsTest.java` |
| TY-02 | A slot carries exactly one type | Def. 1, sec. 3.3 | `TypedSlot` identity contains graph type, disjoint alphabet, and non-negative unbounded ordinal | EXACT | Phase B carrier complete | Constructor rejects absent type/alphabet and negative ordinal | Cross-type identity, alphabet separation, ordering | `theory/TypedSlot.java:7-103`; `TheoryFoundationsTest.java` |
| TY-03 | Ordered, disjoint canonical free and bound alphabets by type | Def. 1; Appendix C.1 | `SlotAlphabet` and `CanonicalSlotAlphabet` provide deterministic disjoint free/bound allocations per type | EXACT | Phase B allocator complete | Rejects `SOURCE` as canonical; least-fresh and first-count construction | Stable per-type counts, freshness, disjointness | `theory/SlotAlphabet.java`; `theory/CanonicalSlotAlphabet.java:10-53` |
| TY-04 | Finite typed slot context | Def. 1, sec. 3.3 | `TypedSlotContext` is an immutable finite sorted set with typed projections and context algebra | EXACT | Phase B carrier complete | Constructor rejects nulls and duplicate slot occurrences | Context laws, mixed types, immutable views | `theory/TypedSlotContext.java:15-180`; `TheoryFoundationsTest.java` |
| EMB-01 | Typed embedding is a type-preserving injection | Def. 2 | Sealed `TypedEmbedding` stores declared contexts and a total immutable source map | EXACT | Phase B carrier complete | Constructor checks exact domain, codomain membership, type preservation, and injection | Cross-type, duplicate image, missing/extra source, outside target rejection | `theory/TypedEmbedding.java:13-190`; `TheoryFoundationsTest.java` |
| EMB-02 | Typed renaming is onto its declared codomain | Def. 2 | Sealed `TypedRenaming` refines a validated embedding with surjectivity | EXACT | Phase B carrier complete | Constructor rejects every proper embedding | Proper injection cannot become renaming | `theory/TypedRenaming.java:8-67`; `TheoryFoundationsTest.java` |
| EMB-03 | Only renamings may be inverted | Def. 2, Prop. 1 | `inverse()` exists only on sealed `TypedRenaming` and `TypedPermutation`; proper embeddings can only request checked conversion | EXACT | Phase B API complete | Closed subtype hierarchy and onto check prevent unchecked inversion | Reflection boundary, non-surjective conversion rejection, inverse laws | `theory/TypedEmbedding.java:13,127-134`; `theory/TypedRenaming.java:35-44`; `TheoryFoundationsTest.java` |
| EMB-04 | Typed permutation is a context automorphism | Def. 2 | Final `TypedPermutation` has one declared context and inherits typed bijection validation | EXACT | Phase B carrier complete | Same source/codomain by construction | Cross-type rejection, identity, inverse, exhaustive small group laws | `theory/TypedPermutation.java:7-45`; `TheoryFoundationsTest.java` |
| EMB-05 | Identity and composition preserve declared contexts and types | Prop. 1 | `identity`, `andThen`, and formal-order `compose(after,before)` preserve exact endpoints | EXACT | Phase B algebra complete | Composition rejects a middle-context mismatch | Identity, associativity, direction, type preservation over seeded chains | `theory/TypedEmbedding.java:91-113`; `TheoryFoundationsTest.java` |
| EMB-06 | Executable embedding predicates | Mission secs. 3.2 and 27 | All five predicates are public and are used by the typed construction boundary | EXACT | Phase G invariant checker must reuse, not duplicate, these predicates | Constructors plus direct predicates fail closed on malformed input | Predicate truth tables | `theory/TypedEmbedding.java:113-190`; `TheoryFoundationsTest.java` |
| INV-01 | Invocation is `m * a` with `m in TEmb(S_a,Gamma)` | Def. 2; Cor. 2 | Immutable `TypedInvocation` pairs `TypedEClassInterface` with a validated embedding | EXACT | Phase D class table must own the interface value | Constructor requires embedding source exactly equal to `S_a`; caller context is embedding codomain | Valid/invalid invocation construction | `theory/TypedInvocation.java:6-76`; `theory/TypedEClassInterface.java` |
| INV-02 | Invocation output is exactly `tau_a` | Cor. 2 | `TypedInvocation.outputType()` is derived only from its immutable e-class interface | EXACT | Phase D must preserve this when interfaces become table-owned | No caller-supplied invocation output field exists | Output preservation under action | `theory/TypedInvocation.java:37-39,46-54`; `TheoryFoundationsTest.java` |
| INV-03 | Identity invocation | Cor. 2 | `TypedInvocation.identity` uses the typed identity renaming on the exact exposed interface | EXACT | Phase B carrier complete | Constructor revalidates source context | Identity support and output | `theory/TypedInvocation.java:20-24`; `TheoryFoundationsTest.java` |
| INV-04 | Embedding action composes as `e . (m*a)=(e o m)*a` | Def. 4; Cor. 2 | `TypedInvocation`, every sealed port variant, binders, and `TypedENode` implement one typed structural action | EXACT | Phase C action algebra complete; graph-owned action in later phases must reuse it | Every action checks its exact source context; binder action uses a disjoint extension and least fresh same-typed bound coordinate | Generated support equivariance, invocation composition, binder capture avoidance, node action | `theory/TypedInvocation.java`; `theory/OnePort.java`; `theory/BindPort.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| PORT-01 | Explicit `One(tau)` port | Def. 3 | Sealed `OnePortSchema` and `OnePort` carry either a same-typed slot or typed opaque invocation | EXACT | Phase C carrier complete | Constructor checks schema type, slot membership, and invocation caller context | Valid/malformed slot and invocation leaves | `theory/OnePortSchema.java`; `theory/OnePort.java`; `TheoryPortsTest.java` |
| PORT-02 | Explicit `Seq(kappa)` preserving order and multiplicity | Def. 3; sec. 3.2 | Distinct immutable `SeqPort` stores its validated elements in source order | EXACT | Phase C carrier complete | Schema/context check per occurrence; no sorting or deduplication | Permutation differs; duplicates retained | `theory/SeqPortSchema.java`; `theory/SeqPort.java`; `TheoryPortsTest.java` |
| PORT-03 | Explicit `Bag(kappa)` discarding order, retaining multiplicity | Def. 3; sec. 3.2 | Distinct immutable `BagPort` sorts by complete structural keys and retains every occurrence | EXACT | Phase C carrier complete | Schema/context validation, deterministic sort, and unequal-key-collision rejection | Shuffled bags equal; changed multiplicity unequal; generated cases | `theory/BagPortSchema.java`; `theory/BagPort.java`; `theory/PortValues.java`; `TheoryPortsTest.java` |
| PORT-04 | Explicit `Set(kappa)` discarding order and duplicate structural classes | Def. 3; sec. 3.2 | Distinct immutable `SetPort` normalizes one shared-context value and deduplicates exact complete structural keys | EXACT | Phase E will supply the one global graph-relative renaming before constructing canonical set candidates | Schema/context validation; no per-element alpha normalization; key collision between unequal values fails closed | Shuffled/duplicate sets, generated global-action support cases | `theory/SetPortSchema.java`; `theory/SetPort.java`; `theory/PortValues.java`; `TheoryPortsTest.java` |
| PORT-05 | Explicit `Bind(tau,kappa)` with fresh same-typed coordinate | Def. 3-4 | `BindPortSchema` and `BindPort` store bound type, fresh typed slot, and recursively typed body | EXACT | Phase C carrier complete; complete binder-block descriptors remain a Phase E-F obligation | Constructor checks same type, freshness, body schema, and `Gamma+bound` context; action avoids capture | Capture avoidance, type/schema/context rejection, support subtraction | `theory/BindPortSchema.java`; `theory/BindPort.java`; `TheoryPortsTest.java` |
| PORT-06 | Port semantics are not one generic collection plus metadata | Def. 3; mission sec. 5 | Sealed `PortSchema` and `PortValue` hierarchies have separate final classes for all five grammar constructors | EXACT | Phase C hierarchy complete | Closed permits clauses plus constructor-specific validation | Sealed-boundary and schema/value mismatch rejection | `theory/PortSchema.java`; `theory/PortValue.java`; `TheoryPortsTest.java` |
| SIG-01 | Typed polymorphic signature `Sigma(f)` | Def. 4 | `OperatorDeclaration` stores quantified type variables, recursive port schemas, output type, structural law declarations, and optional flat port | EXACT | Phase F will attach semantic certificates to the explicit declarations | Exact substitution domain and recursive type instantiation are checked | Polymorphic relation instantiation and malformed substitutions/signatures | `theory/OperatorDeclaration.java`; `theory/InstantiatedOperator.java`; `TheoryPortsTest.java` |
| SIG-02 | Node output type follows instantiated signature | Def. 4; Lemma 2 | Private `TypedENode` constructor derives output solely from `InstantiatedOperator` | EXACT | Phase C construction boundary complete | Port count, schema, context, and empty-unit obligations checked before construction | Wrong count/schema/context and flat bypass rejected | `theory/TypedENode.java`; `TheoryPortsTest.java` |
| LAW-01 | Schema laws are separately certified | Sec. 4; mission sec. 21 | `ContainerLawDeclaration` is explicit and structurally validated, but intentionally is not represented as a semantic certificate | PARTIAL | Phase F must bind a machine-checkable `ContainerLawCertificate` to each declaration | Phase C rejects absent, extra, wrong-kind, and wrong-flag declarations at every recursive `PortPath` | Invalid top-level and nested schema/law combinations rejected | `theory/ContainerLawDeclaration.java`; `theory/PortPath.java`; `theory/OperatorDeclaration.java`; `TheoryPortsTest.java` |
| LAW-02 | Seq=A, Bag=AC, Set=ACI only when certified | Sec. 4; Appendix C.8 | Explicit declarations enforce the structural mapping Seq=A, Bag=AC, Set=ACI and drive flat construction; semantic provenance is absent | PARTIAL | Phase F certificate hierarchy and verifier | Constructor validates every recursive container path; flat port requires declared associativity | A/AC/ACI behavior, absent/wrong law rejection | `theory/ContainerLawDeclaration.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| LAW-03 | Empty container requires a certified unit | Def. 3; sec. 4 | `TypedENode` recursively rejects every empty Seq/Bag/Set path unless its exact `PortPath` declares a unit; semantic certification is absent | PARTIAL | Phase F must require a unit certificate rather than declaration alone | Recursive node-construction check covers containers nested below binders and other containers | Top-level and nested empty values with/without unit | `theory/PortPath.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| FLAT-01 | Every associative construction uses one smart path | Flat-construction contract; Appendix C.2 | `TypedENode` has only a private constructor; ordinary `construct` rejects flat operators and `flatConstruct` is the sole flat entry | EXACT | Phase I adapters must call this already-enforced boundary | Java visibility plus runtime rejection prevents public bypass | Constructor reflection and direct flat-construction bypass rejection | `theory/TypedENode.java`; `TheoryPortsTest.java` |
| FLAT-02 | Visible same-headed syntax is flattened | Flat-construction contract | `FlatApplication` retains visible source syntax until recursive same-operator splicing by `flatConstruct` | EXACT | Phase C implementation complete | Same instantiated operator is compared before child sealing | Left/right nesting equality for Seq plus AC/ACI examples | `theory/FlatApplication.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| FLAT-03 | Opaque invocation is never opened to find hidden same head | Flat-construction contract; sec. 3.7 | `FlatLeaf` can hold `One(invocation)` and exposes no child alternatives; `flatConstruct` copies it as one element | EXACT | Phase D-G graph operations must preserve this boundary | Flatten traversal branches only on visible `FlatApplication` values | Hidden same-head invocation stays one element; different visible head is sealed once | `theory/FlatLeaf.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| SUP-01 | Formal support for slot, invocation, containers, node, binder | Def. 4 | Every sealed Phase B-C carrier computes support structurally from slots, embedding images, unions, and binder subtraction | EXACT | Phase C support algebra complete | No public support field or parser-variable fallback exists in the exact path | Every grammar variant plus node and binder support | `theory/HasSlotSupport.java`; `theory/PortValues.java`; `theory/BindPort.java`; `theory/TypedENode.java`; `TheoryPortsTest.java` |
| SUP-02 | Invocation support is embedding image | Def. 4 | `TypedInvocation.support()` returns the image of its validated total typed embedding | EXACT | Phase B carrier complete | Invocation construction guarantees a legal image | Weakening and exact-image cases | `theory/TypedInvocation.java:41-43`; `theory/TypedEmbedding.java:73-89` |
| SUP-03 | Support equivariance `slots(e.q)=e[slots(q)]` | Cor. 1 | Structural typed action is implemented for invocations, all five ports, binders, and nodes | EXACT | Phase C algebra complete | Every action checks source context and reconstructs a validated value | Generated bag/set equivariance, binder and node equivariance, 128 invocation/context chains | `theory/TypedInvocation.java`; `theory/PortValues.java`; `theory/BindPort.java`; `theory/TypedENode.java`; both theory tests |
| STATE-01 | Explicit state `G=(U,M,H)` | Def. 5 | `TypedSlottedPortEGraph` directly owns a typed renamed union-find, immutable class-record table, and typed canonical-shape hash-cons | EXACT | Phase D state carrier complete; later transitions must mutate it only through the graph boundary | Private state fields, package-private setup transitions, and graph-wide invariant checks | Empty, populated, dirty, and deterministic-state cases | `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| STATE-02 | `M(a)` stores output type `tau_a` | Def. 5 | Every immutable `TypedEClassRecord` owns a `TypedEClassInterface` whose output type is the sole `tau_a` | EXACT | Phase D field complete | Registration, parent links, stored children, and invariant audit reject output mismatches | Cross-output parent/link and invocation rejection | `theory/TypedEClassRecord.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| STATE-03 | `M(a)` stores finite exposed typed context `S_a` | Def. 5 | Every immutable class record owns the exact `TypedSlotContext` through its interface | EXACT | Phase D field complete; certified interface restriction remains Phase F | Class/interface identity and every embedding endpoint are checked | Interface metadata spoofing and context mismatch cases | `theory/TypedEClassRecord.java`; `theory/TypedEClassInterface.java`; `TheoryStateTest.java` |
| STATE-04 | `B_a` stores canonical shapes and witnesses | Def. 5 | `TypedEClassRecord` owns an immutable ordered `CanonicalShape -> ShapeWitness` map | EXACT | Phase D dependent carrier complete; Phase E supplies `canon_G` results | Record construction checks shape output, exact context, exposed interface, and witness uniqueness | Witness round-trip, malformed record, key-collision, and bottom-up ownership cases | `theory/CanonicalShape.java`; `theory/ShapeWitness.java`; `theory/TypedEClassRecord.java`; `TheoryStateTest.java` |
| STATE-05 | Shape witness retains exact slots, ambient support, and exposed interface | Def. 5 | `ShapeWitness` stores all three contexts separately plus the exact-to-ambient typed renaming | EXACT | Phase D witness carrier complete; equality-certificate attachment remains Phase F | Constructor enforces exposed subset of ambient and exact renaming endpoints | Strict redundant ambient-coordinate and malformed-context cases | `theory/ShapeWitness.java`; `TheoryStateTest.java` |
| STATE-06 | `G_a` is a certified typed subgroup | Def. 5; Def. 7 SC | `TypedSymmetryGroup` stores an exact finite typed closure on `S_a`; nontrivial generator provenance is not yet represented | PARTIAL | Phase F must attach and transport symmetry certificates | Public construction is identity-only; package-private Phase D closure validates every typed generator and computes uncapped closure | Identity, closure, inverse, cross-type/context rejection | `theory/TypedSymmetryGroup.java`; `TheoryStateTest.java` |
| UF-01 | Parent edge is typed `U(a)=m*b`, `m:S_b -> S_a` | Def. 5 | `ParentStep` stores exactly the formal parent-interface-to-child-interface embedding | EXACT | Phase D edge carrier complete; certificate field remains Phase F | Constructor checks distinct IDs, equal output types, and exact source/codomain contexts | Direction-sensitive chain and reversed-edge rejection | `theory/ParentStep.java`; `TheoryStateTest.java` |
| UF-02 | Parent pointers form a forest; roots have identity invocation | Def. 5 | `TypedRenamedUnionFind` owns a total assignment; roots are explicit identity invocations and nonroots form a checked forest | EXACT | Phase D forest complete | Registration, leader-only links, cycle checks, and full invariant audit | Root, forest, cycle, generated chain, and branching cases | `theory/ParentAssignment.java`; `theory/TypedRenamedUnionFind.java`; `TheoryStateTest.java` |
| UF-03 | `find` returns leader plus composite typed embedding | Def. 5 | `TypedFindResult` returns the original invocation, leader invocation, and primitive parent path | EXACT | Phase D find result complete | Result recomputes and checks `m_original` after the formal parent-path composite | Identity, multi-edge composition, support image, generated forests | `theory/TypedFindResult.java`; `theory/ParentPath.java`; `TheoryStateTest.java` |
| UF-04 | Path compression composes embeddings in formal direction | Def. 5; Thm. 1 obligation 9 | Compression installs the leader-to-child composite while retaining the full primitive path | EXACT | Phase D embedding component complete | Pre/post find values and every path endpoint are checked | Direction-sensitive three-edge and generated-chain equivalence | `theory/TypedRenamedUnionFind.java`; `theory/ParentAssignment.java`; `TheoryStateTest.java` |
| UF-05 | Path compression preserves certificate derivation | Lemma 5; Thm. 1 obligation 9 | Compression preserves an ordered primitive `ParentPath`, but primitive steps do not yet carry equality certificates | PARTIAL | Phase F must attach `ParentEdgeCertificate` and compose its derivation over the retained path | Path endpoints and composite embedding are already checked; certificate replay is unavailable | Primitive path survives compression; certificate tests remain pending | `theory/ParentPath.java`; `theory/ParentAssignment.java`; `TheoryStateTest.java` |
| HC-01 | `H(p)=a` iff leader `a` owns `p` | Def. 5 | At quiescence `TypedSlottedPortEGraph` enforces the exact bidirectional relation between typed canonical shapes and leader records | EXACT | Phase D hash-cons state and invariant complete | Atomic collision rejection plus graph-wide iff audit | Leader ownership, nonleader rejection, collision atomicity, deterministic state | `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| HC-02 | Explicit dirty versus quiescent state | Def. 5; sec. 3.7 | `GraphStatus` and graph transitions distinguish `DIRTY` and `QUIESCENT`; the Phase G parent work queue/rebuild is not implemented | PARTIAL | Phase G adds deduplicated dirty parents and the administrative fixed point | Parent mutation marks dirty; only validated setup can publish a quiescent Phase D state | Dirty transition and query rejection | `theory/GraphStatus.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| HC-03 | Equality/canonicalization/measurement never use stale H | Def. 5 | Current hash-cons observations reject `DIRTY`; future equality, canonicalization, and measurement clients are not yet integrated | PARTIAL | Phase E-G route every graph query through the same quiescence guard | Public hash lookup/snapshot methods require quiescence | Query during dirty mutation | `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| ALPHA-01 | Indexed typed alpha-equivalence with one free-slot bijection | Def. 6 | `TypedAlphaEquivalence` implements the structural node/port judgement indexed by one `TypedRenaming` | EXACT | Phase E relation complete | Exact schema, operator, type, and context endpoints are checked recursively | Identity, inverse, composition, Seq/Bag/Set, and generated cases | `theory/TypedAlphaEquivalence.java`; `TheoryCanonicalizationTest.java` |
| ALPHA-02 | Binder alpha-renaming is fresh, capture-avoiding, and same-typed | Def. 6; Lemma 2 | Binder comparison extends the one free renaming with the two fresh same-typed bound coordinates | EXACT | Phase E structural binder alpha complete; block automorphisms remain a separate Phase F obligation | Existing `BindPort` construction plus checked disjoint extension reject capture/type mismatch | Differently named bound slots, canonical least-fresh binder, generated witness replay | `theory/TypedAlphaEquivalence.java`; `theory/BindPort.java`; `TheoryCanonicalizationTest.java` |
| ALPHA-03 | Structural alpha relation preserves output type | Prop. 2 | Node relation requires the same instantiated operator and output type | EXACT | Phase E relation complete | Relation fails before descending on operator/output mismatch | Different-operator/output and witness replay cases | `theory/TypedAlphaEquivalence.java`; `TheoryCanonicalizationTest.java` |
| GEQ-01 | Graph-relative invocation relation uses same leader and certified symmetry | Sec. 3.6 graph-relative extension | Named graph-relative relation uses typed find and exactly the stored typed leader group; Phase F provenance is absent | PARTIAL | Phase F must certify every nonidentity group generator consumed here | Quiescent graph, same leader, typed equation `rho o mu = mu' o pi`, and exact group membership are checked | Different IDs/same leader, identity-only rejection, and recorded symmetry acceptance | `theory/TypedAlphaEquivalence.java`; `TheoryCanonicalizationTest.java` |
| GEQ-02 | Structural alpha, leader equality, symmetry, and arbitrary e-class equality remain distinct | Sec. 3.6; mission sec. 22 | Structural alpha, graph-relative alpha, typed find/leader membership, and structural Java equality have separate APIs and tests | EXACT | Phase E distinction complete | No relation delegates to Java equality or infers invocation equality from class membership alone | Four-way distinction and same-class/different-map adversary | `theory/TypedAlphaEquivalence.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryCanonicalizationTest.java` |
| GEQ-03 | Java `equals` remains structural | Mission sec. 22 | Every exact immutable carrier keeps structural equality; graph-relative equality is only in `TypedAlphaEquivalence` | EXACT | Phase E separation complete | No graph lookup occurs from an `equals` implementation | Structural versus shared-leader relation | `theory/TypedENode.java`; `theory/PortValue.java`; `theory/TypedAlphaEquivalence.java`; `TheoryCanonicalizationTest.java` |
| CAN-01 | Canonicalizer first replaces invocations by leaders, composes path embeddings, and returns the effective-kernel transport | Sec. 3.7 step 1; Appendix C.1-C.2; Fig. 4 | `LeaderNormalizer` is an explicit first pass and consumes typed find without opening leaders, but returns neither the ambient inclusion nor a certificate derivation | PARTIAL | Return `K_G(n)`, `iota_n`, and typed `d_n` after PF-TB01-PF-TB03 are resolved | Canonicalization is quiescence-guarded and currently rejects support loss rather than forging a witness | Nonleader path normalization, leader-only output leaves, dirty rejection, strict-support rejection | `theory/LeaderNormalizer.java`; both canonicalizers; `TheoryCanonicalizationTest.java`; `theory-pre-phase-f-audit.md` |
| CAN-02 | One global canonical free-slot context by type | Sec. 3.6; Appendix C.1-C.2 | Both canonicalizers allocate one `canonicalFreeContext` after leader normalization and share each candidate renaming across the complete node | EXACT | Phase E step complete | Every recursive port call receives the same extended global renaming | Coupled pair-set false-dedup adversary and generated alpha variants | Both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-03 | Enumerate all and only type-preserving free-slot bijections | Sec. 3.7 step 2; Fig. 4 | `TypedRenamingEnumerator` deterministically streams every typed bijection without a cap | EXACT | Phase E enumeration complete | Type-count mismatch yields no map; every emitted value is a validated `TypedRenaming` | Seven-slot `7! = 5,040`, cross-type rejection, differential generated cases | `theory/TypedRenamingEnumerator.java`; `TheoryCanonicalizationTest.java` |
| CAN-04 | Use only certified leader symmetries | Sec. 3.7 step 2; Fig. 4 | Canonicalizers use only `G_l.elements()` from the graph-owned typed leader record; nonidentity certificate provenance is not represented | PARTIAL | Phase F attaches/verifies generator certificates and removes the setup-only group constructor | Public group construction remains identity-only; all consumed elements are typed/current-interface permutations | Identity-only rejection and generated S3 orbit differential tests | Both canonicalizers; `theory/TypedSymmetryGroup.java`; `TheoryCanonicalizationTest.java` |
| CAN-05 | Use only certified binder-block automorphisms | Sec. 3.6; Appendix C.1-C.2 | Lexical Bind alpha-renaming is exact, but no complete descriptor-indexed `Aut(beta)` carrier exists | PARTIAL | Phase F implements descriptor and certified binder-block automorphism groups | Canonicalizer performs no heuristic same-type block permutation | Binder alpha tests pass; incompatible descriptor/block tests await Phase F | Both canonicalizers; `theory/BindPort.java`; `TheoryCanonicalizationTest.java` |
| CAN-06 | Seq/Bag/Set canonicalization follows exact container semantics | Sec. 3.7 step 3; Fig. 4 | Reference enumerates full products and production streams local orbit minima into explicit Seq/Bag/Set constructors | EXACT | Phase E container canonicalization complete | Seq preserves position, Bag sorts with multiplicity, Set sorts/deduplicates complete post-renaming keys | Flat ACI, bag multiplicity, generated S3 Seq/Bag/Set differential cases | Both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-07 | Set dedup uses one shared free-slot renaming, never local alpha per element | Sec. 3.6; Appendix C.2 | Set construction occurs only after every element receives the same whole-node renaming and its leader symmetry choice | EXACT | Phase E set rule complete | No recursive call allocates an independent free context | `{(x,y),(y,x)}` retains two elements; symmetry-orbit duplicates collapse | Both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-08 | Deterministic least complete structural key | Sec. 3.6-3.7; Appendix C.1 | Both implementations compare complete Phase B-E `StructuralKey` values and break equal-shape witness ties by complete embedding key | EXACT | Phase E least-candidate selection complete; cross-process replay remains `ORD-02` | Unequal comparator-key collisions fail closed | Repetition, insertion-order foundations, idempotence, reference/production equality | `theory/StructuralKey.java`; both canonicalizers; `TheoryCanonicalizationTest.java` |
| CAN-09 | Canonicalizer returns shape, effective-support renaming, ambient embedding, and source-to-kernel provenance | Appendix C.1-C.2; Fig. 4 | `CanonicalizationResult` stores only source, least `CanonicalShape`, and canonical-to-source `TypedRenaming`; it has no kernel, `iota_n`, `omega_n`, or `d_n` | PARTIAL | Replace the three-field result only after the formal endpoint of `d_n` is fixed | Constructor checks the old support-preserving endpoints and rejects strict support loss | Existing witness replay plus future strict-support transport replay | `theory/CanonicalizationResult.java`; `TheoryCanonicalizationTest.java`; `theory-pre-phase-f-audit.md` |
| CAN-10 | Canonical-shape exactness for stored flat keys | Cor. 3; Appendix C.2 effective-support exactness | Exhaustive and production implementations agree on all generated support-preserving states and reject proper-parent support loss; the main corollary and Appendix C still specify incompatible domains | PARTIAL | Resolve PF-TB01 and replace the main exactness proof with the effective-kernel statement before implementing strict-support results | Both implementations reject support loss with `CanonicalizationDomainException` | 11,162-check differential suite plus smallest proper-embedding rejection | Both canonicalizers; `theory/CanonicalizationDomainException.java`; `TheoryCanonicalizationTest.java`; `theory-pre-phase-f-audit.md` |
| ORD-01 | One deterministic total order covers every relevant field | Sec. 3.6; mission sec. 12 | `StructuralKey` now covers all Phase B-E values, including canonicalization results and every field used to select a least shape/witness | PARTIAL | Extend the same grammar to Phase F certificates and binder descriptors | Immutable ordered fields, ordered maps, deterministic orbit traversal, and unequal-key-collision checks fail closed | Phase B-D order properties plus Phase E least-candidate and idempotence tests | `theory/StructuralKey.java`; `theory/CanonicalizationResult.java`; both canonicalizers; `TheoryCanonicalizationTest.java` |
| ORD-02 | Equality and comparison agree across JVM runs | Mission secs. 12 and 30 | Phase B-E keys have stable length-prefixed serialization and canonicalization is repeatable in one JVM; cross-JVM/worker digest replay remains absent | PARTIAL | Add repeated JVM/worker trace and canonical digest replay | Ordered contexts/maps, normalized containers, and deterministic candidate/witness tie-breaking | Repeated canonicalization and shuffled Phase D registration pass; process replay remains pending | `theory/StructuralKey.java`; `theory/ProductionGraphCanonicalizer.java`; `TheoryCanonicalizationTest.java` |
| SYM-01 | Every symmetry generator has typed witness-equation provenance | Def. 7 SC; Thm. 1 obligation 5; Appendix B | No provenance; public swaps and same-leader correspondence add generators | CONTRADICTED | `SymmetryCertificate` with typed endpoints, equation, induced permutation, and category | `addSymmetryCertified` verifies certificate | Valid rewrite-derived and fake symmetry cases | `EGraphNode.java:293-304,1083-1086`; `SlottedEGraph.java:313-325` |
| SYM-02 | Same type is necessary but insufficient for symmetry | Sec. 3.6; Lemma 3 | Production quantifier swaps check a subset of descriptor fields; direct API checks no type | CONTRADICTED | Verifier checks proof and full descriptor, not type alone | No unchecked group insertion | Same-type non-symmetric slots | `NormalForm.java:403-412`; `SlotPermutationGroup.java:42-61` |
| SYM-03 | Symmetry group closure is finite and consistent on current interface | Def. 5; Prop. 3 | `TypedSymmetryGroup` computes the uncapped finite typed closure on exactly the class interface; certificate transport is absent | PARTIAL | Phase F adds generator certificates and certified transport across restriction | Every element is a typed automorphism; identity, closure, and inverses are audited | Exact closure, inverse, context/type rejection | `theory/TypedSymmetryGroup.java`; `TheoryStateTest.java` |
| BIND-01 | Complete binder descriptor | Sec. 3.6; sec. 4; Appendix C.1/C.7 | `QuantiVar` stores several fields but domain/dependency semantics are strings and not one immutable descriptor | PARTIAL | Immutable descriptor containing type, domain, quantifier, cardinality, disjointness, dependencies, and semantic fields | Descriptor constructor validates references | Field-by-field inequality cases | `QuantiVar.java:27-50,61-105` |
| BIND-02 | `Aut(beta)` contains only descriptor-preserving permutations | Sec. 3.6; Lemma 3 | Adjacent variables are swapped without carrier/domain or dependency checks | CONTRADICTED | Certified binder automorphism group per block | `BinderAutomorphismCertificate` verifier | Same type but domain/dependency mismatch | `NormalForm.java:399-413` |
| BIND-03 | Binder consumer invariance is a declared proof obligation | Sec. 4, Lemma 3 | Formula quantifier permutation is treated as implementation rule with no semantic certificate | ABSENT | Alloy adapter supplies explicit law/certificate category for each admitted binder block | Reject unsupported binder consumers | Formula block accepted; ordered lambda rejected | Paper sec. 5.1 explicitly says no certificate is emitted |
| REST-01 | Interface restriction requires typed factorization certificate | Thm. 1 obligation 6 | Interfaces are intersected/compacted from syntactic use | CONTRADICTED | `restrictInterfaceCertified(class,newContext,certificate)` only | Validate typed subcontext, witness factorization, and transport | Certified and uncertified removal | `EGraphNode.java:1120-1152`; `SlottedEGraph.java:416-455` |
| REST-02 | Restriction transports shapes, parent edges, and certificates and dirties parents | Thm. 1 obligation 6; sec. 3.7 | Slot updates mutate local maps/groups without certificate transport or parent dirty queue | ABSENT | Transactional interface restriction operation | Atomic validation then recursive dirtying | Incident-parent transport trace | `RenamedIdUnionFind.java:28-32`; `SlotPermutationGroup.java:23-39` |
| REST-03 | No other operation mutates class interface | Mission sec. 15 | `recomputeSlots`, `updateSlots`, and compaction mutate interfaces directly | CONTRADICTED | Private immutable class state changed only through certified graph API | Package visibility and static bypass audit | Reflection/source boundary test | `EGraphNode.java:1120-1152`; `RenamedIdUnionFind.java:28-32` |
| CERT-01 | Typed certificate hierarchy | Def. 7; Appendix B; mission sec. 16 | No certificate classes | ABSENT | Sealed certificates for input, congruence, parent edge, symmetry, restriction, container law, and binder automorphism | Typed endpoint validation in constructors | Each valid and malformed certificate | No implementation found |
| CERT-02 | Certificate composition validates typed endpoints | Lemma 5; Thm. 1 | No provenance composition | ABSENT | Composition/transitivity objects with exact middle invocation/context | Fail on endpoint/context mismatch | Legal and illegal composition | No implementation found |
| CERT-03 | Coherent family supports EC, PC, and SC reconstruction | Def. 7 | Shapes, parents, and groups carry no equations | ABSENT | Compact provenance sufficient to reconstruct all three equations | `checkInvariants` validates certificate reachability | Coherence audit on generated state | No implementation found |
| CERT-04 | Rebuild collision records child equations/congruence proof | Lemma 4; Appendix B | Hash collision unions owners directly | CONTRADICTED | `CongruenceCertificate` built from canonical child find proofs | Collision union requires verified proof | Collision with missing proof rejected | `SlottedEGraph.java:401-408`; `IntEGraph.java:84-92` |
| CERT-05 | Leader-kernel extraction retains a typed source-to-kernel derivation `d_n` | Appendix C.1-C.2; Fig. 4 | `LeaderNormalizer` returns syntax only and parent paths have no equality certificates | ABSENT | Define the formal judgement for `d_n`, then compose parent-edge, container-law, and forward-congruence certificates with exact contexts | Construction rejects missing/mismatched child derivations and replays the ambient embedding | Proper parent embedding, nested containers, malformed path proof, and transport composition | `theory/LeaderNormalizer.java`; `theory/ParentPath.java`; `theory-pre-phase-f-audit.md` |
| UNION-01 | Distinct-leader union requires typed derivation | Thm. 1 obligation 4 | Public unions accept endpoint invocations without certificate | CONTRADICTED | `unionCertified(left,right,equalityCertificate)` | Validate type, endpoints, orientation, and parent edge | Input/rewrite/congruence unions | `EGraphNode.java:293-310`; `SlottedEGraph.java:255-311` |
| UNION-02 | New parent edge stores embedding and parent-edge certificate | Thm. 1 obligation 4 | Stores untyped correspondence only | CONTRADICTED | Immutable typed parent edge with `ParentEdgeCertificate` | Parent edge cannot be installed separately | Parent proof replay | `RenamedIdUnionFind.java:55-68`; `SlottedEGraph.java:303-309` |
| UNION-03 | Same-leader event does not infer symmetry | Thm. 1 obligation 5 | Both production and ablation infer symmetry from endpoint correspondence | CONTRADICTED | Same-leader union is a no-op; symmetry has a separate certified API | Union path cannot access group mutation | Same-leader differing images leaves group unchanged | `EGraphNode.java:300-304`; `SlottedEGraph.java:258-260` |
| UNION-04 | Endpoint image mismatch infers neither symmetry nor redundant slot | Thm. 1 obligation 7 | Distinct union fills unmatched positions and counts gaps as redundant | CONTRADICTED | Require prior certified restriction or valid embedding orientation | Reject incomparable interfaces | Image-mismatch adversarial trace | `SlottedEGraph.java:273-308` |
| CONG-01 | Forward congruence only | Sec. 3.2; Lemma 4; Thm. 1 | Ordinary hash collision merging is forward, but parent equality is used to infer slot symmetry | CONTRADICTED | Congruence merges parents from child proofs only; never decomposes parent equality | Separate proof constructors prevent reverse inference | Non-injective operator counterexample | `SlottedEGraph.java:388-408,313-325` |
| CONG-02 | Different operators/types never structurally collide | Def. 6; Appendix C.2 | Existing shape keys include head/atom, but omit graph-level output/type instantiation | PARTIAL | Complete typed tag in shape key | Hash key constructor derives tag from signature | Same children, different operator/type | `SlottedEGraph.java:578-615`; `IntEGraph.java:141-179` |
| REB-01 | First-class deduplicated dirty-parent rebuild | Sec. 3.7 | Slotted arm scans all records in capped rounds; production has no joint hash-cons rebuild | CONTRADICTED | Parent-use index plus deduplicated dirty queue | Mutations enqueue affected parents | Queue dedup and zero-dirty completion | `SlottedEGraph.java:388-413`; `EGraphNode.java:1195-1198` |
| REB-02 | Rebuild removes stale key, finds children, composes embeddings, canonicalizes, normalizes ports, and reinserts | Sec. 3.7 | Partial whole-map reconstruction; no typed contexts/certificates/explicit ports | PARTIAL | Literal rebuild algorithm over typed state | Step assertions in debug mode | Each step exercised by collision trace | `SlottedEGraph.java:394-410` |
| REB-03 | Collision union uses congruence certificate after child derivations exist | Sec. 3.7; Thm. 1 obligation 8 | Collision immediately calls uncertified union | CONTRADICTED | Build and verify child certificate vector before union | Union rejected if proof absent | Delayed-child-proof case | `SlottedEGraph.java:403-408` |
| REB-04 | Rebuild does not open opaque child classes | Sec. 3.7 | Ablation shape rebuild is opaque, but production saturation opens representatives | CONTRADICTED | Exact engine rebuild only canonicalizes invocation leaves | No child-node enumeration from rebuild | Hidden same-head test | `SlottedEGraph.java:374-385`; `EGraphNode.java:466-473` |
| REB-05 | Fixed finite rebuild reaches quiescence; rewrite saturation termination is separate | Prop. 3 | Slotted rebuild stops after 16 rounds and rewriting after 32; zero dirty state is not established | CONTRADICTED | Unbounded finite administrative fixed point with rewrite insertion disabled; rewrite budget remains separate | Rebuild exits only when dirty set empty | Finite batch quiescence and rewrite-loop separation | `SlottedEGraph.java:388-413`; `AlloyRewriteSystem.java:13` |
| FIN-01 | Indexed finite-unfolding representation relation | Def. 7 after EC/PC/SC; Lemma 5 | No graph unfolding model retains shape witnesses/certificates | ABSENT | Bounded unfolding enumerator respecting ambient support and fresh redundant slots | Only coherent states may unfold | Small cyclic/acyclic bounded cases | No implementation found |
| FIN-02 | Finite-unfolding validation harness with positive and malformed traces | Thm. 1; mission sec. 24 | Bounded Alloy semantic checks test formulas, not certificate-preserving finite graph unfoldings | ABSENT | Dedicated generated conformance oracle | Reject malformed trace before mutation | Valid agreement and injected violations | `EGraphSemanticSoundnessCheck.java` is not this theorem harness |
| MUT-01 | Central mutation boundary for U, M, H, groups, interfaces, shapes | Mission sec. 25 | The isolated exact path has immutable records and graph-owned private U/M/H; Phase D setup/link methods are package-private and certified Phase F-G transitions do not yet exist | PARTIAL | Replace setup-only transitions with `insertNode`, `unionCertified`, `addSymmetryCertified`, `restrictInterfaceCertified`, and `rebuild` | No raw union-find or mutable record is public | Reflection/API boundary and mutation-visibility tests | `theory/TypedSlottedPortEGraph.java`; `theory/TypedEClassRecord.java`; `TheoryStateTest.java` |
| MUT-02 | Source and rewrite insertion share controlled construction | Flat contract; mission sec. 25 | Parser adapter, rewrites, normalizer, and tests construct generic terms/nodes directly | CONTRADICTED | Alloy adapter emits typed syntax consumed by one insertion API | Static repository scan | Constructor-bypass audit | `AlloyAstTermAdapter.java:32-123`; all `new EGraphNode` call sites |
| CHK-01 | Full runtime `checkInvariants()` | Mission sec. 26 | `TypedSlottedPortEGraph.checkInvariants()` covers all Phase D U/M/H metadata, forest, child ownership, group, witness, and quiescent hash-cons invariants | PARTIAL | Extend it to certificates, certified restriction, parent uses, and rebuild in Phases F-G | Current graph can be audited without mutation at either status | Metadata spoof, missing child, collision, cycle, and hash ownership cases | `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| CHK-02 | Checker runs after every debug operation and rebuild | Mission sec. 26 | Phase D setup/link transitions invoke invariant checks in the deterministic test harness; configurable always-on production transition mode awaits Phase F-G | PARTIAL | Wire the checker into every certified mutation and rebuild boundary | Package-private transitions fail before publishing malformed state | Generated legal chains/forests and one-fault adversaries | `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| CHK-03 | Dirty-state query rejection/rebuild | Def. 5; mission secs. 10 and 26 | `TypedSlottedPortEGraph` rejects hash-cons queries while dirty; no rebuild operation exists yet | PARTIAL | Phase G deterministically rebuilds or preserves rejection until quiescence | Central quiescence precondition on current H observations | Stale hash query after parent mutation | `theory/GraphStatus.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| TEST-01 | Foundational embedding/renaming/alpha property tests | Mission sec. 27 | Deterministic generated suites cover typed embedding/renaming/permutation laws and indexed structural alpha identity, inverse, composition, and binder cases | EXACT | Foundational and alpha property obligation complete | Seeds `55520260816` and `55520260819` are printed | 1,052 foundation checks plus Phase E alpha groupoid and malformed cases | `theory/TheoryFoundationsTest.java`; `TheoryCanonicalizationTest.java` |
| TEST-02 | Canonicalization/container/support properties | Mission sec. 27 | Generated Phase C-E tests cover typed containers, action/support, global canonicalization, witnesses, idempotence, and reference/production agreement on the support-preserving domain | PARTIAL | Resolve E-TB01 and add certified binder-block cases before total canonicalization coverage | Constructors and both canonicalizers check every current result | 956 Phase C checks and 11,162 Phase E checks; blocker adversary rejects explicitly | `theory/TheoryPortsTest.java`; `TheoryCanonicalizationTest.java` |
| TEST-03 | Mutation-sequence fuzzing with one-fault illegal traces | Mission sec. 28 | Seeded Phase D tests generate 96 typed chains and 64 branching forests and inject targeted malformed state operations | PARTIAL | Extend the generator across all Phase F-G certified transition kinds and every named one-fault trace | Current graph invariants are checked throughout generated traces | Direction, cycle, metadata, missing child, stale query, and collision faults | `theory/TheoryStateTest.java` (seed `55520260818`) |
| TEST-04 | Differential exhaustive reference versus production canonicalizer | Mission sec. 29 | Separate exhaustive Cartesian-orbit and streaming production implementations return exactly equal shape and witness on generated small support-preserving states | PARTIAL | Resolve E-TB01, then extend generation across certified binder blocks in Phase F | Every test compares complete `CanonicalizationResult`, not only a string key | 11,162 checks including 7!, S3 Seq/Bag/Set products, binders, and converse examples | `theory/ExhaustiveGraphCanonicalizer.java`; `theory/ProductionGraphCanonicalizer.java`; `TheoryCanonicalizationTest.java` |
| TEST-05 | Deterministic replay across runs, workers, and insertion orders | Mission sec. 30 | Phase E adds stable engine/canonicalizer IDs, complete result keys, and repeated/idempotent in-JVM replay; process/worker manifests remain untouched | PARTIAL | Phase I records identifiers/digests and compares JVM/worker executions | Ordered immutable state and deterministic witness tie-breaking | Repeated output and shuffled state insertion pass; 1/32-worker replay remains pending | `theory/TypedSlottedPortEGraph.java`; `TheoryCanonicalizationTest.java`; `AblationRunManifest.java` |
| TEST-06 | Mandatory adversarial proof-hazard suite | Mission sec. 34 | Phase D rejects wrong edge direction, cycles, spoofed metadata, context/type mismatch, missing children, stale H, and non-atomic H collision; certificate/restriction/rebuild hazards await their phases | PARTIAL | Complete the ten-hazard suite as Phase E-G operations become representable | Each currently representable malformed action is rejected before state publication | Phase D adversarial cases plus existing legacy capture/disjointness regressions | `theory/TheoryStateTest.java`; `EGraphSaturationTest.java:537-603` |
| INT-01 | Legacy engines are explicitly distinguished from exact engine | Paper sec. 5.2; mission sec. 31 | Exact engine reports `typed-slotted-port-egraph` and `canon-g-production-v1`; legacy arms remain unchanged and no harness imports the theory package | PARTIAL | Add this explicit engine as a new Phase I arm after B-H gates | Package boundary and source-import audit preserve separation | Exact engine tests and unchanged legacy compatibility harness pass | `theory/TypedSlottedPortEGraph.java`; `theory/ProductionGraphCanonicalizer.java`; `theory/package-info.java` |
| INT-02 | Theory-faithful Alloy adapter path | Mission phase I | Existing adapters target `AlloyTerm` or heuristic `EGraphNode` | ABSENT | Typed signature/domain adapter into exact engine | Adapter type-checks every insertion | Representative Alloy formulas | `AlloyAstTermAdapter.java`; `IRAgent.java` |
| INT-03 | Additional evaluation arm and complete manifest fields | Mission sec. 32 | Six existing arms and manifests omit theory-engine/certificate/invariant fields | ABSENT | Seventh exact arm with engine ID, invariant mode, canonicalizer version, certificate mode, seed, workers, and bounds | Manifest compatibility checks | Smoke and full report generation | `EGraphAblationSuite.java`; `AblationRunManifest.java` |
| T1-01 | Bottom-up well-typed insertion; children exist; fresh class exposes the formally selected full support | Thm. 1 obligation 1; Appendix C.2 | Legacy insertion is bottom-up but untyped; additionally, the theorem requires original full support while Appendix C requires effective-kernel support | PARTIAL | Resolve PF-TB02, then implement certified typed insertion over the selected exact kernel and retain original-source provenance | Validate children, kernel support, initial interface, and `d_n` before publication | Bottom-up, missing-child, support-preserving, and strict-support cases | `SlottedEGraph.java:101-152`; `IntEGraph.java:20-41`; `theory-pre-phase-f-audit.md` |
| T1-02 | Canonicalization uses only typed alpha, certified binder automorphisms, and certified container laws | Thm. 1 obligation 2 | Phase E canonicalization is typed, uncapped, and structurally exact; symmetry, binder-block, and law certificates are not yet available | PARTIAL | Phase F gates every nonidentity symmetry/block action and semantic container law with certificates | Current public exact path admits identity group and declared structural port behavior only | Differential canonicalization and no-heuristic-block tests pass | Both canonicalizers; `TheoryCanonicalizationTest.java` |
| T1-03 | Hash-cons only equal canonical typed shapes | Thm. 1 obligation 3 | Complete typed canonical keys and quiescent H exist, but graph insertion/rebuild does not yet require `CanonicalizationResult` | PARTIAL | Phase F-G controlled insertion stores only verified canonical results and certified collision unions | Current H checker rejects stale/nonleader ownership | Typed shape noncollision and quiescence tests pass; insertion integration pending | `theory/CanonicalShape.java`; `theory/TypedSlottedPortEGraph.java`; `TheoryStateTest.java` |
| T1-04 | Every union has endpoint derivation from input equation or forward congruence | Thm. 1 obligation 4 | Unions are uncertified | CONTRADICTED | Typed equality certificate required by API | Certificate verifier | Input and congruence provenance | `EGraphNode.java:293-310`; `SlottedEGraph.java:255-311` |
| T1-05 | Same-leader symmetry requires separate witness equation | Thm. 1 obligation 5 | Same-leader union automatically adds symmetry | CONTRADICTED | Separate certified symmetry transition | Same-leader union cannot mutate group | Endpoint-image adversary | `EGraphNode.java:300-304`; `SlottedEGraph.java:258-260` |
| T1-06 | Slot removal has restricted witness, inclusion equation, and transported certificates | Thm. 1 obligation 6 | Syntactic compaction/intersection removes slots | CONTRADICTED | Certified restriction transaction | Factorization and transport verifier | Redundant-coordinate trace | `EGraphNode.java:1120-1152`; `SlottedEGraph.java:416-455` |
| T1-07 | Different endpoint images alone create no symmetry/redundancy | Thm. 1 obligation 7 | Correspondence logic does both | CONTRADICTED | Reject or demand separate certificates | No heuristic fallback | Mismatched-image trace | `SlottedEGraph.java:273-308,313-325` |
| T1-08 | Rebuild congruence occurs only after child derivations exist | Thm. 1 obligation 8 | Rebuild collisions union without derivations | CONTRADICTED | Congruence proof object assembled before collision union | Verifier blocks absent child proof | Delayed proof trace | `SlottedEGraph.java:394-408`; `IntEGraph.java:84-92` |
| T1-09 | Path compression composes typed embeddings and certificates | Thm. 1 obligation 9 | Phase D composes total typed embeddings in the formal direction and retains primitive path provenance; equality certificates are absent | PARTIAL | Phase F attaches and composes parent-edge certificates along the retained path | Pre/post embedding and path equality already checked | Multi-edge compression passes; proof replay remains pending | `theory/TypedRenamedUnionFind.java`; `theory/ParentPath.java`; `TheoryStateTest.java` |
| RW-01 | Ordered per-phase pipeline | Appendix C.3 | `NormalForm.normalize` follows alpha/beta/branch, NNF, prenex, second branch/NNF, ACI, saturation | EXACT | Preserve this order in Alloy adapter integration | Stage observer and regression tests | Stage-order regression | `NormalForm.java:247-299` |
| RW-02 | Parser markers, END cleanup, lexical alpha, capture-avoiding let beta | Appendix C.4 | Implemented in `NormalForm` and shared rewrite system | PARTIAL | Route resulting nodes through typed smart construction; retain existing semantics | Scope-aware substitution and arity checks | Existing END/let/shadow tests plus typed integration | `NormalForm.java:259-269`; `AlloyRewriteSystem.java:97-109` |
| RW-03 | Implication, iff, formula ITE elimination and Boolean NNF | Appendix C.5 | Implemented and regression-tested | EXACT | Adapt outputs to typed port constructors | Existing branch/negation checks | Implication/IFF/ITE polarity tests | `NormalForm.java:423-460`; `EGraphSaturationTest.java:679-750` |
| RW-04 | Temporal duals and no quantifier crossing temporal phases | Appendix C.6; sec. 5.1 | Separate normal-form phases and dualization exist | EXACT | Keep phase adapter outside exact graph or encode explicit temporal layer | Phase-local traversal | Existing temporal tests | `IRAgent.java:99-155`; `NormalForm.java:110-237` |
| RW-05 | Quantifier negation, empty/constant rules, safe prenex, primitive guards, no temporal crossing | Appendix C.7 | Implemented in normal-form pipeline, including `NOTONE`/`NOTLONE`; bindings are mutable records | PARTIAL | Translate complete immutable binder descriptors and law certificates into exact engine | Side-condition and descriptor validation | Existing quantifier tests plus binder conformance | `NormalForm.java:247-299,800-990`; `QuantiVar.java` |
| RW-06 | Complete A/AC/ACI operator-law table | Appendix C.8 Table 11 | Hard-coded operator sets broadly match table | PARTIAL | Replace implicit sets with typed certified declarations | Signature validation | Table-driven declaration audit | `EGraphNode.java:840-849`; `AlloyRewriteSystem.java:46-54` |
| RW-07 | Local Boolean and relational rules | Appendix C.9 | Implemented by representative-oriented saturation/shared rewriter | PARTIAL | Emit typed input/container-law certificates for every equality-producing rewrite | Rule application records certificate | Rule-by-rule endpoint tests | `EGraphNode.java:448-645`; `AlloyRewriteSystem.java:145-209` |
| RW-08 | Rewrite saturation bound is separate from exact administrative rebuild | Appendix C.10; Prop. 3 | Both are bounded loops in current engines | CONTRADICTED | Keep 32-pass rewrite budget only in rewrite driver; exact rebuild runs to empty dirty set | Distinct counters/states | Rebuild-only quiescence test | `EGraphNode.java:423-427`; `SlottedEGraph.java:388-413` |

## Gate Status

Gate A documents the current mismatch; it does not discharge later gates. The
matrix contains unresolved mandatory rows, so no theory-faithful implementation
claim is permitted yet. See `theory-architecture-map.md` for mutation paths and
`theory-phase-a-audit.md` for the baseline and prioritized mismatch list.
